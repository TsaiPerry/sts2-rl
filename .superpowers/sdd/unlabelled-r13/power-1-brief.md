# Batch `power-1` — unlabelled gap entries to settle

12 entries across 10 record files. **These record files are yours alone; no other agent writes them.**

## `power/artifact`  → `audit/records/power/artifact.json`

- **`power/artifact/AfterModifyingPowerAmountReceived`** (section `hooks`, key `AfterModifyingPowerAmountReceived`, mechanism `power/artifact/AfterModifyingPowerAmountReceived`)
  - what: AfterModifyingPowerAmountReceived
  - issue: The stack-consumption event is hand-inlined. C# consumes the stack via `PowerCmd.Decrement(this)` from AfterModifyingPowerAmountReceived (ArtifactPower.cs:38-41) -- i.e. through the full ModifyAmount pipeline, which is what runs ShouldRemoveDueToAmount. The sim decrements the field and calls _expire() directly. This is audit/records/seam/power_cmd.json's gap G4 (no AfterModifyingPowerAmountGiven/Received machinery exists in the sim, and both real C# listeners -- Artifact and Ruined Helmet -- are hand-inlined instead); per rule 3 the same verdict, cross-referenced. The observable for Artifact specifically is identical, which is why power_cmd calls the inline reimplementation individually correct.
  - citations: ArtifactPower.cs:38-41

## `power/corruption`  → `audit/records/power/corruption.json`

- **`power/corruption/ModifyCardPlayResultPileTypeAndPosition`** (section `hooks`, key `ModifyCardPlayResultPileTypeAndPosition`, mechanism `power/corruption/ModifyCardPlayResultPileTypeAndPosition`)
  - what: ModifyCardPlayResultPileTypeAndPosition
  - issue: The destination-pile DECISION is replaced by an after-the-fact move, and the sim has the right hook available and does not use it. C# (CorruptionPower.cs:27-38) returns (PileType.Exhaust, position) from the pile-resolution chain, so a played Skill never enters the discard pile at all. The sim lets combat._resolve_card_play put the card in the discard pile (combat.py:453), runs the exhaust-keyword check and then the modify_card_play_result_pile chain (combat.py:500-511), and only afterwards -- inside on_card_played (combat.py:514) -- does Corruption pull the card out of the discard pile and append it to the exhaust pile, firing on_card_exhausted itself (powers.py:610-613). Two consequences. (i) CONTENTION ORDER: the sim's modify_card_play_result_pile hook exists (hooks.py:236-246) but accepts only 'discard'/'draw_top', so Nostalgia (powers.py:3595-3612) runs FIRST and can move the Skill to the top of the draw pile, after which Corruption's `card in player.discard_pile` test fails (powers.py:610) and the Skill is never exhausted. In C# both are listeners on ONE chain and the last writer wins, so Corruption exhausts it. Both Nostalgia and Corruption are ported Ironclad-pool powers, so the contention is reachable; which one wins in C# is hook_dispatch G2's listener-order question and is not re-verdicted here. (ii) EVENT TIMING: Feel No Pain and Dark Embrace see on_card_exhausted fired from the middle of Corruption's on_card_played rather than at pile assignment, so their effects land after any on_card_played listener registered before Corruption. Third-file citations, not hashed here.
  - citations: CorruptionPower.cs:27-38, combat.py:453, combat.py:500-511, combat.py:514, hooks.py:236-246, powers.py:3595-3612, powers.py:610, powers.py:610-613

## `power/dark_embrace`  → `audit/records/power/dark_embrace.json`

- **`power/dark_embrace/AfterCardExhausted`** (section `hooks`, key `AfterCardExhausted`, mechanism `power/dark_embrace/AfterCardExhausted`)
  - what: AfterCardExhausted
  - issue: Two divergences. (a) THE DRAW COUNT IS HARD-CODED TO 1. DarkEmbracePower.cs:47 draws `base.Amount`; powers.py:255 is `DrawCmd.draw(self.owner, 1)`. Dormant only because the one ported applier always passes 1 (cards/dark_embrace_card.py:34, including its upgrade, which raises no amount) -- the moment any effect stacks Dark Embrace the sim draws one card where the game draws Amount. Named trigger: a second Dark Embrace play, or any card/relic that stacks it. (b) THE ETHEREAL DEFERRAL IS MISSING -- see the AfterSideTurnEnd entry and the causedByEthereal guard. Third-file citations, not hashed here.
  - citations: DarkEmbracePower.cs:47, cards/dark_embrace_card.py:34, powers.py:255
- **`power/dark_embrace/AfterSideTurnEnd`** (section `hooks`, key `AfterSideTurnEnd`, mechanism `power/dark_embrace/AfterSideTurnEnd`)
  - what: AfterSideTurnEnd
  - issue: DarkEmbracePower.cs:52-60 is half of a two-phase mechanism the sim has none of: an exhaust caused by the Ethereal keyword increments an internal etherealCount instead of drawing, and this hook then draws `Amount * etherealCount` at the side's end -- deliberately, so the cards arrive AFTER the hand flush rather than being flushed away (the source comment at :18-23 says exactly that). The sim draws immediately for every exhaust (powers.py:251-255), so cards Dark Embrace draws off a turn-end ethereal exhaust are discarded by the flush that follows. LIVE the moment both halves co-occur: the sim's ethereal exhausts happen at turn end (cards/base.py's ethereal handling via combat._process_turn_end_cards, combat.py:352-...) and should_ethereal_trigger exists as a hook (hooks.py:696), while Dark Embrace is a ported card. The sim's on_card_exhausted signature (hooks.py:415-419) has no causedByEthereal argument, so this cannot be fixed inside the power. Same missing parameter that turn_structure's gaps G4/G17 record for Joss Paper; per binding rule 3 the shared mechanism -- the absent causedByEthereal flag -- keeps that `gap` verdict, and what is recorded here is Dark Embrace's own deferral being absent on top of it. Third-file citations, not hashed here.
  - citations: DarkEmbracePower.cs:52-60, combat.py:352, hooks.py:415-419, hooks.py:696, powers.py:251-255

## `power/draw_cards_next_turn`  → `audit/records/power/draw_cards_next_turn.json`

- **`power/draw_cards_next_turn/AfterSideTurnStart`** (section `hooks`, key `AfterSideTurnStart`, mechanism `power/draw_cards_next_turn/AfterSideTurnStart`)
  - what: AfterSideTurnStart
  - issue: NARROWED 2026-07-27. The Mayhem-ordering half is CLOSED: MayhemPower moved to the sim's new AutoPrePlay phase, `after_auto_pre_play_phase_entered` (powers.py:3763), dispatched at player.py:221 strictly AFTER `on_player_turn_started` (:218), which is CombatManager.cs:568's RunAutoPrePlayPhase running after Hook.AfterSideTurnStart (:522). So the executed Relax + Mayhem witness now resolves the way the game does, deterministically. WHAT REMAINS is this record's own primary finding, unchanged: RIGHT SLOT, WRONG CONDITION. DrawCardsNextTurnPower.cs:37 removes the power only when participants.Contains(base.Owner) AND `base.AmountOnTurnStart != 0`; powers.py:2917-2920 expires it whenever the owner's turn starts, with no such test, and PowerModel.AmountOnTurnStart (snapshotted in Creature.BeforeTurnStart, Creature.cs:673-679, for exactly this 'prevent same-turn activation when applied mid-turn' purpose) still has NO sim counterpart -- executed grep for `amount_on_turn_start` across sts2_rl/ returns nothing.
  - citations: CombatManager.cs:568, Creature.cs:673-679, DrawCardsNextTurnPower.cs:37, player.py:221, powers.py:2917-2920, powers.py:3763

## `power/nostalgia`  → `audit/records/power/nostalgia.json`

- **`power/nostalgia/g8`** (section `guards`, key `g8`, mechanism `power/nostalgia/g8`)
  - what: Contention with power/corruption and power/rebound on the same chain
  - issue: NARROWED 2026-07-27, and the contention's OUTCOME has INVERTED. The hook moved: `modify_card_play_result_pile` is now consulted at combat.py:567, before the playCount loop (CardModel.cs:1890), but the MOVE it decides happens at combat.py:617-619, AFTER the loop and therefore AFTER `on_card_played` (:600) -- which is where CorruptionPower (powers.py:658-670) and ReboundPower (powers.py:2952-2961) still reach into the piles by hand. So Corruption and Rebound now act FIRST and Nostalgia's redirect finds the card already gone from the discard pile; previously Nostalgia won. C# still has all three on ONE ModifyCardPlayResultPileTypeAndPosition chain with a last-writer-wins rule (Hook.cs:1391-1405) versus three different sim mechanisms, so the gap stands -- only the direction of the wrong answer changed. VERIFIED by reading combat.py:555-620 and powers.py.
  - citations: CardModel.cs:1890, Hook.cs:1391-1405, combat.py:555-620, combat.py:567, combat.py:617-619, powers.py:2952-2961, powers.py:658-670

## `power/rebound`  → `audit/records/power/rebound.json`

- **`power/rebound/ModifyCardPlayResultPileTypeAndPosition`** (section `hooks`, key `ModifyCardPlayResultPileTypeAndPosition`, mechanism `power/rebound/ModifyCardPlayResultPileTypeAndPosition`)
  - what: ModifyCardPlayResultPileTypeAndPosition
  - issue: The destination-pile DECISION is replaced by an after-the-fact move. The sim has the matching hook -- hooks.modify_card_play_result_pile (hooks.py:236-246), dispatched at combat.py:510 -- and this power does not use it, reaching into the piles from on_card_played (combat.py:514) instead. Because combat.py:510 runs FIRST, NostalgiaPower (powers.py:3595-3612), which does use the hook, can move the card to the top of the draw pile before this power looks for it in the discard pile, at which point this power's `card in discard_pile` test fails and it silently does nothing -- neither its effect nor its stack consumption. In C# both are listeners on ONE ModifyCardPlayResultPileTypeAndPosition chain (Hook.cs:1391-1405) and the last writer wins, so both always get a say. Which one wins in C# is hook_dispatch G2's listener-order question and is not re-verdicted here. Both powers are ported Ironclad-pool content, so the contention is reachable. Third-file citations, not hashed here. Rebound is the sharper case of the pair, because Nostalgia and Rebound BOTH want the discard -> draw-top redirect: in C# they contend on one chain and one of them wins; in the sim Nostalgia runs first at combat.py:510, moves the card, and Rebound then finds nothing in the discard pile -- so Rebound gets its effect for free and, worse, never consumes its stack (powers.py:2794's _tick is inside the `card in discard_pile` branch). A Rebound stack therefore survives a play Nostalgia handled, where in C# exactly one AfterModifyingCardPlayResultPileOrPosition fires.
  - citations: Hook.cs:1391-1405, combat.py:510, combat.py:514, hooks.py:236-246, powers.py:2794, powers.py:3595-3612
- **`power/rebound/AfterModifyingCardPlayResultPileOrPosition`** (section `hooks`, key `AfterModifyingCardPlayResultPileOrPosition`, mechanism `power/rebound/AfterModifyingCardPlayResultPileOrPosition`)
  - what: AfterModifyingCardPlayResultPileOrPosition
  - issue: C# consumes the stack from this dedicated after-hook (ReboundPower.cs:32-39 -> PowerCmd.Decrement), which Hook.ModifyCardPlayResultPileTypeAndPosition fires over exactly the listeners that changed the value (Hook.cs:1396-1405, one of hook_dispatch step 38's notification lists). The sim folds the tick into its on_card_played move, so the tick happens if and only if the move happened -- which is the right coupling but the wrong mechanism, and it is exactly what breaks under the Nostalgia contention described on the previous entry. Same absent machinery as audit/records/seam/power_cmd.json's gap G4, one family over.
  - citations: Hook.cs:1396-1405, ReboundPower.cs:32-39

## `power/retain_hand`  → `audit/records/power/retain_hand.json`

- **`power/retain_hand/AfterSideTurnEnd`** (section `hooks`, key `AfterSideTurnEnd`, mechanism `power/retain_hand/AfterSideTurnEnd`)
  - what: AfterSideTurnEnd
  - issue: NARROWED 2026-07-27, and the reason it stands has changed. THE ROOT CAUSE NAMED HERE IS FIXED: `should_take_extra_turn` moved to combat.py:823, AFTER `on_player_turn_end`, `_process_turn_end_cards`, the flush and `after_player_turn_end` (:793-809) -- turn_structure G3, mirroring SwitchFromPlayerToEnemySide (CombatManager.cs:1360-1373). But this power still ticks from `on_enemy_side_end` (powers.py:3542-3543), and an extra turn still skips that, because combat.py:823-831 returns before `_execute_enemy_turn`. So the executed witness stands -- one end_turn with a should_take_extra_turn listener leaves Retain Hand(1) on the player where C# has already decremented at :1307 -- and this entry's closing sentence, that 'moving the tick to after_player_turn_end would NOT fix it', is now FALSE: that slot is exactly what would fix it. VERIFIED by reading combat.py:781-833 and powers.py.
  - citations: CombatManager.cs:1360-1373, combat.py:781-833, combat.py:823, combat.py:823-831, powers.py:3542-3543

## `power/steam_eruption`  → `audit/records/power/steam_eruption.json`

- **`power/steam_eruption/g4`** (section `guards`, key `g4`, mechanism `power/_death_prevention_branch`)
  - what: The prevention branch's HP contract: sim `hp = 1` vs C# leaving the creature at 0 and re-entering KillWithoutCheckingWinCondition
  - issue: NARROWED 2026-07-27. THE FLOOR IS GONE: `_resolve_death`'s prevention arm (cmds.py:51-64) no longer writes `target.hp = 1`; it leaves the creature dead at 0 HP, sets `retained_after_death = True`, fires `on_death(..., True)` and then `after_preventing_death` -- CreatureCmd.cs:560-571's shape. The 1-vs-0 HP number conformance asserts on now matches, and the downstream consequences this entry listed go with it (Feed's `if target.is_dead` scores, and steam_eruption's HP restore no longer depends on the floor -- it moved to `on_death`, powers.py:2144-2151). WHAT REMAINS: the sim still does not model C#'s RE-ENTRY. CreatureCmd.cs:562-565 re-enters `KillWithoutCheckingWinCondition(creature, force, recursion + 1)` while `creature.IsDead`, up to 10 times before throwing InvalidOperationException; `_resolve_death`'s else-arm simply returns. So a prevention that heals nothing is permanent in the sim and re-kills in the game, and the sim still cannot express a prevention that fails. VERIFIED by reading cmds.py:30-64.
  - citations: CreatureCmd.cs:560-571, CreatureCmd.cs:562-565, cmds.py:30-64, cmds.py:51-64, powers.py:2144-2151

## `power/the_bomb`  → `audit/records/power/the_bomb.json`

- **`power/the_bomb/InstanceType`** (section `hooks`, key `InstanceType`, mechanism `power_cmd/G5`)
  - what: InstanceType
  - issue: RE-VERDICTED 2026-07-26 (review fix pass): `deliberate-divergence` -> `gap`. PowerInstanceType.Instanced (TheBombPower.cs:23) means every application is an independently tracked instance with its own Amount and its own DynamicVars.Damage, where the sim's PowerCmd.apply merges by power id via on_stack (cmds.py:308-311). That MECHANISM is audit/records/seam/power_cmd.json's gap G5, which lists this power among its 11 ported InstanceType units, and it is not re-argued here (binding rule 3); what is per-unit is REACHABILITY of two simultaneous instances and what diverges once they exist. **REACHABILITY: two live fuses are plainly reachable.** The Bomb is a ported Uncommon Colorless Skill with NO Exhaust keyword (cards/colorless_skills.py:715-743, mirroring TheBomb.cs:23-26 whose CanonicalKeywords is empty), so it can simply be played on two consecutive turns. EXECUTED 2026-07-26: play it, end the turn, play it again -- the sim holds ONE `the_bomb` power whose fuse list is [[2, 40], [3, 40]] and whose `amount` is `min(turns for turns, _ in self.bombs)` == 2 (powers.py:3770-3772), where C# holds TWO TheBombPower instances with Amount 2 and Amount 3. **WHAT IS AND IS NOT REPRODUCED.** The DAMAGE is exact, and this unit is one of only two of G5's eleven ported units that acknowledges the distinction at all: each fuse carries its own turns_left and its own damage (powers.py:3768, :3771, set_damage at :3774-3777), the turn-end pass explodes every fuse at <= 1 independently and decrements the rest (:3785-3788), and the power only expires when the list empties (:3799-3800). The STATE is not reproduced, and that is why this is a gap rather than a divergence with the same observable outcome (binding rule 2). A different power list (one entry where the game has two) and a different amount (2 where the game shows 2 and 3) is not an internal detail: full_env.py:412 encodes EVERY ALL_POWERS id as a presence bit plus a signed amount at two scales, so the game's two-instance state cannot even be represented in the sim's observation, and any power-list comparison sees one entry for two. The second residue was already named on this entry before it was graded dd, and stands: an effect that removed 'a' Bomb would remove all of them in the sim, and set_damage always writes the NEWEST fuse (:3777), which is correct only because TheBomb.cs:30 calls SetDamage immediately after applying. **RULE 3:** the identical G5 mechanism is `gap` on power/rolling_boulder, whose two-instance case is reachable exactly the same way (a non-exhausting ported Colorless card played twice). Grading it `deliberate-divergence` here and `gap` there was a rule-3 break, and this is the site that moves: a good workaround makes the gap SMALLER -- the damage is right where rolling_boulder's is arithmetically 5 short on two consecutive turns -- not absent.
  - citations: TheBomb.cs:23-26, TheBomb.cs:30, TheBombPower.cs:23, cards/colorless_skills.py:715-743, cmds.py:308-311, full_env.py:412, powers.py:3768, powers.py:3770-3772

## `power/vital_spark`  → `audit/records/power/vital_spark.json`

- **`power/vital_spark/BeforeCombatStart`** (section `hooks`, key `BeforeCombatStart`, mechanism `power/vital_spark/BeforeCombatStart`)
  - what: BeforeCombatStart
  - issue: Identical shape to GalvanicPower's, one card type over (Skills rather than Powers, Tainted rather than Galvanized): the sim adds a `card.affliction is None` test that VitalSparkPower.cs:33-38 does not have, where C#'s CardCmd.Afflict overwrites. Same mechanism, same verdict (rule 3), same dormancy -- GalvanicPower afflicts the disjoint Power-card set and nothing else afflicts at combat start. Third-file citations, not hashed here.
  - citations: VitalSparkPower.cs:33-38
