# Power tier — gap ledger

Every `gap`-verdict entry in the 138 committed power audit records, in one
place. **Generated from `audit/records/power/*.json`, not hand-written** — each line
traces to a record, and the record carries the full reasoning and the file:line
citations. Regenerate after any record changes rather than editing this file.

Companion documents: `.superpowers/sdd/content-power-report.md` (the stream
report — cross-cutting analysis, census results, cost data),
`content-power-report-a.md` (41 player-side units) and
`content-power-report-b.md` (48 enemy units) for the per-unit evidence.

## Counts

| | |
|---|---|
| units audited | 138 |
| unit rollups | 117 gap, 11 waiver, 7 faithful, 3 deliberate-divergence |
| entries (hooks + guards) | 1193 — 727 faithful, 267 gap, 154 waiver, 45 deliberate-divergence |
| **gap entries** | **267**, across 117 units |
| of which LIVE | **64** |
| of which DORMANT | 135 |
| unclassified | 68 |

LIVE / DORMANT is read from the auditor's own wording in each entry (whichever
marker appears first). An entry counted DORMANT names a trigger — the thing
that would make it observable. **`waiver` entries are NOT in this file**: per
binding rule 1 a waiver means genuinely out of scope, whereas "nothing ported
triggers this" is a dormant gap and is included below.

### Three caveats on these numbers, so they are not over-read

1. **An entry is not a distinct bug.** Binding rule 3 gives one mechanism one
   verdict at every site, so a mechanism shared by sibling units is recorded
   once per unit. The eight `TemporaryStrength`/`TemporaryDexterity` units all
   carry the same `AfterSideTurnEnd` slot gap, for instance — eight entries,
   one defect, one one-line fix. The count of *distinct* live mechanisms is
   roughly 28; they are enumerated in the stream report's sections 0 and 4,
   which is the right place to plan fixes from. Use this file to find every
   site a mechanism touches.
2. **The unclassified bucket UNDER-reports live gaps** — it is a labelling
   artifact, not a severity judgement. Those entries simply never write the
   word LIVE or DORMANT. Several are known-live from the stream report; the
   wrong-RNG-stream class is the clearest case, where `aggression`'s entry
   describes `combat._rng.sample` against C#'s
   `UnstableShuffle(Rng.CombatCardSelection)` and is live per report section 0
   item 4, yet lands here unclassified. **Treat the report, not this bucket, as
   authoritative on severity.** Classifying these entry by entry is outstanding
   work.
3. **Two of these findings would change a committed seam record**
   (`turn_structure` G8 and `power_cmd` G6 flipping to live) and rest on
   witnesses executed by continuation sessions rather than re-derived. Re-run
   those before amending the seam docs.

## How to read a dormant gap

Dormant does not mean harmless. It means no *currently ported* content reaches
the divergent path. Every dormant entry below is a latent bug that activates
the moment its trigger is ported — which is why they are recorded as gaps
rather than waived. When porting new content, grep this file for the mechanism
first.

## LIVE gaps

These diverge from the game on content that is already ported. Ordered by
unit. Full text, because these are the actionable ones.

### `adaptable` — AfterDeath

*(hook; record: `audit/records/power/adaptable.json`)*

THE HOOK IS THE WRONG ONE, and the consequences are observable. C# lets the death HAPPEN: Hook.ShouldDie returns true, CreatureCmd.cs:507-508 fires InvokeDiedEvent and computes shouldRemoveFromCombat = false (from this power's ShouldCreatureBeRemovedFromCombatAfterDeath), then AfterDeath sets isReviving and calls TestSubject.TriggerDeadState() -- leaving the Test Subject **dead at 0 HP, retained in combat**, with the combat held open by ShouldStopCombatFromEnding. The sim instead PREVENTS the death from `should_die` (powers.py:3365-3370 returns False), and cmds.py:106-113 then floors the creature at **1 HP** and leaves `is_dead` False. Three observable consequences. (1) HP: 1 vs 0, which conformance asserts on directly. (2) damage_pipeline gap G4 -- C# locks the AfterDamageReceived-skip decision to the pre-Kill snapshot so an arithmetically lethal hit permanently skips the victim's on-damage-received powers, while the sim resets HP to 1 first and only then tests `target.is_dead` (cmds.py:121), so the Test Subject's Enrage and every other on_damage_received listener fires in the sim and not in the game. Not re-verdicted (rule 3). (3) Feed: `Feed.cs:38` computes `shouldTriggerFatal = cardPlay.Target.Powers.All(p => p.ShouldOwnerDeathTriggerFatal())` and AdaptablePower does NOT override it (default true), and `WasTargetKilled` is true even when the death is prevented (DamageResult.cs:89-99 says so in as many words: "This will return true even if the creature was resurrected afterwards (Fairy in a Bottle, etc.)", with :97 giving exactly that example), so **the game grants the +3 max HP for Feeding the Test Subject to death**; the sim's `if target.is_dead` (cards/feed.py:45) is False, so it grants nothing -- and the sim's own docstring (cards/feed.py:17-18) asserts the opposite behaviour is correct. EXECUTED (sim side): after a lethal Strike the Test Subject sits at hp 1 with `is_dead` False. LIVE: Feed is a ported Rare Ironclad Attack and the Test Subject is the ported Act-3 Glory boss. Third-file citations, not hashed here. **THE SHARED OBSERVABLE, WITH A PORTED WITNESS -- Hook.AfterDeath fires on BOTH C# branches and in the sim on NEITHER.** CreatureCmd.cs:519 dispatches `Hook.AfterDeath(..., wasRemovalPrevented: false, deathAnimLength)` on the real-death branch and CreatureCmd.cs:566 dispatches `Hook.AfterDeath(..., wasRemovalPrevented: true, 0f)` on the prevention branch, in both cases to EVERY hook listener and not just to the dying creature's own powers. The sim fires `hooks.on_death` only on its real-death arm (cmds.py:105); its prevention arm (cmds.py:106-113) fires nothing but `after_preventing_death`. So a death any of these three powers touches reaches NO on_death listener at all. WITNESS: `GremlinHorn.cs:24-32` overrides AfterDeath with **no** `wasRemovalPrevented` guard -- its only test is `target.Side != base.Owner.Creature.Side` -- so the game grants +1 Energy (PlayerCmd.GainEnergy, :29) and DRAWS 1 CARD (CardPileCmd.Draw, :30) every time the Test Subject, the Waterfall Giant, Fogmog's Eye with Teeth or The Obscura's Parafright dies, prevented or not, while `sts2_rl/relics/gremlin_horn.py:18-22` (`def on_death`) never runs on any of them. LIVE: Gremlin Horn is a ported Uncommon relic and all four appliers are ported enemies (monsters/glory/test_subject.py, monsters/underdocks/waterfall_giant.py:148-151 + rooms.py:139, monsters/overgrowth/fogmog.py:34-35, monsters/hive/the_obscura.py:38-39). The extra draw is not cosmetic: it perturbs the draw/discard piles for the rest of the fight and, under conformance replay, the RNG stream for the rest of the run. ONE MECHANISM, ONE VERDICT (binding rule 3): power/adaptable, power/illusion and power/steam_eruption all implement a C# AfterDeath body by returning False from `should_die` (powers.py:3365-3369, :1566-1570, :2016-2020), so all three run the sim's single death-PREVENTION branch (cmds.py:106-113) where C# runs the REAL-DEATH branch (CreatureCmd.cs:505-559). None of the three overrides ShouldDie, and all three bodies sit behind `!wasRemovalPrevented` (AdaptablePower.cs:34, IllusionPower.cs:79, SteamEruptionPower.cs:17) -- i.e. they are explicitly EXCLUDED from the branch the sim is executing. All three therefore carry `gap`; steam_eruption's `deliberate-divergence` was corrected to `gap` in the 2026-07-26 review fix pass. The canonical statement of the shared observable (the Gremlin Horn witness) is on power/adaptable's AfterDeath entry.

### `adaptable` — ShouldAllowHitting

*(hook; record: `audit/records/power/adaptable.json`)*

LIVE, and it upgrades a seam gap that was recorded as un-demonstrated. C#'s PowerCmd.Apply<T> refuses to apply anything to a creature `CanReceivePowers` says no to, and CanReceivePowers reuses Hook.ShouldAllowHitting -- which is exactly what this override's own doc comment says it is for ("This is so the ... doesn't receive POWERS while it is reviving"). The sim wires should_allow_hitting into DamageCmd.deal (cmds.py:51-52) but NOT into PowerCmd.apply, which has no such guard at all. That is `power_cmd` gap **G6**, whose text says "No concrete broken interaction is demonstrated (spot-checked callers apply powers only to already-resolved targets)". **That is now falsified. EXECUTED:** a TEST_SUBJECT_BOSS combat (the Test Subject starts with powers ['adaptable', 'enrage']), a lethal Strike, then Vulnerable 2 from the player -- the sim reports `is_reviving True`, `should_allow_hitting(ts) False`, and then still lands `Vulnerable(2)` on the reviving boss, where the game refuses the application outright. Control: a 10-damage Strike in the same window returns 0 HP lost, proving the predicate IS wired into the damage path and only the power path is missing it. Rule-6 co-occurrence: the Test Subject is the ported Act-3 Glory boss and applies this power to itself at combat start, Bash/Vulnerable is ported basic-pool Ironclad content, and the revive window is the remainder of the player's turn after the killing blow -- the ordinary flow of the fight, not a contrived state. The same override exists on IllusionPower and ReattachPower, so all three carry it. Verdict carried per rule 3; the seam record is not edited (half B does not own audits/seam/** or docs/audit/seams/**) -- reported in .superpowers/sdd/content-power-report-b.md section 2.3. Third-file citations, not hashed here.

### `adaptable` — ShouldCreatureBeRemovedFromCombatAfterDeath

*(hook; record: `audit/records/power/adaptable.json`)*

The sim HAS the hook (hooks.py:603, consumed at cmds.py:102-104 to set `retained_after_death`) and this power does not use it, because the sim took the death-prevention route instead (see the AfterDeath entry). Folded into that entry's mechanism: in C# the Test Subject is a retained corpse and in the sim it is a live creature at 1 HP. Carried separately because it is the specific unused hook, and because ReattachPower one file over DOES implement it (powers.py:2338-2341) -- so the sim is inconsistent between two powers with the same C# shape.

### `battleworn_dummy_time_limit` — AfterSideTurnEnd

*(hook; record: `audit/records/power/battleworn_dummy_time_limit.json`)*

SLOT: a side-scoped C# hook implemented with the sim's PER-CREATURE enemy slot. Hook.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), which for the enemy side is one call after ALL enemies have moved (CombatManager.cs:1256 per turn_structure's G5); the sim's on_enemy_turn_end fires inside _run_enemy_turns once per enemy, immediately after that enemy's own move (combat.py:286-345). The sim's side-scoped slot, on_enemy_side_end (combat.py:345), is available and unused. This is turn_structure's G5 and is not re-verdicted here per binding rule 3. SETTLING THE MAIN REPORT'S SECTION 6 ITEM 1, WHICH NAMED THIS UNIT: the report argued G5's dormancy rests on 'every ported listener self-filters to its own owner', which does not reach a listener that ESCAPES its owner mid-dispatch, and speculated that 'the Battle Friend encounters field more than one dummy, so the sim can remove dummy #1 before dummy #2 acts'. That speculation is FALSE and the correction is a narrowing, not a widening: C#'s BattlewornDummyEventEncounter.GenerateMonsters (BattlewornDummyEventEncounter.cs:62-72) returns a _ReadOnlySingleElementList -- exactly ONE dummy, chosen by Setting -- and the sim's three encounters each list a single monster class (monsters/glory/battle_friend.py:67-78). There is never a second dummy, and the dummy's only move is NOTHING_MOVE (battle_friend.py:31-36), so no other creature's turn can be affected by the escape either. This unit therefore does NOT make G5 live; the remaining two candidates (asleep, slumber) are half B's. Recorded as a gap on the slot itself, which is G5's shape regardless.

### `burrowed` — AfterBlockBroken

*(hook; record: `audit/records/power/burrowed.json`)*

The trigger condition is right and the trigger POINT is right -- C# fires on the block actually breaking and cmds.py:78-81 reproduces the exact `Block <= 0 && blockedDamage > 0` semantics its own comment quotes (an exact break counts). Two divergences. (1) The STUN's follow-up state is dropped: C# calls `tunneler.GetStunned()` and THEN `CreatureCmd.Stun(base.Owner, tunneler.StillDizzyMove, "BITE_MOVE")` (BurrowedPower.cs:31-32), naming the state the Tunneler resumes on; the sim calls only `self.owner.get_stunned()` (powers.py:2298) and leaves the stun and the resume state to the monster. Whether that is observable is the Tunneler's record to settle, but it is the same shape as FlutterPower's -- where the follow-up-state re-roll turned out to be a LIVE wrong-RNG-stream gap (audits/power/flutter.json) -- so it is flagged, not waived. (2) ORDER: C# stuns first and removes the power SECOND (BurrowedPower.cs:32-33); the sim expires first (powers.py:2299) and then zeroes the block. Since AfterRemoved is what dumps the block, C#'s stun happens while the block is still there and the sim's after it is gone. Third-file citations, not hashed here.

### `calamity` — AfterCardPlayed

*(hook; record: `audit/records/power/calamity.json`)*

TWO gaps. (1) THE COUNT: hook_dispatch's G4 (the per-CardPlay bracket): CardModel.cs:1904-1965 constructs a fresh CardPlay per replay iteration and fires Hook.BeforeCardPlayed (:1929) and Hook.AfterCardPlayed (:1959) INSIDE the loop, where combat.py:466 fires before_card_played once before the `for _ in range(play_count)` loop (:477-494) and combat.py:514 fires on_card_played once after it. Already LIVE with a Throwing Axe / Pen Nib witness and not re-verdicted here per binding rule 3; what is recorded is this unit's share of it. Throwing Axe is a ported relic from the ported Tanx shrine (relics/throwing_axe.py:30-36, events/tanx.py:13) and power/duplication and power/one_two_punch are ported replay sources. C# fires BeforeCardPlayed/AfterCardPlayed once per replay iteration, so a Throwing-Axe-doubled or Duplication-doubled Attack generates 2 x Amount cards in the game and Amount in the sim -- combat.py:514 fires on_card_played once per card, outside the `for _ in range(play_count)` loop. (2) THE RNG AND THE POOL HELPER: C# is CardFactory.GetForCombat(pool.GetUnlockedCards().Where(Type == Attack), base.Amount, Rng.CombatCardGeneration) (CalamityPower.cs:48-50) -- the WITH-replacement variant, one rng.NextItem per card (CardFactory.cs:140-150) -- and the sim calls random_pool_cards(combat._rng, ...) (powers.py:3493), the LEGACY shared-rng helper, where cards.pool.get_for_combat_parity (pool.py:164-179) is an already-written, already-documented parity port of exactly this call and takes the card_gen accessor. WRONG RNG STREAM. combat._rng is the SHARED unseeded random.Random (combat.py:88), NOT the per-purpose accessor object -- that is combat.combat_rng, built one line later at combat.py:94, whose seven named accessors (combat_rng.py:17-25: shuffle, monster_ai, card_gen, card_selection, targets, energy, potion_gen) are GameRandomAdapters over the matching RunRngSet streams in a parity run. This is the sixth and seventh power unit in this stream with the same defect -- power/aggression (main report section 4 item 8), then power/hello_world, power/entropy, power/stampede and power/confused in half A -- so it is systematic, not incidental, and it is proposed as its own PROMPT.md bug class in .superpowers/sdd/content-power-report-a.md section 7 item 11. LIVE under seed parity by construction: the draw comes off a different stream in a different order AND every later consumer of the shared rng is perturbed. Third-file citations, not hashed here. The with-replacement SEMANTICS are right on both sides (duplicates allowed -- the sim's docstring at powers.py:3477-3478 says so, and the C# doc comment at CardFactory.cs:131-133 contrasts it with GetDistinctForCombat, which is the call power/hello_world gets wrong in the other direction).

### `chains_of_binding` — AfterCardDrawn

*(hook; record: `audit/records/power/chains_of_binding.json`)*

Two divergences. (1) A DROPPED GUARD: C# requires `base.CombatState.CurrentSide == base.Owner.Side` (ChainsOfBindingPower.cs:38), so only cards drawn during the PLAYER's own turn are Bound; the sim has no side test (powers.py:3313-3314), so a card drawn during the ENEMY turn is Bound in the sim and not in the game. Enemy-turn draws are ported and ordinary (any enemy effect that makes the player draw), so this is the live-shaped half. (2) The BUDGET is counted differently: C# counts `CardAfflictedEntry` history entries this turn by this actor with a Bound affliction (ChainsOfBindingPower.cs:40) -- so an affliction the player OVERWRITES still consumes budget, and the history is the source of truth -- where the sim keeps its own `_afflicted_this_turn` counter reset at turn end (powers.py:3311, 3335). Equivalent while nothing else writes Bound; the sim's counter is the cheaper model of the same number. Also `Affliction<Bound>().CanAfflict(card)` becomes `card.affliction is None`, which is the sim's affliction-slot rule. DORMANT overall pending an executed enemy-turn-draw witness against the Queen (monsters/glory/queen.py); labelled dormant per binding rule 6 rather than asserted live. Third-file citations, not hashed here.

### `confused` — ConfusedPower.cs:53 Rng.CombatEnergyCosts.NextInt(4)

*(guard; record: `audit/records/power/confused.json`)*

WRONG RNG STREAM. C# draws the cost from base.Owner.Player.RunState.Rng.CombatEnergyCosts; powers.py:3873 uses combat._rng.randrange(4), the SHARED unseeded random.Random (combat.py:88). combat.combat_rng.energy (combat_rng.py:23,53 -- the combat_energy_costs stream) is the matching accessor and exists. The range is right on both sides (NextInt(4) is 0..3 and randrange(4) is 0..3, so costs 0-3 inclusive, which the sim's docstring states). Same defect as power/aggression's (main report section 4 item 8) and as power/hello_world, power/entropy and power/stampede in half A, and LIVE for the same reason with an unusually wide blast radius: Confused re-costs EVERY card the player draws for the whole combat, so under seed parity a Snecko Eye run diverges on the first draw and every shared-rng consumer after it is perturbed. Both appliers are ported relics (relics/snecko_eye.py:25-28, relics/fake_snecko_eye.py:19-22), so binding rule 6 is satisfied. Third-file citations, not hashed here.

### `crimson_mantle` — AfterPlayerTurnStart

*(hook; record: `audit/records/power/crimson_mantle.json`)*

SLOT. AfterPlayerTurnStart is a POST-draw hook -- hook_dispatch step 27 lists Hook.AfterPlayerTurnStart (882-908) among the multi-pass dispatchers and it runs after SetupPlayerTurn -- but the sim uses on_player_turn_start, which is pre-draw. The sim's on_player_turn_start (player.py:169) fires BEFORE the hand draw (player.py:171-186); C#'s side-turn-start dispatchers run at CombatManager.cs:458 (Before) and :522 (After), i.e. either side of SetupPlayerTurn at :514. EXECUTED: a spy records [on_player_turn_start hand=5, 4x on_card_drawn, on_player_turn_started hand=9]. Third-file citations, not hashed here. Live consequence: Crimson Mantle deals self-damage and then gains block at turn start, and the sim does both BEFORE the hand is drawn. A lethal self-damage tick therefore cancels a hand draw the game has already made, which is conformance-visible even when nothing dies because the draw consumes rng; and Hellraiser's draw-triggered auto-plays (powers.py:707-713) see the block in the sim and not in the game. The sim also has the correct slot available and unused: on_player_turn_started (player.py:186), which is where the phase-aware relics live.

### `curious` — TryModifyEnergyCostInCombat

*(hook; record: `audit/records/power/curious.json`)*

The arithmetic is exact -- originalCost - Amount floored at 0, with the Power-card and cost > 0 gates (CuriousPower.cs:19-31 vs powers.py:2885-2887). What is wrong is everything about HOW it is dispatched, and this power is already the executed witness for two committed seam gaps, so neither is re-verdicted here (binding rule 3): (a) hook_dispatch's G2 (cross-listener order) is LIVE with THIS POWER as its witness -- Curious at 2 stacks on a 1-cost Power card versus the ported relic Spiked Gauntlets (+1): the game runs powers before relics and computes max(0, 1-2) = 0 then +1 = 1, and the sim runs relics before powers and computes 1+1 = 2 then max(0, 2-2) = 0, so the sim charges 0 where the game charges 1. (b) hook_dispatch's G3 (the missing Early/Late phase passes): C#'s TryModifyEnergyCostInCombat is the EARLY pass and TryModifyEnergyCostInCombatLate is a separate later one, which is why power/free_attack and the ported relic Brilliant Scarf are guaranteed to run after Curious in the game and are a coin flip in the sim. Recorded as a gap on this unit because the observable cost of a card is wrong, not merely the ordering.

### `curl_up` — AfterDamageReceived

*(hook; record: `audit/records/power/curl_up.json`)*

LIVE. C# only LATCHES the triggering card here and grants nothing; the sim grants the block and expires the power on the spot. CurlUpPower.cs:34-54 records cardSource in internal Data and returns; CurlUpPower.cs:56-70 (AfterCardPlayed) is what actually gains the block, once the whole card play has resolved. powers.py:309-312 calls BlockCmd.apply and _expire() inside the damage hook, so the SECOND and later hits of a multi-hit attack are absorbed by block the game would not have granted yet. The sim's own docstring (powers.py:307-308) states the weaker half of this correctly -- the triggering hit does take full damage -- and then stops one hit short. Reachable from the ported Louse Progenitor (monsters/hive/louse_progenitor.py:34) plus any multi-hit Attack card. Two guards are also missing; see their own entries. Third-file citations, not hashed here.

### `dark_embrace` — AfterSideTurnEnd

*(hook; record: `audit/records/power/dark_embrace.json`)*

DarkEmbracePower.cs:52-60 is half of a two-phase mechanism the sim has none of: an exhaust caused by the Ethereal keyword increments an internal etherealCount instead of drawing, and this hook then draws `Amount * etherealCount` at the side's end -- deliberately, so the cards arrive AFTER the hand flush rather than being flushed away (the source comment at :18-23 says exactly that). The sim draws immediately for every exhaust (powers.py:251-255), so cards Dark Embrace draws off a turn-end ethereal exhaust are discarded by the flush that follows. LIVE the moment both halves co-occur: the sim's ethereal exhausts happen at turn end (cards/base.py's ethereal handling via combat._process_turn_end_cards, combat.py:352-...) and should_ethereal_trigger exists as a hook (hooks.py:696), while Dark Embrace is a ported card. The sim's on_card_exhausted signature (hooks.py:415-419) has no causedByEthereal argument, so this cannot be fixed inside the power. Same missing parameter that turn_structure's gaps G4/G17 record for Joss Paper; per binding rule 3 the shared mechanism -- the absent causedByEthereal flag -- keeps that `gap` verdict, and what is recorded here is Dark Embrace's own deferral being absent on top of it. Third-file citations, not hashed here.

### `dark_shackles` — AfterSideTurnEnd (inherited, TemporaryStrengthPower.cs:173-181)

*(hook; record: `audit/records/power/dark_shackles.json`)*

SLOT, and LIVE. Inherited from TemporaryStrengthPower.cs:173-181 (a third file, not hashed by this record). AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end (powers.py:866-868) is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. EXECUTED WITNESS for this family, and the sharpest one found for the whole player-side AfterSideTurnEnd group so far, because the effect is a damage-relevant stat revert rather than a bookkeeping reset: with StampedePower 1 (powers.py:1025-1041, which auto-plays an Attack from its own on_player_turn_end) and SetupStrikePower 2, one Strike in hand, ending the turn deals 6 damage when the temp-strength power was applied FIRST (its revert runs first, so the auto-played Strike loses the +2) and 8 when Stampede was applied first. The game always deals 8. So the sim is wrong on exactly the registration orders where the temp power lands before Stampede -- decided by which card the player happened to play first. The enemy-owned leg (powers.py:870-872, on_enemy_side_end, combat.py:345) is in the RIGHT slot; only the player-owned leg is misplaced. Reproduce the affected population with py audit/tools/power_census.py slots.

### `demon_form` — AfterSideTurnStart

*(hook; record: `audit/records/power/demon_form.json`)*

LIVE. The player leg is one slot too early. The sim's on_player_turn_start (player.py:169) fires BEFORE the hand draw (player.py:171-186), where C#'s AfterSideTurnStart is dispatched at CombatManager.cs:522, AFTER SetupPlayerTurn (:514) has reset energy and drawn the hand. The sim's matching post-draw slot is on_player_turn_started (player.py:186). EXECUTED: a spy listener records [on_player_turn_start hand=5, 4x on_card_drawn, on_player_turn_started hand=9]. Third-file citations, not hashed here. EXECUTED co-occurrence witness with two ported Ironclad cards: Hellraiser (cards/hellraiser.py:36) auto-plays every Strike as it is DRAWN, and Demon Form (cards/demon_form_card.py:35) grants Strength at turn start. On the turn after Demon Form 2 is played, the sim grants the Strength first and the auto-played Strikes take it with them -- enemy HP 41 -> 17 -- where the game draws (and auto-plays) first and only then grants Strength, leaving those Strikes un-Strengthened at 41 -> 23. Six damage the game never deals. The enemy leg (powers.py:221-224) has the separate per-creature problem recorded in its own guard entry.

### `draw_cards_next_turn` — AfterSideTurnStart

*(hook; record: `audit/records/power/draw_cards_next_turn.json`)*

Right slot, wrong condition, and the wrongness is reachable. DrawCardsNextTurnPower.cs:37 removes the power only when participants.Contains(base.Owner) AND base.AmountOnTurnStart != 0; powers.py:2753-2754 expires it whenever the owner's turn starts, with no AmountOnTurnStart test. PowerModel.AmountOnTurnStart has NO sim counterpart -- grep -rn amount_on_turn_start sts2_rl/ returns nothing. C# snapshots every power's Amount into it in Creature.BeforeTurnStart (Creature.cs:673-679), called at CombatManager.cs:453 before any other turn-start hook, and its documented purpose (PowerModel.cs:199-205) is exactly 'preventing same-turn activation when applied mid-turn (e.g., via auto-play effects)'. Only three ported powers read it -- this one, HelloWorldPower and SummonNextTurnPower (unported) -- so it is a small but load-bearing field. LIVE, and it widens turn_structure's G8. MayhemPower is C#'s third ported AfterAutoPrePlayPhaseEntered implementer (MayhemPower.cs, applied by the ported Colorless power card Mayhem -- cards/colorless_powers.py:157-174) and G8 does not name it: G8 calls its AutoPrePlay half DORMANT on the grounds that 'the two ported users' effects (play cards / auto-play one Skill) do not read any other turn-start listener's output', naming only Whispering Earring and the Imbued enchantment. Mayhem does read another turn-start listener's output, and the pair is executed below. In the game Mayhem's auto-plays run at CombatManager.cs:568, strictly AFTER Hook.AfterSideTurnStart (:522) and after SetupPlayerTurn's Hook.AfterPlayerTurnStart (:675); in the sim MayhemPower.on_player_turn_started (powers.py:3572-3583) sits in the same dispatch as this power, so the order is registration order. EXECUTED with DrawCardsNextTurnPower 2 (from the ported card Relax, cards/relax.py:38-46) + MayhemPower 1 and Relax on top of the draw pile: applying the power FIRST expires it, Mayhem then plays Relax and the fresh Draw Cards Next Turn(2) survives to next turn -- which is the GAME's answer, because the game removes the old instance at :522 and only reaches Mayhem at :568. Applying Mayhem first makes Relax stack onto the live instance (2 -> 4) and the expiry then deletes the whole thing, so the player loses TWO bonus draws the game grants. Also note the sim uses _expire (powers.py:86-92) where C# uses PowerCmd.Remove (PowerCmd.cs:287-295): the sim skips power.AfterRemoved (PowerCmd.cs:293), which this power does not implement, so that half is inert -- same finding and same treatment as the _expire entries in audits/power/duplication.json and audits/power/flame_barrier.json.

### `duplication` — AfterSideTurnEnd

*(hook; record: `audit/records/power/duplication.json`)*

SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and before the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. THE CONCRETE ROUTE, which applies to every unit in this group: StampedePower (powers.py:1025-1041) auto-plays Attack cards from ITS on_player_turn_end, i.e. inside the very same dispatch, and _process_turn_end_cards runs more card effects immediately after. So in the game every turn-end auto-play happens while this power is still on the creature, and in the sim whether it does depends on hook-registration order between this power and Stampede. Reproduce the affected population with `py audit/tools/power_census.py slots`. Third-file citations, not hashed here. LIVE for this unit: Stampede's turn-end auto-plays are Attack card plays, and Duplication doubles ANY card the owner plays (DuplicationPower.cs:18-25 has no card-type test). In the game Duplication is still present for them; in the sim it is present only if it happens to be registered after Stampede in _listeners. Both are ported (Duplicator potion aside, Stampede is powers.py:1017-1041 and Duplication is reachable from the potion; a non-potion Duplication source would remove the scope caveat entirely).

### `entropy` — EntropyPower.cs:31 the transform rolls on Rng.CombatCardSelection

*(guard; record: `audit/records/power/entropy.json`)*

WRONG RNG STREAM. C# threads player.RunState.Rng.CombatCardSelection into CardCmd.TransformToRandom (CardCmd.cs:323-326 -> Transform), so every replacement is drawn from the CombatCardSelection stream in a fixed order. The sim's CardCmd.transform_to_random picks with hooks.combat._rng.choice(options) (cmds.py:435), and combat._rng is the SHARED unseeded random.Random (combat.py:88), not the per-purpose accessor -- combat.combat_rng.card_selection (combat_rng.py:21,51) is the matching stream and exists. Identical defect to the one already recorded LIVE on power/aggression (report section 4 item 8), and live for the same reason: under seed parity any replay that plays Entropy diverges on the replacement card AND perturbs every later draw off the shared rng. Entropy is a ported Colorless power card (cards/colorless_powers.py:74-94), so both halves of binding rule 6 hold. The option set itself is faithful -- cards.pool.transform_options_in_combat (pool.py:212-226) is a documented port of CardFactory.GetDefaultTransformationOptions(isInCombat: true). Third-file citations, not hashed here.

### `feeding_frenzy` — AfterSideTurnEnd (inherited, TemporaryStrengthPower.cs:173-181)

*(hook; record: `audit/records/power/feeding_frenzy.json`)*

SLOT, and LIVE. Inherited from TemporaryStrengthPower.cs:173-181 (a third file, not hashed by this record). AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end (powers.py:866-868) is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. EXECUTED WITNESS for this family, and the sharpest one found for the whole player-side AfterSideTurnEnd group so far, because the effect is a damage-relevant stat revert rather than a bookkeeping reset: with StampedePower 1 (powers.py:1025-1041, which auto-plays an Attack from its own on_player_turn_end) and SetupStrikePower 2, one Strike in hand, ending the turn deals 6 damage when the temp-strength power was applied FIRST (its revert runs first, so the auto-played Strike loses the +2) and 8 when Stampede was applied first. The game always deals 8. So the sim is wrong on exactly the registration orders where the temp power lands before Stampede -- decided by which card the player happened to play first. The enemy-owned leg (powers.py:870-872, on_enemy_side_end, combat.py:345) is in the RIGHT slot; only the player-owned leg is misplaced. Reproduce the affected population with py audit/tools/power_census.py slots.

### `feel_no_pain` — AfterCardExhausted

*(hook; record: `audit/records/power/feel_no_pain.json`)*

LIVE. The block is dealt as POWERED where C# deals it UNPOWERED, so Dexterity and Frail modify Feel No Pain's block in the sim and not in the game. FeelNoPainPower.cs:23 is `CreatureCmd.GainBlock(Owner, Amount, ValueProp.Unpowered, null)`; powers.py:241 is `BlockCmd.apply(self.hooks, self.owner, self.amount)` with NO props, and BlockCmd.apply defaults props to ValueProp.MOVE (cmds.py:143-144), which is_powered_attack accepts (valueprops.py:47-49 -- Move and not Unpowered), so cmds.py:146-147 runs both block modifier families over it. With Dexterity 3 the sim's Feel No Pain 3 gives 6 block and the game gives 3; under Frail the sim gives 2 and the game 3. Both halves are Ironclad-reachable from the base pool -- Feel No Pain is a ported card (cards/feel_no_pain_card.py) and Frail is applied by ported monsters. Note that eight sibling powers get this right by passing `props=ValueProp.UNPOWERED` explicitly (CrimsonMantle powers.py:648, Rage :1010, Plating :1061, ToricToughness :1218, Skittish :1816-1818, CrabRage :2519, Rampart :3002, BlockNextTurn :3846-3848), so this is an omission at one call site rather than a missing concept. Third-file citations, not hashed here.

### `flex_potion` — AfterSideTurnEnd (inherited, TemporaryStrengthPower.cs:173-181)

*(hook; record: `audit/records/power/flex_potion.json`)*

SLOT, and LIVE. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and the flush (combat.py:661-662); the correct slot, after_player_turn_end (combat.py:665), exists and is unused. The mechanism was EXECUTED on power/setup_strike (StampedePower 1 + a temp-strength power: ending the turn deals 6 or 8 depending on registration order where the game always deals 8), and this unit shares that code path verbatim. Same mechanism as the audited siblings power/setup_strike, power/mangle, power/reptile_trinket and power/shackling_potion; one verdict per mechanism under binding rule 3, so this record cross-references rather than re-argues. Third-file citations (TemporaryStrengthPower.cs), not hashed by this record.

### `flutter` — AfterDamageReceived

*(hook; record: `audit/records/power/flutter.json`)*

The hook SLOT and the guards are right -- FlutterPower.cs:39 is AfterDamageReceived, cmds.py:121-122 is the sim's after-damage slot with the same CreatureBlow guard, and powers.py:2218 reproduces all three C# conditions (`target == base.Owner && result.UnblockedDamage != 0 && props.IsPoweredAttack()`), this one correctly reading `props` through valueprops.is_powered_attack rather than the card. THE GAP IS THE RNG STREAM of the stun's move re-roll. C# takes the next state from the last logged state on the MonsterAi stream: `base.Owner.Monster.MoveStateMachine.StateLog.Last().GetNextState(base.Owner, base.Owner.Monster.RunRng.MonsterAi)` (FlutterPower.cs:47). The sim calls `machine.roll_move(self.owner, self.owner._rng)` (powers.py:2233-2235) -- `_rng`, the monster's construction rng, NOT `_move_rng`. Every other move roll in the sim goes through MachineMonster._move_rng (monsters/state_machine.py:306-312), which is `self._hooks.combat.combat_rng.monster_ai` and whose own comment says move selection "happens on the MonsterAi stream (mirrors MonsterModel.RollMove(targets) => MoveStateMachine.RollMove(..., RunRng.MonsterAi))" and that `self._rng` "is untouched". EXECUTED on a parity combat seeded 933T39V18D with THIEVING_HOPPER_WEAK: `combat_rng.is_parity` True, `hopper._rng` is a plain `random.Random` and `hopper._move_rng` is a `GameRandomAdapter`, and `_rng is _move_rng` is False -- so the roll draws from the wrong stream and, worse, does not advance MonsterAi at all. This is the same defect class as power/aggression's (wrong RNG stream), and it is LIVE: the Thieving Hopper is ported Hive content, it self-applies Flutter (monsters/hive/thieving_hopper.py:113-114), and exhausting those stacks with powered attacks is the ordinary way to fight it, so any conformance replay that stuns a hopper diverges on the move chosen AND on every later MonsterAi draw. Secondary: `roll_move` MUTATES the machine's current state where C#'s `StateLog.Last().GetNextState(...)` queries the last LOGGED state, which are not the same state once a ConditionalBranchState is involved. Third-file citations, not hashed here.

### `free_attack` — BeforeCardPlayed

*(hook; record: `audit/records/power/free_attack.json`)*

DELIBERATE SLOT CHOICE plus an inherited bracket gap. The sim consumes the stack from on_energy_spent rather than before_card_played, and its docstring (powers.py:1137-1141) gives the reason: on_energy_spent fires before the card resolves, so the stack Unrelenting applies during its own resolution is not immediately eaten by Unrelenting itself, and auto-plays fire it with 0 energy so they consume a stack just as C#'s BeforeCardPlayed does. That reasoning holds and the choice is sound. What is a gap is the COUNT: hook_dispatch's G4 (the per-CardPlay bracket): CardModel.cs:1904-1965 constructs a fresh CardPlay per replay iteration and fires Hook.BeforeCardPlayed (:1929) and Hook.AfterCardPlayed (:1959) INSIDE the loop, where combat.py:466 fires before_card_played once before the `for _ in range(play_count)` loop (:477-494) and combat.py:514 fires on_card_played once after it. Already LIVE with a Throwing Axe / Pen Nib witness and not re-verdicted here per binding rule 3; what is recorded is this unit's share of it. Throwing Axe is a ported relic from the ported Tanx shrine (relics/throwing_axe.py:30-36, events/tanx.py:13) and power/duplication and power/one_two_punch are ported replay sources. C#'s BeforeCardPlayed fires once per replay iteration, so a Throwing-Axe-doubled or Duplication-doubled Attack consumes TWO Free Attack stacks in the game; the sim's on_energy_spent fires once per play (combat.py:552 / the energy-spend site), so it consumes ONE -- and the second, free iteration of that Attack is free in both, so the player keeps a stack the game spends. Also the pile guard C#'s BeforeCardPlayed repeats (FreeAttackPower.cs:48-59) is absent for the same reason as above; that half is dormant because a card being played is in Hand or Play by definition.

### `free_attack` — TryModifyEnergyCostInCombatLate

*(hook; record: `audit/records/power/free_attack.json`)*

This power is hook_dispatch G3's LIVE WITNESS from the Late side, and the witness is recorded there with an execution: Tangled (TryModifyEnergyCostInCombat, the EARLY pass, ported at powers.py:1486-1502 and applied by the ported Act-1 Vine Shambler) adds 1 to an Entangled Attack's cost and Free Attack (TryModifyEnergyCostInCombatLate, ported here) sets it to 0. The game always runs Tangled first and Free Attack last, so the Attack is free; in the sim applying Free Attack BEFORE Tangled leaves the Strike at cost 1 and applying Tangled first leaves it at 0. Same gap from this end, same verdict per binding rule 3; power/curious is the other content half of the same phase question. SEPARATELY, this record's own finding: the PILE GUARD is missing. FreeAttackPower.cs:26-39 only zeroes the cost when card.Pile?.Type is Hand or Play; powers.py:1147-1151 zeroes every Attack of the owner's regardless of where it sits, and has no owner test either (a single-player tautology). DORMANT for the cost itself -- energy is only spent for a card played from hand -- but the sim's modify_card_energy_cost is also what previews.py and the RL observation read, so an Attack sitting in the draw or discard pile displays as free where the game shows its real cost. Named trigger: any effect that reads a non-hand card's cost, of which the observation vector is one.

### `hardened_shell` — AfterDamageReceived

*(hook; record: `audit/records/power/hardened_shell.json`)*

Two divergences, one of them the killing-blow class. (a) KILLING BLOW: the sim's on_damage_received is skipped when the hit killed the victim (cmds.py:121, the CreatureCmd.cs:392 guard) -- and C#'s AfterDamageReceived is subject to the same guard, so that half is faithful. (b) The GUARD DIFFERS: C# returns on `result.WasFullyBlocked` and then accumulates `result.UnblockedDamage` (HardenedShellPower.cs:57-62), where the sim accumulates its `amount` argument on `amount > 0` (powers.py:1993-1994). The sim's argument is hp_lost -- POST-block AND POST-modify_hp_lost (cmds.py:86, 122) -- so the cap counts the ALREADY-CAPPED figure, whereas C# counts UnblockedDamage, which is post-block but PRE-HP-loss-modifier. On any hit the cap actually truncates, the game charges the full unblocked amount against the shell and the sim charges only the truncated amount, so the sim's shell absorbs MORE total damage per turn than the game's. LIVE-shaped and reachable in principle the first time the player over-hits a Skulking Colony (a ported Underdocks elite, monsters/underdocks/skulking_colony.py:32-33, Hardened Shell 20 matching SkulkingColony.cs:56's 20m); marked a gap without an executed number because the arithmetic is decided by the same registration order the previous entry executed, and The Boot can re-raise the figure. NOTE the sim's `amount == 0` arm in modify_hp_lost returns `amount` unchanged and the C# one does too, so a fully-blocked hit charges nothing on either side -- WasFullyBlocked is redundant rather than dropped. Third-file citations, not hashed here.

### `hello_world` — HelloWorldPower.cs:27 the draw runs on Rng.CombatCardGeneration via TakeRandom

*(guard; record: `audit/records/power/hello_world.json`)*

WRONG RNG STREAM AND WRONG ALGORITHM, and the correct sim helper exists and is not used. C# is CardFactory.GetDistinctForCombat(..., base.AmountOnTurnStart, base.Owner.Player.RunState.Rng.CombatCardGeneration) (HelloWorldPower.cs:25-27 -> CardFactory.cs:119-129), whose selection is FilterForCombat(...).TakeRandom(count, rng) == UnstableShuffle(rng).Take(n) (IEnumerableExtensions.cs:17). The sim does combat._rng.sample(commons, n) (powers.py:2826), and combat._rng is the SHARED unseeded random.Random (combat.py:88) -- not the per-purpose accessor. cards.pool.get_distinct_for_combat_parity (pool.py:182-204) is an already-written, already-documented parity port of exactly this call, driven by combat.combat_rng.card_gen (combat_rng.py:20,50), and this power does not call it; seven other sites do (potions.py:435,468,1082,1120,1148, cards/infernal_blade.py:40, relics/vexing_puzzlebox.py:26). LIVE under seed parity: the identical defect is already recorded LIVE on power/aggression (report section 4 item 8), and here it is sharper because a wrong-stream/wrong-algorithm pick both names a different card AND perturbs every later draw off the shared rng. Hello World is a ported Trash Heap card (cards/trash_heap_cards.py:186-203) reachable from the ported Trash Heap event, so both halves of binding rule 6 hold. Third-file citations, not hashed here.

### `illusion` — ShouldAllowHitting

*(hook; record: `audit/records/power/illusion.json`)*

LIVE, and it upgrades a seam gap that was recorded as un-demonstrated. C#'s PowerCmd.Apply<T> refuses to apply anything to a creature `CanReceivePowers` says no to, and CanReceivePowers reuses Hook.ShouldAllowHitting -- which is exactly what this override's own doc comment says it is for ("This is so the ... doesn't receive POWERS while it is reviving"). The sim wires should_allow_hitting into DamageCmd.deal (cmds.py:51-52) but NOT into PowerCmd.apply, which has no such guard at all. That is `power_cmd` gap **G6**, whose text says "No concrete broken interaction is demonstrated (spot-checked callers apply powers only to already-resolved targets)". **That is now falsified. EXECUTED:** a TEST_SUBJECT_BOSS combat (the Test Subject starts with powers ['adaptable', 'enrage']), a lethal Strike, then Vulnerable 2 from the player -- the sim reports `is_reviving True`, `should_allow_hitting(ts) False`, and then still lands `Vulnerable(2)` on the reviving boss, where the game refuses the application outright. Control: a 10-damage Strike in the same window returns 0 HP lost, proving the predicate IS wired into the damage path and only the power path is missing it. Rule-6 co-occurrence: the Test Subject is the ported Act-3 Glory boss and applies this power to itself at combat start, Bash/Vulnerable is ported basic-pool Ironclad content, and the revive window is the remainder of the player's turn after the killing blow -- the ordinary flow of the fight, not a contrived state. The same override exists on IllusionPower and ReattachPower, so all three carry it. Verdict carried per rule 3; the seam record is not edited (half B does not own audits/seam/** or docs/audit/seams/**) -- reported in .superpowers/sdd/content-power-report-b.md section 2.3. Third-file citations, not hashed here. NOTE (2026-07-26 fix pass): this record's own appliers make the finding reachable without the Test Subject at all -- Parafright (monsters/hive/the_obscura.py:38-39) and Eye with Teeth (monsters/overgrowth/fogmog.py:34-35) each open the same revive window on their own "death", and a Vulnerable landed on either during it is refused by the game and accepted by the sim.

### `improvement` — ImprovementPower.cs:26 Rng.CombatCardSelection

*(guard; record: `audit/records/power/improvement.json`)*

Recorded so it is not lost when the effect is implemented: the picks must come off combat.combat_rng.card_selection (combat_rng.py:21,51), not combat._rng. WRONG RNG STREAM. combat._rng is the SHARED unseeded random.Random (combat.py:88), NOT the per-purpose accessor object -- that is combat.combat_rng, built one line later at combat.py:94, whose seven named accessors (combat_rng.py:17-25: shuffle, monster_ai, card_gen, card_selection, targets, energy, potion_gen) are GameRandomAdapters over the matching RunRngSet streams in a parity run. This is the sixth and seventh power unit in this stream with the same defect -- power/aggression (main report section 4 item 8), then power/hello_world, power/entropy, power/stampede and power/confused in half A -- so it is systematic, not incidental, and it is proposed as its own PROMPT.md bug class in .superpowers/sdd/content-power-report-a.md section 7 item 11. LIVE under seed parity by construction: the draw comes off a different stream in a different order AND every later consumer of the shared rng is perturbed. Third-file citations, not hashed here. Currently vacuous because the effect does not exist, but it is the seventh instance of the same trap and the one most likely to be reintroduced, since whoever implements the upgrade will not be reading this power's C# for the stream name.

### `juggernaut` — JuggernautPower.cs:24 Rng.CombatTargets.NextItem(hittableEnemies)

*(guard; record: `audit/records/power/juggernaut.json`)*

WRONG RNG STREAM. combat._rng is the SHARED unseeded random.Random (combat.py:88), NOT the per-purpose accessor object -- that is combat.combat_rng, built one line later at combat.py:94, whose seven named accessors (combat_rng.py:17-25: shuffle, monster_ai, card_gen, card_selection, targets, energy, potion_gen) are GameRandomAdapters over the matching RunRngSet streams in a parity run. This is the sixth and seventh power unit in this stream with the same defect -- power/aggression (main report section 4 item 8), then power/hello_world, power/entropy, power/stampede and power/confused in half A -- so it is systematic, not incidental, and it is proposed as its own PROMPT.md bug class in .superpowers/sdd/content-power-report-a.md section 7 item 11. LIVE under seed parity by construction: the draw comes off a different stream in a different order AND every later consumer of the shared rng is perturbed. Third-file citations, not hashed here. Juggernaut is the sharpest of the seven because the RIGHT accessor is used three lines away in the same engine for the same purpose: combat.py:546 picks an auto-play's target with combat_rng.targets.choice(living), citing CardCmd.cs:77's Rng.CombatTargets.NextItem(HittableEnemies) -- and powers.py:792 picks Juggernaut's target with combat._rng.choice(living). Same C# stream, two different sim objects. Reachable: Juggernaut is a ported Ironclad card power and every block gain triggers it, so a Juggernaut run diverges on its first block in a multi-enemy fight.

### `juggling` — AfterCardPlayed

*(hook; record: `audit/records/power/juggling.json`)*

The copy is rebuilt from the class rather than cloned. JugglingPower.cs:48 is `cardPlay.Card.CreateClone()`, which reproduces the card's full live state; powers.py:827-829 constructs `type(card)()` and replays `card.upgrade_level` upgrades onto it. Upgrade level is therefore carried and everything else is not -- a this-combat cost override (ConfusedPower/Snecko Eye, powers.py:3867-3873), an affliction (Entangled, Ringing, Smog, Bound, Galvanized, Tainted -- all ported), or a this-turn free flag would all be dropped. DORMANT only in the narrow sense that no test exercises it; the route is a Snecko-Eye or Tangled combat plus a Juggling trigger, both ported. Named trigger: any Juggling copy of a card carrying per-combat state. Third-file citations, not hashed here.

### `mangle` — AfterSideTurnEnd (inherited, TemporaryStrengthPower.cs:173-181)

*(hook; record: `audit/records/power/mangle.json`)*

SLOT, and LIVE. Inherited from TemporaryStrengthPower.cs:173-181 (a third file, not hashed by this record). AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end (powers.py:866-868) is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. EXECUTED WITNESS for this family, and the sharpest one found for the whole player-side AfterSideTurnEnd group so far, because the effect is a damage-relevant stat revert rather than a bookkeeping reset: with StampedePower 1 (powers.py:1025-1041, which auto-plays an Attack from its own on_player_turn_end) and SetupStrikePower 2, one Strike in hand, ending the turn deals 6 damage when the temp-strength power was applied FIRST (its revert runs first, so the auto-played Strike loses the +2) and 8 when Stampede was applied first. The game always deals 8. So the sim is wrong on exactly the registration orders where the temp power lands before Stampede -- decided by which card the player happened to play first. The enemy-owned leg (powers.py:870-872, on_enemy_side_end, combat.py:345) is in the RIGHT slot; only the player-owned leg is misplaced. Reproduce the affected population with py audit/tools/power_census.py slots.

### `mayhem` — MayhemPower.cs:20 CardPileCmd.AutoPlayFromDrawPile is TWO-PHASE

*(guard; record: `audit/records/power/mayhem.json`)*

CardPileCmd.cs:931-966 first moves ALL `count` cards out of the draw pile into PileType.Play (the loop at :939-955, one ShuffleIfNecessary per pick), and only THEN plays them (:956-965). The sim interleaves: it picks and plays one card per iteration (powers.py:3576-3583). So at Mayhem 2 the game commits both cards before either resolves, and the sim lets the first card's effect change which card is played second. Any draw, reshuffle or pile redirect the first card causes therefore diverges -- and the game's cards sit in PileType.Play while waiting, so a reshuffle the first card triggers excludes them (PROMPT.md bug class 7, pile limbo), where the sim's second pick can be a card the reshuffle just moved. Reachable: Mayhem is Counter-stacked at 1 per play (cards/colorless_powers.py:174), so two plays give Mayhem 2, and the Ironclad pool is full of cards that draw. Not executed -- it needs a two-stack Mayhem plus a drawing first card, and the phase gap above already carries this unit's LIVE label.

### `mayhem` — AfterAutoPrePlayPhaseEntered

*(hook; record: `audit/records/power/mayhem.json`)*

WRONG PHASE, and this is the finding that makes turn_structure's G8 -- the AutoPrePlay and AutoPostPlay phases do not exist in the sim and their two hooks are hand-rolled onto neighbouring slots. Not re-verdicted here (binding rule 3); the verdict is gap on both sides. LIVE on its AutoPrePlay side, which G8 currently calls dormant. G8's dormancy argument is that 'the two ported users' effects (play cards / auto-play one Skill) do not read any other turn-start listener's output', naming Whispering Earring and the Imbued enchantment -- it does not name MayhemPower, which is C#'s third ported AfterAutoPrePlayPhaseEntered implementer and DOES read other turn-start listeners' output. In the game this hook fires at CombatManager.cs:568 -> RunAutoPrePlayPhase (:613-619), strictly AFTER Hook.AfterSideTurnStart (:522) and after SetupPlayerTurn's Hook.AfterPlayerTurnStart (:675); in the sim it shares on_player_turn_started with every AfterSideTurnStart and AfterPlayerTurnStart power, so the order is hook-REGISTRATION order. THREE EXECUTED WITNESSES, all on ported Colorless power cards a single run can hold (binding rule 6 satisfied on both halves of each): (1) PrepTimePower 3 + Mayhem 1, one Strike on top of the draw pile -- Prep Time applied first makes the auto-played Strike deal 9 (6 + Vigor 3, Vigor consumed), Mayhem first makes it deal 6 and leaves Vigor(3) unspent; the game always deals 9. (2) RollingBoulderPower 5 + Mayhem 1 against a 4-HP enemy -- boulder first kills it, ends the combat and Mayhem's own combat.is_over guard (powers.py:3577) leaves the Strike in the DRAW pile; Mayhem first plays it into the DISCARD pile. The game always resolves the boulder first. (3) DrawCardsNextTurnPower 2 (from the ported card Relax) + Mayhem 1 with Relax on top of the draw pile -- power first expires it and the fresh Draw Cards Next Turn(2) survives, which is the game's answer; Mayhem first stacks 2 -> 4 and the expiry then deletes the whole thing, costing the player two bonus draws.

### `minion` — ShouldOwnerDeathTriggerFatal

*(hook; record: `audit/records/power/minion.json`)*

**LIVE.** C# reads this at three card sites -- `Feed.cs:38`, `HandOfGreed.cs:49`, `TheHunt.cs:41`, each `cardPlay.Target.Powers.All(p => p.ShouldOwnerDeathTriggerFatal())` computed BEFORE the attack -- and MinionPower returns **false**, so killing a minion never triggers a Fatal payoff. The sim has no such hook anywhere (`hooks.py` defines none) and `cards/feed.py:45` tests only `target.is_dead`. **EXECUTED**: Feed played into a 4-HP Tough Egg carrying MinionPower 1 raises the sim player's max HP from 80 to 83 (delta **3**) where the game raises it by **0**; the control -- the same egg without MinionPower -- gives delta 3 on both sides, so the divergence is the marker power and nothing else. Rule-6 co-occurrence: Feed is a ported Rare Ironclad Attack (cards/feed.py) and Tough Eggs receive MinionPower from the ported Act-2 Hive Ovicopter (monsters/hive/ovicopter.py:153, `PowerCmd.apply(ctx.hooks, egg, MinionPower, 1, applier=self)`), which lays up to five of them; the Kin Followers are a second ported source. Hand of Greed and The Hunt are unported, so Feed is the whole live surface today -- and it is enough. The fix is a `should_owner_death_trigger_fatal` hook plus one line in cards/feed.py, both outside audit scope. Third-file citations, not hashed here.

### `no_draw` — AfterSideTurnEnd

*(hook; record: `audit/records/power/no_draw.json`)*

SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), which CombatManager calls at :1307 for the player side -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654, before both); the sim's real AfterTurnEnd slot is after_player_turn_end (combat.py:665). Reproduce the affected population with `py audit/tools/power_census.py slots`. Third-file citations, not hashed here. Live consequence for THIS power specifically: the sim removes No Draw before the turn-end card effects and the hand flush, so a turn-end effect that draws (Dark Embrace off an ethereal exhaust, powers.py:251-255) succeeds in the sim and is blocked in the game. Both halves are ported Ironclad-pool content. The sim's correct slot, after_player_turn_end (combat.py:665), is available and unused.

### `nostalgia` — NostalgiaPower.cs:31-42 the history count is over CardPlaysStarted, not CardPlaysFinished

*(guard; record: `audit/records/power/nostalgia.json`)*

C# counts History.CardPlaysStarted entries (pushed at CardModel.cs:1930, once per PLAY-COUNT ITERATION); the sim counts CardPlayedEntry rows, which CombatHistory records from on_card_played -- fired ONCE per card at combat.py:514, outside the `for _ in range(play_count)` loop (:477-494). So any replay source halves the rate at which Nostalgia's allowance is consumed: a Throwing-Axe-doubled or Duplication-doubled Attack advances C#'s count by 2 and the sim's by 1, and the sim then redirects a card the game sends to the discard. This is hook_dispatch's G4 (the per-CardPlay bracket, already LIVE with a Throwing Axe / Pen Nib witness, and whose own text says 'every one of the sim's 48 on_card_played listeners' widens it), so the mechanism is not re-verdicted here per binding rule 3 -- what is recorded is that Nostalgia is one of those listeners and that the consequence is a wrong pile, not just a wrong counter. Reachable: Throwing Axe is a ported relic from the ported Tanx shrine and Nostalgia is a ported Colorless power card (cards/colorless_powers.py:199).

### `one_two_punch` — AfterSideTurnEnd

*(hook; record: `audit/records/power/one_two_punch.json`)*

SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and before the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. THE CONCRETE ROUTE, which applies to every unit in this group: StampedePower (powers.py:1025-1041) auto-plays Attack cards from ITS on_player_turn_end, i.e. inside the very same dispatch, and _process_turn_end_cards runs more card effects immediately after. So in the game every turn-end auto-play happens while this power is still on the creature, and in the sim whether it does depends on hook-registration order between this power and Stampede. Reproduce the affected population with `py audit/tools/power_census.py slots`. Third-file citations, not hashed here. LIVE for this unit for the same reason as power/duplication's, and more sharply: One-Two Punch doubles ATTACKS specifically and Stampede's turn-end auto-plays are exactly Attack plays, so the two powers are made for each other. In the game the stack is still there for them; in the sim it is a registration-order coin flip. Both are ported Ironclad-pool card powers.

### `paper_cuts` — AfterDamageGiven

*(hook; record: `audit/records/power/paper_cuts.json`)*

WRONG HOOK. C#'s `AfterDamageGiven(choiceContext, dealer, result, props, target, cardSource)` is the DEALER-side after-damage event; the sim's counterpart is `on_damage_dealt` (hooks.py:469, fired at cmds.py:123-124), and this power uses `on_damage_received` instead, filtering on `dealer is self.owner`. The reason the port had to do that is itself the finding: cmds.py:123 fires on_damage_dealt only `if dealer is not None and hp_lost > 0`, so the sim's dealer-side event **cannot see a fully-blocked or zero-damage hit at all**, where C#'s AfterDamageGiven sees every one (`result.WasFullyBlocked` is a field it is expected to read). Consequence of the substitution: cmds.py:121's killing-blow guard suppresses the sim's on_damage_received when the hit killed the VICTIM, and cmds.py:119-120's own comment says "AfterDamageGiven (on_damage_dealt) is not guarded and still fires on the kill" -- so this power silently stops working on a lethal hit in the sim and keeps working in the game. Third-file citations, not hashed here. The guards are otherwise exact: `dealer == base.Owner && target.IsPlayer && props.IsPoweredAttack() && result.UnblockedDamage > 0` (PaperCutsPower.cs:18) maps to powers.py:2952-2957, props read directly. `CreatureCmd.LoseMaxHp(choiceContext, target, base.Amount, isFromCard: false)` maps to `CreatureCmd.lose_max_hp(self.hooks, target, self.amount)`; `isFromCard` has no sim analogue and gates only run-history bookkeeping. The live-shaped residue is the killing-blow asymmetry: a lethal hit from the Scroll of Biting costs the player max HP in the game and none in the sim. DORMANT as a practical matter -- max HP on the turn the player dies is only observable through a death PREVENTION, and on that path cmds.py:112 sets hp = 1 so `target.is_dead` is False and the sim fires after all. So the two models agree when Lizard Tail saves you and disagree when nothing does, at which point the run is over. Trigger: any ported effect that reads max HP after a lethal blow. Third-file citations, not hashed here.

### `plating` — BeforeSideTurnEndEarly

*(hook; record: `audit/records/power/plating.json`)*

PHASE, and LIVE with an executed witness. C# is BeforeSideTurnEndEarly -- the EARLY pass of Hook.BeforeTurnEnd (CombatManager.cs:1179) -- and PlatingPower.cs:58-60 says why in as many words: 'We do this in early so that it triggers before end-of-turn damage effects.' The sim has no phase passes at all (hook_dispatch's G3) and fires this from the plain on_player_turn_end dispatch, so the block's position relative to end-of-turn damage is registration order. EXECUTED: Plating 4 on the player plus Constrict 6 (ported -- monsters/overgrowth/slithering_strangler.py:43 applies it to the player, and Constrict's player-owner leg is also on_player_turn_end). Applying Constrict FIRST costs the player 6 HP and leaves 4 unused block; applying Plating first costs 2 HP. The game always costs 2, because Plating's Early pass strictly precedes both the plain BeforeTurnEnd pass and Constrict's own AfterSideTurnEnd at :1307. So the sim can lose up to a full Plating stack of HP every turn on a coin flip. Player-side Plating is reachable four ways: the ported Ironclad card Stone Armor (cards/stone_armor.py:38), the ported relic Gorget (relics/gorget.py:17), a ported Colorless power card (cards/colorless_powers.py:121) and a ported potion (potions.py:592); the Slithering Strangler is a ported Act-1 monster. Binding rule 6 satisfied on both halves. Also note the ENEMY leg uses the per-creature on_enemy_turn_end rather than a side-scoped slot, which is turn_structure's G5 and is not re-verdicted here.

### `prep_time` — Registration-order contention with Mayhem in the collapsed slot

*(guard; record: `audit/records/power/prep_time.json`)*

LIVE, and it widens turn_structure's G8. MayhemPower is C#'s third ported AfterAutoPrePlayPhaseEntered implementer (MayhemPower.cs, applied by the ported Colorless power card Mayhem -- cards/colorless_powers.py:157-174) and G8 does not name it: G8 calls its AutoPrePlay half DORMANT on the grounds that 'the two ported users' effects (play cards / auto-play one Skill) do not read any other turn-start listener's output', naming only Whispering Earring and the Imbued enchantment. Mayhem does read another turn-start listener's output, and the pair is executed below. In the game Mayhem's auto-plays run at CombatManager.cs:568, strictly AFTER Hook.AfterSideTurnStart (:522) and after SetupPlayerTurn's Hook.AfterPlayerTurnStart (:675); in the sim MayhemPower.on_player_turn_started (powers.py:3572-3583) sits in the same dispatch as this power, so the order is registration order. EXECUTED with PrepTimePower 3 + MayhemPower 1 and one Strike on top of the draw pile: applying Prep Time FIRST makes the Mayhem-auto-played Strike deal 9 (6 + Vigor 3, Vigor consumed); applying Mayhem first makes it deal 6 and leaves Vigor(3) unspent. The game always deals 9. Both cards are ported Colorless power cards, so a single run can hold both -- binding rule 6 satisfied on both halves.

### `rage` — AfterSideTurnEnd

*(hook; record: `audit/records/power/rage.json`)*

SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and before the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. THE CONCRETE ROUTE, which applies to every unit in this group: StampedePower (powers.py:1025-1041) auto-plays Attack cards from ITS on_player_turn_end, i.e. inside the very same dispatch, and _process_turn_end_cards runs more card effects immediately after. So in the game every turn-end auto-play happens while this power is still on the creature, and in the sim whether it does depends on hook-registration order between this power and Stampede. Reproduce the affected population with `py audit/tools/power_census.py slots`. Third-file citations, not hashed here. LIVE for this unit: Rage blocks on every Attack the owner plays, and Stampede's turn-end auto-plays are Attack plays, so in the game they each grant Rage block and in the sim they do so only if Rage is registered after Stampede. Both are ported Ironclad card powers (cards/rage_card.py-shaped applier and powers.py:1017-1041).

### `rampart` — RampartPower.cs:23 `CombatManager.Instance.PlayersTakingExtraTurn.Count > 0` -> return

*(guard; record: `audit/records/power/rampart.json`)*

LIVE, and absent from the sim entirely. C# refuses to grant the block on a player's EXTRA turn: _playersTakingExtraTurn is filled at CombatManager.cs:1366-1369 during the side switch and read at :435/:439 inside StartTurn, so it is non-empty when Hook.AfterSideTurnStart fires at :522 for that turn. The sim's extra-turn path (combat.py:648-652) calls `self.player.start_turn()` with no such flag, so on_player_turn_start fires normally and this power grants its block again. EXECUTED: a TURRET_OPERATOR_WEAK combat (enemies [LivingShield(rampart), TurretOperator]) with the ported relic Pael's Eye -- the Turret Operator holds 25 block after the turn-1 start (control, correct in both models); zero it, end the turn WITHOUT playing a card so Pael's Eye claims the extra turn (relics/paels_eye.py:36-40 should_take_extra_turn), and the sim leaves the Turret Operator on 25 block where the game leaves it on 0. Rule-6 co-occurrence, stated explicitly: Pael's Eye comes from the ported Act-2 (Hive) Ancient Pael (events/pael.py:53, OPTION_POOL_3) and Living Shield/Turret Operator is a ported Act-3 (Glory) weak encounter (monsters/glory/turret_operator.py:112-114, monsters/glory/__init__.py:60), and relics persist across acts -- so one run can trivially hold both. Reproduce the extra-turn population with `py audit/tools/power_slot_probes.py extra-turns`. Third-file citations, not hashed here.

### `reattach` — ShouldAllowHitting

*(hook; record: `audit/records/power/reattach.json`)*

LIVE, and it upgrades a seam gap that was recorded as un-demonstrated. C#'s PowerCmd.Apply<T> refuses to apply anything to a creature `CanReceivePowers` says no to, and CanReceivePowers reuses Hook.ShouldAllowHitting -- which is exactly what this override's own doc comment says it is for ("This is so the ... doesn't receive POWERS while it is reviving"). The sim wires should_allow_hitting into DamageCmd.deal (cmds.py:51-52) but NOT into PowerCmd.apply, which has no such guard at all. That is `power_cmd` gap **G6**, whose text says "No concrete broken interaction is demonstrated (spot-checked callers apply powers only to already-resolved targets)". **That is now falsified. EXECUTED:** a TEST_SUBJECT_BOSS combat (the Test Subject starts with powers ['adaptable', 'enrage']), a lethal Strike, then Vulnerable 2 from the player -- the sim reports `is_reviving True`, `should_allow_hitting(ts) False`, and then still lands `Vulnerable(2)` on the reviving boss, where the game refuses the application outright. Control: a 10-damage Strike in the same window returns 0 HP lost, proving the predicate IS wired into the damage path and only the power path is missing it. Rule-6 co-occurrence: the Test Subject is the ported Act-3 Glory boss and applies this power to itself at combat start, Bash/Vulnerable is ported basic-pool Ironclad content, and the revive window is the remainder of the player's turn after the killing blow -- the ordinary flow of the fight, not a contrived state. The same override exists on IllusionPower and ReattachPower, so all three carry it. Verdict carried per rule 3; the seam record is not edited (half B does not own audits/seam/** or docs/audit/seams/**) -- reported in .superpowers/sdd/content-power-report-b.md section 2.3. Third-file citations, not hashed here.

### `reattach` — ShouldOwnerDeathTriggerFatal

*(hook; record: `audit/records/power/reattach.json`)*

Same missing hook as MinionPower's, and here the override is CONDITIONAL: `ShouldOwnerDeathTriggerFatal() => AreAllOtherSegmentsDead()` (ReattachPower.cs:106-109), so killing a segment while others stand must NOT trigger a Fatal payoff and killing the last one must. The sim has no hook and cards/feed.py:45 tests only `target.is_dead`, which is True for a withered segment (it really sits at 0 HP -- the sim's own `_all_others_down` comment says so, powers.py:2334-2336). So Feed on a non-final segment grants +3 max HP in the sim and 0 in the game. LIVE by the same executed witness as power/minion's (Feed x a Minion-marked creature, delta 3 vs 0); reachability here is the ported Hive Decimillipede (monsters/hive/decimillipede.py) plus the ported Rare Ironclad Feed. Third-file citations, not hashed here.

### `reptile_trinket` — AfterSideTurnEnd (inherited, TemporaryStrengthPower.cs:173-181)

*(hook; record: `audit/records/power/reptile_trinket.json`)*

SLOT, and LIVE. Inherited from TemporaryStrengthPower.cs:173-181 (a third file, not hashed by this record). AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end (powers.py:866-868) is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. EXECUTED WITNESS for this family, and the sharpest one found for the whole player-side AfterSideTurnEnd group so far, because the effect is a damage-relevant stat revert rather than a bookkeeping reset: with StampedePower 1 (powers.py:1025-1041, which auto-plays an Attack from its own on_player_turn_end) and SetupStrikePower 2, one Strike in hand, ending the turn deals 6 damage when the temp-strength power was applied FIRST (its revert runs first, so the auto-played Strike loses the +2) and 8 when Stampede was applied first. The game always deals 8. So the sim is wrong on exactly the registration orders where the temp power lands before Stampede -- decided by which card the player happened to play first. The enemy-owned leg (powers.py:870-872, on_enemy_side_end, combat.py:345) is in the RIGHT slot; only the player-owned leg is misplaced. Reproduce the affected population with py audit/tools/power_census.py slots.

### `retain_hand` — AfterSideTurnEnd

*(hook; record: `audit/records/power/retain_hand.json`)*

A DELIBERATE SLOT SHIFT that is observationally correct in the normal case and LIVE through turn_structure's G3 in the extra-turn case. C# decrements at the PLAYER side's Hook.AfterTurnEnd (CombatManager.cs:1307), i.e. after FlushPlayerHand (:1296); the sim ticks at on_enemy_side_end (combat.py:345) instead, and its docstring (powers.py:3531-3533) explains the choice as 'the sim's equivalent post-flush slot'. That reasoning is unnecessary -- after_player_turn_end (combat.py:665) IS the sim's post-flush player-side slot and is unused -- but harmless in the normal case, because on_enemy_side_end still falls between the player's turn N end and turn N+1 start, so Retain Hand 1 retains exactly one hand on both sides. EXECUTED, and it does NOT survive an extra turn: with a should_take_extra_turn listener registered (the shape of the ported Ancient relic Pael's Eye, relics/paels_eye.py:36-46), one end_turn leaves `Retain Hand(1)` still on the player, where the same end_turn without it leaves None. combat.py:648-652 short-circuits at the TOP of end_turn and never reaches on_player_turn_end, the flush, after_player_turn_end OR the enemy turn, so the tick never happens; C# evaluates ShouldTakeExtraTurn in SwitchFromPlayerToEnemySide (CombatManager.cs:1360-1373) AFTER both EndPlayerTurnPhase methods, so Hook.AfterTurnEnd at :1307 has already decremented. The root cause is turn_structure's G3, already LIVE and not re-verdicted; this is a concrete content instance of it, and note that moving the tick to after_player_turn_end would NOT fix it, because combat.py:648-652 skips that too.

### `retain_hand` — ShouldFlush

*(hook; record: `audit/records/power/retain_hand.json`)*

The predicate itself is faithful -- false for the owner, true otherwise (RetainHandPower.cs:19-26), and the sim's should_flush_hand has no player parameter at all, which is a single-player tautology. What differs is what a FALSE result means, and that is turn_structure's G4, which is already LIVE and is not re-verdicted here per binding rule 3: C#'s FlushPlayerHand treats false as 'every card is retained' and then still runs Hook.AfterFlush and PlayerCombatState.EndOfTurnCleanup unconditionally (CombatManager.cs:1327-1346), where combat.py:661-662 guards the whole thing (`if should_flush_hand(): discard_hand()`) so a false result skips on_hand_emptied as well -- and there is no after_flush hook in hooks.py to fire in the first place. Recorded from this end because this power is the sim's ONLY should_flush_hand implementer, i.e. G4's entire trigger surface in the power tier.

### `ringing` — AfterCardEnteredCombat

*(hook; record: `audit/records/power/ringing.json`)*

The owner filter is dropped, which is harmless in single-player, but the SITE is not: C# afflicts from AfterCardEnteredCombat (RingingPower.cs:34-40) and the sim's on_card_entered_combat (hooks.py:402-407) is fired only where the sim happens to call it. Recorded as a gap because the sim's __init__ walks `getattr(owner, 'all_cards', ())` (powers.py:1330) and PlayerCombatState.all_cards is `hand + draw + discard + exhaust` evaluated live (player.py:100-103) -- it does NOT include cards currently in the Play pile, i.e. a card mid-resolution. C# reads PlayerCombatState.AllCards, which is Hand, Draw, Discard, Exhaust AND Play (PlayerCombatState.cs:70-80). So a Ringing applied while a card is mid-play leaves that card unafflicted in the sim and afflicts it in the game. This is PROMPT.md bug class 7 (pile limbo) in its power form. DORMANT: Ringing is applied by an enemy on the enemy's turn, when no player card is mid-play. Named trigger: any player-side or mid-resolution Ringing application. Third-file citations, not hashed here.

### `ritual` — AfterApplied

*(hook; record: `audit/records/power/ritual.json`)*

LIVE. The skip-first-trigger condition is wrong. RitualPower.cs:36-43 sets `WasJustAppliedByEnemy = true` whenever **base.Owner.IsEnemy** -- the applier is not consulted at all. powers.py:194-196 instead sets it when `applier is not None and applier.side != owner.side`. Every ported Ritual source is a MONSTER buffing ITSELF, so the sim's test is False exactly where C#'s is True and the enemy gets one extra Strength application, on the very turn it casts Ritual. EXECUTED with the ported Calcified Cultist (monsters/underdocks/cultists.py:39, `applier=self`): after one round the sim shows Strength(2) and Ritual(2), where the game skips that first AfterSideTurnEnd and shows no Strength at all. Damp Cultist (Ritual 5) and Devoted Sculptor (monsters/glory/devoted_sculptor.py:42) are the same shape, so the error scales with the biggest Ritual in the game. The player-side direction is unaffected: with a player owner C#'s Owner.IsEnemy is false and the sim's applier-side test is also false, so both trigger on the first turn end. Third-file citations, not hashed here.

### `rolling_boulder` — Registration-order contention with Mayhem in the collapsed slot

*(guard; record: `audit/records/power/rolling_boulder.json`)*

LIVE, and it widens turn_structure's G8. MayhemPower is C#'s third ported AfterAutoPrePlayPhaseEntered implementer (MayhemPower.cs, applied by the ported Colorless power card Mayhem -- cards/colorless_powers.py:157-174) and G8 does not name it: G8 calls its AutoPrePlay half DORMANT on the grounds that 'the two ported users' effects (play cards / auto-play one Skill) do not read any other turn-start listener's output', naming only Whispering Earring and the Imbued enchantment. Mayhem does read another turn-start listener's output, and the pair is executed below. In the game Mayhem's auto-plays run at CombatManager.cs:568, strictly AFTER Hook.AfterSideTurnStart (:522) and after SetupPlayerTurn's Hook.AfterPlayerTurnStart (:675); in the sim MayhemPower.on_player_turn_started (powers.py:3572-3583) sits in the same dispatch as this power, so the order is registration order. EXECUTED with RollingBoulderPower 5 + MayhemPower 1 against a 4-HP enemy and one Strike on top of the draw pile: applying the boulder FIRST kills the enemy, ends the combat, and Mayhem's own combat.is_over guard (powers.py:3577) then leaves the Strike in the DRAW pile; applying Mayhem first plays the Strike into the DISCARD pile and the boulder fires after. The game always resolves the boulder first, so the Strike is never played. The final pile of a card is conformance-visible and is in the RL observation, and an extra play perturbs every later shuffle. Both cards are ported Colorless power cards (cards/colorless_powers.py:294-314 and :157-174).

### `rupture` — AfterDamageReceived

*(hook; record: `audit/records/power/rupture.json`)*

LIVE, and the bigger half. C# requires `base.CombatState.CurrentSide == base.Owner.Side` (RupturePower.cs:47): Rupture only pays out for damage the owner takes DURING ITS OWN TURN, i.e. self-inflicted HP loss. powers.py:287 tests only `target is self.owner and amount > 0`, so the sim's Rupture also pays out for every enemy attack that gets through on the ENEMY's turn -- turning a self-harm payoff card into a free Strength engine that grows whenever the player is hit. The guard is not even hard to reach: the sim already exposes `combat.current_side` and InfernoPower uses it for exactly this purpose two hundred lines away (powers.py:757-759). Additionally the accumulate-then-apply-once deferral is absent (see the BeforeCardPlayed entry), so even the in-scope self-damage case grants Strength mid-card. Third-file citations, not hashed here.

### `rupture` — BeforeCardPlayed

*(hook; record: `audit/records/power/rupture.json`)*

Half of a deferral mechanism the sim does not implement at all. RupturePower.cs:31-43 registers every card the owner starts playing during its own side turn in an internal playedCards dictionary; AfterDamageReceived then ACCUMULATES into that entry instead of applying Strength, and AfterCardPlayed applies the accumulated total once the card has fully resolved. The point is that a card which damages its own player several times must not have its later hits boosted by the Strength its earlier hits earned. The sim has no BeforeCardPlayed on Rupture and applies Strength per hit, inline (powers.py:279-289). LIVE: Rupture is a ported Ironclad card (cards/rupture_card.py:35) and the sim's self-damage cards deal multi-instance HP loss, so mid-card Strength changes the same card's remaining damage. Third-file citations, not hashed here.

### `setup_strike` — AfterSideTurnEnd (inherited, TemporaryStrengthPower.cs:173-181)

*(hook; record: `audit/records/power/setup_strike.json`)*

SLOT, and LIVE. Inherited from TemporaryStrengthPower.cs:173-181 (a third file, not hashed by this record). AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end (powers.py:866-868) is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. EXECUTED WITNESS for this family, and the sharpest one found for the whole player-side AfterSideTurnEnd group so far, because the effect is a damage-relevant stat revert rather than a bookkeeping reset: with StampedePower 1 (powers.py:1025-1041, which auto-plays an Attack from its own on_player_turn_end) and SetupStrikePower 2, one Strike in hand, ending the turn deals 6 damage when the temp-strength power was applied FIRST (its revert runs first, so the auto-played Strike loses the +2) and 8 when Stampede was applied first. The game always deals 8. So the sim is wrong on exactly the registration orders where the temp power lands before Stampede -- decided by which card the player happened to play first. The enemy-owned leg (powers.py:870-872, on_enemy_side_end, combat.py:345) is in the RIGHT slot; only the player-owned leg is misplaced. Reproduce the affected population with py audit/tools/power_census.py slots.

### `shackling_potion` — AfterSideTurnEnd (inherited, TemporaryStrengthPower.cs:173-181)

*(hook; record: `audit/records/power/shackling_potion.json`)*

SLOT, and LIVE. Inherited from TemporaryStrengthPower.cs:173-181 (a third file, not hashed by this record). AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end (powers.py:866-868) is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and the flush (combat.py:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. EXECUTED WITNESS for this family, and the sharpest one found for the whole player-side AfterSideTurnEnd group so far, because the effect is a damage-relevant stat revert rather than a bookkeeping reset: with StampedePower 1 (powers.py:1025-1041, which auto-plays an Attack from its own on_player_turn_end) and SetupStrikePower 2, one Strike in hand, ending the turn deals 6 damage when the temp-strength power was applied FIRST (its revert runs first, so the auto-played Strike loses the +2) and 8 when Stampede was applied first. The game always deals 8. So the sim is wrong on exactly the registration orders where the temp power lands before Stampede -- decided by which card the player happened to play first. The enemy-owned leg (powers.py:870-872, on_enemy_side_end, combat.py:345) is in the RIGHT slot; only the player-owned leg is misplaced. Reproduce the affected population with py audit/tools/power_census.py slots.

### `slow` — Once-per-play vs once-per-CardPlay counting

*(guard; record: `audit/records/power/slow.json`)*

hook_dispatch gap G4 (LIVE and already pinned with an executed witness) applies directly: CardModel.cs:1904-1965 constructs a fresh CardPlay per Replay iteration and fires AfterCardPlayed INSIDE the loop, so a Throwing-Axe- or One-Two-Punch-doubled card raises SlowAmount by 2 in the game and by 1 in the sim (combat.py:514 fires on_card_played once after the `for _ in range(play_count)` loop). Not re-verdicted; the per-unit consequence is that the owner takes 10% less bonus damage per doubled card in the sim.

### `speed_potion` — The Dexterity leg's own observable consequence, as distinct from the family's slot verdict

*(guard; record: `audit/records/power/speed_potion.json`)*

RE-DERIVED 2026-07-26 (review fix pass). Stated separately so the AfterSideTurnEnd verdict above is not read as more proven than it is, and re-labelled from an admission that the consequence is "UNPROVEN" to a positive dormancy argument with a named trigger. The SLOT gap is the same code path power/setup_strike executed, so the verdict is inherited under rule 3 -- but setup_strike's witness is a DAMAGE witness (Stampede auto-plays an ATTACK from its own on_player_turn_end, so it sees the reverted Strength). Dexterity modifies BLOCK, so an equivalent witness needs a POWERED block gain landing strictly between combat.py:654 (on_player_turn_end, where the sim reverts) and combat.py:665 (after_player_turn_end, the correct slot). **ENUMERATED 2026-07-26; there is none.** (1) Dexterity's only consumer in the sim is DexterityPower.modify_block_additive (powers.py:129-137), and BlockCmd.apply dispatches the block-modifier families ONLY when `is_powered_attack(props)` (cmds.py:145-147), which is `MOVE in props and UNPOWERED not in props` (valueprops.py:47-49). (2) All four ported turn-end block relics pass `props=ValueProp.UNPOWERED` and therefore skip Dexterity entirely: relics/orichalcum.py:22-25, relics/cloak_clasp.py:19-24, relics/fake_orichalcum.py:25-30, relics/ripple_basin.py:36-39. (3) An AST sweep of every `on_player_turn_end` in sts2_rl finds exactly two bodies that mention block: PlatingPower._gain_block (powers.py:1058-1070), also ValueProp.UNPOWERED and owner-filtered, and ImbalancedPower's `_blocked_this_turn = False` reset (powers.py:1820), which gains none. (4) `_process_turn_end_cards` (combat.py:352-376) runs only the ethereal exhaust pass and the ten ported `on_turn_end_in_hand` bodies (bad_luck, beckon, burn, decay, doubt, infection, regret, shame, toxic, wither), and an AST sweep finds not one of them gains block. So: MECHANISM live and inherited; the DEXTERITY LEG DORMANT. **NAMED TRIGGER:** any ported effect that gains POWERED block (props MOVE without UNPOWERED -- i.e. card block, or a relic/power turn-end gain that drops the UNPOWERED prop) strictly between combat.py:654 and combat.py:665; concretely, a turn-end-in-hand card that gains card block, or an Orichalcum/Cloak Clasp-shaped relic ported without the UNPOWERED prop. Whoever fixes the family (one line: on_player_turn_end -> after_player_turn_end) fixes both legs regardless.

### `speed_potion` — AfterSideTurnEnd (inherited, TemporaryDexterityPower.cs:169-177)

*(hook; record: `audit/records/power/speed_potion.json`)*

SLOT. **LIVENESS CORRECTED 2026-07-26 (review fix pass): this entry used to read "SLOT, and LIVE", which overstated it.** The MECHANISM is live and its `gap` verdict is inherited under binding rule 3 from power/setup_strike, whose executed witness is a STRENGTH witness; THIS unit's own numeric consequence is DORMANT WITH A NAMED TRIGGER, because nothing ported reads Dexterity in the window the wrong slot creates. The enumeration and the trigger are on this record's guard "The Dexterity leg's own observable consequence" below. What follows is the slot argument itself, unchanged. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and the flush (combat.py:661-662); the correct slot, after_player_turn_end (combat.py:665), exists and is unused. The mechanism was EXECUTED on power/setup_strike (StampedePower 1 + a temp-strength power: ending the turn deals 6 or 8 depending on registration order where the game always deals 8), and this unit shares that code path verbatim. TemporaryDexterityPower.cs is a line-for-line clone of TemporaryStrengthPower.cs with DexterityPower substituted for StrengthPower (compare :142-152 with :146-156, :154-167 with :158-171, :169-177 with :173-181), so every verdict the TemporaryStrengthPower family already carries applies here under binding rule 3 -- see power/setup_strike and power/reptile_trinket. power/speed_potion is the ONLY ported TemporaryDexterityPower unit, so this record is where that base class is recorded. Third-file citations, not hashed by this record.

### `stampede` — StampedePower.cs:28 Rng.Shuffle.NextItem(items)

*(guard; record: `audit/records/power/stampede.json`)*

WRONG RNG STREAM. C# picks the Attack to auto-play off base.Owner.Player.RunState.Rng.Shuffle -- the SHUFFLE stream, which is surprising but is what the source says -- and the sim uses combat._rng.choice(candidates) (powers.py:1041), the SHARED unseeded random.Random (combat.py:88). combat.combat_rng.shuffle (combat_rng.py:18,48) is the matching accessor and exists. Same defect as the one already recorded LIVE on power/aggression (main report section 4 item 8) and on power/hello_world and power/entropy in half A's batch 1, and live for the same reason: under seed parity a Stampede play both picks a different Attack and perturbs every later draw off the shared rng. Stampede is a ported Ironclad card (cards/stampede.py:36). Third-file citations, not hashed here.

### `stampede` — AfterAutoPostPlayPhaseEntered

*(hook; record: `audit/records/power/stampede.json`)*

WRONG PHASE. This is turn_structure's G8 -- the AutoPrePlay and AutoPostPlay phases do not exist in the sim and their two hooks are hand-rolled onto neighbouring slots. Not re-verdicted here (binding rule 3); the verdict is gap on both sides. on its AutoPostPlay side, which G8 already labels LIVE with a Cloak Clasp witness. C# fires Hook.AfterAutoPostPlayPhaseEntered at CombatManager.cs:1167, strictly BEFORE Hook.BeforeTurnEnd (:1179), CheckWinCondition (:1181), DoTurnEnd (:1191), FlushPlayerHand (:1296) and Hook.AfterTurnEnd (:1307). The sim puts the auto-plays INSIDE on_player_turn_end (combat.py:654), i.e. inside the BeforeTurnEnd dispatch itself, so their order relative to every other turn-end power is registration order. A SECOND EXECUTED WITNESS beyond G8's, on the other side of the turn-end sequence: SetupStrikePower 2 (a TemporaryStrengthPower, whose C# hook is AfterSideTurnEnd at :1307) + Stampede 1 with one Strike in hand -- applying Setup Strike first makes the auto-played Strike deal 6 (its +2 Strength has already been reverted), applying Stampede first makes it deal 8. The game always deals 8. So Stampede's phase error compounds with the player-side AfterSideTurnEnd slot error (the main report's section 4 item 11): moving the AfterSideTurnEnd group to after_player_turn_end (combat.py:665) fixes THIS witness, but Stampede still needs its own pre-BeforeTurnEnd phase for G8's Cloak Clasp witness, which is a plain BeforeSideTurnEnd relic. Both fixes are required.

### `steam_eruption` — AfterDeath

*(hook; record: `audit/records/power/steam_eruption.json`)*

RE-VERDICTED 2026-07-26 (review fix pass): `deliberate-divergence` -> `gap`. The old rationale was that this unit "repairs the only observable it produces" by restoring `hp = max_hp = 999_999_999` from on_damage_received (powers.py:2022-2033), mirroring WaterfallGiant.cs:305's `CreatureCmd.SetMaxAndCurrentHp(base.Creature, 999999999m)`. **That is false on three counts.** (a) It repairs the HP DISPLAY only. It does not restore the AfterDeath dispatch, so every non-owner listener the game pays out to is still missed -- see the Gremlin Horn witness on power/adaptable's AfterDeath entry, which applies verbatim here (the Waterfall Giant is the ported Underdocks boss, rooms.py:139 and monsters/underdocks/waterfall_giant.py:148-151, and Gremlin Horn is a ported Uncommon relic whose AfterDeath carries no wasRemovalPrevented guard, GremlinHorn.cs:24-32). Feed applies too: SteamEruptionPower does not override ShouldOwnerDeathTriggerFatal (default true, PowerModel.cs:646-649) and Feed.cs:42 gates on DamageResult.WasTargetKilled, which DamageResult.cs:89-99 documents as true even when the death is prevented, so the game grants +3 max HP for Feeding the giant to death and cards/feed.py:45's `if target.is_dead` grants nothing. `InvokeDiedEvent` (CreatureCmd.cs:507) likewise has no counterpart. (b) The repair runs from a dispatch the game deliberately SKIPS on a killing blow -- CreatureCmd.cs:392's `if (!WasTargetKilled || !originalTarget.IsDead)`, which the sim mirrors at cmds.py:115-122 -- and it only fires in the sim because the prevention branch reset hp to 1 first, making `target.is_dead` False. So the repair is BUILT ON damage_pipeline gap G4 rather than being independent of it. (c) It does not fire at all on the non-damage kill path (cmds.py:191-205, which has no on_damage_received call), leaving the giant at hp 1 instead of 999999999; see this record's non-damage-kill guard. LIVE via (a): the Gremlin Horn payout and the Feed payout are both reachable in an ordinary Waterfall Giant fight. ONE MECHANISM, ONE VERDICT (binding rule 3): power/adaptable, power/illusion and power/steam_eruption all implement a C# AfterDeath body by returning False from `should_die` (powers.py:3365-3369, :1566-1570, :2016-2020), so all three run the sim's single death-PREVENTION branch (cmds.py:106-113) where C# runs the REAL-DEATH branch (CreatureCmd.cs:505-559). None of the three overrides ShouldDie, and all three bodies sit behind `!wasRemovalPrevented` (AdaptablePower.cs:34, IllusionPower.cs:79, SteamEruptionPower.cs:17) -- i.e. they are explicitly EXCLUDED from the branch the sim is executing. All three therefore carry `gap`; steam_eruption's `deliberate-divergence` was corrected to `gap` in the 2026-07-26 review fix pass. The canonical statement of the shared observable (the Gremlin Horn witness) is on power/adaptable's AfterDeath entry.

### `strangle` — AfterCardPlayed

*(hook; record: `audit/records/power/strangle.json`)*

The damage TYPING is exact -- C# is `ValueProp.Unblockable | ValueProp.Unpowered` with dealer null and cardSource null (StranglePower.cs:55) and the sim passes `props=DamageProps.NON_CARD_HP_LOSS`, which valueprops.py:44 defines as exactly `UNBLOCKABLE | UNPOWERED`, with no dealer and no card. That is the recurring omitted-`props` shape (power report section 4 item 4) got RIGHT, and it is worth recording as such. The gap is hook_dispatch gap G4, the per-CardPlay bracket: CardModel.cs:1904-1965 builds a fresh CardPlay per Replay iteration and fires BeforeCardPlayed at :1929 and AfterCardPlayed at :1959 INSIDE the `for (int i = 0; i < playCount; i++)` loop, so a doubled card runs Add-then-Remove twice and Strangle deals its damage TWICE; combat.py:466 fires before_card_played once before the `for _ in range(play_count)` loop and combat.py:514 fires on_card_played once after it, so the sim deals it ONCE. G4 is already LIVE with an executed witness and is not re-verdicted; the per-unit consequence is N-times-less Strangle damage on any replayed card. Co-occurrence is reachable: Strangle comes from Mad Science (the ported Act-3 Glory event Tinker Time) and the ported replay sources are Throwing Axe (the Act-3 Tanx shrine), One-Two Punch (powers.py:966), Duplication (powers.py:3919) and two enchantments. Third-file citations, not hashed here.

### `suck` — AfterAttack

*(hook; record: `audit/records/power/suck.json`)*

**LIVE.** WRONG HOOK, and the sim HAS the right one and does not use it. C#'s `AfterAttack(choiceContext, AttackCommand command)` fires ONCE per attack command, after every hit of every target, and is handed the whole `command.Results` (a List<List<DamageResult>>) so it can count hits and act once. The sim ports it onto `on_damage_received` (cmds.py:122), which fires PER HIT and inside the damage pipeline. `hooks.py:344` (before_attack) and `hooks.py:361` (after_attack) both exist -- the main report records them as used by Vigor and Gigantification -- so this is an unused-slot omission, not an absence. Same defect as curl_up's and skittish's (main report section 4 items 5 and 14). Two standing consequences: the KILLING-BLOW guard (cmds.py:121 skips the victim's on_damage_received on a kill, where C#'s AfterAttack is not guarded at all), and effects granted inline being visible to LATER HITS OF THE SAME attack. Third-file citations, not hashed here. Here the inline-effect consequence is a straight damage divergence, because the effect is Strength on the ATTACKER. C# counts the hit groups with any unblocked damage (`num`) and applies `base.Amount * num` ONCE after the whole attack (SuckPower.cs:28-46); the sim applies `self.amount` from inside each hit (powers.py:1629-1630), so hit 2 of a multi-hit attack is boosted by the Strength hit 1 earned. **EXECUTED** with the ported Underdocks Fossil Stalker, whose LASH is `_LASH_DMG = 3` x `_LASH_HITS = 2` (monsters/underdocks/fossil_stalker.py:22-23, 72): the sim takes the player from 80 to **71** (9 damage) where the game deals **6** (3 + 3, both hits unboosted); the final Strength is 6 on both sides, so the total Strength is right and only the mid-attack damage is wrong. Reachable from the first Fossil Stalker LASH the player fails to fully block -- ported act-1 Underdocks content, no co-occurrence needed. The guards themselves are faithful: `command.Attacker != base.Owner`, `command.TargetSide == base.Owner.Side` and `!command.DamageProps.IsPoweredAttack()` map to powers.py:1624-1627. Also note the sim omits the `applier=` C# passes (`base.Owner`, SuckPower.cs:45) -- the same dormant shape as power/high_voltage's, folded in here rather than raised again. Third-file citations, not hashed here.

### `tender` — AfterSideTurnEnd

*(hook; record: `audit/records/power/tender.json`)*

SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), before _process_turn_end_cards (combat.py:658) and before the flush (:661-662); the sim's real AfterTurnEnd slot, after_player_turn_end (combat.py:665), is available and unused. The concrete route for this whole group: StampedePower (powers.py:1025-1041) auto-plays Attacks from ITS on_player_turn_end, so in the game every turn-end auto-play happens while this power is still on the creature and in the sim that depends on registration order. Reproduce with `py audit/tools/power_census.py slots`. Third-file citations, not hashed here. LIVE for this unit: Tender restores Strength and Dexterity at turn end, and Stampede's turn-end auto-plays are Attack plays whose damage scales off Strength. In the game the restore happens AFTER them, so those attacks are still Tenderised; in the sim it happens in the same dispatch, so whether they are depends on registration order between Tender and Stampede. The restore also uses the same applier-less PowerCmd.apply as the drain, so it cannot be credited either.

### `the_bomb` — InstanceType

*(hook; record: `audit/records/power/the_bomb.json`)*

RE-VERDICTED 2026-07-26 (review fix pass): `deliberate-divergence` -> `gap`. PowerInstanceType.Instanced (TheBombPower.cs:23) means every application is an independently tracked instance with its own Amount and its own DynamicVars.Damage, where the sim's PowerCmd.apply merges by power id via on_stack (cmds.py:308-311). That MECHANISM is audits/seam/power_cmd.json's gap G5, which lists this power among its 11 ported InstanceType units, and it is not re-argued here (binding rule 3); what is per-unit is REACHABILITY of two simultaneous instances and what diverges once they exist. **REACHABILITY: two live fuses are plainly reachable.** The Bomb is a ported Uncommon Colorless Skill with NO Exhaust keyword (cards/colorless_skills.py:715-743, mirroring TheBomb.cs:23-26 whose CanonicalKeywords is empty), so it can simply be played on two consecutive turns. EXECUTED 2026-07-26: play it, end the turn, play it again -- the sim holds ONE `the_bomb` power whose fuse list is [[2, 40], [3, 40]] and whose `amount` is `min(turns for turns, _ in self.bombs)` == 2 (powers.py:3770-3772), where C# holds TWO TheBombPower instances with Amount 2 and Amount 3. **WHAT IS AND IS NOT REPRODUCED.** The DAMAGE is exact, and this unit is one of only two of G5's eleven ported units that acknowledges the distinction at all: each fuse carries its own turns_left and its own damage (powers.py:3768, :3771, set_damage at :3774-3777), the turn-end pass explodes every fuse at <= 1 independently and decrements the rest (:3785-3788), and the power only expires when the list empties (:3799-3800). The STATE is not reproduced, and that is why this is a gap rather than a divergence with the same observable outcome (binding rule 2). A different power list (one entry where the game has two) and a different amount (2 where the game shows 2 and 3) is not an internal detail: full_env.py:412 encodes EVERY ALL_POWERS id as a presence bit plus a signed amount at two scales, so the game's two-instance state cannot even be represented in the sim's observation, and any power-list comparison sees one entry for two. The second residue was already named on this entry before it was graded dd, and stands: an effect that removed 'a' Bomb would remove all of them in the sim, and set_damage always writes the NEWEST fuse (:3777), which is correct only because TheBomb.cs:30 calls SetDamage immediately after applying. **RULE 3:** the identical G5 mechanism is `gap` on power/rolling_boulder, whose two-instance case is reachable exactly the same way (a non-exhausting ported Colorless card played twice). Grading it `deliberate-divergence` here and `gap` there was a rule-3 break, and this is the site that moves: a good workaround makes the gap SMALLER -- the damage is right where rolling_boulder's is arithmetically 5 short on two consecutive turns -- not absent.

### `thorns` — BeforeDamageReceived

*(hook; record: `audit/records/power/thorns.json`)*

LIVE, twice over: the sim implements C#'s BEFORE-damage hook with the AFTER-damage one and drops the powered-attack gate. ThornsPower.cs:17-24 is `BeforeDamageReceived` with the guard `target == Owner && dealer != null && (props.IsPoweredAttack() || cardSource is Omnislice)`; powers.py:339-348 is on_damage_received (cmds.py:122, fired after the HP write at cmds.py:92) with the guard `target is self.owner and dealer is not None` and no props test at all. Consequence 1 -- KILLING BLOW (PROMPT.md bug class 2): the sim's on_damage_received is skipped when the hit killed the victim (cmds.py:121, the CreatureCmd.cs:392 guard), but C#'s BeforeDamageReceived runs before any HP loss and is NOT subject to that guard, so the game reflects Thorns off the killing blow and the sim reflects nothing. EXECUTED: a 99-damage Strike into a 3-HP enemy holding Thorns 5 costs the sim's player 0 HP where the game costs it 5; a non-killing hit costs 5 in both (control). Reachable from the first Thorns enemy the player kills -- Spiny Toad (monsters/hive/spiny_toad.py:45) and Toadpole (monsters/underdocks/toadpole.py:75) both self-apply Thorns. Consequence 2 -- MISSING PROPS GATE: on_damage_received fires for every damage type, so the sim reflects Thorns off unpowered non-attack damage that the game ignores. EXECUTED: 3 unpowered non-card damage with dealer = player into a Thorns-5 enemy costs the sim's player 5 HP and the game 0. Reachable with the ported Ironclad card Juggernaut (cards/juggernaut.py:38 -> powers.py:791-794, DamageProps.NON_CARD_UNPOWERED with dealer = the player) against a Toadpole or Spiny Toad; Panache, Inferno, Rolling Boulder, The Bomb and Flame Barrier all deal damage in the same shape. Third-file citations, not hashed here.

## DORMANT gaps

Trimmed to the opening of each entry; the record has the rest, including the
named trigger. Ordered by unit.

### `adaptable`

- **The non-damage kill path: CreatureCmd.kill reproduces both arms but fires no on_damage_received** *(guard)* — Part of the same mechanism, at the second of the sim's two death sites. `CreatureCmd.kill` (cmds.py:191-205) mirrors DamageCmd.deal's should_die / should_remove_from_combat_after_death / on_death fork and the same `target.hp = 1` prevention floor, but it is not a damage event, so it fires neither on_damage_received nor after_preventing_death. […]
- **ShouldStopCombatFromEnding** *(hook)* — No sim counterpart. C# reads this through Hook.ShouldStopCombatFromEnding at CombatManager.cs:196, i.e. inside the win check: while ANY power on ANY creature returns true the combat cannot end. The sim has no such hook -- `hooks.py` defines no `should_stop_combat_from_ending` -- and `combat.py:272-277` decides the win purely from `is_gone` over the non-minion enemies. […]
- **StackType** *(hook)* — PowerStackType.Single with the sim's on_stack overridden to `pass` citing it -- the settled census's 15-unit wrong-reasoning class (`py audit/tools/power_census.py stack`). Single means "Amount is hidden and is always 1" (PowerStackType.cs:10-13); PowerCmd.ModifyAmount (PowerCmd.cs:236) adds unconditionally with no StackType branch, so C# really reaches Amount 2. […]
### `artifact`

- **TryModifyPowerAmountReceived** *(hook)* — The interception is reimplemented outside the hook system entirely, and the debuff test is the wrong one. C# (ArtifactPower.cs:17-36) is a TryModifyPowerAmountReceived listener whose three guards are `target != Owner`, `canonicalPower.GetTypeForAmount(amount) != Debuff`, and `!canonicalPower.IsVisible`; cmds.py:299 instead tests the applied class's static `power_cls.power_type == PowerType.DEBUFF`. […]
### `asleep`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called ONCE for the whole enemy side (CombatManager.cs:1256) after every enemy has moved; the sim implements it with the PER-ENEMY slot on_enemy_turn_end (combat.py:341), which fires immediately after each enemy's own move. The sim HAS the side-scoped slot and does not use it here: on_enemy_side_end (combat.py:345). […]
- **BeforeSideTurnEndVeryEarly** *(hook)* — SLOT, two ways. (a) turn_structure gap G11 owns this exact case and names it: Hook.BeforeTurnEnd(Enemy) (CombatManager.cs:1251) has no sim counterpart, and AsleepPower's BeforeSideTurnEndVeryEarly Plating-drop went onto on_enemy_turn_START (powers.py:1853, whose own comment says "Mirrors BeforeSideTurnEndVeryEarly") -- one slot FURTHER from the C# position than G11's first draft said. […]
### `buffer`

- **ModifyHpLostAfterOstyLate** *(hook)* — The arithmetic is exact -- 0 for the owner, unchanged otherwise (BufferPower.cs:20-27 vs powers.py:4011-4013) -- and the AFTER-Osty position is right, since cmds.py:86 runs after block absorption (:74-81). What is lost is the LATE half, and BufferPower.cs:16-19 states in as many words why it is Late: 'We use Late because other effects may reduce damage taken to 0 too, and it's more player-friendly for them to trigger […]
### `burrowed`

- **AfterRemoved** *(hook)* — C#'s AfterRemoved is `CreatureCmd.LoseBlock(oldOwner, 999999999m)` -- dump ALL the block -- and it runs on EVERY removal path, including the automatic strip when the owner dies (CreatureCmd.cs:533 then each power's AfterRemoved). The sim has no AfterRemoved analogue and hand-inlines the block dump at the single on_block_broken call site (powers.py:2300, comment `# AfterRemoved: LoseBlock(all)`). […]
- **StackType** *(hook)* — PowerStackType.Single with the sim's on_stack overridden to `pass` citing it -- the census's 15-unit wrong-reasoning class (`py audit/tools/power_census.py stack`). Single means "Amount is hidden and is always 1" (PowerStackType.cs:10-13) and PowerCmd.ModifyAmount (PowerCmd.cs:236) adds unconditionally with no StackType branch, so C# really reaches Amount 2. […]
### `calamity`

- **BeforeCardPlayed** *(hook)* — C# uses a TWO-HOOK LATCH the sim collapses into one. CalamityPower.cs:28-40 records amountsForPlayedCards[card] = base.Amount at BeforeCardPlayed and :44 removes it at AfterCardPlayed, so (a) the Amount is SNAPSHOTTED at the start of the play and (b) the after-hook only fires for a card the before-hook admitted. The sim reads self.amount at the end of the play (powers.py:3493) and re-tests the card type there. […]
### `chains_of_binding`

- **BeforeCardPlayed** *(hook)* — WRONG SIDE OF THE PLAY, the same shape as SlothPower's: C# sets `boundCardPlayed` in BeforeCardPlayed (ChainsOfBindingPower.cs:48-65) and the sim sets it in on_card_played, after resolution -- while the sim's `before_card_played` slot (combat.py:466) exists and is used by StranglePower and SurroundedPower. […]
### `confused`

- **StackType** *(hook)* — PowerStackType.Single, and the sim overrides Power.on_stack to `pass` citing it -- which is WRONG about C# and inert about this power. Single means only 'Amount is hidden, and is always 1' (PowerStackType.cs:10-13); PowerCmd.ModifyAmount:236 adds the offset with no StackType branch at all, so in the game a second application really does raise Amount to 2. […]
### `constrict`

- **AfterDeath** *(hook)* — The `wasRemovalPrevented` guard is missing. C# removes this power only when `!wasRemovalPrevented && creature == base.Applier`; the sim tests only the applier (hooks.py:574-580 carries no wasRemovalPrevented argument at all), so a death whose corpse the combat keeps drops the power in the sim and keeps it in the game. […]
- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and before the flush (combat.py:661-662); […]
### `corruption`

- **StackType** *(hook)* — Same PowerStackType.Single misreading as power/hellraiser's StackType entry -- C# adds at PowerCmd.cs:236 regardless. Dormant: Corruption reads Amount nowhere. One of the 15 units in `py audit/tools/power_census.py stack`.
### `crab_rage`

- **CrabRagePower.cs:36 `applier: base.Owner`** *(guard)* — MISSING `applier=`. C# passes `base.Owner` (`PowerCmd.Apply<StrengthPower>(choiceContext, base.Owner, ..., base.Owner, null)`); the sim omits it, so `applier` is None through hooks.modify_power_amount (cmds.py:297), hooks.on_power_applied (cmds.py:327) and hooks.on_power_amount_changed (cmds.py:312), and the granted power's .applier is left None (powers.py:59). Recurring gap shape 5. […]
### `crimson_mantle`

- **CrimsonMantlePower.cs:31 fires the damage UNCONDITIONALLY** *(guard)* — C# calls CreatureCmd.Damage with the DamageVar's BaseValue every turn, including the first, when the value is 0; powers.py:643 guards on `if self.self_damage > 0`. A 0-damage CreatureCmd.Damage is not a no-op in C#: it still runs the pipeline and fires BeforeDamageReceived. […]
### `cruelty`

- **CrueltyPower.cs:19-22 `target == base.Owner` -> unmodified** *(guard)* — Cruelty's self-exclusion is dropped by its consumer. Recorded in full on power/vulnerable's matching guard -- the sim reads Cruelty's amount with no such test, so a Cruelty holder attacking its own Vulnerable self would get the bonus in the sim and not in the game. […]
### `curious`

- **CuriousPower.cs:12-14,32 the TryModify predicate protocol** *(guard)* — C#'s Try* hooks are a predicate chain: the listener returns bool to say 'I changed it' and writes the new value to an out-param, and Hook.ModifyEnergyCostInCombat (Hook.cs:1574-1590) uses that to decide who to notify afterwards and, in some families, to stop looking. […]
### `dampen`

- **AfterApplied** *(hook)* — Two findings. (1) MECHANISM, the same substitution as illusion's: C#'s AfterApplied runs after PowerCmd registers the power; the sim does the work in __init__, i.e. inside `power_cls(...)` at cmds.py:324 and therefore BEFORE hooks.register and hooks.on_power_applied. Unobservable today (nothing reacts to a Dampen application) but it is the mechanism note the main report's section 7 item 6 asks for. […]
- **AfterDeath** *(hook)* — C# tracks a SET of casters (`Data.casters`, added through the public non-override `AddCaster`, DampenPower.cs:15/73-76) and removes the power only when the LAST caster dies (`casters.Remove(creature); if (casters.Count == 0) PowerCmd.Remove(this)`, DampenPower.cs:47-55). The sim keeps a single `self.applier` and expires on that one creature's death (powers.py:3176-3179). […]
- **StackType** *(hook)* — PowerStackType.None (DampenPower.cs:22) with the sim's on_stack overridden to `pass` citing it -- the census's wrong-reasoning class (`py audit/tools/power_census.py stack`, which lists this unit as the single StackType=None member). PowerCmd.ModifyAmount (PowerCmd.cs:236) adds unconditionally regardless of StackType, so a second application really does reach Amount 2 in C#. […]
### `dark_embrace`

- **AfterCardExhausted** *(hook)* — Two divergences. (a) THE DRAW COUNT IS HARD-CODED TO 1. DarkEmbracePower.cs:47 draws `base.Amount`; powers.py:255 is `DrawCmd.draw(self.owner, 1)`. Dormant only because the one ported applier always passes 1 (cards/dark_embrace_card.py:34, including its upgrade, which raises no amount) -- the moment any effect stacks Dark Embrace the sim draws one card where the game draws Amount. […]
### `dark_shackles`

- **ITemporaryPower as a marker interface** *(guard)* — The ITemporaryPower MARKER ITSELF is absent from the sim -- there is no is_temporary attribute, no InternallyAppliedPower, and no should_power_be_removed_on_death hook among hooks.py's 66 dispatchers. C# has five readers of power is ITemporaryPower: IllusionPower.cs:59-66 (Illusions keep their debuffs on death UNLESS the debuff is temporary, so that the wrapper's internal Strength goes away with it), Rend.cs:43-50 an […]
- **TemporaryStrengthPower.cs:141-144 IgnoreNextInstance** *(guard)* — ITemporaryPower.IgnoreNextInstance / _shouldIgnoreNextInstance (TemporaryStrengthPower.cs:30, :141-144, consumed at :148-151 and :162-165) has NO sim counterpart at all. Its one caller is Misery.cs:59-61, which copies an enemy's debuffs and must not re-apply the wrapper's internal StrengthPower; without the flag the copy would double the Strength steal. […]
### `demise`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and before the flush (combat.py:661-662); […]
### `dexterity`

- **Sign-aware power typing on a negative Dexterity application** *(guard)* — SIGN-AWARE TYPING (PROMPT.md bug class 3). GetTypeForAmount (PowerModel.cs:460-471, a third file not hashed by this record) returns PowerType.Debuff for this power at any NEGATIVE amount, because StackType == Counter && AllowNegative. The sim has no get_type_for_amount at all and tests the static class attribute, so a negative-amount application is a Buff to the sim and a Debuff to the game. […]
- **ModifyBlockAdditive** *(hook)* — The sim keys the ownership test on the BLOCK TARGET where C# keys it on the CARD's owner. DexterityPower.cs:16-26: when cardSource != null the test is `cardSource.Owner.Creature != base.Owner -> 0m` and the target is not consulted at all; only for cardSource == null (a monster move) does it fall back to `base.Owner != target`. powers.py:135 tests `target is self.owner` in both cases. […]
### `disintegration`

- **AfterSideTurnEndLate** *(hook)* — Wrong slot AND lost phase, and it is the only power in this group with both. (a) PHASE: this is `AfterSideTurnEndLate`, the second complete pass Hook.AfterTurnEnd runs (Hook.cs:1284-1291), so in the game Disintegration's damage lands after EVERY plain AfterSideTurnEnd listener has finished -- deliberately last. The sim has one unphased pass, which is hook_dispatch gap G3; […]
### `draw_cards_next_turn`

- **ModifyHandDraw** *(hook)* — The count is right (count + Amount, DrawCardsNextTurnPower.cs:32 vs powers.py:2749 -- and correctly NOT the flat +1 that its sibling power/clarity uses; the two classes exist precisely to differ here, ClarityPower.cs:12-15). The GUARD is missing: DrawCardsNextTurnPower.cs:28-31 returns count unchanged when base.AmountOnTurnStart == 0, i.e. when the power landed after this turn's snapshot. […]
### `escape_artist`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called ONCE for the whole enemy side (CombatManager.cs:1256) after every enemy has moved; the sim implements it with the PER-ENEMY slot on_enemy_turn_end (combat.py:341), which fires immediately after each enemy's own move. The sim HAS the side-scoped slot and does not use it here: on_enemy_side_end (combat.py:345). […]
### `feeding_frenzy`

- **ITemporaryPower as a marker interface** *(guard)* — The ITemporaryPower MARKER ITSELF is absent from the sim -- there is no is_temporary attribute, no InternallyAppliedPower, and no should_power_be_removed_on_death hook among hooks.py's 66 dispatchers. C# has five readers of power is ITemporaryPower: IllusionPower.cs:59-66 (Illusions keep their debuffs on death UNLESS the debuff is temporary, so that the wrapper's internal Strength goes away with it), Rend.cs:43-50 an […]
- **TemporaryStrengthPower.cs:141-144 IgnoreNextInstance** *(guard)* — ITemporaryPower.IgnoreNextInstance / _shouldIgnoreNextInstance (TemporaryStrengthPower.cs:30, :141-144, consumed at :148-151 and :162-165) has NO sim counterpart at all. Its one caller is Misery.cs:59-61, which copies an enemy's debuffs and must not re-apply the wrapper's internal StrengthPower; without the flag the copy would double the Strength steal. […]
### `flame_barrier`

- **AfterSideTurnEnd** *(hook)* — The removal condition is inverted from a side comparison into a hard-coded side. FlameBarrierPower.cs:26-32 removes the power whenever `base.Owner.Side != side` -- i.e. at the end of the turn belonging to the side the owner is NOT on, which for a player-held Flame Barrier is the enemy side's end and for an enemy-held one is the PLAYER side's end. […]
### `flex_potion`

- **ITemporaryPower as a marker interface** *(guard)* — The marker itself is absent from the sim -- no is_temporary attribute, no InternallyAppliedPower, no should_power_be_removed_on_death among hooks.py's dispatchers. C# has five readers; Rend, Sleight of Flesh and Misery are unported, but IllusionPower IS ported (powers.py:1560ff) and owns the ShouldPowerBeRemovedOnDeath port. […]
- **TemporaryStrengthPower.cs:141-144 IgnoreNextInstance** *(guard)* — ITemporaryPower.IgnoreNextInstance / _shouldIgnoreNextInstance has NO sim counterpart. Its one caller is Misery.cs:59-61, which copies an enemy's debuffs and must not re-apply the wrapper's internal stat power. DORMANT: Misery is unported (grep -rn misery sts2_rl/ --include=*.py is empty). Named trigger: porting Misery. Dormant gap, not a waiver, per binding rule 1. […]
### `free_attack`

- **The TryModify predicate protocol** *(guard)* — C#'s Try* hooks return bool and write to an out-param, which Hook.ModifyEnergyCostInCombat (Hook.cs:1574-1590) uses to build its notification list; the sim's modify_card_energy_cost (hooks.py:196-201) is a plain fold with neither. Identical entry to power/curious's, same verdict per binding rule 3, dormant with the same named trigger (any AfterModifyingEnergyCostInCombat implementer).
### `galvanic`

- **AfterCardPlayed** *(hook)* — **PROPS.** C# deals the Galvanized damage with `ValueProp.Unpowered | ValueProp.Move` (GalvanicPower.cs:55); the sim passes `DamageProps.NON_CARD_UNPOWERED`, which valueprops.py:42 defines as `UNPOWERED` **alone** -- the MOVE flag is missing. The right constant exists and is one line up: `DamageProps.CARD_UNPOWERED` (valueprops.py:36) is exactly `UNPOWERED | MOVE`. […]
- **BeforeCombatStart** *(hook)* — Right slot -- combat.py:208 fires on_combat_start immediately before `start_turn()` at :209, which turn_structure identifies as the sim's BeforeCombatStart. The divergence is an ADDED GUARD (recurring shape 8): C# afflicts EVERY Power card unconditionally (GalvanicPower.cs:34-38 has no `Affliction == null` test at this site, and `CardCmd.Afflict` overwrites) while the sim skips already-afflicted cards (powers.py:3030 […]
### `gigantification`

- **AfterAttack** *(hook)* — The slot is right (combat.py:492, immediately after the card's on_play inside the play-count loop). The GAP is the IDENTITY the latch is cleared against: C# compares ATTACK-COMMAND identity (`command == internalData.commandToModify`, GigantificationPower.cs:80), the sim compares CARD identity (`card is self._card_to_modify`, powers.py:3986). […]
### `hardened_shell`

- **BeforeSideTurnStart** *(hook)* — C#'s BeforeSideTurnStart (HardenedShellPower.cs:71-77) has NO side filter and NO participants filter, so the cap resets at the start of EVERY side's turn. The sim reproduces that with two listeners: on_player_turn_start unfiltered (powers.py:1996, and BeforeSideTurnStart -> on_player_turn_start is the CORRECT player-side mapping per the section-5 slot table, CombatManager.cs:458) and on_enemy_turn_start filtered to ` […]
- **ModifyHpLostBeforeOstyLate** *(hook)* — The FORMULA is exact -- `target != Owner -> amount`, `amount == 0 -> amount`, else `Math.Min(amount, Amount - damageReceivedThisTurn)` (HardenedShellPower.cs:32-43) vs powers.py:1981-1983 -- and the BeforeOsty/AfterOsty phase collapse is already resolved as faithful by damage_pipeline (Osty redirection is waived, so its steps 8 and 11 fold into one `modify_hp_lost` call). […]
### `hatch`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called ONCE for the whole enemy side (CombatManager.cs:1256) after every enemy has moved; the sim implements it with the PER-ENEMY slot on_enemy_turn_end (combat.py:341), which fires immediately after each enemy's own move. The sim HAS the side-scoped slot and does not use it here: on_enemy_side_end (combat.py:345). […]
### `heist`

- **BeforeDeath** *(hook)* — HOOK-PHASE MISMATCH -- a BEFORE hook ported onto an AFTER hook, the recurring shape section 0 item 5 of the stream report names for thorns/curl_up/skittish/suck, now in a death-time form. C# calls Hook.BeforeDeath UNCONDITIONALLY at CreatureCmd.cs:503, two lines BEFORE Hook.ShouldDie at :505 decides whether the death stands. The sim's on_death fires only INSIDE the should_die-true branch (cmds.py:96-105), i.e. […]
- **InstanceType** *(hook)* — HeistPower.cs:15 PowerInstanceType.Instanced -- the audits/seam/power_cmd.json G5 mechanism, cross-referenced under rule 3. DORMANT here: the only applier is SurprisePower applying one HeistPower to the one Fat Gremlin it spawns (powers.py:1698ff), so two simultaneous instances are unreachable. Eleventh PowerInstanceType unit -- see the note on power/thievery.
### `hello_world`

- **HelloWorldPower.cs:22 base.AmountOnTurnStart >= 1 (used as BOTH the guard and the card count)** *(guard)* — The guard is ported as self.amount < 1 (powers.py:2811) and the count as self.amount (:2825), where C# uses base.AmountOnTurnStart for both (HelloWorldPower.cs:22 and :27). PowerModel.AmountOnTurnStart has NO sim counterpart -- grep -rn amount_on_turn_start sts2_rl/ returns nothing. […]
### `hellraiser`

- **AfterCardDrawnEarly** *(hook)* — Two divergences. (a) PHASE LOSS. This is a phase-suffixed C# hook and the sim has a single unphased dispatch, which is hook_dispatch gap G3 (24 dispatchers lose their phase structure; 27 phase-suffixed hooks declared on AbstractModel). Per binding rule 3 the mechanism keeps G3's `gap` verdict and is cross-referenced rather than re-argued. Third-file citations, not hashed here. […]
- **AfterSideTurnEnd** *(hook)* — HellraiserPower.cs:70-78 resets the per-turn infinite-auto-play counter. The sim tracks no counter (see the AfterCardDrawnEarly entry), so there is nothing to reset. Dormant for the same reason and with the same trigger; carried separately because a fix for the cap must add this reset too, and because the harness requires a verdict per override.
- **StackType** *(hook)* — The sim drops a re-application's offset on the strength of a misreading of PowerStackType.Single. Single means only 'Amount is hidden, and is always 1' (PowerStackType.cs:10-13); PowerCmd.ModifyAmount:236 adds the offset with no StackType branch, so playing Hellraiser twice gives C# Amount 2 and the sim Amount 1. DORMANT for this unit -- Hellraiser reads Amount nowhere, so the value is inert. […]
### `hex`

- **StackType** *(hook)* — PowerStackType.Single with on_stack overridden to `pass` -- the census's wrong-reasoning class. DORMANT with a caveat that is NOT true of the other 14: Hex's Amount IS read -- it is the Hexed affliction's amount (`CardCmd.Afflict<Hexed>(card, base.Amount)`, HexPower.cs:79, vs `CardCmd.afflict(card, HexedAffliction, self.amount)`, powers.py:3209) -- so a second application would afflict LATER cards at 2 in the game an […]
### `high_voltage`

- **HighVoltagePower.cs:23 `participants.Contains(base.Owner)`** *(guard)* — The sim substitutes `if not self.owner.is_dead` (powers.py:3252) -- recurring gap shape 8, a guard the sim changes rather than drops. The two are not the same predicate: a corpse the combat RETAINED is still a side participant in C# (the "death does not mean removal" finding; combat.py:292 keeps a retained corpse taking turns), so C# would grant it Strength where the sim refuses. […]
- **HighVoltagePower.cs:26 `applier: base.Owner`** *(guard)* — MISSING `applier=`. C# passes `base.Owner` as the applier (`PowerCmd.Apply<StrengthPower>(choiceContext, base.Owner, base.Amount, base.Owner, null)`); the sim calls `PowerCmd.apply(self.hooks, self.owner, StrengthPower, self.amount)` with no applier, so `applier` is None through hooks.modify_power_amount (cmds.py:297), hooks.on_power_applied (cmds.py:327) and hooks.on_power_amount_changed (cmds.py:312), and the grant […]
### `illusion`

- **IllusionPower.cs:34-45 FollowUpStateId** *(guard)* — A public settable property with no sim analogue: it lets an applier choose which state the revived creature resumes on, defaulting to the last LOGGED state. Folded into the AfterDeath entry; carried separately because it survives a should_die-to-AfterDeath fix. […]
- **The non-damage kill path: CreatureCmd.kill reproduces both arms but fires no on_damage_received** *(guard)* — Part of the same mechanism, at the second of the sim's two death sites. `CreatureCmd.kill` (cmds.py:191-205) mirrors DamageCmd.deal's should_die / should_remove_from_combat_after_death / on_death fork and the same `target.hp = 1` prevention floor, but it is not a damage event, so it fires neither on_damage_received nor after_preventing_death. […]
- **AfterDeath** *(hook)* — Same wrong-hook shape as AdaptablePower's, and not re-argued here: C# lets the death happen and retains the corpse; the sim prevents the death and floors the owner at 1 HP (cmds.py:112). Consequences (1) HP 1 vs 0 and (2) damage_pipeline gap G4's killing-blow-skip recomputation apply identically. […]
- **ShouldPowerBeRemovedOnDeath** *(hook)* — IllusionPower.cs:59-66 keeps the owner's BUFFS (including itself) through death and drops only non-ITemporaryPower Debuffs -- the source comment explains that Temporary powers are excluded so the temp powers THEY applied also go away. The sim has no `should_power_be_removed_on_death` hook (hooks.py defines none) and never removes powers on death at all, so the whole predicate is unreachable in the sim. […]
- **StackType** *(hook)* — PowerStackType.Single, and this is the one unit in the on_stack family that goes the OTHER way: the sim does NOT override on_stack, so a second application adds. That happens to be what PowerCmd.ModifyAmount does (PowerCmd.cs:236), so it is *more* faithful than the 15 units that override it to `pass` -- but it is still a gap, because C#'s AfterApplied fires only on a genuinely new power while the sim's __init__-time […]
### `imbalanced`

- **AfterDamageGiven** *(hook)* — WRONG HOOK. C#'s `AfterDamageGiven(choiceContext, dealer, result, props, target, cardSource)` is the DEALER-side after-damage event; the sim's counterpart is `on_damage_dealt` (hooks.py:469, fired at cmds.py:123-124), and this power uses `on_damage_received` instead, filtering on `dealer is self.owner`. […]
- **StackType** *(hook)* — PowerStackType.Single with the sim's on_stack overridden to `pass` citing it -- the census's 15-unit wrong-reasoning class (`py audit/tools/power_census.py stack`). Single means "Amount is hidden and is always 1" (PowerStackType.cs:10-13) and PowerCmd.ModifyAmount (PowerCmd.cs:236) adds unconditionally with no StackType branch, so C# really reaches Amount 2. […]
### `improvement`

- **AfterCombatEnd** *(hook)* — THE EFFECT IS ENTIRELY UNIMPLEMENTED. ImprovementPower.cs:17-31 upgrades base.Amount random upgradable DECK cards after the combat ends: it takes PileType.Deck's cards filtered on IsUpgradable, then loops Amount times picking with Rng.CombatCardSelection.NextItem, removing the pick from the candidate list (so the picks are DISTINCT) and calling CardCmd.Upgrade. […]
### `inferno`

- **InfernoPower.cs:48 CombatState.HittableEnemies** *(guard)* — The sim iterates `combat.enemies` filtered on `not enemy.is_gone` (powers.py:762-763) where C# uses HittableEnemies, which additionally consults Hook.ShouldAllowHitting (Creature.cs:308-322). So the sim burns creatures the game considers unhittable -- a mid-revival Illusion or a withered Decimillipede segment (powers.py:1572-1575, :2354-2357 both implement should_allow_hitting). […]
- **AfterPlayerTurnStart** *(hook)* — SLOT, identical to power/crimson_mantle's: AfterPlayerTurnStart is post-draw and the sim uses the pre-draw on_player_turn_start. The sim's on_player_turn_start (player.py:169) fires BEFORE the hand draw (player.py:171-186); C#'s side-turn-start dispatchers run at CombatManager.cs:458 (Before) and :522 (After), i.e. either side of SetupPlayerTurn at :514. […]
### `infested`

- **ShouldStopCombatFromEnding** *(hook)* — No sim counterpart. C# reads this through Hook.ShouldStopCombatFromEnding at CombatManager.cs:196, inside the win check: while any power returns true the combat cannot end. `hooks.py` defines no such hook and combat.py:272-277 decides the win purely from `is_gone` over the non-minion enemies. […]
### `intangible`

- **IntangiblePower.cs:20-23 `!CombatManager.Instance.IsInProgress` -> unmodified** *(guard)* — The sim has no combat-phase guard on any modifier hook. This is the power-level face of audits/seam/power_cmd.json's structural gap G6 (no IsEnding/CanReceivePowers backstop) and of hook_dispatch's gap G8 (no IsOverOrEnding gate on combat dispatches); per binding rule 3 it carries the same `gap` verdict and is cross-referenced rather than re-argued. […]
### `juggernaut`

- **JuggernautPower.cs:21-22 CombatState.HittableEnemies and the empty check** *(guard)* — The sim iterates combat.enemies filtered on not enemy.is_gone where C# uses CombatState.HittableEnemies, which additionally consults Hook.ShouldAllowHitting (Creature.cs:308-322), so the sim aims at creatures the game considers unhittable -- a mid-revival Illusion (powers.py:1572-1575) or a withered Decimillipede segment (powers.py:2354-2357). […]
### `mangle`

- **ITemporaryPower as a marker interface** *(guard)* — The ITemporaryPower MARKER ITSELF is absent from the sim -- there is no is_temporary attribute, no InternallyAppliedPower, and no should_power_be_removed_on_death hook among hooks.py's 66 dispatchers. C# has five readers of power is ITemporaryPower: IllusionPower.cs:59-66 (Illusions keep their debuffs on death UNLESS the debuff is temporary, so that the wrapper's internal Strength goes away with it), Rend.cs:43-50 an […]
- **TemporaryStrengthPower.cs:141-144 IgnoreNextInstance** *(guard)* — ITemporaryPower.IgnoreNextInstance / _shouldIgnoreNextInstance (TemporaryStrengthPower.cs:30, :141-144, consumed at :148-151 and :162-165) has NO sim counterpart at all. Its one caller is Misery.cs:59-61, which copies an enemy's debuffs and must not re-apply the wrapper's internal StrengthPower; without the flag the copy would double the Strength steal. […]
### `nemesis`

- **NemesisPower.cs:22 `participants.Contains(base.Owner)`** *(guard)* — Replaced by `if self.owner.is_dead: return` (powers.py:3435) -- the same substitution as HighVoltage's and Territorial's, and one degree worse here, because the sim's early return also SKIPS THE TOGGLE (`_should_apply` is not flipped), so a Nemesis owner that is dead for one side end and alive for the next resumes on the wrong beat where C# would have flipped. […]
- **StackType** *(hook)* — PowerStackType.Single, and the sim overrides on_stack to `pass` citing it. Per the settled census (`py audit/tools/power_census.py stack`, 15 such units) that reasoning is wrong: Single means "Amount is hidden and is always 1" (PowerStackType.cs:10-13), and PowerCmd.ModifyAmount (PowerCmd.cs:236) adds unconditionally with no StackType branch -- so C# really does reach Amount 2 on a second application while the sim st […]
### `no_draw`

- **StackType** *(hook)* — The sim overrides on_stack to a no-op citing PowerStackType.Single, but Single only means 'Amount is hidden, and is always 1' (PowerStackType.cs:10-13) -- PowerCmd.ModifyAmount:236 adds the offset with no StackType branch. One of the 15 units making this misreading (`py audit/tools/power_census.py stack`). DORMANT for this unit: Amount is read nowhere in it, so the value is inert. […]
### `no_energy_gain`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), which CombatManager calls at :1307 for the player side -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654, before both); the sim's real AfterTurnEnd slot is after_player_turn_end (combat.py:665). […]
- **StackType** *(hook)* — The sim overrides on_stack to a no-op citing PowerStackType.Single, but Single only means 'Amount is hidden, and is always 1' (PowerStackType.cs:10-13) -- PowerCmd.ModifyAmount:236 adds the offset with no StackType branch. One of the 15 units making this misreading (`py audit/tools/power_census.py stack`). DORMANT for this unit: Amount is read nowhere in it, so the value is inert. […]
### `panache`

- **AfterCardPlayed** *(hook)* — The sim iterates `combat.enemies` filtered on `not enemy.is_gone` where C# uses CombatState.HittableEnemies, which additionally consults Hook.ShouldAllowHitting (Creature.cs:308-322). The sim therefore aims at creatures the game considers unhittable -- a mid-revival Illusion (powers.py:1572-1575) or a withered Decimillipede segment (powers.py:2354-2357). […]
- **InstanceType** *(hook)* — PanachePower.cs:35 declares PowerInstanceType.Instanced, so every Panache play is an independently-tracked instance with its own CardsLeft counter; the sim keeps one instance per power id (cmds.py:308) and a second play stacks the damage while sharing one counter. This is audits/seam/power_cmd.json's gap G5, which already names PanachePower among the 11 ported InstanceType powers; […]
### `plating`

- **AfterSideTurnStart** *(hook)* — WRONG SLOT on the player leg. C#'s AfterSideTurnStart is CombatManager.cs:522, POST-draw; the sim's on_player_turn_start is player.py:169, PRE-draw (before modify_hand_draw at :171 and _draw at :183). The sim's post-draw slot, on_player_turn_started (player.py:186), is available and unused -- the same one-line fix as the other misplaced-slot units, and the population is reproducible with `py audit/tools/power_census. […]
- **BeforeSideTurnStart** *(hook)* — SLOT. C# uses the ROUND-1 arm of a turn-start hook as a stand-in for combat start -- PlatingPower.cs:41-56 fires only when side == Player, the owner is NOT a player, and combatState.RoundNumber <= 1, and its own comment says 'We want enemies that start with Plating to also start combat with block'. Dispatched at CombatManager.cs:458, i.e. AFTER Creature.BeforeTurnStart (:453) and BEFORE the block clear at :496. […]
### `plow`

- **AfterDamageReceived** *(hook)* — Right hook and right slot; the threshold matches exactly (`target != base.Owner || result.UnblockedDamage <= 0 || target.CurrentHp > base.Amount -> return`, PlowPower.cs:30, vs powers.py:1297-1300). Three divergences. (1) The sim ADDS `self.owner.is_dead` to the early-out (powers.py:1297) where C# has no such guard -- recurring gap shape 8; […]
### `poison`

- **AfterSideTurnStart** *(hook)* — Three divergences, all DORMANT for one shared reason: **nothing in the sim applies Poison at all.** An executed grep for PoisonPower outside powers.py and the package re-exports returns no applier -- no card, relic, event, monster or potion. […]
### `rampart`

- **RampartPower.cs:27 `base.CombatState.Enemies.Where(c => c.Monster is TurretOperator)`** *(guard)* — powers.py:3000-3002 adds `and not enemy.is_gone` (recurring gap shape 8, a guard the sim ADDS). C#'s CombatState.Enemies is the raw participant list and a corpse the combat retained is still in it, so the game grants block to a dead-but-present Turret Operator and the sim does not. DORMANT: TurretOperator overrides no ShouldCreatureBeRemovedFromCombatAfterDeath, so a dead one leaves the list; […]
- **AfterSideTurnStart** *(hook)* — SLOT, and the sim already has the right one. AbstractModel.AfterSideTurnStart is dispatched by Hook.AfterSideTurnStart at CombatManager.cs:522, i.e. AFTER the player's hand draw; the sim's on_player_turn_start is fired at player.py:169, BEFORE the draw at player.py:183, and the post-draw slot on_player_turn_started (player.py:186 -- whose own comment says it is "the game's AfterPlayerTurnStart / player-side AfterSide […]
### `ravenous`

- **RavenousPower.cs:33 `applier: base.Owner`** *(guard)* — MISSING `applier=`. C# passes `base.Owner` (`PowerCmd.Apply<StrengthPower>(choiceContext, base.Owner, ..., base.Owner, null)`); the sim omits it, so `applier` is None through hooks.modify_power_amount (cmds.py:297), hooks.on_power_applied (cmds.py:327) and hooks.on_power_amount_changed (cmds.py:312), and the granted power's .applier is left None (powers.py:59). Recurring gap shape 5. […]
### `regen`

- **AfterSideTurnEnd** *(hook)* — Both legs are in the wrong slot and the IsDead guard is missing. RegenPower.cs:20-28 is `if (participants.Contains(Owner) && !Owner.IsDead) { Heal(Amount); Decrement; }` on AfterSideTurnEnd. (a) The ENEMY leg uses the per-creature on_enemy_turn_end (powers.py:165-167) instead of the side-scoped on_enemy_side_end -- see the dedicated guard entry. […]
### `reptile_trinket`

- **ITemporaryPower as a marker interface** *(guard)* — The ITemporaryPower MARKER ITSELF is absent from the sim -- there is no is_temporary attribute, no InternallyAppliedPower, and no should_power_be_removed_on_death hook among hooks.py's 66 dispatchers. C# has five readers of power is ITemporaryPower: IllusionPower.cs:59-66 (Illusions keep their debuffs on death UNLESS the debuff is temporary, so that the wrapper's internal Strength goes away with it), Rend.cs:43-50 an […]
- **TemporaryStrengthPower.cs:141-144 IgnoreNextInstance** *(guard)* — ITemporaryPower.IgnoreNextInstance / _shouldIgnoreNextInstance (TemporaryStrengthPower.cs:30, :141-144, consumed at :148-151 and :162-165) has NO sim counterpart at all. Its one caller is Misery.cs:59-61, which copies an enemy's debuffs and must not re-apply the wrapper's internal StrengthPower; without the flag the copy would double the Strength steal. […]
### `rolling_boulder`

- **RollingBoulderPower.cs:37 CombatState.HittableEnemies (TestMode arm)** *(guard)* — The sim iterates combat.enemies filtered on not enemy.is_gone (powers.py:3716) where C# uses CombatState.HittableEnemies, which additionally consults Hook.ShouldAllowHitting (Creature.cs:308-322), so the sim aims at creatures the game considers unhittable -- a mid-revival Illusion (powers.py:1572-1575) or a withered Decimillipede segment (powers.py:2354-2357). […]
### `sandpit`

- **AfterRemoved** *(hook)* — The EFFECT is right and the MECHANISM is not. C#'s AfterRemoved (SandpitPower.cs:91-124) returns early on `oldOwner.IsDead || base.Target.IsDead`, hides the affected creatures, and `CreatureCmd.Kill(..., force: true)` every one that IsPlayer or is an Osty; the sim overrides `_expire` (powers.py:2610-2619) to kill `combat.player` unless `owner_gone` or the player is already dead. Two divergences. […]
- **AfterSideTurnStartLate** *(hook)* — SLOT and PHASE. (a) SandpitPower.cs:70-76 is AfterSideTurnStartLate with `side == CombatSide.Enemy` and NO owner/participants filter, dispatched once per enemy side; the sim uses the per-enemy on_enemy_turn_start (combat.py:301) filtered to `enemy is self.owner`. […]
- **InstanceType** *(hook)* — PowerInstanceType.Instanced (SandpitPower.cs:37). The sim's stacking check (cmds.py:308) always behaves as PowerInstanceType.None -- one shared instance per power id per owner -- so two simultaneous Sandpits on the same Insatiable would merge into one 8-count timer where C# would track two independent 4-count timers, either of which eats the player when it runs out. […]
### `setup_strike`

- **ITemporaryPower as a marker interface** *(guard)* — The ITemporaryPower MARKER ITSELF is absent from the sim -- there is no is_temporary attribute, no InternallyAppliedPower, and no should_power_be_removed_on_death hook among hooks.py's 66 dispatchers. C# has five readers of power is ITemporaryPower: IllusionPower.cs:59-66 (Illusions keep their debuffs on death UNLESS the debuff is temporary, so that the wrapper's internal Strength goes away with it), Rend.cs:43-50 an […]
- **TemporaryStrengthPower.cs:141-144 IgnoreNextInstance** *(guard)* — ITemporaryPower.IgnoreNextInstance / _shouldIgnoreNextInstance (TemporaryStrengthPower.cs:30, :141-144, consumed at :148-151 and :162-165) has NO sim counterpart at all. Its one caller is Misery.cs:59-61, which copies an enemy's debuffs and must not re-apply the wrapper's internal StrengthPower; without the flag the copy would double the Strength steal. […]
### `shackling_potion`

- **ITemporaryPower as a marker interface** *(guard)* — The ITemporaryPower MARKER ITSELF is absent from the sim -- there is no is_temporary attribute, no InternallyAppliedPower, and no should_power_be_removed_on_death hook among hooks.py's 66 dispatchers. C# has five readers of power is ITemporaryPower: IllusionPower.cs:59-66 (Illusions keep their debuffs on death UNLESS the debuff is temporary, so that the wrapper's internal Strength goes away with it), Rend.cs:43-50 an […]
- **TemporaryStrengthPower.cs:141-144 IgnoreNextInstance** *(guard)* — ITemporaryPower.IgnoreNextInstance / _shouldIgnoreNextInstance (TemporaryStrengthPower.cs:30, :141-144, consumed at :148-151 and :162-165) has NO sim counterpart at all. Its one caller is Misery.cs:59-61, which copies an enemy's debuffs and must not re-apply the wrapper's internal StrengthPower; without the flag the copy would double the Strength steal. […]
### `shrink`

- **AfterDeath** *(hook)* — The `wasRemovalPrevented` guard is missing. ShrinkPower.cs:87-93 removes Shrink only when `!wasRemovalPrevented && creature == base.Applier`; the sim tests only `creature is self.applier` (powers.py:1398). A prevented removal (a death whose corpse the combat keeps) therefore drops Shrink in the sim and keeps it in the game. […]
- **AfterSideTurnEnd** *(hook)* — Two divergences in one hook. (a) The `!IsInfinite` guard (ShrinkPower.cs:81, i.e. Amount >= 0) is spelled `self.amount > 0` on both sim legs (powers.py:1390,1394); those agree only because Amount == 0 is unreachable (ShouldRemoveDueToAmount removes at exactly 0), so this half is equivalent rather than identical. […]
- **AllowNegative** *(hook)* — ShrinkPower.cs:38 declares `AllowNegative => true`; the sim's ShrinkPower never sets allow_negative, so it inherits False from Power (powers.py:47). That changes ShouldRemoveDueToAmount (PowerModel.cs:478-489): C# removes an AllowNegative power only at EXACTLY 0 and lets it sit negative, while the sim's stacking branch removes it at any amount < 0 (cmds.py:317-319). […]
### `slippery`

- **AfterDamageReceived** *(hook)* — Right slot (AfterDamageReceived -> cmds.py:122, killing-blow guard on both sides) and the right condition in shape: `target == base.Owner && result.UnblockedDamage >= 1` vs `target is self.owner and amount >= 1`. The divergence is WHICH NUMBER is tested. C#'s `UnblockedDamage` is post-block but PRE-HP-loss-modifier, so the stack is consumed whenever any damage got through the block. […]
- **ModifyHpLostAfterOsty** *(hook)* — The formula is exact: `target != base.Owner -> amount`, `amount < 1m -> amount`, else `1m` (SlipperyPower.cs:19-30) vs powers.py:1520-1522. The BeforeOsty/AfterOsty phase collapse is already resolved as faithful by damage_pipeline (Osty redirection is waived, so its steps 8 and 11 fold into one call). […]
### `sloth`

- **BeforeCardPlayed** *(hook)* — WRONG SIDE OF THE PLAY. C# increments the counter in `BeforeCardPlayed` (SlothPower.cs:31-40), i.e. before the card resolves; the sim increments in `on_card_played`, after. The sim HAS the right slot -- `before_card_played` (combat.py:466), which ChainsOfBindingPower's sibling port and StranglePower both use. […]
### `slow`

- **AfterSideTurnStart** *(hook)* — SLOT. AbstractModel.AfterSideTurnStart is dispatched at CombatManager.cs:522, once per side after that side's turn setup; the sim has NO side-scoped enemy turn-start slot at all (`py audit/tools/power_census.py slots` marks this unit PER-CREATURE:on_enemy_turn_start NO-SIM-SIDE-START-SLOT) and this power uses the per-enemy on_enemy_turn_start (combat.py:301). […]
- **ModifyDamageMultiplicative** *(hook)* — The factor matches (`1m + 0.1m * SlowAmount` at SlowPower.cs:43 vs `1.0 + 0.1 * self._cards_this_turn` at powers.py:1261) and `target != base.Owner -> 1m` matches, but the POWERED test does not: C# is `props.IsPoweredAttack()` (SlowPower.cs:39) and the sim is `card is not None and not card.is_unpowered` (powers.py:1260). […]
### `slumber`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called ONCE for the whole enemy side (CombatManager.cs:1256) after every enemy has moved; the sim implements it with the PER-ENEMY slot on_enemy_turn_end (combat.py:341), which fires immediately after each enemy's own move. The sim HAS the side-scoped slot and does not use it here: on_enemy_side_end (combat.py:345). […]
### `smoggy`

- **StackType** *(hook)* — The sim drops a re-application's offset citing PowerStackType.Single, which only means 'Amount is hidden' (PowerStackType.cs:10-13) -- PowerCmd.ModifyAmount:236 adds regardless. DORMANT: Smoggy reads Amount nowhere (it afflicts with the literal 1). One of the 15 units making this misreading (`py audit/tools/power_census.py stack`).
### `soar`

- **StackType** *(hook)* — PowerStackType.Single, and the sim overrides on_stack to `pass` citing it. Per the settled census (`py audit/tools/power_census.py stack`) that reasoning is wrong: Single means "Amount is hidden and is always 1" (PowerStackType.cs:10-13) and PowerCmd.ModifyAmount (PowerCmd.cs:236) adds unconditionally with no StackType branch. […]
### `speed_potion`

- **ITemporaryPower as a marker interface** *(guard)* — The marker itself is absent from the sim -- no is_temporary attribute, no InternallyAppliedPower, no should_power_be_removed_on_death among hooks.py's dispatchers. C# has five readers; Rend, Sleight of Flesh and Misery are unported, but IllusionPower IS ported (powers.py:1560ff) and owns the ShouldPowerBeRemovedOnDeath port. […]
- **TemporaryDexterityPower.cs:137-140 IgnoreNextInstance** *(guard)* — ITemporaryPower.IgnoreNextInstance / _shouldIgnoreNextInstance has NO sim counterpart. Its one caller is Misery.cs:59-61, which copies an enemy's debuffs and must not re-apply the wrapper's internal stat power. DORMANT: Misery is unported (grep -rn misery sts2_rl/ --include=*.py is empty). Named trigger: porting Misery. Dormant gap, not a waiver, per binding rule 1. […]
### `steam_eruption`

- **The non-damage kill path: CreatureCmd.kill reproduces both arms but fires no on_damage_received** *(guard)* — Part of the same mechanism, at the second of the sim's two death sites. `CreatureCmd.kill` (cmds.py:191-205) mirrors DamageCmd.deal's should_die / should_remove_from_combat_after_death / on_death fork and the same `target.hp = 1` prevention floor, but it is not a damage event, so it fires neither on_damage_received nor after_preventing_death. […]
- **ShouldStopCombatFromEnding** *(hook)* — No sim counterpart. C# reads this through Hook.ShouldStopCombatFromEnding at CombatManager.cs:196, inside the win check: while any power returns true the combat cannot end. `hooks.py` defines no such hook and combat.py:272-277 decides the win purely from `is_gone` over the non-minion enemies. […]
### `stock`

- **ShouldStopCombatFromEnding** *(hook)* — No sim counterpart. C# reads this through Hook.ShouldStopCombatFromEnding at CombatManager.cs:196, inside the win check: while any power returns true the combat cannot end. `hooks.py` defines no such hook and combat.py:272-277 decides the win purely from `is_gone` over the non-minion enemies. […]
### `strangle`

- **InstanceType** *(hook)* — PowerInstanceType.InstancedPerApplier (StranglePower.cs:29) -- and per power_cmd gap G5's corrected census this is the ONLY ported InstancedPerApplier power in the game (the other is OblivionPower, unported). The sim keys stacking on the power id alone (cmds.py:308), so two appliers' Strangles on one target merge into a single instance with a single `_amounts` dict and a single amount, where C# keeps one instance per […]
### `strength`

- **Sign-aware power typing on a negative Strength application** *(guard)* — SIGN-AWARE TYPING (PROMPT.md bug class 3). GetTypeForAmount (PowerModel.cs:460-471, a third file not hashed by this record) returns PowerType.Debuff for this power at any NEGATIVE amount, because StackType == Counter && AllowNegative. The sim has no get_type_for_amount at all and tests the static class attribute, so a negative-amount application is a Buff to the sim and a Debuff to the game. […]
### `suck`

- **Counting GROUPS with unblocked damage, not individual results** *(guard)* — C#'s `num` counts outer lists (per-hit result groups) in which ANY result had unblocked damage, so a single AoE hit that connects with three creatures counts 1. The sim's per-hit hook counts every connecting RESULT, so the same AoE would grant 3x. Folded into the hook entry's rollup; carried separately because it survives an after_attack-slot fix. […]
### `surprise`

- **AfterDeath** *(hook)* — Right hook and the right two spawns (`CreatureCmd.Add<SneakyGremlin>` then `<FatGremlin>`, SurprisePower.cs:23-24, vs powers.py:1704-1706 in the same order, which matters because it fixes the enemy-list indices). The gap is the THIEVERY TRANSFER. […]
- **ShouldStopCombatFromEnding** *(hook)* — No sim counterpart. C# reads this through Hook.ShouldStopCombatFromEnding at CombatManager.cs:196, inside the win check: while any power returns true the combat cannot end. `hooks.py` defines no such hook and combat.py:272-277 decides the win purely from `is_gone` over the non-minion enemies. […]
### `surrounded`

- **SurroundedPower.cs:92 `!wasRemovalPrevented`** *(guard)* — Absent from powers.py:2584-2586, which tests only the side. C# skips the re-facing entirely when a death's REMOVAL was prevented (the creature is still there, so the board did not change); the sim re-runs its `all(...)` scan anyway. […]
- **AfterDeath** *(hook)* — The logic matches SurroundedPower.cs:90-100 -- skip when the dead creature is on the owner's own side, then, if every remaining hittable enemy carries the SAME marker power, re-face on hittableEnemies[0] -- but the sim reads `[e for e in combat.enemies if not e.is_gone]` (powers.py:2590) where C# reads `base.Owner.CombatState.HittableEnemies`. […]
- **ModifyDamageMultiplicative** *(hook)* — The arithmetic and the facing logic are exact -- `dealer == null -> 1m`, `target != base.Owner -> 1m`, then 1.5x only if the dealer holds the marker power OPPOSITE the facing (SurroundedPower.cs:46-72 vs powers.py:2559-2565), and 1.5 is dyadic so hook_dispatch G9 does not bite (`power_census.py multipliers`). […]
- **StackType** *(hook)* — PowerStackType.Single with the sim's on_stack overridden to `pass` -- the settled census's 15-unit wrong-reasoning class (`py audit/tools/power_census.py stack`); PowerCmd.ModifyAmount (PowerCmd.cs:236) adds unconditionally. DORMANT: Surrounded's behaviour is driven entirely by the private `_facing`/`self.facing` state (SurroundedPower.cs:27, powers.py:2547) and NOTHING reads Amount on either side; […]
### `swipe`

- **BeforeDeath** *(hook)* — HOOK SLOT: C# is `BeforeDeath`, fired at CreatureCmd.cs:503 **before** `Hook.ShouldDie` and therefore before any death prevention; the sim uses `hooks.on_death`, fired at cmds.py:105 only on the branch where should_die returned True. Two consequences. […]
- **InstanceType** *(hook)* — PowerInstanceType.Instanced (SwipePower.cs:23): C# creates one SwipePower per stolen card, each with its own `StolenCard` and `Target`. The sim's stacking check (cmds.py:308) always behaves as PowerInstanceType.None, and this power WORKS AROUND it by bundling the instances into a single `stolen_cards` list and documenting the approximation in its own docstring (powers.py:2238-2247). […]
### `tender`

- **AfterCardPlayed** *(hook)* — The applier is dropped. TenderPower.cs:50-51 applies Strength and Dexterity -1 with `applier: base.Applier` -- the creature that applied Tender -- and `silent: true`; powers.py:2115-2116 calls PowerCmd.apply with no applier at all. […]
### `territorial`

- **TerritorialPower.cs:26 `applier: base.Owner`** *(guard)* — MISSING `applier=`. C# passes `base.Owner` as the applier (`PowerCmd.Apply<StrengthPower>(choiceContext, base.Owner, base.Amount, base.Owner, null)`); the sim calls `PowerCmd.apply(self.hooks, self.owner, StrengthPower, self.amount)` with no applier, so `applier` is None through hooks.modify_power_amount (cmds.py:297), hooks.on_power_applied (cmds.py:327) and hooks.on_power_amount_changed (cmds.py:312), and the grant […]
### `the_bomb`

- **TheBombPower.cs:51 / :56 CombatState.HittableEnemies** *(guard)* — The sim iterates combat.enemies filtered on not enemy.is_gone where C# uses CombatState.HittableEnemies, which additionally consults Hook.ShouldAllowHitting (Creature.cs:308-322), so the sim aims at creatures the game considers unhittable -- a mid-revival Illusion (powers.py:1572-1575) or a withered Decimillipede segment (powers.py:2354-2357). […]
### `the_gambit`

- **AfterDamageReceived** *(hook)* — The hook is the RIGHT one this time -- C# really is AfterDamageReceived, not a before-hook, so this unit does NOT have power/thorns's and power/curl_up's defect -- and the guards line up: target == base.Owner, props.IsPoweredAttack() and result.UnblockedDamage > 0 (TheGambitPower.cs:18) vs target is self.owner, is_powered_attack(props) and amount > 0 (powers.py:3826), where the sim's `amount` argument is hp_lost, i.e […]
- **StackType** *(hook)* — PowerStackType.Single, and the sim overrides Power.on_stack to `pass` citing it -- which is WRONG about C# and inert about this power. Single means only 'Amount is hidden, and is always 1' (PowerStackType.cs:10-13); PowerCmd.ModifyAmount:236 adds the offset with no StackType branch at all, so in the game a second application really does raise Amount to 2. […]
### `thievery`

- **InstanceType** *(hook)* — ThieveryPower.cs:17 PowerInstanceType.Instanced, which the sim's single-slot target.powers dict (cmds.py:310-326, keyed on power_cls.id) cannot represent -- the mechanism owned by audits/seam/power_cmd.json G5, cross-referenced not re-verdicted. […]
### `thorns`

- **ThornsPower.cs:19 `props.IsPoweredAttack() || cardSource is Omnislice`** *(guard)* — Absent entirely -- see consequence 2 of the BeforeDamageReceived entry. Carried as its own guard because it survives a hook-slot fix: moving the sim to a before-damage slot would still reflect off Poison and power damage without this test. The Omnislice arm is separately dormant (Omnislice is unported).
### `vigor`

- **ModifyDamageAdditive** *(hook)* — The sim keeps only the FIRST of C#'s four guards. C# (VigorPower.cs:57-77) tests, in order: base.Owner != dealer (present, powers.py:1899), !props.IsPoweredAttack() (present structurally -- cmds.py:56-58 only runs the additive family for powered damage), `commandToModify != null && cardSource != null && cardSource != commandToModify.ModelSource` (ABSENT), and `commandToModify != null && commandToModify.Attacker != de […]
### `vital_spark`

- **AfterPowerAmountChanged** *(hook)* — C# re-syncs every Tainted affliction's Amount to the power's new Amount from `AfterPowerAmountChanged` with a `power != this` guard (VitalSparkPower.cs:79-98), so it fires on ANY amount change -- a stack, a decrement, or an Unsettling-Lamp-doubled application. The sim folds the sync into `on_stack` (powers.py:2443-2449), which cmds.py:311 calls only on the re-application path. […]
- **AfterRemoved** *(hook)* — C#'s AfterRemoved clears every Tainted affliction on EVERY removal path (VitalSparkPower.cs:58-77, guarded by `oldOwner.CombatState == null`); the sim hangs the same sweep on `on_death` filtered to the owner (powers.py:2451-2460) and then calls `self._expire()`. […]
### `vulnerable`

- **CrueltyPower.cs:19-22 `target == base.Owner` -> unmodified** *(guard)* — Cruelty's own self-exclusion is dropped. C# skips the Cruelty bonus when the Vulnerable target IS the Cruelty holder; powers.py:414-416 reads `dealer.powers.get('cruelty')` with no such test, so a Cruelty holder attacking its own Vulnerable self would get the bonus in the sim and not in the game. […]
- **VulnerablePower.cs:41-45 DebilitatePower leg** *(guard)* — DebilitatePower is not ported (`grep -c DebilitatePower sts2_rl/powers.py` returns 0), so the third link of C#'s modifier chain has no sim counterpart. Per binding rule 1 an unported C# side is a DORMANT gap, not a waiver. Named trigger: porting DebilitatePower.
- **ModifyDamageMultiplicative** *(hook)* — The base multiplier and both ported modifiers are right, but the value is computed in FLOAT where C# uses DECIMAL, which puts this hook inside hook_dispatch gap G9's blast radius. C# reads DamageIncrease = 1.5m from the DynamicVar (VulnerablePower.cs:12,27) and threads it through PaperPhrog, then CrueltyPower, then DebilitatePower (:30-45). […]
### `weak`

- **ModifyDamageMultiplicative** *(hook)* — The sim returns the bare literal 0.75 and has no modifier chain at all, where WeakPower.cs:24-35 threads DamageDecrease = 0.75m through PaperKrane (the TARGET's relic, -0.15m) and then DebilitatePower. Neither is ported -- `ls sts2_rl/relics/ | grep -i paper` returns joss_paper, lead_paperweight and paper_phrog but no paper_krane, and `grep -c DebilitatePower sts2_rl/powers.py` returns 0 -- and there is no modify_wea […]
### `withering_presence`

- **AfterCardPlayed** *(hook)* — The mechanism is right -- count the target player's card plays down from 6, add a Wither to HAND at 0, reset to 6 -- and the Wither's upgrade matching is preserved (`aeonglass.MatchWitherToUpgradeCount(wither)` at WitheringPresencePower.cs:37 vs powers.py:3287-3288's `for _ in range(wither_upgrade_count): wither.fake_upgrade()`). Two findings. […]
- **InstanceType** *(hook)* — PowerInstanceType.Instanced (WitheringPresencePower.cs:26): C# creates one instance per OPPONENT, each with its own `Target` (Aeonglass.cs:78-83 loops `GetOpponentsOf(base.Creature)`), and the counter is per-instance. The sim's stacking check always behaves as PowerInstanceType.None. […]

## Unclassified gaps

The entry does not state LIVE or DORMANT in those words. Listed separately
rather than assumed either way — settling them is outstanding work.

### `adaptable`

- **The prevention branch's HP contract: sim `hp = 1` vs C# leaving the creature at 0 and re-entering KillWithoutCheckingWinCondition** *(guard)* — Part of the should_die-for-AfterDeath mechanism, carried as its own entry because it survives any fix that only re-slots the AfterDeath body. cmds.py:112 writes `target.hp = 1` and stops. C# does NOT heal the creature: CreatureCmd.cs:560-571 leaves it at 0 HP, fires Hook.AfterDeath(wasRemovalPrevented: true) and Hook.AfterPreventingDeath, and then, `if (creature.IsDead)`, RE-ENTERS `KillWithoutCheckingWinCondition(cr […]
### `aggression`

- **BeforeSideTurnStart** *(hook)* — The card selection uses the wrong RNG and the wrong shuffle. AggressionPower.cs:28 is `source.ToList().UnstableShuffle(Rng.CombatCardSelection).Take(Amount)` -- an UnstableShuffle drawn from the dedicated CombatCardSelection stream. powers.py:514 is `combat._rng.sample(candidates, min(self.amount, len(candidates)))`, i.e. Python's reservoir sampling on the shared unseeded combat rng. […]
### `artifact`

- **AfterModifyingPowerAmountReceived** *(hook)* — The stack-consumption event is hand-inlined. C# consumes the stack via `PowerCmd.Decrement(this)` from AfterModifyingPowerAmountReceived (ArtifactPower.cs:38-41) -- i.e. through the full ModifyAmount pipeline, which is what runs ShouldRemoveDueToAmount. The sim decrements the field and calls _expire() directly. […]
### `asleep`

- **AsleepPower.cs:48 `participants.Contains(base.Owner)`** *(guard)* — Folded into the AfterSideTurnEnd slot entry: the sim expresses the same intent with `enemy is not self.owner -> return` (powers.py:1860) on a per-creature dispatch. Equivalent for a solo owner, divergent for a multi-enemy one; carried here so a reader sees the guard was checked rather than skipped.
### `automation`

- **InstanceType** *(hook)* — PowerInstanceType.Instanced: in C# every application is an independently tracked instance with its own Amount and its own internal state, where the sim's PowerCmd.apply merges by power id via on_stack (cmds.py:308-311). This is audits/seam/power_cmd.json's gap G5, which lists this power among its 11 ported InstanceType units; the mechanism is not re-verdicted here per binding rule 3. […]
### `clarity`

- **AfterSideTurnStart** *(hook)* — The SLOT is right and the group's usual slot gap does not apply here: Hook.AfterSideTurnStart (CombatManager.cs:522) is post-draw, and the sim's on_player_turn_started (player.py:186) is the post-draw slot -- confirmed by the ordering table in this record's source comments and by py audit/tools/power_census.py slots (which flags the unit only as NO-SIM-SIDE-START-SLOT, i.e. […]
### `corruption`

- **ModifyCardPlayResultPileTypeAndPosition** *(hook)* — The destination-pile DECISION is replaced by an after-the-fact move, and the sim has the right hook available and does not use it. C# (CorruptionPower.cs:27-38) returns (PileType.Exhaust, position) from the pile-resolution chain, so a played Skill never enters the discard pile at all. […]
- **TryModifyEnergyCostInCombatLate** *(hook)* — PHASE LOSS. This is a phase-suffixed C# hook and the sim has a single unphased dispatch, which is hook_dispatch gap G3 (24 dispatchers lose their phase structure; 27 phase-suffixed hooks declared on AbstractModel). Per binding rule 3 the mechanism keeps G3's `gap` verdict and is cross-referenced rather than re-argued. Third-file citations, not hashed here. […]
### `crab_rage`

- **AfterDeath** *(hook)* — Constants and props both checked and both right: `DynamicVars.Strength` is `new PowerVar<StrengthPower>(6m)` and `DynamicVars.Block` is `new BlockVar(99m, ValueProp.Unpowered)` (CrabRagePower.cs:25-29), matching powers.py:2510-2511's STRENGTH_GAIN = 6 / BLOCK_GAIN = 99 and powers.py:2519's explicit `props=VP.UNPOWERED` -- one more of the eight siblings that gets the `props` argument right where feel_no_pain and curl_ […]
### `cruelty`

- **CrueltyPower.cs:27 `amount + base.Amount / 100m`** *(guard)* — The arithmetic is right and the TYPE is not: powers.py:416 computes `mult += cruelty.amount / 100.0` in float where C# uses decimal. `1.5 + n/100` is non-dyadic for most n (10 -> 1.6, 30 -> 1.8), so a Cruelty-boosted Vulnerable is a third non-dyadic damage multiplier beyond the two literals `py audit/tools/power_census.py multipliers` reports, widening hook_dispatch gap G9. […]
### `curl_up`

- **CurlUpPower.cs:40-43 `!props.IsPoweredAttack()` -> return** *(guard)* — Absent. powers.py:309 requires only `target is self.owner and dealer is not None`, and the sim's on_damage_received fires for every damage type (cmds.py:122 is outside the is_powered_attack gate at cmds.py:56), so the sim curls up off unpowered damage -- Juggernaut, Panache, Thorns, Flame Barrier -- that the game ignores. […]
- **CurlUpPower.cs:44-47 `cardSource == null` -> return** *(guard)* — Absent for the same reason: powers.py:309 does not require a card at all, so a dealer-carrying non-card damage source triggers Curl Up in the sim. C# needs a card because the whole mechanism is 'wait until THIS card finishes'. […]
- **CurlUpPower.cs:48-52 the one-card latch** *(guard)* — `if (playedCard != null && cardSource != playedCard) return` keeps the latch on the FIRST qualifying card until it resolves. The sim has no latch because it never defers -- the same gap as the AfterDamageReceived and AfterCardPlayed entries, carried here so the guard-level reading of the C# file is complete.
- **CurlUpPower.cs:63 ValueProp.Unpowered on the block** *(guard)* — powers.py:310-311 calls BlockCmd.apply with no props, which defaults to ValueProp.MOVE (cmds.py:143-144) and so runs the block modifier families (cmds.py:146-147). Identical omission to power/feel_no_pain's, with the same consequence in reverse: an enemy's Curl Up block is scaled by ITS Dexterity/Frail in the sim and flat in the game. Reachable -- the ported Louse Progenitor can be hit by a Frail source. […]
- **AfterCardPlayed** *(hook)* — CurlUpPower.cs:56-70 is where C# gains the block (ValueProp.Unpowered), clears the latch, sets `LouseProgenitor.Curled = true` and calls PowerCmd.Remove. The sim has none of it: the block and the removal moved into AfterDamageReceived (see that entry) and the Curled flag is never set at all. […]
### `dampen`

- **DampenPower.cs:73-76 public void AddCaster(Creature)** *(guard)* — A public non-override method, so the harness does not enumerate it -- recorded so a reader does not think it was skipped (the same courtesy the main report gives CrueltyPower.ModifyVulnerableMultiplier). It is the writer of the `casters` set the AfterDeath entry is about, and the sim has no analogue: the sim's applier is set once by PowerCmd.apply (cmds.py:324) and never added to.
### `dark_embrace`

- **DarkEmbracePower.cs:41 `causedByEthereal`** *(guard)* — The parameter does not exist on the sim's hook, so the branch cannot be taken. Carried as its own guard because it is the root of the AfterSideTurnEnd gap and a fix has to start here.
### `demon_form`

- **DemonFormPower.cs:23 `participants.Contains(base.Owner)`** *(guard)* — Enemy leg: the sim uses the per-creature on_enemy_turn_start (combat.py:301), which runs immediately before THIS enemy's move, where Hook.AfterSideTurnStart runs once at CombatManager.cs:522 before ANY enemy moves. Worse than the AfterSideTurnEnd family, because the sim has no enemy-side-START slot at all to move it to -- that absence is turn_structure gap G5's other half. […]
### `diamond_diadem`

- **The applier's own condition (DiamondDiadem.cs) -- BLOCKED ON THE RELIC TIER** *(guard)* — RE-VERDICTED 2026-07-26 (review fix pass): `waiver` -> `gap`. The old rationale delegated the applier's condition to `audits/relic/diamond_diadem`, WHICH DOES NOT EXIST -- the relic tier is 0 of 258 audited -- so under binding rule 4 (a guard carries max(verdict) of what it aggregates) it was waiving an unaudited verdict, and `waiver` is reserved for genuinely out-of-scope material in any case. […]
### `draw_cards_next_turn`

- **Phase collapse in the sim's single post-draw slot** *(guard)* — PHASE COLLAPSE. The sim's on_player_turn_started (player.py:186) is a single slot serving THREE distinct C# phases that the game runs in a fixed order: Hook.AfterPlayerTurnStart (CombatManager.cs:675, inside the SetupPlayerTurn task launched at :514), then Hook.AfterSideTurnStart (:522), then Hook.AfterAutoPrePlayPhaseEntered (:568 -> RunAutoPrePlayPhase, :613-619). […]
### `duplication`

- **Duplication is one of hook_dispatch G4's replay sources** *(guard)* — hook_dispatch G4 already names powers.py:3919 as a play-count source that widens the per-CardPlay bracket gap: C# builds a fresh CardPlay per iteration and fires Before/AfterCardPlayed inside the loop (CardModel.cs:1904-1965) where combat.py:466/514 fire once. Per binding rule 3 the same verdict, cross-referenced not re-argued. Recorded here so duplication.json names the gap it participates in.
### `enrage`

- **Enrage fires once per CardPlay, not once per Replay iteration** *(guard)* — hook_dispatch gap G4: CardModel.cs:1904-1965 builds a fresh CardPlay per replay iteration and fires Hook.AfterCardPlayed inside the loop, while combat.py:514 fires on_card_played once after the whole `for _ in range(play_count)` loop. A doubled Skill therefore gives C# two Enrage triggers and the sim one. Per binding rule 3 the mechanism keeps G4's `gap` verdict and is cross-referenced rather than re-argued; […]
### `entropy`

- **AfterPlayerTurnStart** *(hook)* — The slot is post-draw on both sides, which is what matters for a hand-transform effect: C#'s Hook.AfterPlayerTurnStart is CombatManager.cs:675, the last statement of SetupPlayerTurn and therefore immediately after CardPileCmd.Draw at :673, and the sim fires on_player_turn_started at player.py:186 immediately after _draw at :183. The gap is that the sim has only ONE post-draw slot: PHASE COLLAPSE. […]
### `escape_artist`

- **EscapeArtistPower.cs:23 `participants.Contains(base.Owner)`** *(guard)* — Folded into the AfterSideTurnEnd entry; the sim's `enemy is self.owner` on a per-creature dispatch is the same intent on the wrong dispatcher.
### `flame_barrier`

- **Killing-blow guard (PROMPT.md bug class 2)** *(guard)* — Both sides hang the reflect on the AFTER-damage hook, so C# skips it when the hit killed the victim (CreatureCmd.cs:392) and so does the sim (cmds.py:121) -- faithful. But note the contrast with power/thorns, whose C# hook is BeforeDamageReceived and therefore fires on the killing blow: the two powers really do differ in the game, and the sim collapses both onto on_damage_received, which is right here and wrong there […]
### `hatch`

- **HatchPower.cs:20 `participants.Contains(base.Owner)`** *(guard)* — Folded into the AfterSideTurnEnd entry. Carried separately because with up to five simultaneous owners this is the one guard in this batch whose per-side vs per-creature reading genuinely differs, unlike the solo-owner units.
### `illusion`

- **The prevention branch's HP contract: sim `hp = 1` vs C# leaving the creature at 0 and re-entering KillWithoutCheckingWinCondition** *(guard)* — Part of the should_die-for-AfterDeath mechanism, carried as its own entry because it survives any fix that only re-slots the AfterDeath body. cmds.py:112 writes `target.hp = 1` and stops. C# does NOT heal the creature: CreatureCmd.cs:560-571 leaves it at 0 HP, fires Hook.AfterDeath(wasRemovalPrevented: true) and Hook.AfterPreventingDeath, and then, `if (creature.IsDead)`, RE-ENTERS `KillWithoutCheckingWinCondition(cr […]
- **ShouldCreatureBeRemovedFromCombatAfterDeath** *(hook)* — Same unused-hook finding as AdaptablePower's, and the same inconsistency with ReattachPower, which does implement it. Folded into the AfterDeath mechanism.
### `improvement`

- **ImprovementPower.cs:19 PileType.Deck filtered on IsUpgradable, and :27's list.Remove making the picks DISTINCT** *(guard)* — Also recorded for the implementation: the candidates are the RUN deck (not the combat piles), the filter is IsUpgradable (so already-upgraded and non-upgradable cards are excluded), the picks are without replacement, and the loop breaks early when the candidate list empties (ImprovementPower.cs:22-25). Vacuous today for the same reason as the previous guard.
### `juggernaut`

- **AfterBlockGained** *(hook)* — The hook, the guards, the props and the dealer are all right -- amount <= 0 and creature == base.Owner (JuggernautPower.cs:19 vs powers.py:781), and CreatureCmd.Damage(target, base.Amount, ValueProp.Unpowered, base.Owner) (:26) vs DamageCmd.deal(..., self.amount, dealer=self.owner, props=DamageProps.NON_CARD_UNPOWERED) (:791-794), where NON_CARD_UNPOWERED is UNPOWERED alone (valueprops.py:42) and so is BLOCKABLE, mat […]
### `juggling`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), before _process_turn_end_cards (combat.py:658) and before the flush (:661-662); […]
### `nostalgia`

- **Contention with power/corruption and power/rebound on the same chain** *(guard)* — Nostalgia is the one power in this group that uses the RIGHT hook, and that is precisely why it wins the contention the other two lose: hooks.modify_card_play_result_pile is dispatched at combat.py:510, BEFORE on_card_played at :514 where CorruptionPower and ReboundPower reach into the piles instead. […]
- **ModifyCardPlayResultPileTypeAndPosition** *(hook)* — ADDED BY HAND -- the harness did not enumerate it. NostalgiaPower.cs:16 is `public override (PileType, CardPilePosition) ModifyCardPlayResultPileTypeAndPosition(...)`, and harness.list_overrides' _OVERRIDE_RE return-type class does not match the parentheses of a tuple return type; […]
### `one_two_punch`

- **One-Two Punch is one of hook_dispatch G4's replay sources** *(guard)* — hook_dispatch G4 names powers.py:966 explicitly. Per binding rule 3 the same verdict, cross-referenced not re-argued.
### `painful_stabs`

- **AfterAttack** *(hook)* — WRONG HOOK, and the sim HAS the right one and does not use it. C#'s `AfterAttack(choiceContext, AttackCommand command)` fires ONCE per attack command, after every hit of every target, and is handed the whole `command.Results` (a List<List<DamageResult>>) so it can count hits and act once. The sim ports it onto `on_damage_received` (cmds.py:122), which fires PER HIT and inside the damage pipeline. […]
- **ShouldCreatureBeRemovedFromCombatAfterDeath** *(hook)* — `=> creature != base.Owner`, i.e. the Test Subject's corpse stays in combat. The sim has the hook (hooks.py:603, consumed at cmds.py:102-104) and this power does not use it -- AdaptablePower on the same creature prevents the death instead (audits/power/adaptable.json), so the phase transition works by a different route. Same unused-hook finding as adaptable's and illusion's; folded into that mechanism.
### `panache`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and before the flush (combat.py:661-662); […]
### `prep_time`

- **AfterSideTurnStart** *(hook)* — The slot is right -- Hook.AfterSideTurnStart (CombatManager.cs:522) and the sim's on_player_turn_started (player.py:186) are both post-draw, which matters because Hellraiser-style auto-plays that fire DURING the draw (CardPileCmd.Draw at :673) must NOT see this Vigor, and on both sides they do not. […]
### `rage`

- **Rage fires once per CardPlay, not once per Replay iteration** *(guard)* — hook_dispatch gap G4, same as power/enrage's matching guard: a doubled Attack gives C# two Rage triggers and the sim one. Per binding rule 3 the same verdict, cross-referenced not re-argued -- and note One-Two Punch and Duplication, both audited in this batch, are the ported play-count sources that make it reachable.
### `ravenous`

- **AfterDeath** *(hook)* — The guards are exact -- `target != base.Owner && target.Side == base.Owner.Side && !base.Owner.IsDead` (RavenousPower.cs:26) maps line-for-line to powers.py:1594-1599 -- and the effect order matches (stun the owner, then grant Strength). Two divergences. (1) MISSING `applier=` on the Strength grant: RavenousPower.cs:33 passes `base.Owner` and powers.py:1602 omits it; […]
### `rebound`

- **AfterModifyingCardPlayResultPileOrPosition** *(hook)* — C# consumes the stack from this dedicated after-hook (ReboundPower.cs:32-39 -> PowerCmd.Decrement), which Hook.ModifyCardPlayResultPileTypeAndPosition fires over exactly the listeners that changed the value (Hook.cs:1396-1405, one of hook_dispatch step 38's notification lists). […]
- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), which runs BEFORE _process_turn_end_cards (combat.py:658) and before the flush (combat.py:661-662); […]
- **ModifyCardPlayResultPileTypeAndPosition** *(hook)* — The destination-pile DECISION is replaced by an after-the-fact move. The sim has the matching hook -- hooks.modify_card_play_result_pile (hooks.py:236-246), dispatched at combat.py:510 -- and this power does not use it, reaching into the piles from on_card_played (combat.py:514) instead. […]
### `regen`

- **RegenPower.cs:22 `!base.Owner.IsDead`** *(guard)* — Absent from the sim -- see clause (c) of the AfterSideTurnEnd entry. Carried as its own guard because it is a distinct omission from the slot problem and would survive a slot fix.
- **RegenPower.cs:22 `participants.Contains(base.Owner)`** *(guard)* — Same mechanism as turn_structure gap G5 (the enemy side is per-enemy in the sim, per-side in the game): the sim's on_enemy_turn_end (combat.py:341) runs immediately after THIS enemy's move, where Hook.AfterTurnEnd -- the dispatcher for AbstractModel.AfterSideTurnEnd (Hook.cs:1267-1292) -- runs once at CombatManager.cs:1256 after EVERY enemy has moved. […]
### `ringing`

- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), before _process_turn_end_cards (combat.py:658) and before the flush (:661-662); […]
- **ShouldPlay** *(hook)* — HISTORY vs FLAG. C# answers 'has the owner played a card this turn' by querying CombatManager.History.CardPlaysStarted for entries that HappenedThisTurn; the sim keeps a boolean set from on_card_played. The two differ during a card's own resolution: C#'s history entry is written when the play STARTS (CardModel.cs:1930), so a card auto-played from inside the first card's resolution is already 'after a card this turn' […]
### `ritual`

- **RitualPower.cs:47 `participants.Contains(base.Owner)`** *(guard)* — Same mechanism as turn_structure gap G5 (the enemy side is per-enemy in the sim, per-side in the game): the sim's on_enemy_turn_end (combat.py:341) runs immediately after THIS enemy's move, where Hook.AfterTurnEnd -- the dispatcher for AbstractModel.AfterSideTurnEnd (Hook.cs:1267-1292) -- runs once at CombatManager.cs:1256 after EVERY enemy has moved. […]
- **AfterSideTurnEnd** *(hook)* — Slot divergence, separate from the AfterApplied defect. The enemy leg (powers.py:205-207) is the per-creature on_enemy_turn_end rather than on_enemy_side_end, and the player leg (powers.py:209-211) is Hook.BeforeTurnEnd rather than the sim's AfterTurnEnd slot after_player_turn_end (combat.py:665). Since Ritual's effect is a Strength gain on a creature that has already acted, the round-end state is usually the same; […]
### `rolling_boulder`

- **AfterPlayerTurnStart** *(hook)* — Post-draw on both sides (CombatManager.cs:675 vs player.py:186), so the slot itself is right. The gap is the phase collapse: PHASE COLLAPSE. The sim's on_player_turn_started (player.py:186) is a single slot serving THREE distinct C# phases that the game runs in a fixed order: Hook.AfterPlayerTurnStart (CombatManager.cs:675, inside the SetupPlayerTurn task launched at :514), then Hook.AfterSideTurnStart (:522), then H […]
- **InstanceType** *(hook)* — PowerInstanceType.Instanced (RollingBoulderPower.cs:24) means every application is an independently tracked instance with its own growing Amount; the sim's PowerCmd.apply merges by id via on_stack (cmds.py:308-311). […]
### `rupture`

- **RupturePower.cs:47 `CurrentSide == Owner.Side`** *(guard)* — Absent -- the core of the AfterDamageReceived gap. Carried as its own guard because it is a one-line omission that survives any fix to the deferral, and because it is the single divergence in this unit with the largest effect on play.
- **AfterCardPlayed** *(hook)* — The payout half of the deferral described on the BeforeCardPlayed entry: RupturePower.cs:60-66 removes the card's accumulator and applies the summed Strength once. Absent from the sim. Carried separately because the harness requires a verdict per override and because a fix needs all three hooks together.
### `shrink`

- **ModifyDamageMultiplicative** *(hook)* — NON-DYADIC FACTOR. C# computes `(100m - DamageDecrease) / 100m` in DECIMAL from the DynamicVar (ShrinkPower.cs:105, DamageDecrease = 30m at :18/:44) = exactly 0.7m; the sim returns the float literal 0.7 (powers.py:1386), which is not a binary fraction. […]
### `skittish`

- **AfterAttack** *(hook)* — C#'s hook is AfterAttack, which fires ONCE per AttackCommand after every hit has landed (SkittishPower.cs:56-69 searches command.Results for a DamageResult whose Receiver is the owner); the sim uses on_damage_received, which fires PER HIT. […]
- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), before _process_turn_end_cards (combat.py:658) and before the flush (:661-662); […]
### `slow`

- **SlowPower.cs:54 `!participants.Contains(base.Owner)` -> no reset** *(guard)* — The sim expresses it as `enemy is self.owner` (powers.py:1250) on the per-creature dispatch. Same intent, wrong dispatcher; folded into the AfterSideTurnStart entry.
### `smoggy`

- **AfterCardEnteredCombat** *(hook)* — Same pile-limbo shape as power/ringing's matching entry: the sim walks `getattr(self.owner, 'all_cards', ())`, and PlayerCombatState.all_cards (player.py:100-103) is hand + draw + discard + exhaust with NO Play pile, where C#'s PlayerCombatState.AllCards includes Play (PlayerCombatState.cs:70-80). […]
- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), before _process_turn_end_cards (combat.py:658) and before the flush (:661-662); […]
### `steam_eruption`

- **The prevention branch's HP contract: sim `hp = 1` vs C# leaving the creature at 0 and re-entering KillWithoutCheckingWinCondition** *(guard)* — Part of the should_die-for-AfterDeath mechanism, carried as its own entry because it survives any fix that only re-slots the AfterDeath body. cmds.py:112 writes `target.hp = 1` and stops. C# does NOT heal the creature: CreatureCmd.cs:560-571 leaves it at 0 HP, fires Hook.AfterDeath(wasRemovalPrevented: true) and Hook.AfterPreventingDeath, and then, `if (creature.IsDead)`, RE-ENTERS `KillWithoutCheckingWinCondition(cr […]
- **ShouldCreatureBeRemovedFromCombatAfterDeath** *(hook)* — The sim has the hook and this power does not use it, because it prevents the death instead. Same unused-hook finding as adaptable's and illusion's, and the same inconsistency with reattach, which does use it. Folded into the AfterDeath mechanism; carried separately because it is a concrete unused sim slot.
### `tangled`

- **AfterApplied** *(hook)* — The sim adds a guard C# does not have, and it changes the outcome. TangledPower.cs:23-30 afflicts EVERY Attack card with Entangled unconditionally -- there is no `Affliction == null` test, unlike its own AfterCardEnteredCombat at :34 and unlike Ringing's and Smoggy's AfterApplied -- so in the game Tangled OVERWRITES an existing affliction on Attack cards. […]
- **AfterSideTurnEnd** *(hook)* — SLOT. AbstractModel.AfterSideTurnEnd is dispatched by Hook.AfterTurnEnd (Hook.cs:1267-1292), called for the player side at CombatManager.cs:1307 -- AFTER the turn-end card effects and the hand flush. The sim's on_player_turn_end is Hook.BeforeTurnEnd (combat.py:654), before _process_turn_end_cards (combat.py:658) and before the flush (:661-662); […]
- **TryModifyEnergyCostInCombat** *(hook)* — This is hook_dispatch gap G3's own primary witness: Tangled is the EARLY-phase cost modifier and FreeAttackPower is the Late one, and the sim has a single unphased pass, so the result depends on the order the two powers happened to land. G3 records the executed evidence (applying Free Attack before Tangled leaves a Strike at cost 1; applying Tangled first leaves it at 0, where the game always makes it free). […]
### `territorial`

- **TerritorialPower.cs:23 `participants.Contains(base.Owner)`** *(guard)* — Same substitution as HighVoltagePower's: the sim tests `not self.owner.is_dead` (powers.py:1273) where C# tests side participation, which a retained corpse still satisfies. Identical mechanism, identical verdict (rule 3), identical dormancy -- Byrdonis is never retained after death.
### `toric_toughness`

- **InstanceType** *(hook)* — PowerInstanceType.Instanced: in C# every application is an independently tracked instance with its own Amount and its own internal state, where the sim's PowerCmd.apply merges by power id via on_stack (cmds.py:308-311). This is audits/seam/power_cmd.json's gap G5, which lists this power among its 11 ported InstanceType units; the mechanism is not re-verdicted here per binding rule 3. […]
### `unmovable`

- **ModifyBlockMultiplicative** *(hook)* — The sim replaces C#'s HISTORY QUERY with hand-rolled per-power state, and the two disagree about when the allowance resets. C# counts, on every call, History.Entries.OfType<BlockGainedEntry>() where e.HappenedThisTurn(base.CombatState) && e.Actor == target && e.Props.IsCardOrMonsterMove() && e.CardPlay != cardPlay (UnmovablePower.cs:35) -- so the window is the game's own turn window and the exclusion is 'gains from T […]
### `vital_spark`

- **BeforeCombatStart** *(hook)* — Identical shape to GalvanicPower's, one card type over (Skills rather than Powers, Tainted rather than Galvanized): the sim adds a `card.affliction is None` test that VitalSparkPower.cs:33-38 does not have, where C#'s CardCmd.Afflict overwrites. Same mechanism, same verdict (rule 3), same dormancy -- GalvanicPower afflicts the disjoint Power-card set and nothing else afflicts at combat start. […]

## Regenerating

```
py audit/tools/gap_ledger.py > audit/content/power/gap-ledger.md
```
