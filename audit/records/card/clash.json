{
  "unit": "card/clash",
  "game_source": {
    "path": "src\\Core\\Models\\Cards\\Clash.cs",
    "sha256": "76bd8112589d91fdf91b116e7ffc17709703be96d8cd8a032417e657398b53cc"
  },
  "sim_source": {
    "path": "sts2_rl\\cards\\trash_heap_cards.py",
    "sha256": "f93265bdcd41fa82dd09ab914501270537ae907e02f97679dd58dd2345183e20"
  },
  "extra_sources": [
    {
      "path": "src/Core/Commands/CardCmd.cs",
      "sha256": "8520d56b8db0c5c996dd0701f381d4e173e20731e50b056eed96c93ad4551904",
      "side": "game"
    },
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21",
      "side": "game"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "fc5d23e271b3b6c5618d6d81d545b406bd5eb3c9954b7dca28552a162fd5ed9f",
      "side": "sim"
    }
  ],
  "hooks": {
    "VisualCardPool": {
      "maps_to": "no counterpart; the sim has no card-pool artwork concept",
      "verdict": "faithful",
      "rationale": "`=> ModelDb.CardPool<IroncladCardPool>()` (Clash.cs:16) selects the card's ART pool, not its generation pool. Presentation."
    },
    "ctor": {
      "maps_to": "class attrs (trash_heap_cards.py:60-64) + _init_vars (66-68)",
      "verdict": "faithful",
      "rationale": "`base(0, CardType.Attack, CardRarity.Event, TargetType.AnyEnemy)` (Clash.cs:24-27) == cost 0 / ATTACK / EVENT / ANY_ENEMY."
    },
    "CanonicalVars": {
      "maps_to": "ClashCard._damage = 14 (trash_heap_cards.py:68)",
      "verdict": "faithful",
      "rationale": "`new DamageVar(14m, ValueProp.Move)` (Clash.cs:18) == `_damage = 14`, powered on both sides."
    },
    "IsPlayable": {
      "maps_to": "ClashCard.should_play_card (trash_heap_cards.py:73-78)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-29 (round 7): THE HOOK IS RIGHT NOW. `IsPlayable` is consulted in exactly one place -- `CardModel.CanPlay` (CardModel.cs:1759-1762), the MANUAL play path. `CardCmd.AutoPlay` checks only the Unplayable KEYWORD and `Hook.ShouldPlay` (CardCmd.cs:57-71), and Clash implements neither, so an AUTO-PLAYED Clash fires whatever else is in hand. The sim routes IsPlayable through `should_play_card`, which `auto_play_card` also consults, so the fix is the shape the entry named: `if auto_play: return True`, with the two C# call sites cited inline. EXECUTED: a mixed hand plus `auto_play_card` deals 14 where it dealt 0, and a MANUAL play is still refused.  Pinned by test/test_card_residue_gaps.py.  NOT CLOSED: nothing at this site.  The text it replaced read: The PREDICATE is faithful -- `CardPile.GetCards(Owner, PileType.Hand).All(c => c.Type == CardType.Attack)` (Clash.cs:20) == `all(c.card_type == CardType.ATTACK for c in self.combat.player.hand)`, and the sim correctly returns True for every other card so the listener only vetoes itself. THE HOOK IS WRONG. C# `IsPlayable` is consulted in exactly ONE place: `CardModel.CanPlay` (CardModel.cs:1759-1762, `if (!IsPlayable) reason |= UnplayableReason.BlockedByCardLogic`), which is the MANUAL play path. `CardCmd.AutoPlay` does not call CanPlay -- it checks only the Unplayable KEYWORD and `Hook.ShouldPlay` (CardCmd.cs:57-71), and Clash implements neither. So in the game an AUTO-PLAYED Clash fires regardless of what else is in hand. The sim routes the gate through `should_play_card`, which `CombatState.auto_play_card` DOES consult (combat.py:536, with `auto_play=True`), and Clash's implementation ignores that flag -- so the sim moves Clash to the discard pile without playing it. Observable: a mixed hand plus any auto-play source -- the game deals 14, the sim deals 0. LIVE: Clash is a ported Event Attack (Trash Heap) and four ported cards auto-play Attacks from a pile -- card/beat_down (Attacks from the discard pile), card/havoc, card/catastrophe and card/cascade (top of the draw pile) -- plus relics/events that auto-play. The fix shape is `if auto_play: return True`."
    },
    "OnPlay": {
      "maps_to": "ClashCard.on_play (trash_heap_cards.py:80-85)",
      "verdict": "faithful",
      "rationale": "`DamageCmd.Attack(14).FromCard(this).Targeting(cardPlay.Target)` (Clash.cs:32-34) == the sim's single `DamageCmd.deal(..., dealer=ctx.player, card=self)`."
    },
    "OnUpgrade": {
      "maps_to": "ClashCard._on_upgrade (trash_heap_cards.py:70-71)",
      "verdict": "faithful",
      "rationale": "`DynamicVars.Damage.UpgradeValueBy(4m)` (Clash.cs:39) == `self._damage += 4` (14 -> 18)."
    },
    "ShouldGlowGoldInternal": {
      "maps_to": "no counterpart",
      "verdict": "faithful",
      "rationale": "`protected override bool ShouldGlowGoldInternal => IsPlayable` (Clash.cs:22) is the golden-glow highlight in the hand UI. Presentation, out of scope, and the sim has no card-visual layer."
    }
  },
  "guards": [],
  "verdict": "faithful",
  "audited": "2026-07-26"
}
