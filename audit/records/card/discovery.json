{
  "unit": "card/discovery",
  "game_source": {
    "path": "src\\Core\\Models\\Cards\\Discovery.cs",
    "sha256": "3b68ad6836f9e1399cbac62814d931d671f326dfa99b1089b264ee0af2621a82"
  },
  "sim_source": {
    "path": "sts2_rl\\cards\\colorless_skills.py",
    "sha256": "582eae297c48ba02774195e37e6ddb76d3874c28f7007d4c54267ed41da7e6d8"
  },
  "extra_sources": [
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21",
      "side": "game"
    },
    {
      "path": "sts2_rl/cards/base.py",
      "sha256": "bb2bd40b8e6f238335476bd5b8cbc2463773cc692acfdc5cf6d9214ac8591a3b",
      "side": "sim"
    },
    {
      "path": "sts2_rl/cards/pool.py",
      "sha256": "879d6791db6dd9ec5d35355d75e3e784be4c380fb7d0bcd65b0ede93ff8cd339",
      "side": "sim"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "bb188f24250cb28e3602288181b4a87ef0e7a9c1e72900c3a255b73848ccd582",
      "side": "sim"
    },
    {
      "path": "sts2_rl/combat_rng.py",
      "sha256": "f8c7d64ea444f18a173615c6011234e7aa923004e96bba7e68bda6157a509a1f",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "670603b2b78e5d8ffb15b0b5b4b5ab51777d0c00a29505b5ed37b4220d88e3e8",
      "side": "sim"
    },
    {
      "path": "test/test_rng_tripwire.py",
      "sha256": "c34c8bc722ef8c237fc8f09cb3bb0b09e789c97f16fc3cc11619ffabc270a008",
      "side": "sim"
    }
  ],
  "hooks": {
    "CanonicalKeywords": {
      "maps_to": "DiscoveryCard.exhausts, set in _init_vars (colorless_skills.py:212)",
      "verdict": "faithful",
      "rationale": "`{ CardKeyword.Exhaust }` (Discovery.cs:15) == `self.exhausts = True`. Crucially this is set in `_init_vars`, NOT as a class attribute, precisely so the upgrade's `RemoveKeyword` can be undone by `downgrade()`'s rebuild -- and `py audit/tools/card_probes.py downgrade` confirms discovery is NOT among the five sticky cards. This is the pattern aggression/apparition/hello_world/juggling/wish are missing."
    },
    "ctor": {
      "maps_to": "class attrs (colorless_skills.py:206-210) + _init_vars (211-212)",
      "verdict": "faithful",
      "rationale": "`base(1, CardType.Skill, CardRarity.Uncommon, TargetType.Self)` (Discovery.cs:17-20) == cost 1 / SKILL / UNCOMMON / SELF."
    },
    "OnPlay": {
      "maps_to": "DiscoveryCard.on_play (colorless_skills.py:218-225)",
      "verdict": "gap",
      "issue": "The SHAPE is right -- generate 3 distinct cards, choose 1, make it free this turn, add it to hand -- and both sides apply `SetToFreeThisTurn` BEFORE the pile add, which matters because the sim's `set_free_this_turn` is cleared by the turn-start reset (cards/base.py:265-269) exactly like C#'s EndOfTurn expiration. TWO DIVERGENCES. (1) WRONG RNG STREAM: `CardFactory.GetDistinctForCombat(Owner, Owner.Character.CardPool.GetUnlockedCards(...), 3, Owner.RunState.Rng.CombatCardGeneration)` (Discovery.cs:27) names the CombatCardGeneration stream; the sim calls `random_pool_cards(ctx.combat._rng, 3, distinct=True)` (colorless_skills.py:222), i.e. the unseeded shared `random.Random`, even though `ctx.combat.combat_rng.card_gen` maps to `combat_card_generation` (combat_rng.py:20,52). `random_pool_cards` also uses `rng.sample` (pool.py:158) where GetDistinctForCombat draws from the named stream, so the draw count differs too. Same mechanism and same verdict as card/anointed's and card/beat_down's OnPlay entries (rule 3); LIVE, and this card is one of the 21 the `shared-rng` probe lists. (2) `canSkip: true` (Discovery.cs:28) is not modelled: `CombatState.select_cards` always returns a card when candidates exist (combat.py:575-581), so the sim cannot decline. DORMANT for the skip half -- declining is strictly worse play and no replay command for it is wired -- but it is a missing player choice, not a re-architecture. REGRESSION TEST: `test/test_rng_tripwire.py:33` (`test_no_wrong_stream_draws_in_random_run`, 20 seeded full runs) is the standing gate for this whole family. It wraps `RunState.rng` and asserts ZERO in-combat draws on it; `CombatState` receives that same object as `self._rng` (run.py:1146-1148, combat.py:88), so a `ctx.combat._rng` draw taken from inside a card's `on_play` is recorded and fails the gate -- verified by playing this family's shape under a `Tripwire` and getting a hit at the `card.on_play` frame (combat.py:490). The gate is green today, so these 20 seeds never reach this card's draw; routing the site to the named `combat_rng` accessor is verified by keeping it green."
    },
    "OnUpgrade": {
      "maps_to": "DiscoveryCard._on_upgrade (colorless_skills.py:215-216)",
      "verdict": "faithful",
      "rationale": "`RemoveKeyword(CardKeyword.Exhaust)` (Discovery.cs:43) == `self.exhausts = False`."
    }
  },
  "guards": [
    {
      "what": "BLAST RADIUS of creature_card_cmds step 52 (Downgrade skips Enchantment.ModifyCard())",
      "verdict": "faithful",
      "rationale": "The seam's step 52 names a SOULS-enchanted Discovery as its witness: `DowngradeInternal` re-applies `Enchantment?.ModifyCard()` (CardModel.cs:2145) and the sim's `downgrade()` does not, so an enchantment that removes Exhaust is not re-applied after upgrade+downgrade. Flagged here, not re-verdicted: the mechanism is `Card.downgrade`'s, and Discovery's own keyword handling is correct."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
