{
  "unit": "card/hidden_gem",
  "game_source": {
    "path": "src\\Core\\Models\\Cards\\HiddenGem.cs",
    "sha256": "f78f17dc5542626381acf6b879fbb49063df9fb779bacc32b3bc0ee589bce286"
  },
  "sim_source": {
    "path": "sts2_rl\\cards\\colorless_skills.py",
    "sha256": "de6f1cb802f22773f1d0d1ec8132e67cf2dc7edcc0e302ddad615d39dbd4d174"
  },
  "extra_sources": [
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21",
      "side": "game"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "351acc27ce4b62f37d5f9d93cb88e087b5c4de6ac3a7de43105c57c2bd85d963",
      "side": "sim"
    },
    {
      "path": "sts2_rl/combat_rng.py",
      "sha256": "f8c7d64ea444f18a173615c6011234e7aa923004e96bba7e68bda6157a509a1f",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "222a4b444f5b111b10fd4c5bccfd4d0cfc0901138724911e31a7a123a521251b",
      "side": "sim"
    },
    {
      "path": "test/test_rng_tripwire.py",
      "sha256": "f6bed2b1db59d7bab3be63259896987637e4f92d665c830fd7ed6fac0a80eba7",
      "side": "sim"
    }
  ],
  "hooks": {
    "CanBeGeneratedInCombat": {
      "maps_to": "HiddenGemCard.can_be_generated_in_combat = False (colorless_skills.py:298)",
      "verdict": "faithful",
      "rationale": "`=> false` (HiddenGem.cs:16) == the class attribute -- correctly mapped, like card/feed and card/hand_of_greed."
    },
    "ctor": {
      "maps_to": "class attrs (colorless_skills.py:293-298) + _init_vars (300-302)",
      "verdict": "faithful",
      "rationale": "`base(1, CardType.Skill, CardRarity.Rare, TargetType.Self)` (HiddenGem.cs:22-25) == cost 1 / SKILL / RARE / SELF."
    },
    "CanonicalVars": {
      "maps_to": "HiddenGemCard._replay = 2 (colorless_skills.py:302)",
      "verdict": "faithful",
      "rationale": "`new IntVar(\"Replay\", 2m)` (HiddenGem.cs:18) == `_replay = 2`, the amount added to the chosen card's `BaseReplayCount`."
    },
    "OnPlay": {
      "maps_to": "HiddenGemCard.on_play (colorless_skills.py:307-329)",
      "verdict": "gap",
      "issue": "The empty-pile early return, the two-tier filter and the prefer-then-fall-back structure are all faithful. C#'s obfuscated type tests decode as expected: `(uint)(type - 5) <= 1u` excludes the two type values above Power (Curse and Quest) == the sim's `card_type not in (CardType.CURSE, CardType.QUEST)`, and `(uint)(type - 1) <= 2u` is the Attack/Skill/Power preference == the sim's `preferred` list; `options = preferred or eligible` is exactly C#'s `(list3.Count == 0) ? list2 : list3`. TWO DIVERGENCES. (1) WRONG RNG STREAM: `Owner.RunState.Rng.CombatCardSelection.NextItem(items)` (HiddenGem.cs:53) vs the sim's `ctx.combat._rng.choice(options)` (colorless_skills.py:321), where `ctx.combat.combat_rng.card_selection` maps to `combat_card_selection` (combat_rng.py:21,55). Same mechanism and same verdict as card/anointed's OnPlay entry (rule 3); LIVE, and one of the 21 `shared-rng` cards. (2) THE ALREADY-REPLAYING FILTER IS TOO NARROW: C#'s third clause is `c.GetEnchantedReplayCount() < 1`, and `GetEnchantedReplayCount()` is `Enchantment?.EnchantPlayCount(BaseReplayCount) ?? BaseReplayCount` (CardModel.cs:1129-1132) -- note the null branch returns BASEREPLAYCOUNT, so a card that is already replaying for ANY reason is excluded, including one a previous Hidden Gem raised. The sim's `_has_replay_enchantment` (colorless_skills.py:324-329) only checks whether the enchantment id is `spiral` or `glam`, so it never looks at `base_replay_count` at all. Observable: play two Hidden Gems and the sim can stack both on the SAME draw-pile card (5 plays) where the game must pick two different ones. LIVE -- Hidden Gem is a ported Rare colorless Skill and nothing stops a second copy. Presentation, folded in: TriggerAnim (29) and `CardCmd.Preview` (57). REGRESSION TEST: `test/test_rng_tripwire.py:33` (`test_no_wrong_stream_draws_in_random_run`, 20 seeded full runs) is the standing gate for this whole family. It wraps `RunState.rng` and asserts ZERO in-combat draws on it; `CombatState` receives that same object as `self._rng` (run.py:1146-1148, combat.py:88), so a `ctx.combat._rng` draw taken from inside a card's `on_play` is recorded and fails the gate -- verified by playing this family's shape under a `Tripwire` and getting a hit at the `card.on_play` frame (combat.py:490). The gate is green today, so these 20 seeds never reach this card's draw; routing the site to the named `combat_rng` accessor is verified by keeping it green."
    },
    "OnUpgrade": {
      "maps_to": "HiddenGemCard._on_upgrade (colorless_skills.py:304-305)",
      "verdict": "faithful",
      "rationale": "`DynamicVars[\"Replay\"].UpgradeValueBy(1m)` (HiddenGem.cs:63) == `self._replay += 1` (2 -> 3)."
    }
  },
  "guards": [
    {
      "what": "the card raises `BaseReplayCount` on ANOTHER card rather than doing anything itself (HiddenGem.cs:56)",
      "verdict": "faithful",
      "rationale": "`cardModel.BaseReplayCount += Replay` == `card.base_replay_count += self._replay`, and the sim's normal play path feeds that field into `modify_card_play_count` as `1 + card.base_replay_count` (combat.py:469-470), matching C#'s `GetEnchantedReplayCount() + 1` (CardModel.cs:2017). So Hidden Gem is the card-side SOURCE of gap G4's blast radius, and card/drum_of_battle -- which reads the hook with a bare 1 and so ignores `base_replay_count` -- is the one ported consumer that gets it wrong."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
