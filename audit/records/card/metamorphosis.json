{
  "unit": "card/metamorphosis",
  "game_source": {
    "path": "src\\Core\\Models\\Cards\\Metamorphosis.cs",
    "sha256": "4bdffa4fe4edfaeafd34d6d7a2318604beb4053a2b8c9a4d147fd497052767b4"
  },
  "sim_source": {
    "path": "sts2_rl\\cards\\event_cards.py",
    "sha256": "e2252c0e6b225be22f1da3051e67dc0f235f6b82ec2ccf0ea6158a20701730df"
  },
  "extra_sources": [
    {
      "path": "src/Core/Commands/CardPileCmd.cs",
      "sha256": "4bc599d4f58de90db206f2509c6467d7b769bed92a67df7d3fc681a21959fe47",
      "side": "game"
    },
    {
      "path": "sts2_rl/cards/base.py",
      "sha256": "bb2bd40b8e6f238335476bd5b8cbc2463773cc692acfdc5cf6d9214ac8591a3b",
      "side": "sim"
    },
    {
      "path": "sts2_rl/cards/pool.py",
      "sha256": "879d6791db6dd9ec5d35355d75e3e784be4c380fb7d0bcd65b0ede93ff8cd339",
      "side": "sim"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "bb188f24250cb28e3602288181b4a87ef0e7a9c1e72900c3a255b73848ccd582",
      "side": "sim"
    },
    {
      "path": "sts2_rl/combat_rng.py",
      "sha256": "f8c7d64ea444f18a173615c6011234e7aa923004e96bba7e68bda6157a509a1f",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "670603b2b78e5d8ffb15b0b5b4b5ab51777d0c00a29505b5ed37b4220d88e3e8",
      "side": "sim"
    },
    {
      "path": "test/test_rng_tripwire.py",
      "sha256": "c34c8bc722ef8c237fc8f09cb3bb0b09e789c97f16fc3cc11619ffabc270a008",
      "side": "sim"
    }
  ],
  "hooks": {
    "CanonicalKeywords": {
      "maps_to": "MetamorphosisCard.exhausts = True (event_cards.py:289)",
      "verdict": "faithful",
      "rationale": "`{ CardKeyword.Exhaust }` (Metamorphosis.cs:14) == `exhausts = True`."
    },
    "ctor": {
      "maps_to": "class attrs (event_cards.py:284-289) + _init_vars (291-293)",
      "verdict": "faithful",
      "rationale": "`base(2, CardType.Skill, CardRarity.Event, TargetType.Self)` (Metamorphosis.cs:18-21) == cost 2 / SKILL / EVENT / SELF."
    },
    "CanonicalVars": {
      "maps_to": "MetamorphosisCard._cards = 3 (event_cards.py:293)",
      "verdict": "faithful",
      "rationale": "`new CardsVar(3)` (Metamorphosis.cs:16) == `_cards = 3`."
    },
    "OnPlay": {
      "maps_to": "MetamorphosisCard.on_play (event_cards.py:298-304)",
      "verdict": "gap",
      "issue": "The pool filter (character-pool ATTACKS), the count, the free-for-the-combat marking and the destination pile remain faithful in shape: `CardFactory.GetForCombat` WITH replacement (matching `random_pool_cards(..., distinct` unset), `item.SetToFreeThisCombat()` == `card.set_cost_this_combat(0)` (event_cards.py:303), and PileType.Draw. THE PILE POSITION IS NO LONGER PART OF THIS GAP: `CardPileCmd.add_to_draw` (cmds.py:606-628) inserts at a RANDOM index rather than appending, and its parity arm draws `p = crng.shuffle.randrange(count + 1)` and maps the game's top-first index onto the sim's reversed pile with `player.draw_pile.insert(count - p, card)` (cmds.py:623-625) -- exactly `CardPilePosition.Random => card.Owner.RunState.Rng.Shuffle.NextInt(targetPile.Cards.Count + 1)` (CardPileCmd.cs:514). (The original issue text cited the wrong C# stream for this: CardPileCmd.cs:946's CombatCardSelection draw belongs to AutoPlayFromDrawPile's Random position, not to Add's.) WHAT REMAINS -- WRONG RNG STREAM AND WRONG DRAW ALGORITHM FOR THE CARD GENERATION: C# names `base.Owner.RunState.Rng.CombatCardGeneration` (Metamorphosis.cs:25-27), while the sim still calls `random_pool_cards(ctx.combat._rng, self._cards, _CT.ATTACK)` (event_cards.py:302) on the unseeded shared `combat._rng`, with NO `crng.is_parity` branch anywhere in the method, even though `ctx.combat.combat_rng.card_gen` maps to `combat_card_generation` (combat_rng.py:20,52). `random_pool_cards` also picks with `rng.choice` per card (pool.py:160) rather than the game's `NextItem(FilterForCombat(pool))` draw sequence, and the ready-made with-replacement port `get_for_combat_parity` (cards/pool.py:164-176) has no metamorphosis call site -- only stoke.py:35-43 uses it. card/infernal_blade (infernal_blade.py:37-41) and card/mad_science's Chaos rider (mad_science.py:141-152) show the correct shape: branch on `crng.is_parity`, pass `crng.card_gen`. SECONDARY RESIDUE: the LEGACY arm of `add_to_draw` takes its random index from the shared `hooks.combat._rng` (cmds.py:627), so the default/RL path spends a shared-rng draw where the game spends a Shuffle draw. LIVE, and this card is one of the 21 the `shared-rng` probe lists. REGRESSION TEST: `test/test_rng_tripwire.py:33` (`test_no_wrong_stream_draws_in_random_run`, 20 seeded full runs) is the standing gate for this family; it wraps `RunState.rng` and asserts ZERO in-combat draws on it, and `CombatState` receives that same object as `self._rng`, so a `ctx.combat._rng` draw from inside `on_play` fails the gate. The gate is green today only because those 20 seeds never reach this card; routing the generation to `crng.card_gen` is verified by keeping it green.  NARROWED 2026-07-28 (Task 0, ledger-vs-code pass after the Tier 1 fix campaign): the rest of this entry is closed in today's code. The issue it replaced read: The pool filter (character-pool ATTACKS), the count, the free-for-the-combat marking and the destination pile are all faithful in shape: `CardFactory.GetForCombat` WITH replacement (so duplicates are allowed, matching `random_pool_cards(..., distinct` unset), `item.SetToFreeThisCombat()` == `card.set_cost_this_combat(0)` (cards/base.py:234-237 documents that verb as EnergyCost.SetThisCombat's mirror), and PileType.Draw. TWO DIVERGENCES. (1) THE PILE POSITION IS WRONG AND COSTS RNG: C# adds at `CardPilePosition.Random` (Metamorphosis.cs:31), which `CardPileCmd`'s Random branch resolves with a draw on the CombatCardSelection stream (the same `Rng.CombatCardSelection.NextItem(drawPile.Cards)` shape CardPileCmd.cs:946 uses for AutoPlayFromDrawPile's Random position); the sim's `add_to_draw` appends to the end of the draw pile, i.e. deterministically on TOP, taking no draw at all. So the three generated Attacks are scattered through the game's draw pile and sit on top of the sim's, AND the sim is three CombatCardSelection draws short. Observable immediately -- the very next draw is a generated Attack in the sim and usually is not in the game. (2) WRONG RNG STREAM: C# names `Owner.RunState.Rng.CombatCardGeneration`; the sim draws on the unseeded shared `combat._rng` even though `ctx.combat.combat_rng.card_gen` maps to `combat_card_generation` (combat_rng.py:20,52), and `random_pool_cards` uses `rng.sample` / `rng.choice` (pool.py:157-161) rather than the game's draw sequence. Same mechanism and same verdict as card/discovery's OnPlay entry (rule 3). LIVE, and this card is one of the 21 the `shared-rng` probe lists. card/infernal_blade and card/mad_science's Chaos rider both show the correct shape -- branch on `crng.is_parity` and use `crng.card_gen`. REGRESSION TEST: `test/test_rng_tripwire.py:33` (`test_no_wrong_stream_draws_in_random_run`, 20 seeded full runs) is the standing gate for this whole family. It wraps `RunState.rng` and asserts ZERO in-combat draws on it; `CombatState` receives that same object as `self._rng` (run.py:1146-1148, combat.py:88), so a `ctx.combat._rng` draw taken from inside a card's `on_play` is recorded and fails the gate -- verified by playing this family's shape under a `Tripwire` and getting a hit at the `card.on_play` frame (combat.py:490). The gate is green today, so these 20 seeds never reach this card's draw; routing the site to the named `combat_rng` accessor is verified by keeping it green."
    },
    "OnUpgrade": {
      "maps_to": "MetamorphosisCard._on_upgrade (event_cards.py:295-296)",
      "verdict": "faithful",
      "rationale": "`DynamicVars.Cards.UpgradeValueBy(2m)` (Metamorphosis.cs:37) == `self._cards += 2` (3 -> 5). Note the upgrade adds TWO, not one -- the only +2 CardsVar upgrade in the pool so far, and easy to transcribe as +1."
    }
  },
  "guards": [],
  "verdict": "gap",
  "audited": "2026-07-26"
}
