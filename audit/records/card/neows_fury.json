{
  "unit": "card/neows_fury",
  "game_source": {
    "path": "src\\Core\\Models\\Cards\\NeowsFury.cs",
    "sha256": "27bdce687aec1a04323c8bd52e763c053bdf1ea8ca6c5b83ffe62c926f5f3c43"
  },
  "sim_source": {
    "path": "sts2_rl\\cards\\neows_fury.py",
    "sha256": "bbd33fee525f35f6d1aa19cefa9b7d528a3d212268f5985f21d7eba2a49e27e3"
  },
  "extra_sources": [
    {
      "path": "src/Core/Commands/CardPileCmd.cs",
      "sha256": "4bc599d4f58de90db206f2509c6467d7b769bed92a67df7d3fc681a21959fe47",
      "side": "game"
    },
    {
      "path": "src/Core/Entities/Cards/CardPile.cs",
      "sha256": "8aa6ed9f80eabbb282950baf2b67a2ca02a6dd5e95308a7cf9dd2da0d3535c6e",
      "side": "game"
    },
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21",
      "side": "game"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "9e2c8a38776f6f0bb505b5b916976f6657c19360ad862d68022c19765848979e",
      "side": "sim"
    },
    {
      "path": "sts2_rl/player.py",
      "sha256": "f5b830ff2b35f6d56c8f3844c62d82b338c9ec4107fb918f54d61ae6a333a0aa",
      "side": "sim"
    }
  ],
  "hooks": {
    "CanBeGeneratedInCombat": {
      "maps_to": "NOT PORTED -- the sim relies on rarity instead (neows_fury.py:378-379)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): Flag pair corrected against the C#, read line-by-line for all six cards: none overrides CanBeGeneratedByModifiers or CanonicalKeywords, so the sim's is_playable=False AND can_be_generated_by_modifiers=False were BOTH wrong and can_be_generated_in_combat=False was missing. These Statuses are playable no-effect cards in the game (the shape the sim already models for card/beckon). LIVENESS CORRECTION: the recorded dormancy checked only pool_card_ids and curse_pool_ids, but a THIRD consumer exists -- transform_options_in_combat's STATUS branch, reached by ported Entropy. Pre-fix this genuinely leaked all four bad cards as transform options for every reachable Status card, including frantic_escape, which The Insatiable really does put in piles (test_hive.py::TestTheInsatiable). Reproduced by execution by both the implementer and the task reviewer, and closed by the same fix -- the STATUS branch's filter is the correct mirror of GetFilteredTransformationOptions (CardFactory.cs:189-203). So this mechanism was recorded DORMANT but was reachable; it is fixed, not merely re-argued. Pinned in test/test_card_generation_pool.py.  The text it replaced read: `public override bool CanBeGeneratedInCombat => false` (NeowsFury.cs:15) has no `can_be_generated_in_combat = False` counterpart; the sim's comment says the ANCIENT rarity already keeps it out of `pool_card_ids`. That is true today, so the OUTCOME matches -- but it is a different mechanism, and the flag is the one C# actually exposes: `CardModel.CanBeGeneratedInCombat` (CardModel.cs:642) is read by FilterForCombat regardless of rarity, so any future in-combat generator pointed at a pool that includes Ancients would pick up Neow's Fury in the sim and not in the game. DORMANT with the trigger named. Contrast card/feed, card/hand_of_greed and card/hidden_gem, which set the flag explicitly, and card/disintegration / card/frantic_escape / card/mind_rot, which set the WRONG flag -- three different treatments of one override across six cards."
    },
    "CanonicalKeywords": {
      "maps_to": "NeowsFuryCard.exhausts = True (neows_fury.py:380)",
      "verdict": "faithful",
      "rationale": "`{ CardKeyword.Exhaust }` (NeowsFury.cs:17) == `exhausts = True`."
    },
    "ctor": {
      "maps_to": "class attrs (neows_fury.py:373-380) + _init_vars (382-385)",
      "verdict": "faithful",
      "rationale": "`base(1, CardType.Attack, CardRarity.Ancient, TargetType.AnyEnemy)` (NeowsFury.cs:25-28) == cost 1 / ATTACK / ANCIENT / ANY_ENEMY."
    },
    "CanonicalVars": {
      "maps_to": "_damage = 10, _cards = 2 (neows_fury.py:384-385)",
      "verdict": "faithful",
      "rationale": "`new DamageVar(10m, ValueProp.Move)` + `new CardsVar(2)` (NeowsFury.cs:19-23) == both numbers, both declared."
    },
    "OnPlay": {
      "maps_to": "NeowsFuryCard.on_play (neows_fury.py:391-407)",
      "verdict": "gap",
      "issue": "Attack first, then the hand-size-capped selection: `Math.Min(Cards.IntValue, CardPile.MaxCardsInHand - Hand.Cards.Count)` (NeowsFury.cs:36) == `min(self._cards, PlayerCombatState.MAX_HAND_SIZE - len(ctx.player.hand))`, with both skipping everything when the result is <= 0 (37 == 399-400), and MaxCardsInHand is 10 on both sides (CardPile.cs:21, player.py:56). The `predicate=lambda c: c is not self` is a CORRECT compensation for the sim's discard-pile limbo, matching card/headbutt. THE SELECTION RANGE IS WRONG. C# passes `new CardSelectorPrefs(prompt, 0, num)` (NeowsFury.cs:39) -- MinSelect **0**, MaxSelect num -- so the player may take FEWER than the cap, or none at all. The sim's `CardSelectCmd.from_pile(..., count=count)` resolves through `CombatState.select_cards`, which clamps `count = min(count, len(candidates))` and then takes exactly that many (combat.py:575-581), with no way to express a range. So the sim cannot decline part of the return. DORMANT: taking fewer cards back is strictly worse play, no replay command encodes a partial selection, and the auto-select shortcut C# would take at `candidateCount <= MinSelect` never fires with MinSelect 0 unless the discard pile is empty (where both sides do nothing). It becomes live for any conformance replay of a run where the player declined. This is the min/max-range half of creature_card_cmds guard N10, recorded at a card site because Neow's Fury is the first unit in the pool whose C# prefs actually use a RANGE rather than a fixed count."
    },
    "OnUpgrade": {
      "maps_to": "NeowsFuryCard._on_upgrade (neows_fury.py:387-389)",
      "verdict": "faithful",
      "rationale": "`DynamicVars.Damage.UpgradeValueBy(4m)` + `DynamicVars.Cards.UpgradeValueBy(1m)` (NeowsFury.cs:45-46) == `_damage += 4` (10 -> 14) and `_cards += 1` (2 -> 3). Both upgraded numbers checked."
    }
  },
  "guards": [
    {
      "what": "the chosen cards are moved with `CardPileCmd.Add(list, PileType.Hand)` in C# (NeowsFury.cs:39) and by direct list mutation in the sim (neows_fury.py:405-407)",
      "verdict": "gap",
      "issue": "The sim pops the chosen cards out of `player.discard_pile` and appends them to `player.hand` in place (neows_fury.py:405-407) instead of routing through a pile-add verb, so nothing fires for the pile transition. C# `CardPileCmd.Add` runs `Hook.AfterCardChangedPiles` (CardPileCmd.cs:635) and enforces `MaxCardsInHand` inside `AddInternal` (the cap is separately enforced here by the `count` computation in OnPlay, so only the hook is dropped). SAME MECHANISM AND SAME VERDICT AS card/anointed's `CardPileCmd.Add` guard (rule 3) -- this is the card-level instance of creature_card_cmds's 'no unified AfterCardChangedPiles' structural finding (docs/audit/seams/creature_card_cmds.md:643-666), recorded here because it is Neow's Fury's own code doing the bypass. DORMANT: the sim has no `after_card_changed_piles` listener at all, so nothing observable is dropped yet. TRIGGER: the first ported model overriding `AfterCardChangedPiles`. Was recorded faithful while its own rationale said \"Same structural point as card/anointed's guard\" and card/anointed carries `gap`; the report also files it under the DORMANT-GAP family \"Direct pile mutation instead of a pile verb\" (§5)."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
