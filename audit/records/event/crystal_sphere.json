{
  "unit": "event/crystal_sphere",
  "game_source": {
    "path": "src\\Core\\Models\\Events\\CrystalSphere.cs",
    "sha256": "eb7331c7fdbd2d5dbd439450dfdbbe692fd83aa4b725012d1cf239e6d64fd2df"
  },
  "sim_source": {
    "path": "sts2_rl\\events\\crystal_sphere.py",
    "sha256": "9cc995057bd92ced914d27f31edfeed71c7416f2594357731e249e95d88cb679"
  },
  "extra_sources": [
    {
      "path": "sts2_rl/events/base.py",
      "sha256": "70bda89d8e4509e892ea50283ae24c5763e09f94b6121106140c40cc30e870e1",
      "side": "sim"
    }
  ],
  "hooks": {
    "IsDeterministic": {
      "maps_to": "CrystalSphere.cs:34 `IsDeterministic => false` marks the event as consuming RNG at CalculateVars time. The sim has no IsDeterministic surface; the property's only consumer is the event-selection machinery, and this unit's CalculateVars is unreachable behind is_allowed (see below), so the flag has nothing to gate.",
      "verdict": "faithful"
    },
    "CalculateVars": {
      "maps_to": "nothing. CrystalSphere.cs:44-47 rolls `UncoverFutureCost += base.Rng.NextInt(1, 50)`; events/crystal_sphere.py has no calculate_vars, inheriting the base no-op (events/base.py:109-110)",
      "verdict": "gap",
      "live": false,
      "issue": "SETTLED 2026-07-29 (round 11). This entry's own text said it inherits the DEFERRED-PORT guard's verdict; the brief for this pass asked which of two readings is correct -- 'the event is stubbed off, so the sub-entries are dormant' or 'the stub itself is the live gap' -- and required an explicit answer rather than a re-statement of the deferral. ANSWER: the stub (IsAllowed's hard `return False`) IS the live gap, recorded at the sibling `IsAllowed` entry below; THIS entry, `CalculateVars`, is a genuine consequence of that gap and is DORMANT today, not live, because nothing in the sim can reach it while `IsAllowed` stays hard-False -- not merely 'unlikely to be called' but provably never called by any live code path. EXECUTED (`.superpowers/sdd/unlabelled/probes/misc-crystal-sphere-witness.py`): built a `RunState` with `act_index = 1` (act 2, `CurrentActIndex > 0`) and `gold = 250` (>= the 100-gold gate) -- ordinary, common run state -- and confirmed `CrystalSphere.is_allowed(run)` still returns `False`, AND that `events.allowed_events(run, SHARED_EVENTS)` (the exact filter `sts2_rl/rooms.py:451` and `events/__init__.py:146` both apply before an event is ever selected for a '?' node) excludes `'crystal_sphere'` from its candidate list. Since `is_allowed` is checked at SELECTION time, before any `CrystalSphere` instance is built or `.begin()` (which is what calls `calculate_vars()`, `events/base.py:173`) is ever invoked, `calculate_vars` cannot run through any reachable path today. Concrete trigger: fixing `IsAllowed` to the real gate (or porting the minigame and dropping the stub) is exactly what would make this entry live -- at that point CrystalSphere.cs:44-47's RNG roll would need a sim counterpart too, since UncoverFutureCost's `NextInt(1, 50)` is otherwise a silent no-op relative to the event's own `Rng` stream."
    },
    "IsAllowed": {
      "maps_to": "CrystalSphere.is_allowed (events/crystal_sphere.py:30-33) hard-returns False where CrystalSphere.cs:49-56 asks `Players.All(p => p.Gold >= 100) && CurrentActIndex > 0`",
      "verdict": "gap",
      "live": true,
      "issue": "SETTLED 2026-07-29 (round 11), answering this entry's own question explicitly: the DEFERRED-PORT stub is not merely a scope exclusion, it IS the live divergence, and this hook is where it lives. DIVERGENCE: CrystalSphere.cs:49-56 gates entry on `Players.All(p => p.Gold >= 100) && CurrentActIndex > 0` (acts 2+ with >=100 gold); `events/crystal_sphere.py:30-33` hard-returns `False` unconditionally, a deliberate stub (docs/superpowers/plans/2026-07-19-shared-events.md) because the payout is an 11x11 reveal minigame with no headless analogue. OBSERVABLE: a run in act 2 or 3 holding >=100 gold that lands on a shared-pool '?' node the shuffle would route to Crystal Sphere in the real game instead silently gets a DIFFERENT event (or the node resolves some other way) -- the game presents a real choice the sim can never present, for a gate that is ordinary, common run state (not a rare edge case). TRIGGER, executed: `.superpowers/sdd/unlabelled/probes/misc-crystal-sphere-witness.py` builds a `RunState(gold=250)` with `act_index = 1` and shows `CrystalSphere.is_allowed(run) == False` while the gate's own two preconditions (act index > 0, gold >= 100) both hold, and that `'crystal_sphere' not in events.allowed_events(run, SHARED_EVENTS)` -- the actual selection-time filter (`rooms.py:451`, `events/__init__.py:146`) that decides which shared event fills a node. FIX: the smallest change that makes this hook true to the source is porting the real gate (`all(p.gold >= 100 for p in players) and run.act_index > 0`) -- but that alone would surface an event whose body (`initial_options`) is `[]`, i.e. an event with a real gate and no content, which is worse than today's never-surfaced stub; the honest fix is the whole DEFERRED-PORT payout (the minigame's two non-presentation side effects: UncoverFutureCost's `LoseGold(50 + NextInt(1,50), Spent)` and PaymentPlan's `AddCurseToDeck<Debt>`, both already flagged portable by this record's own DEFERRED-PORT guard) landing together with this gate. RADIUS: the 18-id SHARED_EVENTS shuffle (draw count/order parity, currently preserved because the stub consumes no RNG) and the event-selection pipeline in `rooms.py`/`events/__init__.py`; no other hook in this record depends on IsAllowed's answer."
    }
  },
  "guards": [
    {
      "what": "DEFERRED PORT: the whole event is a stub. CrystalSphere.cs's payout is the CrystalSphereMinigame (Events/Custom/CrystalSphereEvent/), driven 3 times for UNCOVER_FUTURE (after LoseGold(50 + NextInt(1,50), GoldLossType.Spent)) and 6 times for PAYMENT_PLAN (after AddCurseToDeck<Debt>)",
      "verdict": "gap",
      "live": true,
      "issue": "The sim registers the id so the 18-id shared-pool shuffle keeps its draw count and order (events/crystal_sphere.py:1-14, an SP2 requirement) but forces is_allowed False so the event is never surfaced. Under rule 1 an unported C# side is a GAP, not a waiver: the gate is reachable (act 2+ with 100 gold), and the divergence is that a real event node in the game becomes a node the sim can never present. The two side effects OUTSIDE the minigame are portable on their own -- a gold loss of 50 + NextInt(1,50) and an added Debt curse -- so the deferral is wider than the un-portable part strictly requires. Recorded as a gap with the deferral decision named: docs/superpowers/plans/2026-07-19-shared-events.md. DORMANT is the wrong label here (the trigger is ordinary run state), so: LIVE, with the caveat that the sim never presents the node at all rather than presenting it wrongly. `\"live\": true` ADDED 2026-07-29 (round 11): the prose already said LIVE, but gap_queue.py's `_liveness` heuristic was reading this entry as DORMANT -- it takes whichever of the tokens LIVE/DORMANT appears FIRST in the text, and the sentence 'DORMANT is the wrong label here... so: LIVE' says DORMANT first while arguing the opposite. Verified: `py audit/tools/gap_queue.py list` showed `event/crystal_sphere/g1` as `dormant` before this edit. An explicit `\"live\"` boolean outranks the prose scan by the tool's own stated contract, so this closes that tooling defect for this entry without touching the shared tool itself (see this batch's report, SURPRISES)."
    },
    {
      "what": "no RNG is consumed by the stub",
      "maps_to": "events/crystal_sphere.py:13 states it and the code shows it: is_allowed is a bare `return False` with no draw, so gating the event off cannot shift any parity counter. This is the reason the stub is safe to leave in the pool.",
      "verdict": "faithful"
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
