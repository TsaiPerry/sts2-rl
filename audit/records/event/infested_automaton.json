{
  "unit": "event/infested_automaton",
  "game_source": {
    "path": "src\\Core\\Models\\Events\\InfestedAutomaton.cs",
    "sha256": "4ad1a8b6be3f4fd38bf39c03a53e03a6d102fa7ace82684eb751a3da7767a62f"
  },
  "sim_source": {
    "path": "sts2_rl\\events\\infested_automaton.py",
    "sha256": "9f2a892edd9957367fd788a75a09d0e3e50beabfa8302fd4c16557d89084abe5"
  },
  "extra_sources": [
    {
      "path": "sts2_rl/cards/pool.py",
      "sha256": "03b13195368f9b62ffdaef867883b236c1573dd14c05724c40c439ede14a6fbf",
      "side": "sim"
    },
    {
      "path": "sts2_rl/events/base.py",
      "sha256": "70bda89d8e4509e892ea50283ae24c5763e09f94b6121106140c40cc30e870e1",
      "side": "sim"
    },
    {
      "path": "sts2_rl/events/brain_leech.py",
      "sha256": "dcccfa7348ca10d72727a0cd03772b29e60af4e2ca752f457456339ed022e5cf",
      "side": "sim"
    },
    {
      "path": "sts2_rl/rewards.py",
      "sha256": "1ae7e53a0abfc78396ea4b115f87a530cb9ccd088b56e6fffe9883215defb681",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "ca9ffc808f1fe7903d57dce0e818a41837e3dd42f81140a56db3301a58a2a10f",
      "side": "sim"
    }
  ],
  "hooks": {},
  "guards": [
    {
      "what": "both branches are single-card grants into the deck with the same shape: build CardCreationOptions over the CHARACTER pool with a predicate, CreateForReward(1), null-guard, then CardPileCmd.Add(card, PileType.Deck) (InfestedAutomaton.cs:28-52)",
      "maps_to": "events/infested_automaton.py:27-39. The pool is right (the sim's default is IRONCLAD_POOL, cards/pool.py:141), the null guard is reproduced (`for card in ...` over a possibly-empty list, and `if candidates:`), and run.add_card runs the deck-entry hooks CardPileCmd.Add runs (run.py:341-354) -- so the egg relics upgrade the grant in both. CanonicalVars is Array.Empty, so there are no constants to check.",
      "verdict": "faithful"
    },
    {
      "what": "STUDY's predicate `c.Type == CardType.Power` (InfestedAutomaton.cs:30)",
      "maps_to": "events/infested_automaton.py:28 passes CardType.POWER to random_pool_cards, which filters through pool_card_ids(card_type) (cards/pool.py:152). Same predicate.",
      "verdict": "faithful"
    },
    {
      "what": "TOUCH_CORE's predicate `EnergyCost != null && EnergyCost.Canonical == 0 && !EnergyCost.CostsX` (InfestedAutomaton.cs:41-45)",
      "maps_to": "events/infested_automaton.py:33-36 `make_card(cid).energy_cost == 0 and not make_card(cid).energy_cost_x`. Reading `energy_cost` off a FRESHLY constructed card is the Canonical value -- no cost modifier has been applied to it -- so the two predicates select the same set. The C# null-check has no analogue because a sim Card always has an int cost.",
      "verdict": "faithful"
    },
    {
      "what": "TOUCH_CORE's `CardCreationFlags.NoCardPoolModifications` (InfestedAutomaton.cs:45), which STUDY does NOT set",
      "maps_to": "neither branch of the sim runs a pool-modification hook at all -- random_pool_cards takes the static pool (cards/pool.py:152). For TOUCH_CORE that matches the flag exactly. For STUDY the flag is absent in the source, so a pool-modifying hook WOULD apply there; no ported relic implements one (`grep -rn modify_card_pool sts2_rl` returns nothing), so the asymmetry is currently unobservable and is recorded here so a future pool-modifier port is caught.",
      "verdict": "faithful"
    },
    {
      "what": "EV-6: both InfestedAutomaton branches (InfestedAutomaton.cs:31, 46) calls `CardFactory.CreateForReward(owner, n, CardCreationOptions.ForNonCombatWithDefaultOdds(pool, filter))`; the sim uses `random_pool_cards` (cards/pool.py:136-161), which is the port of a DIFFERENT factory",
      "maps_to": "Closed 2026-07-28: both branches now call rewards.create_reward_cards (events/infested_automaton.py:36-43), the faithful CardFactory.CreateForReward port -- rarity roll with escalation off the non-mutating base odds (`mutate_pity=False`, the CardCreationSource.Other path), the act-scaled upgrade draw, the distinct-card constraint, the GetUnlockedCards pool, and `run.rewards_rng` (rewards.py:242-316). Was: `random_pool_cards`, the port of CardFactory.GetForCombat, so all three observables held -- no rarity odds, no upgrade draw, wrong stream (InfestedAutomaton.cs:30-31, 41-46).",
      "verdict": "faithful"
    },
    {
      "what": "EV-3: TOUCH_CORE's uniform pick (events/infested_automaton.py:38) and STUDY's (via random_pool_cards' rng argument); the source draws these on the per-player Rewards stream inside CreateForReward rolls on the sim's shared run RNG (`self.rng`) where C# rolls on the per-event `base.Rng`",
      "maps_to": "Closed 2026-07-28: the per-event Rng is threaded at this site -- both branches go through rewards.create_reward_cards, which draws on `run.rewards_rng` (rewards.py:262-264) -- the per-player Rewards stream CreateForReward uses, matching `base.Rng` (InfestedAutomaton.cs:30-31, 41-46). `Event.event_rng` is built by make_event_rng(seed, ID) in events/base.py:96-99; legacy (RL) runs keep the documented shared-rng fallback. Was: the draw came off the shared run rng. Re-run of `py audit/tools/event_probes.py eventrng` on 2026-07-28: 25 modules now carry a parity branch and only 4 roll shared-only (dense_vegetation, potion_courier, punch_off, wood_carvings) -- this unit is not one of them.",
      "verdict": "faithful"
    }
  ],
  "verdict": "faithful",
  "audited": "2026-07-26"
}
