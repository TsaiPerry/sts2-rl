{
  "unit": "event/morphic_grove",
  "game_source": {
    "path": "src\\Core\\Models\\Events\\MorphicGrove.cs",
    "sha256": "1a1f37cdab4f130ca7556226417414612fa6e547d95f653341a5d8b8cb8553a4"
  },
  "sim_source": {
    "path": "sts2_rl\\events\\morphic_grove.py",
    "sha256": "df7287ffd04d4cf5ea31bc20a2d879b81ee43c65b2a0bd7d0e69c258e940b554"
  },
  "extra_sources": [
    {
      "path": "src/Core/CardSelection/CardSelectorPrefs.cs",
      "sha256": "be907ced510772bbac02fd30e7553a360e426c4868c4436224d454d0f8177086",
      "side": "game"
    },
    {
      "path": "src/Core/Commands/CardSelectCmd.cs",
      "sha256": "951f39b352d758559ae5de718183c2dfc05d31fb9e5ea24d99ad85d19b3f79b4",
      "side": "game"
    },
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21",
      "side": "game"
    },
    {
      "path": "sts2_rl/events/base.py",
      "sha256": "8547770f3b1d30c4034bb8253bf66ed8b217271782e64698cba960dd0d470306",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "6d735699fb4c6963d78b58b1cb1b713f33070af14b0865e49c4206aa2967d040",
      "side": "sim"
    },
    {
      "path": "src/Core/Commands/CardCmd.cs",
      "sha256": "8520d56b8db0c5c996dd0701f381d4e173e20731e50b056eed96c93ad4551904",
      "side": "game"
    },
    {
      "path": "sts2_rl/events/the_legends_were_true.py",
      "sha256": "f50bd77603852d4a0091b735fd9d9ee731ee1bb844bfa7854c54dc8bb7f53df6",
      "side": "sim"
    }
  ],
  "hooks": {
    "IsShared": {
      "maps_to": "MorphicGrove.cs:20 `IsShared => true`; the sim carries shared membership as registry position (events/__init__.py) and this unit is in the 18-id pool.",
      "verdict": "faithful"
    },
    "IsAllowed": {
      "maps_to": "MorphicGrove.is_allowed (events/morphic_grove.py:28-30) `run.gold >= 100 and len(run.transformable_cards()) >= 2`, matching MorphicGrove.cs:24-27's `p.Gold >= 100 && p.Deck.Cards.Count(c => c.IsTransformable) >= 2`. IsTransformable in the deck equals IsRemovable, which run.transformable_cards mirrors (run.py). The literal 2 matches the `_transformCount` const (MorphicGrove.cs:18) the sim ports as _TRANSFORMS. Quantifier collapses -- see the multiplayer guard.",
      "verdict": "faithful"
    }
  },
  "guards": [
    {
      "what": "CanonicalVars MaxHpVar(5) and the _transformCount const 2 (MorphicGrove.cs:18, 22)",
      "maps_to": "events/morphic_grove.py:10-12 _MAX_HP_GAIN = 5, _TRANSFORMS = 2, _MIN_GOLD = 100. All three exact.",
      "verdict": "faithful"
    },
    {
      "what": "Loner (MorphicGrove.cs:38-42): CreatureCmd.GainMaxHp(5)",
      "maps_to": "events/morphic_grove.py:47-49 gain_max_hp(5), which raises max HP then heals the same amount, as GainMaxHp does.",
      "verdict": "faithful"
    },
    {
      "what": "Group (MorphicGrove.cs:44-53): LoseGold(Owner.Gold) -- ALL of it -- THEN select 2 for transformation, then TransformToRandom each in turn",
      "maps_to": "events/morphic_grove.py:38-45: lose_gold(self.run.gold) then select 2 then a transform loop. The gold-first order matters because the sim's own lose_gold floors at 0 (run.py) and the source's LoseGold does too, so `LoseGold(Gold)` is exactly 'set gold to 0' in both.",
      "verdict": "faithful"
    },
    {
      "what": "the transform count is a selection of 2, not 2 independent picks",
      "maps_to": "MorphicGrove.cs:47 passes `new CardSelectorPrefs(..., 2)` to a single FromDeckForTransformation call and loops the RESULT; events/morphic_grove.py:40-44 does the same -- one select_cards(..., 2) then a loop -- so a card cannot be picked twice in either.",
      "verdict": "faithful"
    },
    {
      "what": "GoldLossType.Stolen is not modelled",
      "verdict": "deliberate-divergence",
      "rationale": "MorphicGrove.cs:46 passes GoldLossType.Stolen where most sites pass Spent; run.lose_gold (run.py) takes no type. Verified the distinction has no ported consumer: `grep -rn GoldLossType src` shows it selects the loss VFX/SFX and the run-history bucket, and no ported relic reads it (the sim's Relic base has no gold-loss hook at all). Same observable, so deliberate-divergence rather than gap -- but it is a run-history field, so a future history/telemetry port would need it."
    },
    {
      "what": "BR-G3 (blast radius): MorphicGrove's GROUP (MorphicGrove.cs:50) calls RunState.transform_card (run.py), which swaps the deck entry without running the deck-entry hooks C#'s CardCmd.Transform fires",
      "maps_to": "Closed 2026-07-28: RunState.transform_card (run.py) now runs BOTH deck-entry hooks around the swap -- `relic.modify_card_being_added_to_deck` before the insert and `relic.after_card_added_to_deck` after it -- which is what CardCmd.Transform does for a PileType.Deck transform (Hook.ModifyCardBeingAddedToDeck at CardCmd.cs:430, Hook.AfterCardChangedPiles at CardCmd.cs:447). Was: a bare deck-slot swap, so the egg relics never saw the replacement. This unit is out of seam guard G3's radius on this leg.",
      "verdict": "faithful"
    },
    {
      "what": "EV-3: Group's two `CardCmd.TransformToRandom(item, base.Rng, ...)` calls (MorphicGrove.cs:50) -- events/morphic_grove.py:44 calls run.transform_card with no pick_rng, so run.py falls back to `self.rng.choice` rolls on the sim's shared run RNG (`self.rng`) where C# rolls on the per-event `base.Rng`",
      "maps_to": "Closed 2026-07-28: the per-event Rng is threaded at this site -- GROUP passes `pick_rng=self.event_rng` into run.transform_card (events/morphic_grove.py:46), matching `base.Rng` (MorphicGrove.cs:50). `Event.event_rng` is built by make_event_rng(seed, ID) in events/base.py; legacy (RL) runs keep the documented shared-rng fallback. Was: the draw came off the shared run rng. Re-run of `py audit/tools/event_probes.py eventrng` on 2026-07-28: 25 modules now carry a parity branch and only 4 roll shared-only (dense_vegetation, potion_courier, punch_off, wood_carvings) -- this unit is not one of them.",
      "verdict": "faithful"
    },
    {
      "what": "EV-10 (site): run.transformable_cards (run.py) reuses the REMOVAL predicate for the TRANSFORM screen, so Quest cards are offered where CardSelectCmd.FromDeckForTransformation (CardSelectCmd.cs:487) filters `c.Type != CardType.Quest && c.IsTransformable`. This unit calls it twice -- once as the IsAllowed gate (events/morphic_grove.py:30) and once as the GROUP selection (events/morphic_grove.py:41)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-29 (round 7): The GATE now uses the predicate the gate wants. MorphicGrove.cs:26 counts `c.IsTransformable` and has NO Quest clause; CardSelectCmd.FromDeckForTransformation (CardSelectCmd.cs:487) is `c.Type != CardType.Quest && c.IsTransformable`. They are DIFFERENT predicates and this unit calls both: the GROUP screen wants the Quest-filtered one (`run.transformable_cards()`, closed in an earlier round) and the IsAllowed gate wants the bare one. events/morphic_grove.py:30 now calls `run.removable_cards()`, which is the IsTransformable half alone -- and run.py's own docstring said so in as many words. EXECUTED: deck [strike, spoils_map] with 100 gold is now ALLOWED, matching the game's `Count(c => c.IsTransformable) >= 2` of 2, where the sim REFUSED an event node the game serves and shifted every later event pick. The selection leg is re-pinned alongside it, so the two cannot drift back into using one predicate for both.  Pinned by test/test_event_live_tail.py.  NOT CLOSED: nothing at this site.  The text it replaced read: NARROWED 2026-07-28 AND REVERSED IN DIRECTION -- STILL A GAP. The SELECTION leg is CLOSED: RunState.transformable_cards (run.py) now filters `card_type != CardType.QUEST`, matching CardSelectCmd.FromDeckForTransformation (CardSelectCmd.cs:487), so events/morphic_grove.py:41's GROUP screen offers what the game offers. The GATE leg is now WRONG THE OTHER WAY, which is fresh damage from that fix: MorphicGrove.cs:26 counts `c.IsTransformable` with NO Quest clause, but events/morphic_grove.py:30 still calls `run.transformable_cards()`, which now EXCLUDES Quest cards -- and run.py says so in as many words ('A gate that counts IsTransformable directly (MorphicGrove.cs:26) has no Quest clause and must use removable_cards instead'). EXECUTED 2026-07-28 (ad-hoc, repo root): deck [strike, spoils_map] with gold 100 -> sim MorphicGrove.is_allowed False, game `Count(c => c.IsTransformable) >= 2` True (count 2). The sim now REFUSES an event node the game serves, which shifts every later event pick -- the exact consequence this record had correctly disproven in the opposite direction. LIVE with ported content: spoils_map is granted by events/the_legends_were_true.py:40 on an act-1 event. The fix is one word: `removable_cards()` at events/morphic_grove.py:30."
    },
    {
      "what": "`runState.Players.All(...)` (MorphicGrove.cs:26)",
      "verdict": "waiver",
      "rationale": "multiplayer-only: the C# predicate is `runState.Players.All(p => ...)`, a quantifier over the party. The sim is single-player, so the quantifier collapses to the one player's clause, which the sim implements exactly. Out of scope per the shared contract's multiplayer waiver."
    },
    {
      "what": "the Transform static hover tip (MorphicGrove.cs:33)",
      "verdict": "waiver",
      "rationale": "presentation only: colour/dialogue/hover-tip/layout/audio metadata with no game-state reader. Out of scope per the shared contract's 'presentation/animation/SFX' waiver."
    }
  ],
  "verdict": "deliberate-divergence",
  "audited": "2026-07-26"
}
