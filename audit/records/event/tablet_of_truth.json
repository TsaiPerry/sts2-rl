{
  "unit": "event/tablet_of_truth",
  "game_source": {
    "path": "src\\Core\\Models\\Events\\TabletOfTruth.cs",
    "sha256": "75b20df028a330c6e52116565777ea4107c182e8e6a6651fa8380e448224c67d"
  },
  "sim_source": {
    "path": "sts2_rl\\events\\tablet_of_truth.py",
    "sha256": "f0b1a22f3b1424b8aacb53242c41cab526b9842c2bdac79f7485162f6b30218e"
  },
  "extra_sources": [
    {
      "path": "src/Core/Commands/CreatureCmd.cs",
      "sha256": "d0a68f1fa60d3c1d05cc87f9e8585a6f6f6529c7e078a856c032a0c1a94f73e8",
      "side": "game"
    },
    {
      "path": "src/Core/Runs/RunState.cs",
      "sha256": "19d266c5ddcdd3d3812190330c69527f70625dc10f357f970a1f38e9423f526c",
      "side": "game"
    },
    {
      "path": "sts2_rl/events/base.py",
      "sha256": "8c41cb03e5a2d36200abfccf13a43b0b7da84780102fcd6b2e6ae57265d7e351",
      "side": "sim"
    },
    {
      "path": "sts2_rl/potion_pools.py",
      "sha256": "7755c9d3ead187a051c3b1b9e98f94d7de551673ce9e71d3af695b46cb933b90",
      "side": "sim"
    },
    {
      "path": "sts2_rl/potions.py",
      "sha256": "72840c98dd4eb14b3f77142dc08078d535fb5f223a14b5d7fea981010502b1a7",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/tungsten_rod.py",
      "sha256": "ab9b89a56591508c28c6b75c117837bebf0de377e47ed960c0f75c90b2bced29",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "670603b2b78e5d8ffb15b0b5b4b5ab51777d0c00a29505b5ed37b4220d88e3e8",
      "side": "sim"
    },
    {
      "path": "src/Core/Entities/Creatures/Creature.cs",
      "sha256": "f2483a7c55b0b0a248552a9ff458237d3682eb9f1b893866dcb32a0e07ffc8f1",
      "side": "game"
    }
  ],
  "hooks": {},
  "guards": [
    {
      "what": "CanonicalVars SmashHPGain 20 and DecipherMaxHpLoss 3 (TabletOfTruth.cs:37-41), and GetDecipherCost's ladder 6 / 12 / 24 / MaxHp - 1 (TabletOfTruth.cs:76-93)",
      "maps_to": "events/tablet_of_truth.py:5-6 _SMASH_HEAL = 20, _INITIAL_COST = 3, and events/tablet_of_truth.py:44-46 `{1: 6, 2: 12, 3: 24, 4: self.run.max_hp - 1}` keyed by deciphers ALREADY done -- the same key the source switches on (DecipherCount is incremented before GetDecipherCost is called, TabletOfTruth.cs:62-68). The case-4 value reads max HP AFTER the 4th decipher's loss in both, which is the whole point of the ladder's last rung.",
      "verdict": "faithful"
    },
    {
      "what": "Smash (TabletOfTruth.cs:52-57): Heal(20)",
      "maps_to": "events/tablet_of_truth.py:37-39 run.heal(20). Plain CreatureCmd.Heal -- no hooks in C# either (CreatureCmd.cs:691-703), so turn_structure step 38a does NOT apply; `py audit/tools/event_probes.py heal` lists this event under the plain-heal verb.",
      "verdict": "faithful"
    },
    {
      "what": "Decipher's control flow (TabletOfTruth.cs:59-74): LoseMaxHpAndUpgrade(current cost), then DecipherCount++, then finish at 5, otherwise recompute the cost and present DECIPHER + GIVE_UP",
      "maps_to": "events/tablet_of_truth.py:48-61 in that exact order, with _MAX_DECIPHERS = 5 and the DECIPHER_{n} page keys. GIVE_UP appears only from the second page on, in both (the INITIAL page offers DECIPHER + SMASH, TabletOfTruth.cs:43-50 / events/tablet_of_truth.py:31-35).",
      "verdict": "faithful"
    },
    {
      "what": "LoseMaxHpAndUpgrade's upgrade leg (TabletOfTruth.cs:110-124): the 5th decipher (_decipherCount == 4) upgrades EVERY upgradable deck card and takes NO draw; every earlier one upgrades ONE via base.Rng.NextItem(list), guarded on the list being non-empty; the kill branch returns before either",
      "maps_to": "events/tablet_of_truth.py:70-82 reproduces all three arms and, crucially, the DRAW COUNT on each: zero on the whole-deck arm, zero on the empty-list arm, exactly one otherwise, and zero on the early return. The candidate list is the deck's upgradable cards in DECK ORDER in both (run.upgradable_cards, run.py:368-369, vs `Deck.Cards.Where(IsUpgradable)`), which is what makes a NextItem index reproducible.",
      "verdict": "faithful"
    },
    {
      "what": "the one draw is on the CORRECT stream -- this is one of the six events with a parity Rng branch, so EV-3 does not apply",
      "maps_to": "events/tablet_of_truth.py:79-82: `if self.event_rng is not None: self.event_rng.next_item(upgradables).upgrade()` else the legacy `self.rng.choice`. Event.event_rng is make_event_rng(seed, 'TABLET_OF_TRUTH') (events/base.py:84-88), the per-event Rng the source's `base.Rng` is, and next_item is NextItem's index form over the same list. Counted on every path per the orobas lesson (that event was correct-stream but took one FEWER draw than the game because C# ran NextItem over a list that still held a locked option): here the source has no such list, both engines guard the same `Count != 0`, and both skip the draw entirely on the whole-deck and kill arms. Confirmed by `py audit/tools/event_probes.py eventrng`, which lists tablet_of_truth among the modules WITH a parity branch.",
      "verdict": "faithful"
    },
    {
      "what": "the kill branch's guard and its LoseMaxHp amount (TabletOfTruth.cs:103-107): `if (!(hp < MaxHp))` -- i.e. cost >= max HP -- LoseMaxHp(MaxHp - 1) and then Kill, NOT LoseMaxHp(cost)",
      "maps_to": "events/tablet_of_truth.py:66-69 `if cost >= self.run.max_hp: run.lose_max_hp(run.max_hp - 1); run.kill(); return`. Same predicate, same substituted amount, same early return. This is also why EV-2's second leg stays dormant on this unit: the source deliberately never asks LoseMaxHp for more max HP than the player has.",
      "verdict": "faithful"
    },
    {
      "what": "the sim short-circuits the page flow once the player is dead",
      "verdict": "deliberate-divergence",
      "rationale": "events/tablet_of_truth.py:51 adds `self.run.is_dead or` to the finish test, which the source does not have -- C# runs DecipherCount++ and then presents the next DECIPHER page even after CreatureCmd.Kill (TabletOfTruth.cs:62-73). Same observable outcome: in both engines the player is dead and the run is over, and CreatureCmd.Kill's own tail already tears the run down when every player is dead (CreatureCmd.cs:450-464), so the page the source builds is never read. Recorded as a divergence rather than faithful because the event's page state genuinely differs; the reachable trigger is a low-max-HP run (e.g. 20/20: costs 3 then 6 leave max HP 11, and the third cost of 12 takes the kill branch), which `py audit/tools/event_probes.py kill` executes."
    },
    {
      "what": "EV-1 (second verb): Decipher's kill branch (TabletOfTruth.cs:106) via events/tablet_of_truth.py:68 calls RunState.kill (run.py:304-305), which sets hp = 0. C#'s CreatureCmd.Kill(creature, force: false) runs Hook.BeforeDeath and then Hook.ShouldDie / Hook.AfterPreventingDeath before the creature actually dies (CreatureCmd.cs:439-448, 489-507), over RunState.IterateHookListeners, which yields the potion belt outside combat (RunState.cs:545-596)",
      "verdict": "gap",
      "issue": "NARROWED 2026-07-28 -- STILL A GAP, and this is the verb that moved LEAST. RunState.kill (run.py:347-348) is STILL a bare `self.hp = 0`: no Hook.BeforeDeath, no Hook.ShouldDie / Hook.AfterPreventingDeath pass, and no call into `_resolve_death` -- the pass RunState.lose_hp gained (run.py:315-345). C#'s CreatureCmd.Kill(creature, force: false) reaches KillWithoutCheckingWinCondition (CreatureCmd.cs:448, :489), whose Hook.ShouldDie is at CreatureCmd.cs:505 and whose Hook.AfterPreventingDeath is at CreatureCmd.cs:567, and TabletOfTruth passes force:false. EXECUTED 2026-07-28 (`py audit/tools/event_probes.py kill`): a 20/20 run holding a belt Fairy that deciphers three times ends 1/0 and DEAD with the Fairy still in the belt. Note that wiring run.kill to _resolve_death would not by itself close this: FairyInABottle.after_preventing_death (potions.py:1313-1316) still no-ops outside combat, which is the surviving half of the lose_hp leg."
    },
    {
      "what": "EV-2: every Decipher's max-HP loss (TabletOfTruth.cs:105, 109) via events/tablet_of_truth.py:67, 70 calls RunState.lose_max_hp (run.py:316-321), which floors max HP and CLAMPS current HP. C#'s CreatureCmd.LoseMaxHp (CreatureCmd.cs) damages the overflow through the damage pipeline (Unblockable|Unpowered) FIRST, against the UNFLOORED new max, and only then calls SetMaxHp(Math.Max(1m, newMaxHp))",
      "maps_to": "Closed 2026-07-28: RunState.lose_max_hp (run.py:366-380) now has the shape of CreatureCmd.LoseMaxHp (CreatureCmd.cs:811-828) -- `new_max = max_hp - amount` UNFLOORED, then `if new_max < hp: lose_hp(hp - new_max)` so the overflow is real HP loss that runs the relic ModifyHpLost pass and the death pass, and only then `max_hp = max(1, new_max)` followed by SetMaxHpInternal's clamp `hp = min(hp, max_hp)` (Creature.cs:493-501). Leg (2) now MATCHes: 80/80 losing 100 max HP kills the player on both sides (`py audit/tools/event_probes.py maxhp`). Leg (1)'s witness does not survive re-reading the source: the probe's hardcoded `C#=71` ignores SetMaxHpInternal's `CurrentHp = Math.Min(CurrentHp, MaxHp)`, which pulls the GAME back to 70 as well, so Tungsten Rod's -1 is unobservable through HP at any max-HP-loss amount (the overflow can never leave current HP above the new max). Both sides read on 2026-07-28; the sim's 70/70 is correct.",
      "verdict": "faithful"
    },
    {
      "what": "`.ThatWillKillPlayerIf(p => p.Creature.MaxHp <= DecipherMaxHpLoss)` on both DECIPHER options (TabletOfTruth.cs:47, 71)",
      "verdict": "waiver",
      "rationale": "presentation only: the option's lethality warning badge. The predicate itself duplicates the kill branch's real guard, which IS modelled (see the kill-branch entry). Note the badge's predicate is `MaxHp <= cost` while the branch's is `cost >= MaxHp` -- the same test -- so the warning cannot disagree with the outcome in either engine."
    },
    {
      "what": "the Log.Error default arm of GetDecipherCost returning 999999999 (TabletOfTruth.cs:89-92) and the CustomScaledWait pauses / CardPreviewStyle arguments on the upgrade loop (TabletOfTruth.cs:115-118, 123)",
      "verdict": "waiver",
      "rationale": "presentation and diagnostics only. The default arm is unreachable in both engines -- the source only calls GetDecipherCost when DecipherCount is 1-4 (it returns early at 5, TabletOfTruth.cs:63-67), and the sim's dict lookup covers exactly those four keys (events/tablet_of_truth.py:46), so a 5th lookup would raise rather than silently return a sentinel. The waits and preview styles are animation pacing."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
