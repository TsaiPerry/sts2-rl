{
  "unit": "event/trash_heap",
  "game_source": {
    "path": "src\\Core\\Models\\Events\\TrashHeap.cs",
    "sha256": "a523afae4ea419efd533887815ad761a1239ece2a1c94247bb256c185cedf2cd"
  },
  "sim_source": {
    "path": "sts2_rl\\events\\trash_heap.py",
    "sha256": "274df0b400c0d7ff86e638ab84cd3bad08e026bf660586bc07dde6e1ab18b1be"
  },
  "extra_sources": [
    {
      "path": "src/Core/Events/EventOption.cs",
      "sha256": "02a8c24eb58efdb08f14b8801a598400c4bd590b675faf9b947413096562118c",
      "side": "game"
    },
    {
      "path": "src/Core/Models/Potions/FairyInABottle.cs",
      "sha256": "f1cf24bf8230d51b14d9d105ff39690e9ba85c2cc8ceaccb3dae000dbf13911b",
      "side": "game"
    },
    {
      "path": "src/Core/Runs/RunState.cs",
      "sha256": "19d266c5ddcdd3d3812190330c69527f70625dc10f357f970a1f38e9423f526c",
      "side": "game"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "351acc27ce4b62f37d5f9d93cb88e087b5c4de6ac3a7de43105c57c2bd85d963",
      "side": "sim"
    },
    {
      "path": "sts2_rl/events/base.py",
      "sha256": "8c9f057de8325bf93fd5647aa83efa95103275c08ad8421d4e5dc8e7407d0144",
      "side": "sim"
    },
    {
      "path": "sts2_rl/potion_pools.py",
      "sha256": "b3dbb02683cba19edbaf16582d72b176caea22acb85cd1b27a8f96d9e0aa6801",
      "side": "sim"
    },
    {
      "path": "sts2_rl/potions.py",
      "sha256": "5bb444cb19b486459ae9f75173b7ca275c87c79e14c7020396eaa33047f79c99",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "222a4b444f5b111b10fd4c5bccfd4d0cfc0901138724911e31a7a123a521251b",
      "side": "sim"
    }
  ],
  "hooks": {
    "IsAllowed": {
      "maps_to": "TrashHeap.cs:48-51 `runState.Players.All(player => player.Creature.CurrentHp > 5)`. events/trash_heap.py:37-38 `run.hp > 5`. An integer strict comparison on both sides -- no decimal/float boundary to lose (contrast unrest_site's 0.70m). The Players.All quantifier is waived separately below.",
      "verdict": "faithful"
    }
  },
  "guards": [
    {
      "what": "CanonicalVars HpLossVar(8m) and GoldVar(100) (TrashHeap.cs:42-46)",
      "maps_to": "events/trash_heap.py:11-12 `_HP_LOSS = 8`, `_GOLD = 100`.",
      "verdict": "faithful"
    },
    {
      "what": "the two FIXED pools (TrashHeap.cs:19-40) -- 5 relics (DarkstonePeriapt, DreamCatcher, HandDrill, MawBank, TheBoot) and 10 cards (Caltrops, Clash, Distraction, DualWield, Entrench, HelloWorld, Outmaneuver, Rebound, RipAndTear, Stack), neither of them the run's grab bag or card pool",
      "maps_to": "events/trash_heap.py:15-19 `_RELICS` and `_CARDS` hold the same ids in the same source order, which is the order NextItem indexes into. EXECUTED (`py audit/tools/event_probes_b.py ids`): all 5 relics and all 10 cards are ported, so both branches are fully reachable (rule 6).",
      "verdict": "faithful"
    },
    {
      "what": "EV-1: DiveIn (TrashHeap.cs:64) deals DynamicVars.HpLoss = 8 through CreatureCmd.Damage with ValueProp.Unblockable | Unpowered. The gate only guarantees 6 HP, so DIVE_IN can be lethal on entry can reduce the player to 0 HP. C# routes event damage through CreatureCmd.Damage, whose Hook.ShouldDie / Hook.AfterPreventingDeath pass runs over RunState.IterateHookListeners (RunState.cs:545-596), which yields the POTION BELT outside combat; RunState.lose_hp (run.py:294-302) just subtracts",
      "verdict": "gap",
      "issue": "MECHANISM EV-1, one verdict at all 18 lose_hp sites (rule 3). Fairy in a Bottle (FairyInABottle.cs:33-45: ShouldDie false for its owner, AfterPreventingDeath heals to max(MaxHp*0.3, 1)) IS ported (potions.py:1222-1250) and IS in the reward pool (potion_pools.py:49), but the sim's copy returns early when `self.combat is None` (potions.py:1246-1248) and is only registered as a listener by CombatState (combat.py:164-166), so it is inert during an event. EXECUTED (`py audit/tools/event_probes.py lethal`): a run at 5 HP holding a belt Fairy that loses 15 HP ends DEAD at hp=-10 with the Fairy still in the belt; the game ends alive at 24 HP with the Fairy consumed. LIVE -- both sides ported and reachable, and the divergence ends the run. Secondary: run.lose_hp does not clamp at 0 either, so the sim carries negative HP."
    },
    {
      "what": "DiveIn's ordering (TrashHeap.cs:64-67): damage, then `base.Rng.NextItem(Relics)` -> RelicCmd.Obtain",
      "maps_to": "events/trash_heap.py:46-49 lose_hp then run.add_relic(rng.choice(...)). Rng.NextItem is `list[NextInt(Count)]` and random.Random.choice is `seq[_randbelow(len(seq))]` -- one draw, uniform, same shape (only the STREAM is wrong; see EV-3). run.add_relic accepts the id string and runs the relic's AfterObtained (run.py:546-553), which is RelicCmd.Obtain's job.",
      "verdict": "faithful"
    },
    {
      "what": "Grab (TrashHeap.cs:72-75): PlayerCmd.GainGold(Gold.BaseValue) FIRST, then CreateCard(base.Rng.NextItem(Cards)) -> CardPileCmd.Add(card, PileType.Deck)",
      "maps_to": "events/trash_heap.py:51-54 gain_gold(100) then run.add_card(make_card(rng.choice(_CARDS))), same order. run.add_card runs the deck-entry hook CardPileCmd.Add runs (run.py:341-354), so the egg relics upgrade a matching grab in both and this is NOT in creature_card_cmds G3's radius.",
      "verdict": "faithful"
    },
    {
      "what": "EV-3: both of TrashHeap's `base.Rng.NextItem` picks -- the relic (events/trash_heap.py:48) and the card (:53). EXECUTED (`py audit/tools/event_probes_b.py rng5b`): trash_heap has no event_rng branch at all rolls on the sim's shared run RNG (`self.rng`) where C# rolls on the per-event `base.Rng`",
      "verdict": "gap",
      "issue": "MECHANISM EV-3, one verdict at every site (rule 3). Each C# EventModel gets its own Rng seeded from the run seed + event id, and the sim already models it: Event.__init__ builds `self.event_rng` from make_event_rng(seed, ID) for string-seeded runs (events/base.py:84-88), and 6 modules branch on it. EXECUTED (`py audit/tools/event_probes.py eventrng`): 28 of the 34 event modules that roll anything roll ONLY on `self.rng`. LIVE for the parity path that the conformance harness grades -- a shared-rng draw both takes a number the game never takes off that stream and fails to advance the event stream, desyncing the rest of the run. In legacy (RL) mode both are the same random.Random, so the observable is parity-only -- which is an exercised sim mode, not an unreachable one."
    },
    {
      "what": "`runState.Players.All(player => ...)` (TrashHeap.cs:50)",
      "verdict": "waiver",
      "rationale": "multiplayer-only: the C# predicate is `runState.Players.All(p => ...)`, a quantifier over the party. The sim is single-player, so the quantifier collapses to the one player's clause, which the sim implements exactly. Out of scope per the shared contract's multiplayer waiver."
    },
    {
      "what": "`.ThatDoesDamage(base.DynamicVars.HpLoss.IntValue)` on DIVE_IN (TrashHeap.cs:57) and the PreviewCardPileAdd(..., 2f) on GRAB (:74)",
      "verdict": "waiver",
      "rationale": "presentation only: EventOption.cs:186-189 shows ThatDoesDamage only sets the WillKillPlayer predicate that flashes the option red. presentation only: colour/dialogue/hover-tip/layout/audio metadata with no game-state reader. Out of scope per the shared contract's 'presentation/animation/SFX' waiver."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
