{
  "unit": "event/zen_weaver",
  "game_source": {
    "path": "src\\Core\\Models\\Events\\ZenWeaver.cs",
    "sha256": "7cf5ba4e7c71eb4ba0537312fc1dc1339ce9f98f2633a2d1fff53269ff4181b3"
  },
  "sim_source": {
    "path": "sts2_rl\\events\\zen_weaver.py",
    "sha256": "02b76e4627a9829ec646ff3fb7f4de37bf210309b73fc92e454014d2205973c5"
  },
  "hooks": {
    "IsAllowed": {
      "maps_to": "ZenWeaver.is_allowed (events/zen_weaver.py:30-32) `run.gold >= 125`, matching ZenWeaver.cs:32-35 `Players.All(p => p.Gold >= DynamicVars['EmotionalAwarenessCost'].BaseValue)` -- the gate reads the MIDDLE price, not the cheapest, which the sim reproduces by naming _EMOTIONAL_COST. Multiplayer quantifier waived separately.",
      "verdict": "faithful"
    }
  },
  "guards": [
    {
      "what": "CanonicalVars BreathingTechniquesCost 50, EmotionalAwarenessCost 125, ArachnidAcupunctureCost 250 (ZenWeaver.cs:25-30)",
      "maps_to": "events/zen_weaver.py:11-13 _BREATHING_COST = 50, _EMOTIONAL_COST = 125, _ARACHNID_COST = 250. Exact.",
      "verdict": "faithful"
    },
    {
      "what": "GenerateInitialOptions (ZenWeaver.cs:37-44): BREATHING_TECHNIQUES is NEVER locked (no gold check at all, even though it costs 50), the other two lock below 125 and 250, and both locked options share ONE loc key, `...options.LOCKED`, via CreateLockedOption (:76-79)",
      "maps_to": "events/zen_weaver.py:34-44 reproduces all three, including the shared 'LOCKED' key for both -- the sim does not invent per-option locked keys here, matching the source (contrast waterlogged_scriptorium, which really does have two distinct locked keys). The unlocked BREATHING option is not a bug on either side: PlayerCmd.LoseGold and run.lose_gold (run.py:335-337) both floor at 0, so buying it with less than 50 gold simply zeroes the purse.",
      "verdict": "faithful"
    },
    {
      "what": "BreathingTechniques (ZenWeaver.cs:46-56): LoseGold(50) FIRST, then TWO separately-created Enlightenment cards added to the Deck pile in one CardPileCmd.Add(array, PileType.Deck) call",
      "maps_to": "events/zen_weaver.py:46-50: lose_gold(50) then two run.add_card(make_card('enlightenment')) calls. 'enlightenment' IS ported. The bulk Add is not a different verb: CardPileCmd.Add(cards, pile) loops `foreach (CardModel card5 in cards)` and runs the per-card deck-entry path inside the loop (CardPileCmd.cs:307-341), so two singleton adds are the same dispatch sequence. run.add_card runs Hook.ModifyCardBeingAddedToDeck (run.py:341-354), so the egg relics can upgrade the Enlightenments here exactly as the game allows -- this is an ADD, not a transform, so creature_card_cmds G3 does not apply.",
      "verdict": "faithful"
    },
    {
      "what": "EmotionalAwareness / ArachnidAcupuncture (ZenWeaver.cs:58-74) share RemoveCardsAndProceed(cost, count), which selects and REMOVES first and pays SECOND -- the opposite order to BreathingTechniques -- with counts 1 and 2",
      "maps_to": "events/zen_weaver.py:52-63 _remove_and_pay does select_cards -> remove_cards -> lose_gold, preserving that inverted order, with the same two counts. run.removable_cards (run.py:360-362) is CardModel.IsRemovable, which is what FromDeckForRemoval's screen enforces, and run.select_cards reproduces CardSelectCmd's auto-select-all rule for a deck smaller than the ask (CardSelectCmd.cs:576-579).",
      "verdict": "faithful"
    },
    {
      "what": "this event consumes NO randomness on any path, so EV-3 does not apply",
      "maps_to": "EXECUTED (`py tools/audit/event_probes_c.py drawcount`): BREATHING_TECHNIQUES, EMOTIONAL_AWARENESS and ARACHNID_ACUPUNCTURE each take 0 shared-rng and 0 event-rng draws with the card choice pinned, matching a source with no CalculateVars and no Rng call.",
      "verdict": "faithful"
    },
    {
      "what": "PlayerCmd.LoseGold's `GoldLossType.Spent` argument (ZenWeaver.cs:48, :73) is unmodelled -- run.lose_gold (run.py:335-337) takes no loss type",
      "verdict": "deliberate-divergence",
      "rationale": "SAME observable, so deliberate-divergence rather than gap (rule 2), and the SAME verdict the mechanism already carries at event/morphic_grove (GoldLossType.Stolen) and event/round_tea_party (ThatWontSaveToChoiceHistory) -- rule 3. GoldLossType.cs:3-5's own doc comment is 'Used to help flavor how the gold was lost in the Map History', and a grep for `GoldLossType` across src finds no reader outside the enum declaration and the call sites that pass it."
    },
    {
      "what": "HoverTipFactory.FromCardWithCardHoverTips<Enlightenment> on the BREATHING_TECHNIQUES option (ZenWeaver.cs:40) and CardCmd.PreviewCardPileAdd (:54)",
      "verdict": "waiver",
      "rationale": "presentation only: colour/dialogue/hover-tip/layout/audio metadata with no game-state reader. Out of scope per the shared contract's 'presentation/animation/SFX' waiver."
    },
    {
      "what": "IsAllowed's `runState.Players.All(p => ...)` quantifier (ZenWeaver.cs:34)",
      "verdict": "waiver",
      "rationale": "multiplayer-only: the C# predicate is `runState.Players.All(p => ...)`, a quantifier over the party. The sim is single-player, so the quantifier collapses to the one player's clause, which the sim implements exactly. Out of scope per the shared contract's multiplayer waiver."
    }
  ],
  "verdict": "deliberate-divergence",
  "audited": "2026-07-26"
}
