{
  "unit": "power/cruelty",
  "game_source": {
    "path": "src\\Core\\Models\\Powers\\CrueltyPower.cs",
    "sha256": "9407d8e2f7a7c32fed9a29ad3868953e9e93f776560d3b9682eccc28c68948f5"
  },
  "sim_source": {
    "path": "sts2_rl\\powers.py",
    "sha256": "c639bbbe8867fd8b295f85422a4eb0315764af32ae785c4ce010a0d4a29e6cf2"
  },
  "extra_sources": [
    {
      "path": "src/Core/Commands/PowerCmd.cs",
      "sha256": "d344a2ecdd846e26a02fcd14d3ebf48f6c99056fa3b3e0ccbaa43fe9901dc993",
      "side": "game"
    },
    {
      "path": "src/Core/Models/Powers/VulnerablePower.cs",
      "sha256": "d4cc015ea3ab96f8245de67d7a5a64799d5ba09f8d77ccaf7a9708065be9d204",
      "side": "game"
    },
    {
      "path": "sts2_rl/cmds.py",
      "sha256": "12e358d13101d8ae2bf4340ede3a9c64bdf367c6783daf6454fec2a00cc9e461",
      "side": "sim"
    }
  ],
  "hooks": {
    "Type": {
      "maps_to": "powers.py:659 `power_type = PowerType.BUFF`",
      "verdict": "faithful"
    },
    "StackType": {
      "maps_to": "powers.py:64-66 Power.on_stack",
      "verdict": "faithful",
      "rationale": "PowerStackType.Counter. The sim has no StackType concept; Power.on_stack (powers.py:64-66) adds unconditionally, which is what PowerCmd.ModifyAmount does (PowerCmd.cs:236). Third-file citations, not hashed here."
    }
  },
  "guards": [
    {
      "what": "CrueltyPower.cs:17 ModifyVulnerableMultiplier is NOT a hook",
      "verdict": "faithful",
      "rationale": "It is `public decimal`, not `public override`, so the harness enumerates no hook for it and there is no dispatcher: VulnerablePower calls it directly off `dealer.GetPower<CrueltyPower>()` (VulnerablePower.cs:35-39). The sim reproduces exactly that shape -- CrueltyPower's class body is passive (powers.py:651-659) and VulnerablePower reads `dealer.powers.get('cruelty')` inline (powers.py:414-416). Faithful, and this is why the unit's hook list is only the two declaration properties."
    },
    {
      "what": "CrueltyPower.cs:19-22 `target == base.Owner` -> unmodified",
      "verdict": "gap",
      "issue": "Cruelty's self-exclusion is dropped by its consumer. Recorded in full on power/vulnerable's matching guard -- the sim reads Cruelty's amount with no such test, so a Cruelty holder attacking its own Vulnerable self would get the bonus in the sim and not in the game. Per binding rule 3 the same mechanism carries the same verdict at both records, and it is DORMANT for the same executed reason: no ported effect deals POWERED attack damage from a creature to itself (the self-damage sites all pass NON_CARD_HP_LOSS, which is UNPOWERED and never reaches the multiplicative family, cmds.py:56). Carried here as well as on power/vulnerable because it is Cruelty's own guard and a reader of cruelty.json must not conclude the unit is clean."
    },
    {
      "what": "CrueltyPower.cs:23-26 `!props.IsPoweredAttack()` -> unmodified",
      "verdict": "faithful",
      "rationale": "Enforced by DamageCmd.deal's is_powered_attack gate (cmds.py:56-58) before Vulnerable's hook -- and therefore before the Cruelty read -- runs at all."
    },
    {
      "what": "CrueltyPower.cs:27 `amount + base.Amount / 100m`",
      "verdict": "gap",
      "live": false,
      "issue": "SETTLED 2026-07-30 (round 11), by execution -- arithmetic worked out in full, not hand-waved. The TYPE mismatch is real: powers.py:416 computes `mult += cruelty.amount / 100.0` in Python `float` where C# accumulates in `decimal` the whole way through ModifyDamageInternal's chained `num *= item.ModifyDamageMultiplicative(...)` (Hook.cs:2511-2538, confirmed a per-listener chain, not a product-then-apply-once -- and the sim's `modify_damage_multiplicative` dispatcher, hooks.py:381-410, already matches that chained shape correctly, own docstring citing the exact Shrink+Vulnerable worked example). `1.5 + n/100.0` genuinely IS non-dyadic (inexact in IEEE-754 binary64) for MOST integer n -- EXECUTED (`fractions.Fraction` vs the sim's actual float path, all (n, K) pairs with n in 1..199, K in 1..199): 59 combinations where `int(K * (1.5 + n/100.0))` (the sim's actual DamageCmd.deal truncation, cmds.py:236) disagrees by exactly 1 from the exact rational value C#'s decimal pipeline computes and then truncates identically (e.g. n=55, K=60 -> sim 122, exact 123; n=80, K=100 -> sim 229, exact 230). BUT: every one of those 59 mismatches occurs at an n that is NOT a multiple of 25 (51, 55, 57, 72, 76, 80, 82, ...), and CrueltyPower's amount is STRUCTURALLY constrained to always be a multiple of 25 in ported content -- `grep -rn CrueltyPower --include=*.py sts2_rl/ | grep -v powers.py` finds exactly one applier, cards/cruelty.py:37-38 (`PowerCmd.apply(..., CrueltyPower, self._power_amount, ...)` with `_power_amount` 25 unupgraded / 50 upgraded, cards/cruelty.py:29,32), CrueltyPower's own StackType is Counter (plain addition, this record's own StackType guard above), and no other relic/event/power in the sim ever touches `cruelty.amount` (no `modify_power_amount` listener applies to a Buff-typed power; Ruined Helmet is Strength-only, Unsettling Lamp is Debuff-only) -- so the ONLY achievable values are 0, 25, 50, 75, 100, 125, ... (sums of 25s and 50s), and EVERY multiple of 25 divided by 100 reduces to k/4 for integer k, which IS exactly representable in binary floating point (denominator is a power of 2). RE-EXECUTED, confirming exactness rather than assuming it: the same probe restricted to n in {25, 50, 75, 100} and K in 1..399 finds ZERO mismatches -- `1.5 + n/100.0` is bit-exact for every realistically-achievable Cruelty amount, so the float/decimal TYPE difference has NO observable consequence for Cruelty specifically, even though the type mismatch itself is real and the census's underlying hook_dispatch G9 concern (genuinely non-dyadic literals elsewhere, e.g. Shrink's 0.7) is not affected by this finding either way. DOES NOT WIDEN G9: Cruelty's own contribution is inert. Per binding rule 3, the shared aggregation mechanism's G9 verdict is unaffected by this record either way; this entry only settles Cruelty's OWN factor as dormant. HANDOFF: power/vulnerable.json's matching `ModifyDamageMultiplicative` hook entry (not owned by this batch) currently repeats the same 'widens G9' claim this executed check disproves for Cruelty's contribution -- flagged for that record's owner, not edited here."
    },
    {
      "what": "Cruelty has no turn hook and never decays",
      "verdict": "faithful",
      "rationale": "C# declares none and the sim's class body has none. Both keep Cruelty for the whole combat."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
