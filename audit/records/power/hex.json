{
  "unit": "power/hex",
  "game_source": {
    "path": "src\\Core\\Models\\Powers\\HexPower.cs",
    "sha256": "feaef7aa442f007a8be314e0f02dcfb9bf2aa4ee2c9a313d0d87bc39fa55f574"
  },
  "sim_source": {
    "path": "sts2_rl\\powers.py",
    "sha256": "9231e3f0931fe70004341cb72aa6305043391e0ee6199417f5e3c5d34f199be0"
  },
  "extra_sources": [
    {
      "path": "src/Core/Models/PowerModel.cs",
      "sha256": "2cc44bddbdd6de6e1d3ded50110a0aace66bc37a942dbf1c5af2af08b9a80dbc",
      "side": "game"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "9e2c8a38776f6f0bb505b5b916976f6657c19360ad862d68022c19765848979e",
      "side": "sim"
    },
    {
      "path": "sts2_rl/monsters/glory/knights.py",
      "sha256": "c0ef52dd119eee94e8cdf1d8f4a25ab2e22cb35c3589c30f1a099b10563c418c",
      "side": "sim"
    }
  ],
  "hooks": {
    "Type": {
      "maps_to": "powers.py:3201 `power_type = PowerType.DEBUFF`",
      "verdict": "faithful"
    },
    "StackType": {
      "maps_to": "powers.py:3203-3204 on_stack -> pass",
      "verdict": "faithful",
      "issue": "Closed 2026-07-30 (tier-2 campaign): (Task 25.) The 15 `on_stack` no-op overrides are DELETED; the base `Power.on_stack` adds, matching PowerCmd.ModifyAmount (PowerCmd.cs:236), which adds unconditionally with no StackType branch -- PowerStackType.Single means 'Amount is hidden and displayed as 1' (PowerStackType.cs:10-13), not 'does not stack'. All 16 units re-verified against their own C#: 15 are StackType.Single, and NONE overrides InstanceType (PowerModel.cs:144 default None), so none needs T1's instance machinery instead of amount-adding. Two per-site exceptions, both verified: (1) DAMPEN's C# StackType is actually `None`, not Single (DampenPower.cs:22) -- the queue's 16-unit list was wrong about it; its only applier, MagiKnight, dedupes caller-side (MagiKnight.cs:78-96: re-application calls AddCaster only and never reaches PowerCmd.Apply), so the deletion is inert through the real caller and T28's caster-set machinery is untouched and re-pinned. (2) HEX genuinely reads Amount (HexPower.cs:79 feeds CardCmd.Afflict<Hexed>(card, Amount)), so this deletion closes a real divergence that remains DORMANT only because SpectralKnight's move machine never loops back to HEX and KnightsElite fields exactly one -- a second Hex applier makes it live. ILLUSION needed no override and is unchanged (the opposite-sign case the record notes). No reachable re-application among the other 13: Burrowed and Soar re-apply only after their own expire/remove, always onto a fresh instance. Pins: test/test_stack_type_single.py (39 tests).  The text it replaced read: PowerStackType.Single with on_stack overridden to `pass` -- the census's wrong-reasoning class. DORMANT with a caveat that is NOT true of the other 14: Hex's Amount IS read -- it is the Hexed affliction's amount (`CardCmd.Afflict<Hexed>(card, base.Amount)`, HexPower.cs:79, vs `CardCmd.afflict(card, HexedAffliction, self.amount)`, powers.py:3209) -- so a second application would afflict LATER cards at 2 in the game and at 1 in the sim. It stays dormant only because the sole applier, monsters/glory/knights.py:119-120, applies it once and KNIGHTS_ELITE fields exactly one Spectral Knight (knights.py:129-131). Trigger: a second Spectral Knight or a second Hex application. Third-file citations, not hashed here."
    },
    "TryModifyKeywordsInCombat": {
      "maps_to": "powers.py:3206-3211 `card.is_ethereal = True` + powers.py:3233-3239 restore",
      "verdict": "deliberate-divergence",
      "rationale": "C# grants Ethereal through the keyword-modifier chain, gated on the card carrying the Hexed affliction, so the grant is live-computed and \"when this power is removed, it stops contributing here automatically, so any Ethereal from another source (e.g. MusicBox) is left untouched\" (HexPower.cs:21-25). The sim has no keyword-modifier hook, so it writes `card.is_ethereal` directly and stashes the previous value in `card._hex_prev_ethereal` (powers.py:3210-3211), restoring it on removal (powers.py:3238) -- which reproduces the \"leave other sources untouched\" property by save/restore instead of by non-contribution, and the sim's own docstring (powers.py:3195-3197) says exactly that. Observably identical for one Hex; it would diverge if a second source toggled Ethereal DURING the Hex, because the stashed value would then be stale. No ported source does (the sim's other Ethereal writers are card-static), so this is a deliberate-divergence with the mechanism named rather than a gap. Third-file citations, not hashed here."
    },
    "AfterApplied": {
      "maps_to": "powers.py:3213-3223 HexPower.__init__",
      "verdict": "faithful",
      "rationale": "C# afflicts every card in `PlayerCombatState.AllCards` via `Afflict`, which itself tests `card.Affliction == null` (HexPower.cs:39-45, 75-81); the sim's __init__ walks `owner.all_cards` with the same `card.affliction is None` test (powers.py:3221-3223). The __init__-for-AfterApplied substitution is the same mechanism as illusion's and dampen's. The `all_cards`-omits-Play concern does NOT bite: Hex is applied by the Magi/Spectral Knight's move on the enemy turn, and the sim's Play limbo only exists between combat.py:433 and combat.py:498 inside a player card play."
    },
    "AfterCardEnteredCombat": {
      "maps_to": "powers.py:3225-3227 on_card_entered_combat",
      "verdict": "faithful",
      "rationale": "HexPower.cs:47-53 afflicts any newly-entering card owned by the owner's player; powers.py:3225-3227 does the same with the same `card.affliction is None` test inside `_afflict`. The `card.Owner == base.Owner.Player` test is a single-player tautology."
    },
    "AfterDeath": {
      "maps_to": "powers.py:3229-3231 HexPower.on_death",
      "verdict": "faithful",
      "rationale": "`!wasRemovalPrevented && creature == base.Applier` (HexPower.cs:57) maps to `if creature is self.applier: self._expire()` (powers.py:3230-3231). This is one of the four sim sites that READ `self.applier`, and it is right -- the applier IS threaded here, because monsters/glory/knights.py:120 passes `applier=self`. Worth contrasting with the enemy self-buffs in this half that omit it."
    },
    "AfterRemoved": {
      "maps_to": "powers.py:3233-3240 HexPower._expire override",
      "verdict": "faithful",
      "rationale": "C# sweeps `PlayerCombatState.AllCards` and clears every Hexed affliction (HexPower.cs:63-73); the sim sweeps `owner.all_cards` and additionally restores `is_ethereal` from the stash. The `all_cards`-omits-Play risk IS real here -- unlike AfterApplied, this sweep runs when the APPLIER dies, which normally happens inside a player card play, i.e. exactly when a card is in the sim's Play limbo -- so it was **EXECUTED** rather than argued: a KNIGHTS_ELITE combat, Hex 1 applied by the Spectral Knight (all five hand cards come back ethereal with a Hexed affliction), then the knight killed by a Strike played from that hand -- and the played card comes out with `ethereal = False`, `affliction = None`, in the discard pile, exactly like the four cards that never left hand. The sweep reaches it. Recorded as checked because the hypothesis was that it would not."
    }
  },
  "guards": [
    {
      "what": "HexPower.cs:28 `card.Owner != base.Owner.Player` -> false",
      "verdict": "faithful",
      "rationale": "Single-player tautology; omitted by the sim."
    },
    {
      "what": "HexPower.cs:32 `!(card.Affliction is Hexed)` -> false in the keyword modifier",
      "verdict": "deliberate-divergence",
      "rationale": "The sim has no per-query gate because it writes the flag instead of contributing to a chain; the affliction is still the thing that decides which cards get the flag (powers.py:3209's `CardCmd.afflict(...) is not None` guards the write). Folded into the TryModifyKeywordsInCombat mechanism."
    },
    {
      "what": "Cards that already carry a DIFFERENT affliction are skipped",
      "verdict": "faithful",
      "rationale": "`card.Affliction == null` on both sides (HexPower.cs:77, powers.py:3222/3226), so a card already Entangled or Ringing is never Hexed. This is the inverse of the tangled-power gap the main report records (section 4 item 13), where the sim ADDED an `Affliction == null` test C# does not have -- here C# has it and the sim matches. Worth recording so the two are not conflated."
    },
    {
      "what": "IsVisible",
      "verdict": "waiver",
      "rationale": "Closed game-wide, re-verified 2026-07-26. EXECUTED: `py audit/tools/power_census.py visible` reports 0 IsVisibleInternal overrides of 260 files. That 260 is the .cs files DIRECTLY under src/Core/Models/Powers and EXCLUDES the 16 in Powers/Mocks/ (276 including them); a recursive `grep -rn IsVisibleInternal src/Core/Models/Powers/`, which does cover Mocks/, also returns 0 -- so IsVisible is provably always true for every power in the decompiled game, mocks included. CITATION FIX (2026-07-26 review fix pass): the property lives in src/Core/Models/PowerModel.cs, NOT under Models/Powers/ -- `IsVisible` is the non-virtual property at PowerModel.cs:150-160 and the overridable virtual is `IsVisibleInternal` at PowerModel.cs:167."
    }
  ],
  "verdict": "deliberate-divergence",
  "audited": "2026-07-26"
}
