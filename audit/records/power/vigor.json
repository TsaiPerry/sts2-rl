{
  "unit": "power/vigor",
  "game_source": {
    "path": "src\\Core\\Models\\Powers\\VigorPower.cs",
    "sha256": "4422ba55e4d4e0bd85b06b95e5c88bceb6947374255d9d5587c3933c86efc11e"
  },
  "sim_source": {
    "path": "sts2_rl\\powers.py",
    "sha256": "7b8fdd4f644f525d60c08fee405ddfe4dceaedfa05de64a80bb8e4412ea26ca1"
  },
  "extra_sources": [
    {
      "path": "src/Core/Commands/PowerCmd.cs",
      "sha256": "d344a2ecdd846e26a02fcd14d3ebf48f6c99056fa3b3e0ccbaa43fe9901dc993",
      "side": "game"
    },
    {
      "path": "sts2_rl/cmds.py",
      "sha256": "59d6315eee45195c01a59d38ee15cdf3654970f5818bd970ab6101ced93dc18d",
      "side": "sim"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "e96cce165539bdcaf6d9a89cb7a3771f369033898750bb681da8b411a00a96a3",
      "side": "sim"
    },
    {
      "path": "sts2_rl/hooks.py",
      "sha256": "3d39a103f9636007fe654337d08443cc48445a6b0a31c52c20f080487699bf0a",
      "side": "sim"
    }
  ],
  "hooks": {
    "Type": {
      "maps_to": "powers.py:2408 power_type = PowerType.BUFF",
      "verdict": "faithful"
    },
    "StackType": {
      "maps_to": "powers.py:137-139 Power.on_stack",
      "verdict": "faithful",
      "rationale": "PowerStackType.Counter. The sim has no StackType concept; Power.on_stack (powers.py:137-139) adds unconditionally, which is what PowerCmd.ModifyAmount does (PowerCmd.cs:236 -- no StackType branch). Third-file citations, not hashed here."
    },
    "BeforeAttack": {
      "maps_to": "powers.py:2434-2444 VigorPower.before_attack (hooks.py, combat.py and Monster._execute_attack)",
      "verdict": "faithful",
      "rationale": "One of only TWO power units that use the sim's before_attack/after_attack slots (the other is power/gigantification); power/thorns, power/curl_up and power/skittish all have C# before/after-attack hooks and reach for on_damage_received instead, which is the main report's section 4 items 2, 5 and 14. The latch semantics match: C# stores the command AND snapshots Amount (VigorPower.cs:52-53), the sim stores _amount_when_attack_started (powers.py:2444), and both bail when something is already latched (C# :40-43, sim powers.py:2435). C#'s attacker guard (:31-34) is powers.py:2435's `dealer is not self.owner`; C#'s IsPoweredAttack guard (:35-38, repeated redundantly at :48-51) is powers.py:2442's `card.is_unpowered`, with the sim's comment at powers.py:2438-2441 explaining that card is None for monster attacks and that those are always powered -- which also covers C#'s :44-47 `ModelSource != null && !(ModelSource is CardModel)` arm, since a null ModelSource is exactly the non-card case both sides let through. Citations re-verified and renumbered 2026-08-04 (class moved from 1880 to 2397; a stale `:1904`/`:1906-1910` self-reference -- leftover from before the class's own line numbers were substituted -- is corrected here to the actual current lines)."
    },
    "ModifyDamageAdditive": {
      "maps_to": "powers.py:2420-2432 VigorPower.modify_damage_additive (hooks.py, cmds.py)",
      "verdict": "gap",
      "issue": "The sim keeps only the FIRST of C#'s four guards. C# (VigorPower.cs:57-77) tests, in order: base.Owner != dealer (present, powers.py:2430-2431), !props.IsPoweredAttack() (present structurally -- cmds.py only runs the additive family for powered damage), `commandToModify != null && cardSource != null && cardSource != commandToModify.ModelSource` (ABSENT), and `commandToModify != null && commandToModify.Attacker != dealer` (ABSENT, and redundant with the first for a single-attacker command). The missing third guard is the real one: it confines the bonus to the LATCHED card, so while an attack is in flight a DIFFERENT card's powered damage from the same dealer gets nothing in the game and gets the full Vigor in the sim. DORMANT: reaching it needs powered damage with a different cardSource to resolve between before_attack (combat.py) and after_attack (combat.py), i.e. a card that plays another card mid-resolution, and no ported Ironclad card does that -- the ported auto-play sources (power/mayhem at turn start, power/stampede at turn end, Howl From Beyond) all fire outside any attack bracket. Named trigger: porting a Havoc-style play-another-card Attack, or any effect that issues a second powered card attack inside one play. Note the additive AGGREGATION shape is hook_dispatch's G9, which settled by execution that the additive family is exactly equal to C#'s running fold over integers -- not re-verdicted here. Citations renumbered 2026-08-04."
    },
    "AfterAttack": {
      "maps_to": "powers.py:2446-2455 VigorPower.after_attack (combat.py)",
      "verdict": "faithful",
      "rationale": "Consumes exactly the snapshotted amount, not the current one -- C# is PowerCmd.ModifyAmount(this, -internalData.amountWhenAttackStarted) (VigorPower.cs:84) and the sim is `self.amount -= consumed` where consumed is the snapshot (powers.py:2450-2452). That is the load-bearing detail: Vigor gained DURING the attack survives it on both sides. The sim then fires on_power_amount_changed for the delta (powers.py:2453) and _expires at <= 0 (powers.py:2454-2455), which is what ModifyAmount's ShouldRemoveDueToAmount does (PowerCmd.cs:247-250). The sim's _expire (powers.py:189-200) pops the power and unregisters it; C#'s PowerCmd.Remove (PowerCmd.cs:287-295) additionally fires power.AfterRemoved, which this power does not implement. Observationally identical, same treatment as the _expire entries in audit/records/power/duplication.json and audit/records/power/flame_barrier.json per binding rule 3. C#'s AfterAttack has no dealer guard (it relies on command identity, VigorPower.cs:82) where the sim tests dealer is self.owner plus a non-None latch (powers.py:2448) -- equivalent, since the latch is only ever set for the owner. Citations renumbered 2026-08-04."
    }
  },
  "guards": [
    {
      "what": "The attack boundary itself",
      "verdict": "faithful",
      "rationale": "combat.py brackets each play-count iteration of an Attack card with before_attack/after_attack, and Monster._execute_attack does the same for monster moves; the sim's docstring at powers.py:2403-2404 says so. That is C#'s AttackCommand lifetime. Note a MULTI-HIT card resolves inside ONE iteration on both sides, so Vigor's bonus applies per hit and is consumed once -- the sim's docstring at powers.py:2398-2401 states it and it matches C#, where ModifyDamageAdditive runs per damage instance and AfterAttack once per command. Citation renumbered 2026-08-04 (the stale `:1870-1871` self-reference is corrected to the docstring's actual current lines)."
    },
    {
      "what": "VigorPower.cs:13-18,24-27 the per-instance Data",
      "verdict": "faithful",
      "rationale": "powers.py:2410-2418 initialises the latch in __init__, the sim's equivalent of InitInternalData. Vigor is NOT PowerInstanceType.Instanced (it is absent from `py audit/tools/power_census.py instance`), so one shared latch per owner is correct on both sides and power_cmd's G5 does not apply."
    },
    {
      "what": "Vigor's ported appliers all pass applier=",
      "verdict": "faithful",
      "rationale": "power/prep_time (powers.py:4625, `PowerCmd.apply(self.hooks, self.owner, VigorPower, self.amount)` with no applier=) is the one that does NOT -- see that record's applier guard -- while Akabeko and the Terror Eel apply it directly. Nothing reads Vigor's applier on either side, so the omission is dormant there and is recorded on that unit rather than duplicated here. Citation renumbered 2026-08-04 (power/prep_time's class moved from powers.py:3695 to powers.py:4615)."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
