{
  "unit": "relic/bag_of_marbles",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\BagOfMarbles.cs",
    "sha256": "74dc0706aded95fe9d8639e42101d57dc9c8a4737f6a03a7e576381488ded908"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\bag_of_marbles.py",
    "sha256": "4052d144fa73200d3436a6375070f7a326d6ea695b82c806df271485d18e777e"
  },
  "extra_sources": [
    {
      "path": "src/Core/Combat/CombatState.cs",
      "sha256": "b3150c69fae0c33b48b7957c3a67bc4e5867d1641e05062cd3f5edd0a9d9f2e2",
      "side": "game"
    },
    {
      "path": "src/Core/Entities/Creatures/Creature.cs",
      "sha256": "f2483a7c55b0b0a248552a9ff458237d3682eb9f1b893866dcb32a0e07ffc8f1",
      "side": "game"
    },
    {
      "path": "sts2_rl/player.py",
      "sha256": "5f44427a06d75c1e4f43694fed9d1bbcb8d7b315cb5eba122af9b8ee63a62e6a",
      "side": "sim"
    },
    {
      "path": "sts2_rl/powers.py",
      "sha256": "7b8fdd4f644f525d60c08fee405ddfe4dceaedfa05de64a80bb8e4412ea26ca1",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/base.py",
      "sha256": "5774c975c5b5b9de05e5fda4ce5fd82e5571b01de06d81c0d7e77b8319936770",
      "side": "sim"
    }
  ],
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.COMMON (bag_of_marbles.py:16)",
      "verdict": "faithful"
    },
    "BeforeSideTurnStart": {
      "maps_to": "before_side_turn_start (bag_of_marbles.py:18-26), the real pre-block-clear slot",
      "verdict": "faithful",
      "issue": "STALE at this pass: the maps_to text still described the old `on_player_turn_started` (POST-draw) binding, which is gone from the file. `bag_of_marbles.py:18` now defines `before_side_turn_start` directly, matching G1's already-recorded fix (CombatManager.cs:458's slot); this rollup entry had not been updated when G1 closed. `PowerCmd.apply`'s own `can_receive_powers` backstop (round 5, already documented in R6-report.md) was the reason the stale slot was unobservable via power state even before this fix; the candidate-set swap (G2, now also closed) is C#-literal at the loop-membership level, verified via `test_bag_of_marbles_candidate_set_excludes_an_unhittable_enemy`."
    }
  },
  "guards": [
    {
      "what": "G1 (CLOSED): the sim binds C#'s BeforeSideTurnStart to on_player_turn_started, which was FIVE ordering steps later",
      "verdict": "faithful",
      "issue": "Closed by the turn-start-split fix (power/_side_turn_slot family): the sim gained a real `before_side_turn_start` dispatcher (CombatManager.cs:458's slot) and bag_of_marbles.py:18 was moved onto it. EXECUTED 2026-07-30 (round 11): `py -m pytest test/test_turn_start_split.py -k bag_of_marbles` passes -- `test_each_relic_listens_on_the_hook_it_overrides[bag_of_marbles-before_side_turn_start]` confirms the relic implements `before_side_turn_start` and no other turn-start slot, and `test_before_side_turn_start_runs_ahead_of_the_block_clear` pins the ordering (a `before_side_turn_start` listener still sees last turn's block; `on_block_cleared` sees it gone) using this exact relic's own mechanism as its worked example. `grep -n 'def before_side_turn_start' sts2_rl/relics/bag_of_marbles.py` confirms the method name. The text it replaced described the OLD `on_player_turn_started` binding, which is gone from the file entirely today."
    },
    {
      "what": "G2 (CLOSED): `combatState.HittableEnemies` (BagOfMarbles.cs:28) vs the sim's candidate set (bag_of_marbles.py:25)",
      "verdict": "faithful",
      "issue": "CLOSED: `Relic.hittable_enemies()` now exists (relics/base.py:495-497, `CombatState.hittable_enemies` -> combat.py:116-118/380) and mirrors `CombatState.HittableEnemies` (CombatState.cs:142, `!IsDead && Hook.ShouldAllowHitting`); `bag_of_marbles.py:25` calls it directly (`for enemy in self.hittable_enemies():`), replacing the old `living_enemies()` (relics/base.py:486-493, `not e.is_gone` only, still kept as the deliberately-broader helper for relics that should NOT respect ShouldAllowHitting). EXECUTED: test/test_r14_relic_candidates.py::test_bag_of_marbles_candidate_set_excludes_an_unhittable_enemy makes one enemy unhittable-but-alive and asserts it is excluded from Vulnerable while the spectator still receives it. The previous DORMANT gap (an alive-but-unhittable enemy wrongly receiving Vulnerable) no longer applies; power_cmd.json's G6 (the missing CanReceivePowers backstop inside PowerCmd.apply itself) is unaffected and remains a separate, still-open mechanism under rule 3."
    },
    {
      "what": "N1: `participants.Contains(Owner.Creature)` and `TurnNumber <= 1` (BagOfMarbles.cs:25)",
      "verdict": "faithful",
      "rationale": "The participants check restricts a both-sides hook to the owner's own side turn; the sim's before_side_turn_start only fires for the player, so it is structurally satisfied. `self.turn <= 1` matches `TurnNumber <= 1` with the executed turn numbering above."
    },
    {
      "what": "N2: the multi-target PowerCmd.Apply overload (BagOfMarbles.cs:28) vs the sim's per-enemy loop",
      "verdict": "faithful",
      "rationale": "Both iterate the enemy list in index order and apply the same amount with the same applier; the sim's loop makes the order explicit. The enemy SET is G2's subject, not this entry's."
    },
    {
      "what": "N3: Flash() (BagOfMarbles.cs:27) and the Vulnerable hover tip",
      "verdict": "waiver",
      "rationale": "RelicModel.Flash() / base.Status = RelicStatus.* are presentation only (the relic card's glow and its greyed/active state in the relic bar); the sim has no relic UI. Waived under the shared contract's presentation/animation exclusion, not under an unreachability claim."
    }
  ],
  "verdict": "waiver",
  "audited": "2026-08-04"
}
