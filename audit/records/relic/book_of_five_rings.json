{
  "unit": "relic/book_of_five_rings",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\BookOfFiveRings.cs",
    "sha256": "6c04d5b94b3814d04454fc6ce981881272500601fbaf1326b42a581eda5be8f6"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\book_of_five_rings.py",
    "sha256": "c41551898bbae0e22d7bfc454b8f7b3f2daee644d1c15ff950a60d7813621c84"
  },
  "extra_sources": [
    {
      "path": "src/Core/Models/RelicModel.cs",
      "sha256": "82d29bc5857bbf1dadaf26a6d55dbb05c19af0067bf8a642dd6d225999303f7b",
      "side": "game"
    },
    {
      "path": "sts2_rl/relics/base.py",
      "sha256": "5774c975c5b5b9de05e5fda4ce5fd82e5571b01de06d81c0d7e77b8319936770",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "6d735699fb4c6963d78b58b1cb1b713f33070af14b0865e49c4206aa2967d040",
      "side": "sim"
    }
  ],
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.COMMON (book_of_five_rings.py:11)",
      "verdict": "faithful"
    },
    "ShowCounter": {
      "maps_to": "no counterpart",
      "verdict": "waiver",
      "rationale": "RelicModel.Flash() / base.Status / ShowCounter / DisplayAmount / CanonicalVars / ExtraHoverTips / Cmd.Wait / CardCmd.Preview are presentation only -- the relic bar's glow, counter and hover text, and animation timing. The sim has no relic UI. Waived under the shared contract's presentation/animation exclusion, not under an unreachability claim."
    },
    "DisplayAmount": {
      "maps_to": "no counterpart",
      "verdict": "waiver",
      "rationale": "RelicModel.Flash() / base.Status / ShowCounter / DisplayAmount / CanonicalVars / ExtraHoverTips / Cmd.Wait / CardCmd.Preview are presentation only -- the relic bar's glow, counter and hover text, and animation timing. The sim has no relic UI. Waived under the shared contract's presentation/animation exclusion, not under an unreachability claim."
    },
    "IsAllowed": {
      "maps_to": "is_allowed (sts2_rl/relics/book_of_five_rings.py:31) -> is_before_act3_treasure_chest (sts2_rl/relics/base.py:89), consulted by RunState._drop_disallowed_from_grab_bag (sts2_rl/run.py:1080) before every pool pull",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: pool eligibility is modelled now. `Relic.is_allowed(run)` exists on the sim's base (sts2_rl/relics/base.py:253, default True) and this relic overrides it with `is_before_act3_treasure_chest(run)` (sts2_rl/relics/book_of_five_rings.py:31), the `TotalFloor < 41` predicate of RelicModel.IsBeforeAct3TreasureChest (RelicModel.cs:452-456). RunState._drop_disallowed_from_grab_bag (sts2_rl/run.py:1080) applies it before EVERY pull and REMOVES the rejects from the bag -- pull_relic_from_front (run.py:970), pull_relic_from_back (run.py:1023) and the Shop-rarity bag (run.py:1060) are the only three pull paths -- mirroring RelicGrabBag.RemoveDisallowedRelicsFromDeques (RelicGrabBag.cs:218-270), and `Relic.is_allowed_at_neow` now DELEGATES to is_allowed (sts2_rl/relics/base.py:266-277 = RelicModel.cs:443-446) so the Neow pools (sts2_rl/events/neow.py:72) cannot read a stale flag. Verified by execution against today's code: every one of the 17 cluster members returns is_allowed False at total_floor 60 and True at floor 0. WAS: the sim's Relic base declared no is_allowed member at all and populate_relic_grab_bags shuffled the pool once at run init with no per-pull filter, so the relic stayed pullable for the whole run and a late pull also shifted every subsequent pull."
    },
    "AfterCardChangedPiles": {
      "maps_to": "after_card_added_to_deck (sts2_rl/relics/book_of_five_rings.py), dispatched by RunState.add_card (sts2_rl/run.py:536) and RunState.transform_card (sts2_rl/run.py:800)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: the relic is implemented. sts2_rl/relics/book_of_five_rings.py's `after_card_added_to_deck` increments a run-scoped `cards_added` and heals HEAL = 20 whenever `cards_added % CARDS == 0` with CARDS = 5 -- BookOfFiveRings.cs:67-83's running modulo over CardsVar(5)/HealVar(20), with the owner-alive gate as `if run.is_dead: return`. RunState.add_card dispatches the hook over every relic (sts2_rl/run.py:536) and RunState.transform_card now does too (sts2_rl/run.py:800). WAS: a behaviourless stub whose docstring claimed deck edits happen between combats and so could not be modelled."
    }
  },
  "guards": [
    {
      "what": "N1: the counter is a RUNNING MODULO over the whole run (`CardsAdded % 5`), not a reset-to-zero counter",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: the relic is implemented. sts2_rl/relics/book_of_five_rings.py's `after_card_added_to_deck` increments a run-scoped `cards_added` and heals HEAL = 20 whenever `cards_added % CARDS == 0` with CARDS = 5 -- BookOfFiveRings.cs:67-83's running modulo over CardsVar(5)/HealVar(20), with the owner-alive gate as `if run.is_dead: return`. RunState.add_card dispatches the hook over every relic (sts2_rl/run.py:536) and RunState.transform_card now does too (sts2_rl/run.py:800). WAS: a behaviourless stub whose docstring claimed deck edits happen between combats and so could not be modelled."
    },
    {
      "what": "N2: `base.Owner.Creature.IsDead` and `card.Owner != base.Owner` early bails (BookOfFiveRings.cs:69-72)",
      "verdict": "waiver",
      "rationale": "The owner check is the multiplayer filter (out of scope). The IsDead check guards a deck add resolving while the owner is dead, which in the sim can only happen between rooms, when the run is already over. Neither is a divergence the stub could be said to have got wrong -- but both belong in the fix."
    },
    {
      "what": "N3: the pile filter is `pile.Type == PileType.Deck`, so combat-pile moves do NOT count",
      "verdict": "faithful",
      "rationale": "AfterCardChangedPiles fires for every pile transition in the game, including hand->discard during combat; only the Deck pile increments the counter. The sim's after_card_added_to_deck is already deck-only by construction (relics/base.py:364-366 documents it as 'AfterCardChangedPiles, filtered to a card entering the run's deck pile'), so the filter is free when the port is written. Recorded so the fix does not hang the counter off a combat hook."
    }
  ],
  "verdict": "waiver",
  "audited": "2026-07-26"
}
