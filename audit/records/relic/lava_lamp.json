{
  "unit": "relic/lava_lamp",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\LavaLamp.cs",
    "sha256": "ffe3a17d65b89b10d9f6750ef2aa6273fccfe6adac2ea3412a5187af61baf242"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\lava_lamp.py",
    "sha256": "f78446c0911c2b3826d34ff0f8f7e933ca58f5ca53b6b27a1b5eebfe957d903d"
  },
  "extra_sources": [
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21",
      "side": "game"
    },
    {
      "path": "sts2_rl/cards/base.py",
      "sha256": "b1717a7dfa9d3776aeb8bc86ab67220925e88d9c318696092e00b5c1b065cfbf",
      "side": "sim"
    },
    {
      "path": "sts2_rl/hooks.py",
      "sha256": "9c3e688e90a65df969ac129be51eb0d7d5ae9a412bf31b490d623b380b72bc92",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/base.py",
      "sha256": "e6b5c55a887cca456072370ea5d9f5718ba28163cf1bfa64b81579218db5ad3f",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/glitter.py",
      "sha256": "965af417b76bb79c967a15db1bc2e0e7d861a2db6db9932db1bf936e7d95c88d",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/lead_paperweight.py",
      "sha256": "fe99206571e396ff03c9ed01cbb96e33c2fef8ab59bb429c48590d3bb3a11c9b",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/silver_crucible.py",
      "sha256": "e9375424cc41d2dbb18f7f49b1dc08bc6246d1d0a7c57b4257510a904cccd033",
      "side": "sim"
    },
    {
      "path": "sts2_rl/rewards.py",
      "sha256": "1ae7e53a0abfc78396ea4b115f87a530cb9ccd088b56e6fffe9883215defb681",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "ca9ffc808f1fe7903d57dce0e818a41837e3dd42f81140a56db3301a58a2a10f",
      "side": "sim"
    }
  ],
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.SHOP (lava_lamp.py:13)",
      "verdict": "faithful"
    },
    "AfterRoomEntered": {
      "maps_to": "after_room_entered (sts2_rl/relics/lava_lamp.py) -- clears took_damage_this_combat on every room entry, dispatched over run.relics by RunState.enter_point",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27 FOR THIS ARM ONLY. sts2_rl/relics/lava_lamp.py is a real implementation now: `after_room_entered` (lava_lamp.py:31-32) clears `took_damage_this_combat` on EVERY room entry (LavaLamp.cs:36-40, whose parameter is AbstractRoom, not CombatRoom), and `on_damage_received` (lava_lamp.py:34-46) sets it only for the player, for amount > 0, and NOT when ValueProp.UNBLOCKABLE is in props (LavaLamp.cs:48-60). The sim omits LavaLamp.cs:44-47's `CurrentRoom is CombatRoom` gate here as well, but that half is structurally satisfied: `on_damage_received` is dispatched only from DamageCmd.deal (sts2_rl/cmds.py), and out-of-combat HP loss goes through RunState.lose_hp (sts2_rl/run.py:304-316), which fires no such hook -- so the latch cannot be set outside a combat. THE THIRD ARM IS NOT CLOSED: TryModifyCardRewardOptionsLate has no room guard and no structural substitute for one; see that entry and G1, both re-opened 2026-07-28. WAS: a behaviourless stub whose docstring called the whole thing 'a card-reward modifier applied after combat, so this is a no-op stub' -- false at all three sites."
    },
    "AfterDamageReceived": {
      "maps_to": "on_damage_received (sts2_rl/relics/lava_lamp.py) -- target is the player, amount > 0, ValueProp.UNBLOCKABLE excluded",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27 FOR THIS ARM ONLY. sts2_rl/relics/lava_lamp.py is a real implementation now: `after_room_entered` (lava_lamp.py:31-32) clears `took_damage_this_combat` on EVERY room entry (LavaLamp.cs:36-40, whose parameter is AbstractRoom, not CombatRoom), and `on_damage_received` (lava_lamp.py:34-46) sets it only for the player, for amount > 0, and NOT when ValueProp.UNBLOCKABLE is in props (LavaLamp.cs:48-60). The sim omits LavaLamp.cs:44-47's `CurrentRoom is CombatRoom` gate here as well, but that half is structurally satisfied: `on_damage_received` is dispatched only from DamageCmd.deal (sts2_rl/cmds.py), and out-of-combat HP loss goes through RunState.lose_hp (sts2_rl/run.py:304-316), which fires no such hook -- so the latch cannot be set outside a combat. THE THIRD ARM IS NOT CLOSED: TryModifyCardRewardOptionsLate has no room guard and no structural substitute for one; see that entry and G1, both re-opened 2026-07-28. WAS: a behaviourless stub whose docstring called the whole thing 'a card-reward modifier applied after combat, so this is a no-op stub' -- false at all three sites."
    },
    "TryModifyCardRewardOptionsLate": {
      "maps_to": "modify_card_reward_options_late (sts2_rl/relics/lava_lamp.py), dispatched in the second complete pass of rewards.create_reward_cards (sts2_rl/rewards.py:314-315)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-29 (round 6): arm 3 landed. `TryModifyCardRewardOptionsLate` opens on `if (!(base.Owner.RunState.CurrentRoom is CombatRoom)) return false;` (LavaLamp.cs:66-69) and the port had no counterpart, so an EVENT's card reward was upgraded too. RunState keeps no current-room object, so relics/lava_lamp.py latches the room kind in `after_room_entered` -- the hook that already fires on every room entry and already clears TookDamageThisCombat -- and the reward arm reads it. Arms 1 and 2 were already closed and are unchanged. Two tests pin it: the existing damage-free case now enters a MONSTER room first (it used to upgrade with no room at all), and a new EVENT-room case asserts nothing is upgraded. STILL OPEN: guard G2, the CloneCard -- unchanged and still dormant.  The text it replaced read: NARROWED 2026-07-27, RE-OPENED 2026-07-28: the relic exists, but the reward arm is missing LavaLamp.cs:66-69's room gate. CLOSED (arms 1 and 2): `after_room_entered` (lava_lamp.py:31-32) clears `took_damage_this_combat` on EVERY room entry (LavaLamp.cs:36-40, whose parameter is AbstractRoom, not CombatRoom), and `on_damage_received` (lava_lamp.py:34-46) sets it only for the player, only for amount > 0 and not when ValueProp.UNBLOCKABLE is in props (LavaLamp.cs:48-60). The Late PHASE is real too: rewards.create_reward_cards runs a complete plain pass and then a complete Late pass (sts2_rl/rewards.py:312-315). STILL OPEN (arm 3, the room gate): TryModifyCardRewardOptionsLate opens with `if (!(base.Owner.RunState.CurrentRoom is CombatRoom)) return false;` (LavaLamp.cs:66-69) and sts2_rl/relics/lava_lamp.py:48-53 has NO room guard at all -- it tests only `took_damage_this_combat`. Because `after_room_entered` clears that latch on every room entry, the latch is always False when a NON-combat card screen is built, so the sim upgrades every out-of-combat card offer the game leaves alone. LIVE: four ported callers reach create_reward_cards with modify_hooks defaulting to or set True outside a combat reward -- relics/lead_paperweight.py:24-27, relics/orrery.py:30, events/room_full_of_cheese.py:52 and events/brain_leech.py:44 (its second call at :60-62 correctly passes modify_hooks=False). EXECUTED 2026-07-28 against today's code (RunState(rng=random.Random(0)); relics.append(make_relic('lava_lamp')); create_reward_cards(run, RarityOddsType.REGULAR, count=3, mutate_pity=False, modify_hooks=True)) -> [('drum_of_battle', 1), ('blood_wall', 1), ('true_grit', 1)] -- all three options upgraded with no combat anywhere in sight. This record's own note N2 states the trap verbatim (\"a G1 fix that omits the room check would wrongly upgrade the latter\"); the fix omitted it. WAS: the whole relic was a behaviourless stub whose docstring called it 'a card-reward modifier applied after combat, so this is a no-op stub' -- false at all three sites. Rule 7: relics/orrery.py, events/room_full_of_cheese.py and events/brain_leech.py are not hashed by this record."
    }
  },
  "guards": [
    {
      "what": "G1 (LIVE): the whole relic is missing -- a damage-free combat's card rewards are not upgraded -- and the port's stated premise is FALSE",
      "verdict": "faithful",
      "issue": "Closed 2026-07-29 (round 6): arm 3 landed. `TryModifyCardRewardOptionsLate` opens on `if (!(base.Owner.RunState.CurrentRoom is CombatRoom)) return false;` (LavaLamp.cs:66-69) and the port had no counterpart, so an EVENT's card reward was upgraded too. RunState keeps no current-room object, so relics/lava_lamp.py latches the room kind in `after_room_entered` -- the hook that already fires on every room entry and already clears TookDamageThisCombat -- and the reward arm reads it. Arms 1 and 2 were already closed and are unchanged. Two tests pin it: the existing damage-free case now enters a MONSTER room first (it used to upgrade with no room at all), and a new EVENT-room case asserts nothing is upgraded. STILL OPEN: guard G2, the CloneCard -- unchanged and still dormant.  The text it replaced read: NARROWED 2026-07-27, RE-OPENED 2026-07-28: the relic exists, but the reward arm is missing LavaLamp.cs:66-69's room gate. CLOSED (arms 1 and 2): `after_room_entered` (lava_lamp.py:31-32) clears `took_damage_this_combat` on EVERY room entry (LavaLamp.cs:36-40, whose parameter is AbstractRoom, not CombatRoom), and `on_damage_received` (lava_lamp.py:34-46) sets it only for the player, only for amount > 0 and not when ValueProp.UNBLOCKABLE is in props (LavaLamp.cs:48-60). The Late PHASE is real too: rewards.create_reward_cards runs a complete plain pass and then a complete Late pass (sts2_rl/rewards.py:312-315). STILL OPEN (arm 3, the room gate): TryModifyCardRewardOptionsLate opens with `if (!(base.Owner.RunState.CurrentRoom is CombatRoom)) return false;` (LavaLamp.cs:66-69) and sts2_rl/relics/lava_lamp.py:48-53 has NO room guard at all -- it tests only `took_damage_this_combat`. Because `after_room_entered` clears that latch on every room entry, the latch is always False when a NON-combat card screen is built, so the sim upgrades every out-of-combat card offer the game leaves alone. LIVE: four ported callers reach create_reward_cards with modify_hooks defaulting to or set True outside a combat reward -- relics/lead_paperweight.py:24-27, relics/orrery.py:30, events/room_full_of_cheese.py:52 and events/brain_leech.py:44 (its second call at :60-62 correctly passes modify_hooks=False). EXECUTED 2026-07-28 against today's code (RunState(rng=random.Random(0)); relics.append(make_relic('lava_lamp')); create_reward_cards(run, RarityOddsType.REGULAR, count=3, mutate_pity=False, modify_hooks=True)) -> [('drum_of_battle', 1), ('blood_wall', 1), ('true_grit', 1)] -- all three options upgraded with no combat anywhere in sight. This record's own note N2 states the trap verbatim (\"a G1 fix that omits the room check would wrongly upgrade the latter\"); the fix omitted it. WAS: the whole relic was a behaviourless stub whose docstring called it 'a card-reward modifier applied after combat, so this is a no-op stub' -- false at all three sites. Rule 7: relics/orrery.py, events/room_full_of_cheese.py and events/brain_leech.py are not hashed by this record."
    },
    {
      "what": "G2 (DORMANT, but the fix must not reproduce it): C# UPGRADES A CLONE -- `RunState.CloneCard(card)` then `CardCmd.Upgrade(card2)` then `cardReward.ModifyCard(card2, this)` (LavaLamp.cs:83-85) -- and the sim has no clone helper",
      "verdict": "gap",
      "issue": "PROMPT.md bug class 17. CardModel.CreateClone is ClonePreservingMutability (CardModel.cs:2168-2179) and carries the card's enchantment, affliction, keyword edits and local energy-cost modifiers as well as its upgrade level; .superpowers/sdd/content-relic-sweeps.md sweep E establishes by execution that every sim rebuild drops all but the upgrade level, and audit/records/relic/burning_sticks.json G3 records the mechanism as LIVE at its own site. The reason C# clones at all here is that a CardCreationResult keeps the ORIGINAL alongside the modified card, so the un-upgraded option survives for the run-history screen. DORMANT AT THIS SITE for a reason that is specific and worth stating: the cards being cloned are freshly generated reward options that carry no per-instance state yet -- create_reward_cards makes them with make_card and, apart from the upgrade roll, nothing has touched them -- EXCEPT that the LATE pass runs after relic/glitter and relic/silken_tress may already have enchanted them, in which case C#'s clone keeps the enchantment and a sim rebuild would drop it. So the fix must mutate the option in place (as relics/silver_crucible.py:28-34 and relics/glitter.py:16-21 do) rather than copy it. Filed so that closing G1 does not open a class-17 instance. Rule 7: src/Core/Models/CardModel.cs, rewards.py, relics/glitter.py, relics/silken_tress.py and relics/silver_crucible.py are not hashed by this record."
    },
    {
      "what": "N1: `card.IsUpgradable` guards the Upgrade (LavaLamp.cs:81) -- PROMPT.md bug class 14",
      "verdict": "faithful",
      "rationale": "C# checks IsUpgradable (`CurrentUpgradeLevel < MaxUpgradeLevel`, CardModel.cs:785-789) BEFORE cloning and upgrading, so it never marks a curse or status as upgraded. Recorded because class 14 has fired twice already (astrolabe G1, bone_tea) and the sim's Card.upgrade() (cards/base.py:146-147) is a bare increment: a G1 fix MUST reproduce this guard, and `Card.is_upgradable` (cards/base.py:168-170) is the member to use. Nothing to compare today -- the port has no call -- so this entry is a pre-emptive pin rather than a divergence. Rule 7: src/Core/Models/CardModel.cs and cards/base.py are not hashed by this record."
    },
    {
      "what": "N2: `base.Owner.RunState.CurrentRoom is CombatRoom` on BOTH AfterDamageReceived and TryModifyCardRewardOptionsLate (LavaLamp.cs:44-47, 66-69), and `player != base.Owner` (line 70)",
      "verdict": "faithful",
      "rationale": "PROMPT.md bug class 15 (paired hooks rarely carry the same guard set) checked and CLEARED for this unit: the two behavioural hooks carry the SAME room guard, and the reward hook adds only the multiplayer owner comparison, which the contract puts in maps_to rather than treating as a divergence. The room guard confines the relic to combat card rewards -- it must not upgrade an event's or a shop's card options -- and since the sim's modify_card_reward_options is reached from create_reward_cards for BOTH combat rewards and Lead Paperweight's out-of-combat offer (relics/lead_paperweight.py:24-28), a G1 fix that omits the room check would wrongly upgrade the latter. Recorded as the concrete trap in the fix. Rule 7: relics/lead_paperweight.py and rewards.py are not hashed by this record."
    },
    {
      "what": "N3: [SavedProperty] on TookDamageThisCombat (LavaLamp.cs:22-34) and the absence of any BeforeCombatStart/AfterCombatEnd reset",
      "verdict": "faithful",
      "rationale": "The flag is serialised because AfterRoomEntered, not a combat boundary, owns its lifetime -- so the belt_buckle shape (PROMPT.md bug class 13) is not what a fix needs; a per-room clear is. `py audit/tools/relic_probes.py sweep-reset` puts lava_lamp in the 200-relic 'holds no state' bucket only because the port holds no state at all, so the sweep gives no guidance here and this entry supplies it."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
