{
  "unit": "relic/molten_egg",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\MoltenEgg.cs",
    "sha256": "b706622e797a32ba54e62bc05f71c67d7ace9f29254ba5111f49dbf14ac04571"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\molten_egg.py",
    "sha256": "79166517ac3a47cba97ce5270675ce5a487c3b396fde70e54e5bc6f296322ce8"
  },
  "extra_sources": [
    {
      "path": "src/Core/Commands/CardCmd.cs",
      "sha256": "8520d56b8db0c5c996dd0701f381d4e173e20731e50b056eed96c93ad4551904",
      "side": "game"
    },
    {
      "path": "src/Core/Helpers/Models/EggRelicHelper.cs",
      "sha256": "31070f6972195dea1da18fc87b45a47b2c3e71385873a0c89807d2fd34dd530d",
      "side": "game"
    },
    {
      "path": "src/Core/Hooks/Hook.cs",
      "sha256": "c75c8e9d5c5015b9e3b230af72e47c585e8d42bde412633e910cc68cc0be6588",
      "side": "game"
    },
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21",
      "side": "game"
    },
    {
      "path": "src/Core/Models/RelicModel.cs",
      "sha256": "82d29bc5857bbf1dadaf26a6d55dbb05c19af0067bf8a642dd6d225999303f7b",
      "side": "game"
    },
    {
      "path": "src/Core/Runs/CardCreationOptions.cs",
      "sha256": "5b63615ad2ef4d4bab8f2b4ce2ca80b555a755df955e2c6ff5a1911d7e8d3b35",
      "side": "game"
    },
    {
      "path": "src/Core/Runs/RunState.cs",
      "sha256": "19d266c5ddcdd3d3812190330c69527f70625dc10f357f970a1f38e9423f526c",
      "side": "game"
    },
    {
      "path": "sts2_rl/conformance/runner.py",
      "sha256": "614a2e5074d84de88168f2420d90970ea2c17b03ea08de173f8bfe7b173b1839",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/_eggs.py",
      "sha256": "cd60f1cee6043d72cf3f6fa56180bca7c5dcd4757826a3bb1ad2d151014f2c1a",
      "side": "sim"
    },
    {
      "path": "sts2_rl/rewards.py",
      "sha256": "1ae7e53a0abfc78396ea4b115f87a530cb9ccd088b56e6fffe9883215defb681",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "ca9ffc808f1fe7903d57dce0e818a41837e3dd42f81140a56db3301a58a2a10f",
      "side": "sim"
    },
    {
      "path": "src/Core/Factories/CardFactory.cs",
      "sha256": "69bba37c3d0ea3efc423264dd796fc4d22a49dcff3290c56ba9813365e8030dd",
      "side": "game"
    },
    {
      "path": "src/Core/Models/Relics/FrozenEgg.cs",
      "sha256": "09fdd62baaf545bfb544722d07a921db4cf406726cf327e636db96706efb7ceb",
      "side": "game"
    },
    {
      "path": "src/Core/Models/Relics/LastingCandy.cs",
      "sha256": "66b57fa6350bd39c568093feeeb220d2be09e01826d43ea3b16f8e00e3dbb530",
      "side": "game"
    },
    {
      "path": "src/Core/Models/Relics/ToxicEgg.cs",
      "sha256": "5199ee81e31f3273550d0fddf884df69a6d876df34c3b94f5105c5e946701d60",
      "side": "game"
    },
    {
      "path": "src/Core/Runs/CardCreationFlags.cs",
      "sha256": "1e43d3793d138f8ede4db48e429d03ef515105f547ee8ced0dd3438cd613bc11",
      "side": "game"
    }
  ],
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.RARE, inherited from EggRelic (relics/_eggs.py:28) rather than declared in molten_egg.py",
      "verdict": "faithful",
      "rationale": "Matches MoltenEgg.cs:14. Rule 7 note: the value lives in relics/_eggs.py, which this record does NOT hash -- sweep C's first correction was exactly this shared-base blind spot (a body-only scan reported all three eggs as unimplemented stubs)."
    },
    "IsAllowed": {
      "maps_to": "is_allowed (sts2_rl/relics/_eggs.py:35) -> is_before_act3_treasure_chest (sts2_rl/relics/base.py:82-84), consulted by RunState._drop_disallowed_from_grab_bag (sts2_rl/run.py:769) before every pool pull",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: pool eligibility is modelled now. `Relic.is_allowed(run)` exists on the sim's base (sts2_rl/relics/base.py:198, default True) and this relic overrides it with `is_before_act3_treasure_chest(run)` (sts2_rl/relics/_eggs.py:35), the `TotalFloor < 41` predicate of RelicModel.IsBeforeAct3TreasureChest (RelicModel.cs:452-456). RunState._drop_disallowed_from_grab_bag (sts2_rl/run.py:769) applies it before EVERY pull and REMOVES the rejects from the bag -- pull_relic_from_front (run.py:731), pull_relic_from_back (run.py:752) and the Shop-rarity bag (run.py:783) are the only three pull paths -- mirroring RelicGrabBag.RemoveDisallowedRelicsFromDeques (RelicGrabBag.cs:218-270), and `Relic.is_allowed_at_neow` now DELEGATES to is_allowed (sts2_rl/relics/base.py:211-222 = RelicModel.cs:443-446) so the Neow pools (sts2_rl/events/neow.py:72) cannot read a stale flag. Verified by execution against today's code: every one of the 17 cluster members returns is_allowed False at total_floor 60 and True at floor 0. WAS: the sim's Relic base declared no is_allowed member at all and populate_relic_grab_bags shuffled the pool once at run init with no per-pull filter, so the relic stayed pullable for the whole run and a late pull also shifted every subsequent pull."
    },
    "TryModifyCardRewardOptionsLate": {
      "maps_to": "EggRelic.modify_card_reward_options (relics/_eggs.py:39-42), dispatched once from rewards.create_reward_cards (rewards.py:297-301)",
      "verdict": "gap",
      "issue": "NARROWED 2026-07-28. N4 is CLOSED: the sim no longer collapses C#'s two dispatch phases \u2014 sts2_rl/rewards.py:312-315 runs a complete `modify_card_reward_options` pass over every relic and THEN a complete `modify_card_reward_options_late` pass, matching Hook.TryModifyCardRewardOptions' two IterateHookListeners loops (src/Core/Hooks/Hook.cs:1445-1467); the two hooks are separate base members (sts2_rl/relics/base.py:241-251) and sts2_rl/relics/_eggs.py:46-49 is a Late implementer, i.e. Molten Egg now upgrades in the same phase the game does. G3 remains waived (the `CardCreationFlags.NoHookUpgrades` gate at MoltenEgg.cs:27-30 is dead C# code). WHAT REMAINS is guard G4, the misplaced upgrade-LEVEL filter. C# applies `if (card.CurrentUpgradeLevel >= 1) return false;` ONLY on the deck-add hook (MoltenEgg.cs:58-61); the card-reward hook (MoltenEgg.cs:21-33) and the merchant hook (MoltenEgg.cs:35-41) both delegate to EggRelicHelper.UpgradeValidCards, whose only test is `card.Type == cardType && card.IsUpgradable` (EggRelicHelper.cs:15) \u2014 no level check. The sim funnels all three paths through one predicate: sts2_rl/relics/_eggs.py:41-44 `_applies` ends `return not (self.ONLY_UNUPGRADED and card.upgrade_level >= 1)`, and it is called by the reward hook (_eggs.py:46-49), the merchant hook (_eggs.py:51-52) and the deck-add hook (_eggs.py:54-58) alike, with ONLY_UNUPGRADED = True set at sts2_rl/relics/molten_egg.py:17. So an already-upgraded Attack sitting in a reward offer or on the merchant shelf is skipped where the game would upgrade it again. DORMANT while the ported card set stands: `is_upgradable` is `upgrade_level < max_upgrade_level` (sts2_rl/cards/base.py:209-211) and no ported card overrides max_upgrade_level above its default 1 (`grep -rn \"max_upgrade_level\" --include=*.py sts2_rl/ | grep -v \"= 0\"` matches only the base default), so today no card can be both upgraded and upgradable. Porting any multi-upgrade card (Searing Blow-shaped) makes G4 observable immediately. N1 (C# upgrades a CloneCard and calls cardReward.ModifyCard; the sim upgrades in place) is unchanged.  NARROWED 2026-07-28 (Task 0, ledger-vs-code pass after the Tier 1 fix campaign): the rest of this entry is closed in today's code. The issue it replaced read: Rollup of guards G3, G4 and N4 per binding rule 4. The upgrade itself is right; the NoHookUpgrades gate is dead C# code and waived (G3), the sim applies an upgrade-LEVEL filter here that C# applies only on the deck-add path (G4), and the sim's single pass collapses C#'s TryModifyCardRewardOptions / ...Late two-pass (N4)."
    },
    "ModifyMerchantCardCreationResults": {
      "maps_to": "EggRelic.modify_merchant_card_results (relics/_eggs.py:44-45), which delegates to the reward path; dispatched from shop.py",
      "verdict": "gap",
      "issue": "Same body as the reward path in C# too -- MoltenEgg.cs:35-41 calls the identical EggRelicHelper.UpgradeValidCards -- and notably has NO NoHookUpgrades check, so the delegation is faithful in shape. Carries guard G4's verdict (the extra upgrade-level filter) per binding rule 4."
    },
    "TryModifyCardBeingAddedToDeck": {
      "maps_to": "EggRelic.modify_card_being_added_to_deck (relics/_eggs.py:47-51), dispatched from RunState.add_card (run.py:345-348)",
      "verdict": "gap",
      "issue": "Rollup of guards G2 and G5 per binding rule 4. All four of MoltenEgg.cs:43-65's guards are reproduced (N1-N3) and the add_card route works (executed: a Bash added to the deck arrives at upgrade_level 1), but the DECK-TRANSFORM route into the deck never reaches the hook at all (G2), and C# substitutes a CLONE where the sim mutates in place (G5)."
    }
  },
  "guards": [
    {
      "what": "G1 (LIVE): the IsBeforeAct3TreasureChest pool gate has no sim counterpart",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: pool eligibility is modelled now. `Relic.is_allowed(run)` exists on the sim's base (sts2_rl/relics/base.py:198, default True) and this relic overrides it with `is_before_act3_treasure_chest(run)` (sts2_rl/relics/_eggs.py:35), the `TotalFloor < 41` predicate of RelicModel.IsBeforeAct3TreasureChest (RelicModel.cs:452-456). RunState._drop_disallowed_from_grab_bag (sts2_rl/run.py:769) applies it before EVERY pull and REMOVES the rejects from the bag -- pull_relic_from_front (run.py:731), pull_relic_from_back (run.py:752) and the Shop-rarity bag (run.py:783) are the only three pull paths -- mirroring RelicGrabBag.RemoveDisallowedRelicsFromDeques (RelicGrabBag.cs:218-270), and `Relic.is_allowed_at_neow` now DELEGATES to is_allowed (sts2_rl/relics/base.py:211-222 = RelicModel.cs:443-446) so the Neow pools (sts2_rl/events/neow.py:72) cannot read a stale flag. Verified by execution against today's code: every one of the 17 cluster members returns is_allowed False at total_floor 60 and True at floor 0. WAS: the sim's Relic base declared no is_allowed member at all and populate_relic_grab_bags shuffled the pool once at run init with no per-pull filter, so the relic stayed pullable for the whole run and a late pull also shifted every subsequent pull."
    },
    {
      "what": "G2 (LIVE): a deck-level TRANSFORM fires Hook.ModifyCardBeingAddedToDeck in C# and no deck-add hook in the sim, so a transformed-in Attack arrives UN-upgraded",
      "verdict": "faithful",
      "rationale": "CardCmd.cs:427-447 fires Hook.ModifyCardBeingAddedToDeck (:430) before the Deck AddInternal (:439) and Hook.AfterCardChangedPiles (:447) after it. RunState.transform_card now runs the same two sim-side hooks in the same order \u2014 run.py:567-570, the pile edit at :571-581, and run.py:582-583 \u2014 outside the parity/legacy branch, so both the conformance path and the RL path get them. Executed today: with molten_egg held, `transform_card(-> bash)` yields upgrade_level 1, matching what `add_card(bash)` yields and what C# yields; the sibling observables in the issue (lucky_fysh +15 gold, bing_bong deck 10 -> 11, toxic_egg Skill -> 1) all flipped as well.  RECONCILED 2026-07-28 (Task 0, ledger-vs-code pass after the Tier 1 fix campaign): this entry was recorded `gap` against pre-campaign code and the divergence is closed today. The issue it replaced read: MECHANISM: CardCmd.Transform on a **Deck** pile fires BOTH deck-add hooks -- `Hook.ModifyCardBeingAddedToDeck` (CardCmd.cs:427-434) and `Hook.AfterCardChangedPiles` (CardCmd.cs:447) -- because a transform replacement really does enter the deck. RunState.transform_card (run.py:406-465) fires NEITHER: it deletes the original and appends the replacement straight onto self.deck, bypassing run.add_card (run.py:341-354), which is the only place the sim dispatches either hook. REACHABILITY (LIVE, executed -- py audit/tools/relic_probes_b09.py b09-molten / b09-fysh): with molten_egg held, `run.add_card(bash)` yields upgrade_level 1 (correct) but `run.transform_card(-> bash)` yields upgrade_level 0 where C# yields 1; with lucky_fysh held, transform_card grants 0 gold where C# grants 15. Both sides ported: deck transforms are reached by relic/astrolabe, relic/pandoras_box and the ported Wood Carvings / Nonupeipe content, and the conformance runner compares the deck as (id, upgrade_level) pairs (conformance/runner.py:618), so the missing upgrade is a parity failure and not merely a play-strength one. ONE mechanism at two sites in this batch (relic/lucky_fysh and relic/molten_egg) -- same verdict at both per binding rule 3; the fix is one call to the add_card hook pair from transform_card, not two relic fixes. Rule 7: leans on run.py and conformance/runner.py, neither hashed here."
    },
    {
      "what": "G3: `if (options.Flags.HasFlag(CardCreationFlags.NoHookUpgrades)) return false` (MoltenEgg.cs:27-30) has no counterpart in the sim, which has no card-creation flag concept",
      "verdict": "faithful",
      "rationale": "MECHANISM NoHookUpgrades, one verdict at all THREE egg sites (relic/toxic_egg N1, relic/frozen_egg G2, relic/molten_egg G3) after the 2026-07-26 relic fix pass reconciled them -- they previously split two waivers against one gap on identical facts, which binding rule 3 forbids. VOCABULARY RULING (2026-07-26 relic fix pass), applied at every dead-in-the-source site in this tier so it stops contradicting itself. When a C# member the sim omits is DEAD IN THE GAME SOURCE ITSELF -- zero overrides, zero producers, zero callers anywhere under src/ -- the correct verdict is `faithful`, not `waiver`. PROMPT.md v6 item 3, which the relic stream itself wrote, settles the principle for the mirror-image case: 'a waiver is in-scope-adjacent and declined, and here there is no source behaviour to be faithful to.' A waiver claims something in scope was deliberately not audited; here there is nothing to decline, because the member provably cannot fire in any run of the shipping game and cannot become live by porting more content -- it would take a change to the game's own source. `faithful` also states the stronger and more useful fact: the two codebases have the SAME observable. Before this pass the relic tier split 8 waivers against 5 faithfuls on identical reasoning; these entries are now all `faithful` (binding rule 3). NOTE: this ruling does NOT touch MULTIPLAYER gates (`player != base.Owner`, `Players.Count == 1`), which stay `waiver` -- the shared contract names multiplayer as out of scope outright, and unlike a dead member a player count really can differ in the shipping game; the sim simply does not model that mode. AT THIS SITE the member is dead, and the grep is EXECUTED and exhaustive: `grep -rn NoHookUpgrades src/` returns exactly FOUR lines in the whole game source -- the enum member `NoHookUpgrades = 4` (src/Core/Runs/CardCreationFlags.cs:29) and the three egg relics READING it (FrozenEgg.cs:27, MoltenEgg.cs:27, ToxicEgg.cs:27). THREE READERS, ZERO PRODUCERS. The two composite flags that could set it indirectly are dead too: `grep -rn 'CardCreationFlags.NoUpgrades\\b' src/` returns NOTHING, and `grep -rn NoModifications src/` returns only its own declaration (src/Core/Runs/CardCreationFlags.cs:68). `CardCreationOptions.Flags` has a private setter and is writable only through `WithFlags` (src/Core/Runs/CardCreationOptions.cs:57, 211-215), and all 29 `WithFlags` call sites were enumerated (`py audit/tools/relic_probes_b09.py b09-molten`) with the same result. So the guard can never be true for any card creation the shipping game performs. THIS ALSO RETRACTS relic/frozen_egg G2's stated dormancy trigger, which was 'porting any C# effect that creates cards WithFlags(NoHookUpgrades) -- the shape exists in the source's card-generation callers'. IT DOES NOT: no such caller exists anywhere under src/, so there is no unported content that would make this live, which is exactly what distinguishes a dead member from a dormant gap. The genuinely live sibling flag is `NoModifyHooks`, which HAS a real producer -- LastingCandy.cs:127 builds `WithFlags(CardCreationFlags.NoModifyHooks | CardCreationFlags.NoCardPoolModifications)` and CardFactory.cs:104 gates the whole `Hook.TryModifyCardRewardOptions` dispatch on it. That is a live C# path with no sim counterpart and it belongs to relic/lasting_candy's record, not to the eggs. Rule 7: src/Core/Runs/CardCreationFlags.cs, src/Core/Runs/CardCreationOptions.cs, src/Core/Factories/CardFactory.cs and src/Core/Models/Relics/LastingCandy.cs are cited and not hashed by this record."
    },
    {
      "what": "G4 (DORMANT): the sim applies Molten Egg's already-upgraded refusal to ALL THREE paths; C# applies it ONLY to the deck-add path, because EggRelicHelper.UpgradeValidCards has no upgrade-level check",
      "verdict": "gap",
      "issue": "MECHANISM: the reward and merchant paths both go through `EggRelicHelper.UpgradeValidCards(cards, CardType.Attack, this)` (MoltenEgg.cs:31, :39), whose only filter is `card.Type == cardType && card.IsUpgradable` (EggRelicHelper.cs:12-21) -- no level test. The `card.CurrentUpgradeLevel >= 1 -> return false` clause exists ONLY in TryModifyCardBeingAddedToDeck (MoltenEgg.cs:58-61). The sim puts the test in the shared `EggRelic._applies` predicate (relics/_eggs.py:34-37, `ONLY_UNUPGRADED`) and calls it from all three, so the sim refuses an already-upgraded Attack on the reward and merchant paths where C# would upgrade it again. REACHABILITY (DORMANT, executed census -- py audit/tools/relic_probes_b09.py b09-molten): **0 of the ported cards have max_upgrade_level > 1**, so a level-1 card is never IsUpgradable and BOTH sides skip it via the IsUpgradable clause they share; the extra ONLY_UNUPGRADED test is therefore unobservable today. Porting any card with MaxUpgradeLevel >= 2 makes it live -- the same population relic/archaic_tooth guard G1 in batch 1 is waiting on. Also worth recording as a near-miss of PROMPT.md bug class 19: molten_egg.py:12 and relics/_eggs.py:30 both cite \"MoltenEgg.cs:56\" for the already-upgraded refusal, but :56 is the `return false` of the IsUpgradable clause and the level check is at :58 -- the semantics are right and only the line number drifted. Rule 7: leans on relics/_eggs.py and EggRelicHelper.cs, neither hashed here."
    },
    {
      "what": "G5: C# swaps in a full CLONE and upgrades the clone (`newCard = base.Owner.RunState.CloneCard(card); CardCmd.Upgrade(newCard)`, MoltenEgg.cs:62-63); the sim upgrades the card IN PLACE and hands the same object back (relics/_eggs.py:47-51)",
      "verdict": "deliberate-divergence",
      "rationale": "PROMPT.md bug class 17 is a sim 'clone' that is really a shallow REBUILD and so drops enchantment, affliction and local cost modifiers. This site is the class checked and found to run the OTHER way: RunState.CloneCard is `ClonePreservingMutability()` + AddCard (RunState.cs:394-399), and mutating in place preserves strictly MORE per-instance state than any rebuild could -- there is nothing for the sim to lose. The resulting deck entry has the same id and the same upgrade level, which is what a replay compares (conformance/runner.py:618 diffs the deck as (id, upgrade_level) pairs), so binding rule 2's same-observable test is met. Two residues, both bookkeeping: C# registers the clone on the run via AddCard and leaves the ORIGINAL model in the card db, and C# routes the upgrade through CardCmd.Upgrade -- whose IsUpgradable guard (CardModel.cs:785-789) the sim's bare `Card.upgrade()` lacks (PROMPT.md bug class 14 / sweep D). The guard is not needed HERE because _applies already filters on `card.is_upgradable`, which is exactly why sweep D classifies both _eggs.py upgrade sites as dormant rather than as the astrolabe/bone_tea defect. Rule 7: leans on relics/_eggs.py, RunState.cs, CardModel.cs and conformance/runner.py, none hashed here."
    },
    {
      "what": "N1: `card.Owner != base.Owner` on the deck-add path and `player != base.Owner` on the other two (MoltenEgg.cs:24, 37, 46)",
      "verdict": "faithful",
      "rationale": "Multiplayer-structural in all three places -- one player, so every card and every reward is the owner's. The bool return values are C#'s 'I modified the collection' signal for the AfterModifyingRewards callbacks; the sim's hooks return None or the replacement card and have nothing to signal."
    },
    {
      "what": "N2: `card.Type != CardType.Attack` (MoltenEgg.cs:50) and the type passed to UpgradeValidCards (lines 31, 39)",
      "verdict": "faithful",
      "rationale": "CARD_TYPE = CardType.ATTACK (molten_egg.py:16) on all three paths, matching all three C# sites. Recorded explicitly because the sts2 egg-to-type mapping is NOT the sts1 one -- Toxic is Skill, Frozen is Power, Molten is Attack -- and relics/_eggs.py's own module docstring flags it as the trap. Verified against the sibling sources rather than assumed."
    },
    {
      "what": "N3: `!card.IsUpgradable` (MoltenEgg.cs:54) vs `card.is_upgradable` (relics/_eggs.py:35)",
      "verdict": "faithful",
      "rationale": "CardModel.IsUpgradable is `CurrentUpgradeLevel < MaxUpgradeLevel` (CardModel.cs:785-789), which the sim's is_upgradable mirrors, and it is the clause that keeps the egg off the 35 ported cards with max_upgrade_level == 0 (every curse and status). Present on all three sim paths and on all three C# paths (the other two via UpgradeValidCards' own IsUpgradable test)."
    },
    {
      "what": "N4: the sim has ONE modify_card_reward_options pass where C# runs TryModifyCardRewardOptions and then TryModifyCardRewardOptions**Late** as two complete passes",
      "verdict": "gap",
      "issue": "MECHANISM: Hook.TryModifyCardRewardOptions (Hook.cs:1445-1468) walks every listener's non-Late override and then walks every listener's Late override, so a Late modifier is guaranteed to see the finished output of every plain one. Molten Egg is a LATE modifier (MoltenEgg.cs:21), which matters for an UPGRADER: running last means the upgrade is applied to whatever card the plain-phase relics finally settled on. The sim has a single flat pass over run.relics (rewards.py:297-301) with no phase structure. This is audit/records/seam/hook_dispatch.json gap G3 (the missing Late phase) at a second site, carrying that record's verdict per binding rule 3, and the same shape as relic/brilliant_scarf guard G2 on the energy-cost hook. DORMANT at this site: a divergence needs a ported plain-phase modify_card_reward_options listener whose output Molten Egg's upgrade depends on -- i.e. one that REPLACES a reward card with a different one -- and the ported listeners either upgrade, enchant or add to the same list. Porting a plain-phase relic that swaps a reward card's identity makes it live. Rule 7: leans on rewards.py and Hook.cs, neither hashed here."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
