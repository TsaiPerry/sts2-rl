{
  "unit": "relic/prayer_wheel",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\PrayerWheel.cs",
    "sha256": "381a30c20ccef43ba13d43ae7b00ea5f5665ac63369649486408c4b3aa69c8fc"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\prayer_wheel.py",
    "sha256": "c7dd90df2b936870b1d7f1ceb4cd7e444cfc1dcec9d2f61df1b85ac0d1b2a7c8"
  },
  "extra_sources": [
    {
      "path": "sts2_rl/relics/base.py",
      "sha256": "5774c975c5b5b9de05e5fda4ce5fd82e5571b01de06d81c0d7e77b8319936770",
      "side": "sim"
    },
    {
      "path": "sts2_rl/rewards.py",
      "sha256": "94925ea14c0058d99a11278e6999e75110ed7e9803de9cfc9cfc2b6846194bf2",
      "side": "sim"
    },
    {
      "path": "src/Core/Rewards/RewardsSet.cs",
      "sha256": "9b19a53f2c623ea7d439d6a37cabc03dd4315cd15287f261a1163fa0bdab8ebb",
      "side": "game"
    }
  ],
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.RARE (prayer_wheel.py:13), matching PrayerWheel.cs:12",
      "verdict": "faithful",
      "rationale": "Obtainable: Rare in the transcribed shared grab bag (py audit/tools/relic_probes_b12.py b12-pool reports bag=Rare)."
    },
    "TryModifyRewards": {
      "maps_to": "modify_combat_rewards (prayer_wheel.py:24-31), dispatched by rewards.py",
      "verdict": "faithful",
      "rationale": "STALE at round 11: this entry's own guards[G1] was already closed 2026-07-28 by the gap-queue continuation's section-A fix pass, and the prose here was never updated to match. EXECUTED (this pass): prayer_wheel.py:24-31 now appends `CardRewardGroup(room_type=RoomType.MONSTER)` to `rewards.card_rewards` on a Monster room -- CombatRewards now models a LIST of card-reward groups rather than the single `cards` field the stale text describes, so the second pick-one-of-3 the game's `TryModifyRewards` (PrayerWheel.cs:14-26) adds has somewhere to live. The group is appended unpopulated, matching RewardsSet.cs:137-143's populate-after-both-modify-passes order. A Monster room now offers TWO card choices, as in the game."
    }
  },
  "guards": [
    {
      "what": "G1 (LIVE): normal-enemy combats offer ONE card choice instead of two, and the stub's stated premise ('an out-of-combat reward modifier, so this is a no-op stub') is false",
      "verdict": "faithful",
      "maps_to": "PrayerWheel.modify_combat_rewards (sts2_rl/relics/prayer_wheel.py) -- appends an unpopulated CardRewardGroup on Monster rooms",
      "rationale": "CLOSED 2026-07-28 by the gap-queue continuation's section-A fix pass (this is a CODE change, not a re-reading of the old code). The structural blocker is gone: CombatRewards models a LIST of CardRewards (`card_rewards`), so the relic's second pick-one-of-3 has somewhere to live and the driver offers it as its own decision. The group is appended UNPOPULATED and drawn by apply_reward_modifiers after both hook passes, mirroring RewardsSet.cs:137-143 (`foreach (Reward item in Rewards.Except(second)) if (!item.IsPopulated) item.Populate()`), so its three Rewards-stream draws land behind everything TryModifyRewardsLate did. The room gate reads the new `rewards.room` (RewardsSet.Room), which is None on a custom set -- `room == null || room.RoomType != Monster` (PrayerWheel.cs:20-23). EXECUTED: four tests in test/test_relic_tier1_gaps.py (two groups on Monster, one on Elite/Boss, two separate decisions, and the populate-after-the-late-pass order).  The gap it replaced read: NARROWED 2026-07-27. The stub PREMISE finding is discharged -- the docstring no longer rests on a false claim -- but the relic is STILL a no-op, and the reason is now written into the port. The premise half is settled: rewards.py:842 dispatches `modify_combat_rewards` over every relic and five other relics use it. WHAT REMAINS is the structural blocker the port now names -- a second pick-one-of-THREE reward group that rewards.CombatRewards cannot express -- plus the note that the second CardReward must go through create_reward_cards so it consumes the same Rewards-stream draws."
    },
    {
      "what": "N1: `if (player != base.Owner) return false;` (PrayerWheel.cs:16-19) and the bool return",
      "verdict": "faithful",
      "rationale": "The owner test is multiplayer scoping: the sim has one player and rewards.py dispatches modify_combat_rewards over run.relics for that player, so it is structurally satisfied. The `bool` return is TryModifyRewards' 'did I change anything' signal, which the game uses to decide whether to re-render; the sim's hook returns None and mutates in place (relics/base.py:168-170), which is the house shape for this hook family and carries no observable. Recorded so the fix does not try to reproduce the return value. [line refresh: relics/base.py:286-290 declares the hook (was :168-170); rewards.py:842 dispatches it (was :526-527)]"
    },
    {
      "what": "N2: `new CardReward(CardCreationOptions.ForRoom(player, RoomType.Monster), 3, player)` -- the count is a literal 3 and there is no CanonicalVars block",
      "verdict": "faithful",
      "rationale": "PrayerWheel.cs is 27 lines with no CanonicalVars and no AscensionHelper.GetValueIfAscension, so 3 is the pinned non-ascension option count and it matches the sim's own normal card-reward width (ROOM_RARITY_ODDS-driven groups of three; the probe prints 3 options on every screen). The port pins no constant at all today, so there is nothing for PROMPT.md bug class 19 to catch -- but a fix must use 3 and the Monster-room creation options, not the options of the room actually being resolved: `CardCreationOptions.ForRoom(player, RoomType.Monster)` is hard-coded to Monster even though the enclosing guard has already established the room IS a Monster room, so the two agree today and would still agree if the guard were ever relaxed."
    },
    {
      "what": "N3: the relic overrides nothing else -- no combat hook and no IsAllowed gate",
      "verdict": "faithful",
      "rationale": "PrayerWheel.cs overrides exactly Rarity and TryModifyRewards. Checked deliberately against the two whole C# concepts PROMPT.md bug class 16 says to look for on every unit: there is no IsAllowed override (so this relic is NOT part of sweep B's 17-relic IsBeforeAct3TreasureChest cluster, unlike relic/planisphere in the same batch) and no named RNG stream in the file, so neither half of class 16 applies. Recorded because 'the stub drops only one hook' is what makes this a one-relic fix rather than a base-class one."
    }
  ],
  "verdict": "faithful",
  "audited": "2026-07-30"
}
