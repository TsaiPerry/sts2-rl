{
  "unit": "relic/ringing_triangle",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\RingingTriangle.cs",
    "sha256": "b22768d0aaef05a63bd42900d41a2e39e66a659065b789d96d09075708e0d293"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\ringing_triangle.py",
    "sha256": "c23067eaa242dc5cb34add1185ae3be7483f62105d80e3c3cccffc6abfb8dbc3"
  },
  "extra_sources": [
    {
      "path": "src/Core/Combat/CombatManager.cs",
      "sha256": "38d7193b2ec448cec170d7c96645059b2f7ef46ae2f12a6a3863299b30125840",
      "side": "game"
    },
    {
      "path": "src/Core/Hooks/Hook.cs",
      "sha256": "c75c8e9d5c5015b9e3b230af72e47c585e8d42bde412633e910cc68cc0be6588",
      "side": "game"
    },
    {
      "path": "src/Core/Models/Relics/JossPaper.cs",
      "sha256": "97237a51eda6199f48455edf7e83c061a73b97980279ee4a6cfdd149561b6bf2",
      "side": "game"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "351acc27ce4b62f37d5f9d93cb88e087b5c4de6ac3a7de43105c57c2bd85d963",
      "side": "sim"
    },
    {
      "path": "sts2_rl/hooks.py",
      "sha256": "d9419bf7fdb562fae7df6bd8e5864420a2952a3f616ed936bcd5fbc5d9ef16cd",
      "side": "sim"
    },
    {
      "path": "sts2_rl/player.py",
      "sha256": "e79fdc3f4bed064cd27e1695df81463b2af7f8b353f82ab59a09dbf5a3b51a0d",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relic_pools.py",
      "sha256": "9b154acbbf60e149f0081eb586c0faaacc6a459bfb173ff52dc2ce859d5b925a",
      "side": "sim"
    }
  ],
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.SHOP (ringing_triangle.py:13)",
      "verdict": "faithful",
      "rationale": "Matches RingingTriangle.cs:11. In the transcribed shared grab bag as Shop (relic_pools.py:127), executed by `py audit/tools/relic_probes_b13.py pool`."
    },
    "ShouldFlush": {
      "maps_to": "should_flush_hand (ringing_triangle.py:15-17), aggregated by hooks.should_flush_hand (hooks.py:644-652) and consulted once at combat.py:661",
      "verdict": "gap",
      "issue": "Rollup of guard G1 per binding rule 4. The PREDICATE is exact -- `TurnNumber > 1` (RingingTriangle.cs:21) against `self.turn > 1`, and both aggregators are first-false-wins (Hook.ShouldFlush, Hook.cs:2301-2311, vs hooks.py:648-652) -- but the sim uses the false result to skip the whole flush CALL where C# uses it to retain every card and then runs the flush tail anyway."
    },
    "ExtraHoverTips": {
      "maps_to": "no counterpart",
      "verdict": "waiver",
      "rationale": "RelicModel.Flash() / base.Status = RelicStatus.* / ShowCounter / DisplayAmount / InvokeDisplayAmountChanged / CanonicalVars / ExtraHoverTips / the N*Vfx nodes are presentation only -- the relic bar's glow, counter and hover text. The sim has no relic UI. Waived under the shared contract's presentation/animation exclusion, not under an unreachability claim."
    }
  },
  "guards": [
    {
      "what": "G1 (LIVE): a false ShouldFlush skips the sim's on_hand_emptied, and Joss Paper's Ethereal credit hangs on it",
      "verdict": "gap",
      "issue": "MECHANISM: C#'s FlushPlayerHand treats `ShouldFlush == false` as 'every card is retained' -- cardsToFlush is empty and the batched Add is skipped -- but it still runs Hook.AfterFlush AND PlayerCombatState.EndOfTurnCleanup (CombatManager.cs:1327-1346). The sim guards the entire call: `if self.hooks.should_flush_hand(): self.player.discard_hand()` (combat.py:661-662), and player.discard_hand is the ONLY caller of on_hand_emptied (player.py:197). This is audit/records/seam/turn_structure.json gap G4 at the site that record already names ('Ringing Triangle is a ported shop relic ... both return false from should_flush_hand'); one verdict per mechanism, binding rule 3 -- the seam owns the machinery, this record owns the relic's observable. EXECUTED at this relic's own site (`py audit/tools/relic_probes_b13.py ringing-triangle`): with [joss_paper] alone, five Ethereal exhausts followed by the sim's own turn-end flush gate leave cards_exhausted=0, _ethereal_pending=0 and the draw pile 4 -> 3 (the card was drawn); with [joss_paper, ringing_triangle] the same five exhausts leave _ethereal_pending stuck at 5 and the draw pile 4 -> 4 (no card drawn). C# credits from AfterSideTurnEnd (JossPaper.cs:116-120), which fires unconditionally, so the game draws in both cases. REACHABILITY (LIVE, both sides ported): Ringing Triangle is Shop rarity in the transcribed grab bag, Joss Paper is a ported Uncommon relic in the same bag, and the Ethereal trigger needs no second relic -- cards/apparition.py is a ported Ancient Skill with exhausts=True and is_ethereal=True (turn_structure G17's witness). The observable is a missing card draw on turn 1 for the rest of that combat's credit cycle."
    },
    {
      "what": "N1: `self.turn > 1` is read at the RIGHT moment -- the sim's turn counter has not yet been incremented when the flush gate runs",
      "verdict": "faithful",
      "rationale": "Binding rule 5's requirement: this could not be settled by reading. combat.end_turn consults should_flush_hand at combat.py:661 and increments `self.turn` at combat.py:674, after the enemy side. EXECUTED (`py audit/tools/relic_probes.py turn-order` and `py audit/tools/relic_probes_b13.py ringing-triangle`): on_player_turn_end fires with turn=1, `should_flush_hand()` is False on turn 1 and True on turn 2, and the turn-1 hand of 5 survives into turn 2 (which then holds 9 cards: the 5 retained plus a fresh draw). So the predicate reads TurnNumber as C# does at the point C# reads it."
    },
    {
      "what": "N2: `player != base.Owner -> return true` (RingingTriangle.cs:17-20) and the per-card Retain partition",
      "verdict": "faithful",
      "rationale": "The owner clause is single-player-structural: hooks.should_flush_hand takes no player argument because there is one. The partition C# performs when the flag is TRUE -- `!flag || card.ShouldRetainThisTurn -> retain, else flush` (turn_structure step 61) -- is reproduced by player.discard_hand's `flushed = [c for c in self.hand if not c.retain]` (player.py:192-196), so on a normal turn the two agree card for card. Only the flag == false arm diverges, which is G1's subject."
    },
    {
      "what": "N3: the relic's C# hover tip is the Retain KEYWORD, but neither side marks the retained cards as Retain",
      "verdict": "faithful",
      "rationale": "RingingTriangle.cs:13 shows the Retain keyword tip, and it would be easy to port the relic as 'give every card Retain on turn 1' -- which would differ observably, because a Retain card also survives a MID-turn discard effect and a false ShouldFlush does not. Both implementations do the same weaker thing: they veto the end-of-turn flush and leave the cards' keywords alone. Checked deliberately; the tip is presentation and the mechanism is not the keyword. Related: audit/records/seam/turn_structure.json guard G7 records that the sim has no single-turn-Retain state at all, and this relic does not need one."
    },
    {
      "what": "N4: the relic holds no state, and PlayerCombatState.EndOfTurnCleanup being skipped alongside AfterFlush is NOT double-counted here",
      "verdict": "faithful",
      "rationale": "RingingTriangle.cs has no fields (sweep-reset puts it in the 'no state at all' bucket), so bug class 13 does not apply. The other two things C# does inside the skipped arm -- Hook.AfterFlush and EndOfTurnCleanup -- are dormant on their own evidence and are verdicted at their own sites: turn_structure G4 records after_flush as having no ported listener (C#'s only implementer, Bookmark.cs, is unported) and G7 records EndOfTurnCleanup as dormant because `has_single_turn_retain` / `single_turn_sly` / `exhaust_on_next_play` have zero hits in sts2_rl/. So G1's live path is on_hand_emptied specifically, and this entry records that the other two legs were checked rather than assumed. NOTE per binding rule 7: turn_structure.json, combat.py and player.py are not hashed by this record."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
