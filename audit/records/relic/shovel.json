{
  "unit": "relic/shovel",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\Shovel.cs",
    "sha256": "21947d0774964a18e0484d101f3cc4a1aca142b4d448092665820226143006a8"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\shovel.py",
    "sha256": "4c61a17cd8ef598701a968593bcd91e6ff3e441b615ef34dc98b9b4021b1b4d8"
  },
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.RARE (shovel.py:15)",
      "verdict": "faithful",
      "rationale": "Matches Shovel.cs:11. Obtainable: Rare in the transcribed grab bag (relic_pools.py:131, 'RELIC.SHOVEL'). Rule 7: relic_pools.py is not hashed here."
    },
    "IsAllowed": {
      "maps_to": "no counterpart -- `Relic` declares no is_allowed member at all (executed: `hasattr(Relic, 'is_allowed')` is False), and populate_relic_grab_bags shuffles the pool once at run init with no per-pull filter",
      "verdict": "gap",
      "issue": "Rollup of guard G1 per binding rule 4. Shovel.cs:13-16 returns `RelicModel.IsBeforeAct3TreasureChest(runState)`, which is `runState.TotalFloor < 41` in single-player (RelicModel.cs:452-456), so the game stops offering Shovel from the Act 3 treasure chest onward. The sim keeps it pullable for the whole run."
    },
    "TryModifyRestSiteOptions": {
      "maps_to": "modify_rest_site_options (shovel.py:17-22), dispatched by RunState over cards then relics (run.py:1070-1072)",
      "verdict": "gap",
      "issue": "Rollup of guard G2 per binding rule 4. The DIG option's effect matches -- RelicCmd.Obtain(RelicFactory.PullNextRelicFromFront(Owner)) (DigRestSiteOption.cs:29) maps to run.obtain_relic_from_grab_bag() (shovel.py:21), and the default overload's Rewards-stream rarity roll is what pull_relic_from_front already uses -- but the sim ADDS an availability precondition C# does not have (G2)."
    }
  },
  "guards": [
    {
      "what": "G1 (LIVE, confirming sweep B cluster (a)): the `TotalFloor < 41` pool gate is entirely unmodelled",
      "verdict": "gap",
      "issue": "MECHANISM, with the enforcement point named because sweep B did not: C# enforces IsAllowed in the PULL path, not at population. RelicGrabBag.GetAvailableDeque calls RemoveDisallowedRelicsFromDeques(runState) on every pull (RelicGrabBag.cs:218-225, and again after a rarity refresh), and RelicFactory.PullNextRelicFromFront goes through it (RelicFactory.cs:47) -- so from floor 41 Shovel is physically removed from its rarity deque and can never be pulled again, by a chest, a shop, an elite reward or another Shovel dig. The sim's pull path (run.pull_relic_from_front, run.py:559-594) filters on rarity and shop-legality only. REACHABILITY (LIVE, executed -- py tools/audit/relic_probes_b14.py shovel): `hasattr(Relic, 'is_allowed')` is False; at total_floor = 60 shovel is still in run.relic_grab_bag; and 400 post-floor-41 pulls across 400 seeds returned shovel once, where C# returns it zero times. Both sides ported and nothing further is needed: Rare relics are pulled by every treasure chest and elite reward, and any run reaching act 3 passes floor 41. Note the observable is not one relic -- a wrong pull shifts every subsequent pull, so a single late Shovel changes the whole remaining relic sequence. This is one of the 17 relics in sweep B cluster (a) and the fix is one base-class member plus one filter in the pull path; PROMPT.md v6 binding item 2 adds the constraint that is_allowed_at_neow must be made to DELEGATE to it at the same time. Rule 7: RelicGrabBag.cs, RelicFactory.cs, RelicModel.cs, run.py, relic_pools.py and .superpowers/sdd/content-relic-sweeps.md are not hashed here."
    },
    {
      "what": "G2 (DORMANT): the sim refuses to OFFER the DIG option when the grab bag is empty; C# always offers it and grants RelicFactory.FallbackRelic instead",
      "verdict": "gap",
      "issue": "MECHANISM: Shovel.TryModifyRestSiteOptions adds `new DigRestSiteOption(player)` unconditionally (Shovel.cs:24) and DigRestSiteOption overrides nothing that could disable it -- RestSiteOption.IsEnabled is the base `=> true` (RestSiteOption.cs:37). If the bag has nothing left, PullFromFront returns null and RelicFactory substitutes FallbackRelic (RelicFactory.cs:47), so the player still gets A relic. shovel.py:18-19 returns early on `not run.has_available_relics()`, so the option does not appear at all; and run.pull_relic_from_front returns None on an empty bag (run.py:585-586), so the sim has no fallback either. Two divergences in one guard: a missing rest-site option, and no fallback relic. DORMANT, and quantified rather than asserted: EXECUTED (relic_probes_b14.py shovel) the bag holds 122 relics at run start and a DIG consumes exactly one (122 -> 121), so emptying it needs ~122 relic acquisitions in a run that offers on the order of 15-20. Porting a relic sink that drains the bag -- or a run length that does -- makes it live; so would any content that empties a single rarity deque, because C#'s GetAvailableDeque then ESCALATES Shop -> Common -> Uncommon -> Rare (RelicGrabBag.cs:227-237) while the sim falls back to `relic_grab_bag.pop(0)` regardless of rarity (run.py:595). That last difference is shared-machinery, not Shovel's, and is reported in the lessons file rather than verdicted here. Rule 7: RestSiteOption.cs, DigRestSiteOption.cs, RelicFactory.cs, RelicGrabBag.cs and run.py are not hashed here."
    },
    {
      "what": "N1: `if (player != base.Owner) return false;` (Shovel.cs:20-23) -- the sim's hook takes the run, not a player",
      "verdict": "faithful",
      "rationale": "A multiplayer guard: only the Shovel's own owner gets a DIG button. modify_rest_site_options(run, options) (relics/base.py:233-237) has no player parameter and cannot express it; structurally satisfied because the sim is single-player. Noted in the mapping per the shared contract's scope section rather than filed as a divergence. Rule 7: relics/base.py is not hashed here."
    },
    {
      "what": "N2: the rarity roll's STREAM -- `PullNextRelicFromFront(player)` -> `RollRarity(player)` -> `player.PlayerRng.Rewards` (RelicFactory.cs:26-29)",
      "verdict": "faithful",
      "rationale": "Checked against PROMPT.md bug class 16's named-stream trap, and this one is wired: shovel.py:21 calls run.obtain_relic_from_grab_bag() with no rarity_rng, and pull_relic_from_front's documented default is `self.rewards_rng` -- the per-player Rewards stream under a parity rng_set and the legacy shared RNG otherwise (run.py:577-595). So the default overload's stream is the one the sim uses. EXECUTED (relic_probes_b14.py shovel): a DIG moves the bag 122 -> 121 and adds a Rare-rolled relic to run.relics. Rule 7: run.py and RelicFactory.cs are not hashed here."
    },
    {
      "what": "N3: RelicCmd.Obtain vs run.obtain_relic_from_grab_bag, and `SharedRelicGrabBag.Remove` (RelicFactory.cs:48)",
      "verdict": "faithful",
      "rationale": "obtain_relic_from_grab_bag pulls and then calls run.add_relic (run.py:631-636), which is the sim's RelicCmd.Obtain -- it fires after_obtained, so a dug Sand Castle or Signet Ring applies its pickup effect exactly as a chest-granted one would. The SharedRelicGrabBag.Remove line is the multiplayer second bag keeping in sync with the per-player one; the sim has a single bag, so the removal has no counterpart to need. Rule 7: run.py is not hashed here."
    },
    {
      "what": "N4: the relic holds no state, and `ShouldDisableRemainingRestSiteOptions` is not overridden on either side (PROMPT.md classes 10, 13)",
      "verdict": "faithful",
      "rationale": "Shovel.cs has two members and no fields, so there is no per-combat or per-run counter to reset -- and, unlike Girya, no use limit: the option is offered at every rest site on both sides. The sim's default should_disable_remaining_rest_site_options is True (relics/base.py:239-243), which matches C# not overriding it -- a rest-site visit ends after one action, so DIG is once per rest site. Read both files. Rule 7: relics/base.py is not hashed here."
    },
    {
      "what": "N5: DoLocalPostSelectVfx / DoRemotePostSelectVfx and the sfx (DigRestSiteOption.cs:33-52)",
      "verdict": "waiver",
      "rationale": "RelicModel.Flash() / FlashSfx / base.Status = RelicStatus.* / ShowCounter / DisplayAmount / InvokeDisplayAmountChanged / UpdateDisplay / CanonicalVars / ExtraHoverTips / Title / Cmd.Wait / CardCmd.PreviewCardPileAdd / CardPreviewStyle are presentation only -- the relic bar's glow, counter, sfx and hover text, the localized name, and animation timing. The sim has no relic UI. Waived under the shared contract's presentation/animation exclusion, not under an unreachability claim."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
