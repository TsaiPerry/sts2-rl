{
  "unit": "relic/venerable_tea_set",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\VenerableTeaSet.cs",
    "sha256": "f7c950036fcd45178fe5fc5f41bb92a6b4e0af35625bd01696ec26af35c515f6"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\venerable_tea_set.py",
    "sha256": "29b58aa52af9550ade2e5f1e4a25a464d182ff1649993e84f34296c5c786964a"
  },
  "extra_sources": [
    {
      "path": "src/Core/Combat/CombatManager.cs",
      "sha256": "38d7193b2ec448cec170d7c96645059b2f7ef46ae2f12a6a3863299b30125840",
      "side": "game"
    },
    {
      "path": "src/Core/Commands/PlayerCmd.cs",
      "sha256": "8630f180d829942738c860f96dc6621cae01e7c2402fd33cee871fe21f42f668",
      "side": "game"
    },
    {
      "path": "src/Core/Models/Powers/NoEnergyGainPower.cs",
      "sha256": "304884e9f63d266f3745e852c3f34fa0e198eb23e688d86a50f622f8cbc7aebc",
      "side": "game"
    },
    {
      "path": "src/Core/Rooms/RestSiteRoom.cs",
      "sha256": "2724e3a214e09825df9ea136b0275f8f068174b95a65cbb8731b3e24f0e32960",
      "side": "game"
    },
    {
      "path": "sts2_rl/cmds.py",
      "sha256": "59d6315eee45195c01a59d38ee15cdf3654970f5818bd970ab6101ced93dc18d",
      "side": "sim"
    },
    {
      "path": "sts2_rl/player.py",
      "sha256": "5f44427a06d75c1e4f43694fed9d1bbcb8d7b315cb5eba122af9b8ee63a62e6a",
      "side": "sim"
    },
    {
      "path": "sts2_rl/powers.py",
      "sha256": "7b8fdd4f644f525d60c08fee405ddfe4dceaedfa05de64a80bb8e4412ea26ca1",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/base.py",
      "sha256": "5774c975c5b5b9de05e5fda4ce5fd82e5571b01de06d81c0d7e77b8319936770",
      "side": "sim"
    },
    {
      "path": "sts2_rl/relics/eternal_feather.py",
      "sha256": "12ebb50025729e4f4e7d9e022cdf7d578efa2d199add9dc838b36d8db189d475",
      "side": "sim"
    },
    {
      "path": "sts2_rl/run.py",
      "sha256": "6d735699fb4c6963d78b58b1cb1b713f33070af14b0865e49c4206aa2967d040",
      "side": "sim"
    }
  ],
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.COMMON (venerable_tea_set.py:19)",
      "verdict": "faithful",
      "rationale": "VenerableTeaSet.cs:17. Confirmed in the transcribed grab bag as Common (py audit/tools/relic_probes_b17.py pool -> bag=Common), so the relic is obtainable from any Common pull."
    },
    "GainEnergyInNextCombat": {
      "maps_to": "self._pending, written by after_room_entered (venerable_tea_set.py:29-38) and consumed by on_energy_reset (venerable_tea_set.py:40-44)",
      "verdict": "faithful",
      "rationale": "STALE (round 11 re-settle): this rollup summarised guard G1, which was closed 2026-07-29 round 7 -- the guard entry itself already reads `faithful` and this hooks-level entry was simply never revisited. EXECUTED (source read, sts2_rl/relics/venerable_tea_set.py): `after_room_entered` now sets `self._pending = True` on `room_type == RoomType.REST_SITE`, giving the [SavedProperty]'s persistence (which was always fine -- the relic instance lives on RunState.relics for the whole run) a real writer. EXECUTED (`py -m pytest test/test_false_premise_stubs.py -k venerable -q`): 4 passed. Nothing gap-classified remains under this hook."
    },
    "AfterRoomEntered": {
      "maps_to": "after_room_entered (venerable_tea_set.py:29-38), dispatched by Relic.after_room_entered's base declaration (relics/base.py:350) from RunState's room-entry loop (run.py:1502)",
      "verdict": "faithful",
      "rationale": "STALE (round 11 re-settle), and the whole of this record's finding. This rollup summarised guard G1, closed 2026-07-29 round 7 -- the guard entry itself already reads `faithful` and this hooks-level entry was simply never revisited. EXECUTED (source read): `after_room_entered(self, run, point, room_type)` now checks `room_type == RoomType.REST_SITE` at the room-ENTRY dispatch itself (relics/base.py:350 -> run.py:1502, called for every relic on entering every room), matching VenerableTeaSet.cs:41-49's `AfterRoomEntered`/`room is RestSiteRoom` latch -- keyed on room entry, not on whether the player actually rested, so merely walking into (and leaving) a campfire arms the relic on both sides now. The SECOND, smaller divergence this entry named (the constructor parameter's docstring conflating 'rested' with 'entered a rest site') is also resolved by construction: the constructor argument is now dead weight kept only so a test can pre-arm the relic, and the real trigger is the room-type check, not the argument. Same mechanism and same closure as this record's own `GainEnergyInNextCombat` hook entry -- one fix closed both halves at once, because the writer IS the room-entry latch. EXECUTED (`py -m pytest test/test_false_premise_stubs.py -k venerable -q`): 4 passed. Rule 7: RestSiteRoom.cs is NOT hashed by this record."
    },
    "AfterEnergyReset": {
      "maps_to": "on_energy_reset (venerable_tea_set.py:27-32)",
      "verdict": "faithful",
      "rationale": "VenerableTeaSet.cs:51-59 gains Energy.BaseValue and then clears the flag; venerable_tea_set.py:29-32 does the same two steps in the same order with ENERGY = 2. The hook slot matches: turn_structure step 18 is `await Hook.AfterEnergyReset(state, player)` immediately after the energy assignment (CombatManager.cs:650), and the sim's on_energy_reset sits in exactly that slot (player.py:369-372 (_setup_player_turn) / on_energy_reset fires right after; executed order from `py audit/tools/relic_probes.py turn-order` is on_block_cleared -> on_energy_reset -> on_player_turn_start). Because the flag is cleared on first use, both sides fire on turn 1 only even though the hook runs every turn. EXECUTED with the flag forced true (py audit/tools/relic_probes_b17.py teaset): turn-1 energy 5 instead of 3, i.e. the 2 lands correctly. The `base.Owner == player` clause is structurally satisfied (one player). Same verdict and same basis as relic/fake_venerable_tea_set's AfterEnergyReset entry, per binding rule 3. Rule 7: CombatManager.cs, player.py and audit/records/relic/fake_venerable_tea_set.json are NOT hashed by this record."
    }
  },
  "guards": [
    {
      "what": "G1 (LIVE): FROZEN CONSTRUCTOR STATE -- nothing in the sim ever sets `_pending`, so the relic does nothing in any run that picks it up",
      "verdict": "faithful",
      "issue": "Closed 2026-07-29 (round 7): `after_room_entered` latches the relic on `RoomType.REST_SITE`, which is VenerableTeaSet.cs's AfterRoomEntered / `room is RestSiteRoom`. The constructor argument survives only so a test can pre-arm the relic; `make_relic(id)` still passes none, and that no longer matters because the latch is written by the room dispatch `run.py` already made to every relic. Copied from the sibling relics/eternal_feather.py, which read the same hook with the same room test all along -- so the constructor-injection premise ('whether you rested is out-of-combat state') was false, PROMPT.md bug class 12. EXECUTED: enter a rest site, then start a combat -- turn-1 energy is 5 where it was 3, and `_pending` is back to False.  Pinned by test/test_false_premise_stubs.py.  NOT CLOSED: relic/fake_venerable_tea_set G2's THIRD relic, relic/girya, is untouched -- its lifts come from a rest-site OPTION the relic itself appends, a different path, and it is not this entry's line.  The text it replaced read: MECHANISM: the port's entire trigger is `self._pending = rested`, a constructor default (venerable_tea_set.py:23-25). RunState.add_relic builds relics through `make_relic(id)`, which is `_RELIC_CLASSES[relic_id]()` with no arguments (sts2_rl/relics/base.py:73-74, called from run.py:546-552), so `_pending` is False for the whole run and the spend half at venerable_tea_set.py:29 can never run. The sim is NOT missing the pipeline the port needs: Relic.after_room_entered is a real base hook (sts2_rl/relics/base.py:194-195) and run.py:983 dispatches it to every relic on entering EVERY room with the room type in hand -- and sts2_rl/relics/eternal_feather.py:19-22 is a ported relic that uses exactly that hook with exactly the RoomType.REST_SITE test this relic needs. PROMPT.md bug class 12 applied: the premise behind the constructor-injection design ('whether you rested is out-of-combat state, injected via the constructor') is false today. REACHABILITY (LIVE, executed -- py audit/tools/relic_probes_b17.py teaset): make_relic('venerable_tea_set') gives _pending=False; the class has no after_room_entered override; the next combat's turn-1 energy is 3 where C# gives 5; forcing _pending=True gives 5, which isolates the defect to the missing latch. Both sides ported: Common in the transcribed grab bag (probe `pool`), rest sites are a ported room type that dispatches after_room_entered, and every act has them. RULE 3 -- this is the SAME mechanism audit/records/relic/fake_venerable_tea_set.json guard G1 already verdicted `gap` and proved LIVE by execution (batch 5, turn-1 energy 3 vs 4 there because the knock-off's EnergyVar is 1); this record confirms it at the real relic's site with its own execution rather than re-deriving it, and reaches the same verdict. That record's guard G2 named this unit as pre-populated work for batch 17 and issued no verdict for it; this is that verdict. The fix is one method copied from sts2_rl/relics/eternal_feather.py:19-22. Rule 7: sts2_rl/relics/base.py, run.py, sts2_rl/relics/eternal_feather.py, sts2_rl/relics/fake_venerable_tea_set.py and audit/records/relic/fake_venerable_tea_set.json are NOT hashed by this record."
    },
    {
      "what": "N1: EnergyVar(2) (VenerableTeaSet.cs:19) read as `Energy.BaseValue` at the single call site (:56) vs the sim's ENERGY = 2 (venerable_tea_set.py:21)",
      "verdict": "faithful",
      "rationale": "No AscensionHelper.GetValueIfAscension anywhere in the file, so 2 is the pinned non-ascension value. Checked against the knock-off sibling deliberately: FakeVenerableTeaSet is EnergyVar(1) and its port pins 1, so the pair is not a copy-paste of one number (PROMPT.md bug class 29 -- siblings may differ on purpose)."
    },
    {
      "what": "N2: PlayerCmd.GainEnergy's four guards (PlayerCmd.cs:29-43) -- `amount <= 0`, `!CombatManager.IsEnding`, Hook.AfterModifyingEnergyGain, and `finalAmount > 0` -- against the sim's EnergyCmd.gain (cmds.py:1848-1879)",
      "verdict": "faithful",
      "rationale": "Traced rather than assumed, RE-VERIFIED 2026-08-04: EnergyCmd.gain now DOES carry the `!IsEnding` guard (`if is_ending(hooks): return`, cmds.py:1875, per card/_is_dead_early_return / relic/lantern G1 -- was absent at the original audit, is present now, closing what used to be a dormant-only equivalence with a real one). (a) `amount <= 0`: the argument is the pinned constant 2, so the guard cannot fire regardless. (b) `finalAmount > 0`: the sim instead does `player.energy += amount` unconditionally after the modify_energy_gain fold (cmds.py:1877-1878), which is a no-op at 0 -- and the only ported modify_energy_gain listener is NoEnergyGainPower (powers.py:861-871), which returns exactly 0, so both sides leave the energy unchanged (also already clamped to >=0 by hooks.modify_energy_gain per cmds.py's own comment, hooks.py:215). (c) Hook.AfterModifyingEnergyGain has exactly ONE override in the whole game source (NoEnergyGainPower.cs:28) and its body is presentation; the sim has no such hook, which is a seam-level omission not observable here. (d) `!IsEnding` is now directly ported (see above), so this clause no longer rests on creature_card_cmds/G14's dormant-family argument at all -- it is simply faithful. Rule 7: PlayerCmd.cs, powers.py, NoEnergyGainPower.cs and audit/records/seam/creature_card_cmds.json are NOT hashed by this record; cmds.py IS hashed by this record."
    },
    {
      "what": "N3: [SavedProperty], the `if (_gainEnergyInNextCombat != value)` change guard, base.Status Active/Normal, Flash(), CanonicalVars and ExtraHoverTips (VenerableTeaSet.cs:19-21, 23-39, 55)",
      "verdict": "waiver",
      "rationale": "RelicModel.Flash() / base.Status = RelicStatus.* / ShowCounter / DisplayAmount / CanonicalVars / ExtraHoverTips are presentation only -- the relic bar's glow, counter and hover text. The sim has no relic UI. Waived under the shared contract's presentation/animation exclusion, not under an unreachability claim. [SavedProperty] is save serialization; the sim's relic instance persists on RunState.relics for the whole run, which is the persistence the flag needs."
    },
    {
      "what": "N4: `undo_after_obtained` is absent from this port",
      "verdict": "faithful",
      "rationale": "PROMPT.md v6 binding item 3: the helper has no C# counterpart (nothing in the game un-picks a relic), so a port lacking it is not diverging from the source, which is the only question this audit asks. No operational consequence here either -- this relic's pickup mutates nothing outside run.relics, so a conformance-runner swap needs no unwind. Rule 7: sts2_rl/relics/base.py:206-... is not hashed here."
    }
  ],
  "verdict": "waiver",
  "audited": "2026-07-26"
}
