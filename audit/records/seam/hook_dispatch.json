{
  "unit": "seam/hook_dispatch",
  "game_sources": [
    {
      "path": "src/Core/Hooks/Hook.cs",
      "sha256": "c75c8e9d5c5015b9e3b230af72e47c585e8d42bde412633e910cc68cc0be6588"
    },
    {
      "path": "src/Core/Models/AbstractModel.cs",
      "sha256": "6dae61e75edc2eb867a02d217089a562a7198564bf5f3823a466e132c7a20137"
    },
    {
      "path": "src/Core/Combat/CombatState.cs",
      "sha256": "b3150c69fae0c33b48b7957c3a67bc4e5867d1641e05062cd3f5edd0a9d9f2e2"
    },
    {
      "path": "src/Core/Runs/RunState.cs",
      "sha256": "19d266c5ddcdd3d3812190330c69527f70625dc10f357f970a1f38e9423f526c"
    },
    {
      "path": "src/Core/Entities/Players/Player.cs",
      "sha256": "ffde0e9e7ef543bcf6a41016f76ce0e7c1924a64aeb351fde03e64ddba0022fe"
    },
    {
      "path": "src/Core/Entities/Players/PlayerCombatState.cs",
      "sha256": "c908ea3144a466dc0d979ec44ade207dd692d6b23b9512afbcccae3468fea123"
    },
    {
      "path": "src/Core/Models/MonsterModel.cs",
      "sha256": "9d1f6a08d79769d8a293d0ace1c96a789f6818d33b2cb7d4200e615c1c6cdbea"
    },
    {
      "path": "src/Core/Models/AfflictionModel.cs",
      "sha256": "8aee0ce0b30603c24b325ed4c02aa67e1c2182817b163a0e85a03a195df03e41"
    },
    {
      "path": "src/Core/Models/EnchantmentModel.cs",
      "sha256": "f4efb3d8568fedf92c4bd5ec3243d09c5af7a108426c6d3c53fe191b9849efd9"
    },
    {
      "path": "src/Core/Models/BadgeModel.cs",
      "sha256": "4dfe350508af69b6d9654b121acc5b76976203b4862444532552633b84f7bb34"
    },
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21"
    },
    {
      "path": "src/Core/Models/Powers/BufferPower.cs",
      "sha256": "bf95bf58bf220b5e5dfe112a32834d857be8f9ce44c0ebbebb69a6caef805231"
    },
    {
      "path": "src/Core/Models/Powers/TangledPower.cs",
      "sha256": "f52ae733202234b2cf4dfe54c05d3657e24a996e867d6142048b2d106650d022"
    },
    {
      "path": "src/Core/Models/Powers/FreeAttackPower.cs",
      "sha256": "3b5e91b54cfa618c14e9567d99c0fab1292e057db822817207c4e4ddda4a00d2"
    },
    {
      "path": "src/Core/Models/Powers/CuriousPower.cs",
      "sha256": "f3df630baac3366c2e297a029df30eee1abd014d0e752fa4142c540ee545ec6c"
    },
    {
      "path": "src/Core/Models/Relics/SpikedGauntlets.cs",
      "sha256": "f68af9cb8e3369692db2538a06e81460a51fde3b66648e4bc2ccc9f472aa8faa"
    },
    {
      "path": "src/Core/Models/Relics/BrilliantScarf.cs",
      "sha256": "7256a929a0dff043f6d1a106df17917a47f6759b35c1b412476574d1e304ee04"
    },
    {
      "path": "src/Core/Models/Relics/ThrowingAxe.cs",
      "sha256": "c7bb04ac13201a2ee892bc860caa37c0f2eff33efe31606c762442b3019a4c48"
    },
    {
      "path": "src/Core/Models/Relics/PenNib.cs",
      "sha256": "c2ca39be064bf9504c5d9bf9f0931fbe7158db6b176b303d098d9ee2cf422cde"
    },
    {
      "path": "src/Core/Models/Afflictions/Hexed.cs",
      "sha256": "8e5285643e5b594aa10eafebfbba15d02b46d8baf99226fda8b0a33a0710f824"
    },
    {
      "path": "src/Core/Models/Relics/WhiteBeastStatue.cs",
      "sha256": "3007ded1a11f5287f780cc4b53b61e924b4166bc9ca83e1af371bfb5dc25a704"
    },
    {
      "path": "src/Core/Models/Relics/WingedBoots.cs",
      "sha256": "7d0b9d0e3d301b54cb277d84a017c7fe1e74872943512e47a61fed20f843f7e7"
    },
    {
      "path": "src/Core/Models/Modifiers/Flight.cs",
      "sha256": "3772bb6e1684d20ae783ce5c2014f75cfa3e071c21e23f33fa99b5bc7c2340f8"
    },
    {
      "path": "src/Core/Models/Relics/Fiddle.cs",
      "sha256": "2326900ac466ae4a1847b3ceb1a37d635cdc9c93730fc387109cd83342acd037"
    },
    {
      "path": "src/Core/Models/Monsters/KinPriest.cs",
      "sha256": "c1c696b0d31d17faf018edff4daebb9e63afa93afd5cdd32a180938ab2b476b9"
    },
    {
      "path": "src/Core/Models/Monsters/Vantom.cs",
      "sha256": "ada9810348ccd6259f9ca82d41ce33106af796f124004d668cf2f511502d5d67"
    },
    {
      "path": "src/Core/Models/Potions/FairyInABottle.cs",
      "sha256": "f1cf24bf8230d51b14d9d105ff39690e9ba85c2cc8ceaccb3dae000dbf13911b"
    }
  ],
  "sim_sources": [
    {
      "path": "sts2_rl/hooks.py",
      "sha256": "9c3e688e90a65df969ac129be51eb0d7d5ae9a412bf31b490d623b380b72bc92"
    },
    {
      "path": "sts2_rl/combat.py",
      "sha256": "9e2c8a38776f6f0bb505b5b916976f6657c19360ad862d68022c19765848979e"
    },
    {
      "path": "sts2_rl/cmds.py",
      "sha256": "cb19497cf0a2e6a827fd8ea912c4b2ccc00305ec72f5a38e378d26365b5b9a9c"
    },
    {
      "path": "sts2_rl/player.py",
      "sha256": "f5b830ff2b35f6d56c8f3844c62d82b338c9ec4107fb918f54d61ae6a333a0aa"
    },
    {
      "path": "sts2_rl/powers.py",
      "sha256": "9231e3f0931fe70004341cb72aa6305043391e0ee6199417f5e3c5d34f199be0"
    },
    {
      "path": "sts2_rl/history.py",
      "sha256": "d0716956d1ca2374c001887accb895d93d9589e16221098239cb2522f9a8b048"
    },
    {
      "path": "sts2_rl/relics/base.py",
      "sha256": "e6b5c55a887cca456072370ea5d9f5718ba28163cf1bfa64b81579218db5ad3f"
    },
    {
      "path": "sts2_rl/monsters/base.py",
      "sha256": "0fd1857a4001e42a5b39a50ce658b7bd767f7d3b6e42a9733f11850af06e180f"
    },
    {
      "path": "sts2_rl/afflictions.py",
      "sha256": "f8c53bb835784d1241ad8ae24cf38827ae1677784874664914e02427de68abd1"
    },
    {
      "path": "sts2_rl/relics/spiked_gauntlets.py",
      "sha256": "f46a1d09206e1ae5d8e2205312b7954a6298e0b827adff470a1b39f3653fcad7"
    },
    {
      "path": "sts2_rl/relics/brilliant_scarf.py",
      "sha256": "ed49589ae906accc00dee801b102c37b5c89c66c431a332fd40921a38517b4b2"
    },
    {
      "path": "sts2_rl/relics/pen_nib.py",
      "sha256": "155157f36c78fc82e57cd38c6285f3d083d4d0e81b09f6ff8975826f6f2ea1e1"
    },
    {
      "path": "sts2_rl/relics/throwing_axe.py",
      "sha256": "3ca1723abe8e39501df9c51b8a1f096dca7ee94990538b7947eb8cf9aae1ebc3"
    },
    {
      "path": "sts2_rl/cards/unrelenting.py",
      "sha256": "da71a11d760801e67b3655ac25ec52aae3ea85dee7740ccf6f69379c917f4d50"
    },
    {
      "path": "sts2_rl/monsters/overgrowth/vine_shambler.py",
      "sha256": "50453eb9dfae1c9f0f8b6e36a4b8f4f8a8401a4579b77b3992f0354cb872a9df"
    },
    {
      "path": "sts2_rl/cards/mad_science.py",
      "sha256": "2ede1a12f1aa80dfde0f095c34997387210e6856011a40c954199fe33aa37c73"
    },
    {
      "path": "sts2_rl/events/tanx.py",
      "sha256": "da7e5f6b2b2811e2b211e0fc6f0af420d903bef871435d06e2871844e6f1a054"
    },
    {
      "path": "sts2_rl/relics/daughter_of_the_wind.py",
      "sha256": "cb1e959da2e7115dbbf253f5e0fc8ab4400f2da8a4982c135a2c4abe6fb287e5"
    },
    {
      "path": "sts2_rl/rewards.py",
      "sha256": "1ae7e53a0abfc78396ea4b115f87a530cb9ccd088b56e6fffe9883215defb681"
    },
    {
      "path": "sts2_rl/previews.py",
      "sha256": "7b07d17672f3bb530df0491e0beadd48d94aca5be39087d577aaa6b637bf42de"
    },
    {
      "path": "sts2_rl/env.py",
      "sha256": "feedc90f3e16b8eea315266f0d475bdbc0386aa3b6ffeec0c44373d35e938f83"
    },
    {
      "path": "sts2_rl/cards/base.py",
      "sha256": "b1717a7dfa9d3776aeb8bc86ab67220925e88d9c318696092e00b5c1b065cfbf"
    },
    {
      "path": "sts2_rl/relics/paels_eye.py",
      "sha256": "404484e5433027a2742642c9bae296d7ac6533fe9567f2e38bc571ac9db8956b"
    },
    {
      "path": "sts2_rl/enchantments.py",
      "sha256": "044485116b3b5da76128dff3179154c664c899a8e4e49a0c5b62e208fd0c4369"
    },
    {
      "path": "sts2_rl/events/tinker_time.py",
      "sha256": "9332376f529f4a18d2804beeb4dc0559521d2f5e0ebd61bf2694ff78b268a6f8"
    },
    {
      "path": "sts2_rl/events/__init__.py",
      "sha256": "c170436e0540caaebac2e6703e83d385e003d7ea79a8dc6010b46e4d58908e87"
    },
    {
      "path": "sts2_rl/monsters/overgrowth/the_kin.py",
      "sha256": "fa84560a3875e11397cd94b9df007dca8df3ccf2b046d621de166630d3da3ff1"
    },
    {
      "path": "sts2_rl/monsters/overgrowth/shrinker_beetle.py",
      "sha256": "37bf6cdde5a527615bbb931c083ecd1c8c65cff8573a6096a78806e08d90f57e"
    },
    {
      "path": "sts2_rl/potions.py",
      "sha256": "63c0df752ca09e9de59285b0c9bc9be809da18e69b61b65424d75f37ee1ffc6b"
    },
    {
      "path": "sts2_rl/cards/whirlwind.py",
      "sha256": "d35891525ea7ea6cfe9b84058be9b46e1c383b85884c05e173b6057bc638fe77"
    },
    {
      "path": "sts2_rl/full_env.py",
      "sha256": "e58ad6d137eb72e99eae66e885d5ca6818e03d1f7517715a7eceef928051a4a8"
    }
  ],
  "steps": [
    {
      "what": "1. CombatState.IterateHookListeners walks _allies then _enemies as one index space; allies (players) always precede enemies (CombatState.cs:412-415)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: HookSystem._ordered() (sts2_rl/hooks.py) sorts the registration list into the game's derived walk before every dispatch, keyed on (creature rank, listener category, registration index). The player/ally side ranks 0 and the enemies rank 1..N in combat-list order, so allies always precede enemies exactly as CombatState.cs:412-415 iterates _allies then _enemies as one index space. The result is cached and invalidated on listener-list or enemy-order change rather than re-derived per dispatch, which is the same order by construction. WAS: one flat registration-order list with no creature grouping."
    },
    {
      "what": "2. Per creature, list.AddRange(creature.Powers) FIRST -- a creature's powers precede its relics, potions, orbs and cards (CombatState.cs:416)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: listener classes declare a `hook_category` and _ordered() sorts on it -- CAT_POWER(0) -> CAT_RELIC(1) -> CAT_POTION(2) -> CAT_CARD(3) within one creature (sts2_rl/hooks.py), so a creature's powers now precede its relics, potions and cards as CombatState.cs:416-467 walks them. The recorded witness inverts with it: with CuriousPower and Spiked Gauntlets both held, Hook.ModifyEnergyCostInCombat now runs the power first. CombatHistory is a sim-only listener (note N3) at CAT_HISTORY(-1), ahead of the walk. WAS: the sim's effective category order was History -> Cards -> Relics -> Potions -> Powers, powers LAST where the game runs them first."
    },
    {
      "what": "3. creature.Player == null -> list.Add(creature.Monster): the MonsterModel itself is a listener (CombatState.cs:417-421; MonsterModel.cs:51)",
      "verdict": "faithful",
      "issue": "Closed 2026-08-01 (tier-2 round 13): FIXED. MonsterModel is now a real listener in C#'s slot (CombatState.cs:417-421, immediately after that creature's powers), and BOTH sim shims that faked this -- monsters/glory/aeonglass.py and monsters/glory/queen.py -- are DELETED, their handlers moved onto the monsters themselves with behavior unchanged. THE DYING CREATURE RECEIVES ITS OWN AfterDeath: Monster.hook_contains reads a new `combat_removal_committed` EVENT flag, set at the two CombatState.RemoveCreature statements (CreatureCmd.cs:529 under the :523/:527 gates, and :601 via CreatureEscaped) plus MonsterModel.cs:450's deferred completion -- NOT the eager `Creature.is_removed_from_combat` prediction, which is `is_gone and not retained_after_death` and so goes true at the HP WRITE, before ShouldDie (:505), ShouldCreatureBeRemovedFromCombatAfterDeath (:508) and AfterDeath (:519) have run. CombatState._perform_move mirrors the IsPerformingMove deferral (MonsterModel.cs:440/:447-451). This is load-bearing for the entry's OWN named trigger: KinPriest's AfterDeath has an explicit `else if (creature == base.Creature)` arm (KinPriest.cs:104-107), and 8 of the 10 C# monster AfterDeath overrides are self-death-only -- so the very port G5 exists to unblock lands precisely on this hook. Found by the reviewer AFTER the first pass shipped a predicate that silently dropped the dying monster, with the whole suite green; the reviewer's own proposed remedy was then shown INERT by execution and replaced by this event-vs-prediction diagnosis. WHAT REASONING THIS REPLACES: that MonsterModel simply was not a listener -- true, but the reason it could not just be added is that the sim predicts removal where C# commits it.  The text it replaced read: gap G5 (MonsterModel is not a sim listener): CombatState.cs:420 adds creature.Monster to the listener list and MonsterModel.cs:51 declares ShouldReceiveCombatHooks => true. CORRECTED IN THE FIX PASS -- the earlier '71 C# monster models override at least one AbstractModel hook' was wrong; it counted every `public override` in src/Core/Models/Monsters, and 123 of those are MinInitialHp/MaxInitialHp, 84 TakeDamageSfxType, 82 GenerateAnimator, 65 AfterAddedToRoom etc., none of which is an AbstractModel hook. Re-derived with `py audit/tools/dormancy_probes.py cs-monster-hooks` (every `public override <name>` in those 127 files intersected with AbstractModel.cs's 181 `public virtual` methods): exactly 12 monster models override a hook -- Aeonglass (AfterCardGeneratedForCombat, AfterDeath), Crusher (AfterCurrentHpChanged, BeforeDeath), DecimillipedeSegment, KinPriest, LagavulinMatriarch (AfterDamageReceived, AfterDeath), Queen, Rocket (AfterCurrentHpChanged, BeforeDeath), SoulFysh (AfterCardChangedPilesLate, AfterDeath), TestSubject, TheInsatiable, Vantom, WaterfallGiant. sts2_rl/monsters/base.py:78-81 stores _hooks and never calls register. DORMANT as a machinery gap, evidence executed (`py audit/tools/dormancy_probes.py monster-hooks`): across 113 sim Monster subclasses, 0 define any of HookSystem's 66 hook names, and the sim Monster base never calls hooks.register. Concrete trigger, already contended in ported content: KinPriest.cs:81-108's AfterDeath fires AllFollowerDeathResponse when the last KinFollower dies, and the ported sim KinPriest (monsters/overgrowth/the_kin.py:67-110) has no counterpart anywhere -- but that missing BEHAVIOUR is Task 10's content finding; what this record owns is that the sim has no listener category to hang it on. Vantom.cs:97-105's AfterDeath is presentation only (a music parameter)."
    },
    {
      "what": "4. Guard: !player.IsActiveForHooks -> continue, skipping that player's relics/potions/orbs/cards but NOT the powers already added (CombatState.cs:424-427; Player.cs:112,272,438,859-870)",
      "verdict": "faithful",
      "issue": "Closed 2026-08-01 (tier-2 round 13): FIXED (structural half). `!player.IsActiveForHooks -> continue` -- which skips relics/potions/orbs/cards but NOT the powers already added at CombatState.cs:416 -- is implemented in HookSystem._derive, and PlayerCombatState.is_active_for_hooks mirrors all four C# lifecycle sites (Player.cs:272/:438 init, :857-860 DeactivateHooks from CreatureCmd.cs:553, :868-871 ActivateHooks from Creature.cs:477-485). NOTE THE NEAR-MISS: shipping IsActiveForHooks WITHOUT the ActivateHooks mirror silently kills every AfterCombatEnd relic effect on a fight won at 0 HP (proven by execution both ways: Chosen Cheese max_hp 80->80 instead of 80->81), and NOTHING IN THE SUITE CATCHES IT. WHAT REASONING THIS REPLACES: the entry's dormancy evidence was the stale_listener_plugin result 'the only hit is on_enemy_side_end -> IntangiblePower x10 over 191,270 calls / 2476 passed / 30 xfailed'. THAT RESULT NO LONGER REPRODUCES AND DID NOT REPRODUCE ON THE PRE-ROUND-13 TREE EITHER -- re-run on HEAD: 175,517 calls, ZERO hits, 3685 passed. The probe had been closed by this record's own step 11 on 2026-07-28 and the number was never refreshed; five entries cite it.  The text it replaced read: gap G7 (no per-item liveness re-check, no IsActiveForHooks): C# yields `if (Contains(item))` LAZILY, per item (CombatState.cs:482-488), and Contains (549-599) drops any relic/potion/card/affliction/enchantment/orb whose HasBeenRemovedFromState is set or whose owner is not IsActiveForHooks (Player.cs:112,272,438,859-870); every sim dispatcher walks a `list(self._listeners)` snapshot with no re-check. DORMANT, evidence executed and REPRODUCIBLE FROM THE COMMITTED TREE: `py -m pytest test/ -q -p audit.tools.stale_listener_plugin`. The plugin (audit/tools/stale_listener_plugin.py) shadows every listener's bound hook method for the duration of one dispatch with a probe that re-checks `listener in hooks._listeners` AT THE MOMENT THE DISPATCHER CALLS IT -- exactly C#'s lazy Contains -- so a listener removed by an earlier listener in the same walk is caught. Over the whole suite (2476 passed / 30 xfailed) and 191,270 instrumented listener calls the only hit is on_enemy_side_end -> IntangiblePower x10 (Nemesis removing Intangible mid-dispatch) -- and C# makes that same call, because the PowerModel arm of Contains (CombatState.cs:599) checks only Owner.CombatState != null and never that the power is still on its owner. Concrete trigger: a listener that melts a relic, discards a potion or removes a card from combat while a later listener of the same category is still pending in that dispatch."
    },
    {
      "what": "5. Player's relics in player.Relics order, skipping IsMelted ones (CombatState.cs:428-435); sim: relics/base.py:153-155 registers them AFTER the cards",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: relics carry hook_category = CAT_RELIC and _ordered() places them after that player's powers and before its potions and cards (sts2_rl/hooks.py), which is CombatState.cs:428-435's slot; the sort is stable, so within the category they keep run-relic order, matching the player.Relics walk. The IsMelted filter has no sim analogue because a melted relic is unregistered rather than skipped. WAS: relics registered after the cards and so dispatched after them."
    },
    {
      "what": "6. Player's PotionSlots in slot order, skipping null slots (CombatState.cs:436-443); sim: combat.py:164-166 registers held_potions at setup but player.py:120 APPENDS a mid-combat potion at the list tail regardless of its slot",
      "verdict": "faithful",
      "issue": "Closed 2026-08-01 (tier-2 round 13): FIXED (the G2 slot half; this entry had been marked live:false). HookSystem._derive walks player.potions BY INDEX (CombatState.cs:436-443), skipping null slots, so BELT-SLOT order decides dispatch order and registration order is never consulted. Pinned by test_potions_dispatch_in_belt_slot_order_not_registration_order, which is RED on the pre-round-13 tree. WHAT REASONING THIS REPLACES: the entry's whole settled analysis rested on '_ordered()'s sort key is (rank, hook_category, i) where i is that registration-order index' -- THERE IS NO SORT KEY ANY MORE. The dormancy argument (only FairyInABottle implements a potion hook, so no pair exists to reorder) is still true and is kept as the reason this was never observable.  The text it replaced read: SETTLED 2026-07-29 (round 11). The SLOT half of gap G2 is real and still present in code, and it is DORMANT with executed enumeration. CODE (re-read against today's player.py, which has moved since the 2026-07-27 narrowing but not fixed the divergence): `PlayerCombatState.add_potion` (player.py:143-157) now correctly fills the potion's own belt array at the first null index -- `self.potions[self.potions.index(None)] = potion` -- so the potion's SLOT is right; but it then calls `self._hooks.register(potion)` (player.py:156), which appends to `HookSystem._listeners`, and `_ordered()`'s sort key is `(rank, hook_category, i)` where `i` is that registration-order index (hooks.py `_ordered`), not the potion's belt-slot index. So a potion procured into slot 0 after slot 0's previous occupant was consumed still ties AFTER a potion sitting in slot 1 that registered earlier, where CombatState.cs:436-443 walks `_potionSlots` in slot order (slot 0 before slot 1) regardless of registration time. EXECUTED enumeration (`.superpowers/sdd/unlabelled/probes/misc-potion-hook-census.py`, an MRO-aware scan of all 51 sim Potion subclasses in sts2_rl/potions.py against HookSystem's dispatch surface): exactly ONE potion class implements any hook at all -- `FairyInABottle` (`after_preventing_death`, `should_die`) -- and zero hooks have two or more distinct potion implementers. Since order between two listeners of the SAME hook only matters when at least two DIFFERENT potion classes with that hook can be dispatched together, and only one class exists, the belt-slot-vs-registration-order divergence has no pair of listeners to reorder today; a same-class duplicate (two Fairies in the belt) is also inert because should_die's first-False-short-circuit picks whichever copy is asked first and both copies' should_die/after_preventing_death bodies are identical, so the outcome (one Fairy consumed, the owner healed) does not depend on which physical slot is queried first. Concrete trigger: porting a second potion class that implements any of the same hook FairyInABottle does (should_die, after_preventing_death) or any other hook at all, held simultaneously with an existing hook-implementing potion. ORIGINAL: potions were dispatched in the flat registration order with no category at all."
    },
    {
      "what": "7. Guard: player.PlayerCombatState == null -> continue; out of combat a player contributes only relics and potions (CombatState.cs:444-447)",
      "verdict": "faithful",
      "maps_to": "no sim counterpart needed: a HookSystem exists only inside a CombatState, so player.PlayerCombatState is never null while a combat dispatch is running (combat.py:106-117)"
    },
    {
      "what": "8. OrbQueue.Orbs join the listener list (CombatState.cs:448)",
      "verdict": "waiver",
      "rationale": "Orbs are the Defect's mechanic and the sim is Ironclad-only, so there is no orb system to order. This is a CHARACTER-scope exclusion, not multiplayer: see note N7. (It used to cite N1, 'multiplayer plumbing', which misfiled it.)"
    },
    {
      "what": "9. Every card in AllPiles order -- Hand, Draw, Discard, Exhaust, Play -- and per card the card, then its Affliction, then its Enchantment; the list is REBUILT every dispatch (CombatState.cs:449-467; PlayerCombatState.cs:70-80)",
      "verdict": "faithful",
      "issue": "Closed 2026-08-01 (tier-2 round 13): FIXED. The combat listener list is now DERIVED PER DISPATCH from live pile membership (CombatState.cs:449-467 walking AllPiles per PlayerCombatState.cs:70-80) instead of frozen at combat start, so a card that changes pile changes its dispatch position. PILE ORIENTATION IS INCLUDED: CardPile is top-first (CardPile.cs:160-167 Insert(0, ...); a draw takes Cards.FirstOrDefault(), CardPileCmd.cs:843) where the sim stores the draw pile's top at the END (player.py:581; CardPileCmd.add_to_draw converts a game index p to sim index count - p), so HookSystem._derive walks the DRAW PILE REVERSED. The pile's storage order is unchanged -- only the listener walk is flipped. Hand, discard and exhaust append at the end in both engines and need no flip. Measured cost of the reversal in isolation: nil (-0.4%). The inherited census figure of SIX hook-implementing card classes is wrong -- it is NINE (Bolas, Clash, Drum of Battle, Enthralled, Howl from Beyond, Normality, Regret, Stomp, Thrumming Hatchet; the old count missed HowlFromBeyondCard.after_auto_post_play_phase_entered among others). WHAT REASONING THIS REPLACES: that a frozen registration order was equivalent because no card hook could observe cross-card order -- the census behind that claim was itself undercounted.  The text it replaced read: gap G1 (card listener order is per-pile and re-derived in C#, frozen at combat start in the sim): CombatState.cs:449-467 walks AllPiles = Hand, Draw, Discard, Exhaust, Play (PlayerCombatState.cs:70-80) on EVERY dispatch, so a card that moves pile moves position in the listener list. combat.py:124 registers player.all_cards once, in the fixed order hand+draw+discard+exhaust (player.py:100-103), and never reorders. DORMANT, evidence executed (`py audit/tools/dormancy_probes.py card-hooks`: an MRO-aware scan of all 203 classes in _CARD_CLASSES against HookSystem's 66 hook names, ignoring anything defined on the Card base itself): card classes implement only six hooks, none able to observe cross-card order -- on_card_entered_combat (1: Stomp), on_card_exhausted (1: Drum of Battle), on_energy_spent (1: Stomp), on_player_turn_start (2: Bolas, Thrumming Hatchet, both self-only), on_player_turn_end (2: Howl from Beyond, Regret -- neither reads state the other writes), should_play_card (3: Clash, Enthralled, Normality -- an AND predicate whose sim dispatcher hooks.py:690-694 has no preventer out-param, so the vetoer's identity is unobservable). Concrete trigger: porting a card whose hook reads state another card's hook writes, or giving should_play_card a preventer out-param."
    },
    {
      "what": "10. After every creature: Modifiers, then BadgeModels, then MultiplayerScalingModel (CombatState.cs:470-481)",
      "verdict": "waiver",
      "rationale": "Modifiers are custom/daily-run mutators loaded from save.Modifiers (RunState.cs:296,344) and empty in a standard run; BadgeModels are cloned into every run (RunState.cs:332) but BadgeModel.cs:3-9 says they exist to track badge stats, and an executed grep over src/Core/Models/Badges/ finds NO badge overriding any Should*/Modify*/TryModify* hook -- they are pure event listeners. Out of scope for the same reason ascension values are. See note N4."
    },
    {
      "what": "11. Yield phase: foreach (item in list) if (Contains(item)) yield return item -- the list is eager but Contains is evaluated lazily, per item, when the enumerator reaches it (CombatState.cs:482-488)",
      "verdict": "faithful",
      "rationale": "NUMBERS CORRECTED 2026-08-01 (tier-2 round 13). The quoted '2476 passed / 30 xfailed ... 191,270 instrumented listener calls ... IntangiblePower x10' is a PRE-FIX measurement that this very entry's own fix invalidated. Fresh: 178,595 calls, 0 hits, 3826 passed / 6 xfailed (HEAD control: 175,517 calls, 0 hits). Also: the lazy filter is now TWO legs, not one -- registration (`id(l) in _live`) and model state (`hook_contains()`). Verdict unchanged (faithful).  The text it replaced read: FIXED 2026-07-28. `HookSystem` maintains a `_live` set of registered listener ids (updated by `register`/`unregister`) and `_each` skips any listener that is no longer registered at the moment the walk reaches it -- the lazy `if (Contains(item)) yield return item` of CombatState.cs:482-488. A listener unregistered by an earlier listener in the SAME dispatch is now skipped rather than still invoked; previously the sim walked an eager snapshot with no membership re-check, which is what `audit/tools/stale_listener_plugin.py` was written to instrument. The set keeps the check O(1) on the dispatch path, and `unregister` only drops the mark when the last copy of a listener goes, because `_listeners` can legitimately hold the same object twice.  RECONCILED 2026-07-28 (Task 0, ledger-vs-code pass after the Tier 1 fix campaign): this entry was recorded `gap` against pre-campaign code and the divergence is closed today. The issue it replaced read: gap G7 (no per-item liveness re-check, no IsActiveForHooks): C# yields `if (Contains(item))` LAZILY, per item (CombatState.cs:482-488), and Contains (549-599) drops any relic/potion/card/affliction/enchantment/orb whose HasBeenRemovedFromState is set or whose owner is not IsActiveForHooks (Player.cs:112,272,438,859-870); every sim dispatcher walks a `list(self._listeners)` snapshot with no re-check. DORMANT, evidence executed and REPRODUCIBLE FROM THE COMMITTED TREE: `py -m pytest test/ -q -p audit.tools.stale_listener_plugin`. The plugin (audit/tools/stale_listener_plugin.py) shadows every listener's bound hook method for the duration of one dispatch with a probe that re-checks `listener in hooks._listeners` AT THE MOMENT THE DISPATCHER CALLS IT -- exactly C#'s lazy Contains -- so a listener removed by an earlier listener in the same walk is caught. Over the whole suite (2476 passed / 30 xfailed) and 191,270 instrumented listener calls the only hit is on_enemy_side_end -> IntangiblePower x10 (Nemesis removing Intangible mid-dispatch) -- and C# makes that same call, because the PowerModel arm of Contains (CombatState.cs:599) checks only Owner.CombatState != null and never that the power is still on its owner. Concrete trigger: a listener that melts a relic, discards a potion or removes a card from combat while a later listener of the same category is still pending in that dispatch."
    },
    {
      "what": "12. Contains by type (CombatState.cs:549-599): a PowerModel needs only Owner.CombatState != null (it is NOT checked against its owner's power list); relics/potions/cards/afflictions/enchantments/orbs additionally need !HasBeenRemovedFromState; a monster needs Creature.CombatState != null; badges/modifiers/achievements always pass; anything else throws",
      "verdict": "gap",
      "issue": "NARROWED 2026-08-01 (tier-2 round 13), NOT closed. C#'s lazy per-item Contains re-check (CombatState.cs:482-488, arms at :549-599) is now implemented as a hook_contains() predicate on each listener base: Card (:593), Relic (:597), Affliction (:591), Enchantment (:589) and Monster (:585). Orb (:587) is waived under N7; Badge/Modifier/Achievement/MultiplayerScaling (:575-583) under N1/N4. The Power (:599) and Potion (:595) arms were landed by the controller at fold time from the lane's corrected diffs. THREE RESIDUES REMAIN: (a) the Relic arm's first leg, HasBeenRemovedFromState, is declared but NEVER SET in production -- machinery-only, like is_melted; every sim relic removal is a bare run.relics.remove(...) (events/ranwid_the_elder.py, events/relic_trader.py, relics/toy_box.py, conformance/runner.py), none of which is a RelicCmd.Remove, so the live leg of that arm is Owner.IsActiveForHooks alone; (b) the Card arm's first leg is set at only 2 of its 3 C# sites -- CardPileCmd.cs:189 (played Power card) and CardCmd.cs:506 (transform original), cleared in reset_combat_state (= CardModel.cs:1228 / AfterCloned) -- the deck-removal site (CardPileCmd.cs:79) was landed at fold time in run.py::remove_cards and is dormant (a removed deck card is never registered in a later combat); (c) step 16 stays LEFT-OPEN under N5. IMPORTANT CORRECTION TO THE OLD PREMISE, which was DOUBLY WRONG: it said 'every one of the 66 sim dispatchers walks a list(self._listeners) snapshot with no re-check'. There are 94 dispatchers, not 66, they all route through _each, and _each ALREADY re-checked registration (`id(l) in _live`) per item before this round -- so the registration half of the lazy check was never missing; only the STATE half was. WHAT REASONING THIS REPLACES: that whole snapshot-with-no-re-check premise, plus the report's own arm table, which presented both HasBeenRemovedFromState legs as implemented without saying they were never set.  The text it replaced read: gap G7 (no per-item liveness re-check, no IsActiveForHooks): C# yields `if (Contains(item))` LAZILY, per item (CombatState.cs:482-488), and Contains (549-599) drops any relic/potion/card/affliction/enchantment/orb whose HasBeenRemovedFromState is set or whose owner is not IsActiveForHooks (Player.cs:112,272,438,859-870); every sim dispatcher walks a `list(self._listeners)` snapshot with no re-check. DORMANT, evidence executed and REPRODUCIBLE FROM THE COMMITTED TREE: `py -m pytest test/ -q -p audit.tools.stale_listener_plugin`. The plugin (audit/tools/stale_listener_plugin.py) shadows every listener's bound hook method for the duration of one dispatch with a probe that re-checks `listener in hooks._listeners` AT THE MOMENT THE DISPATCHER CALLS IT -- exactly C#'s lazy Contains -- so a listener removed by an earlier listener in the same walk is caught. Over the whole suite (2476 passed / 30 xfailed) and 191,270 instrumented listener calls the only hit is on_enemy_side_end -> IntangiblePower x10 (Nemesis removing Intangible mid-dispatch) -- and C# makes that same call, because the PowerModel arm of Contains (CombatState.cs:599) checks only Owner.CombatState != null and never that the power is still on its owner. Concrete trigger: a listener that melts a relic, discards a potion or removes a card from combat while a later listener of the same category is still pending in that dispatch."
    },
    {
      "what": "13. ModHelper.IterateAllCombatStateSubscribers last, never Contains-filtered (CombatState.cs:489-492)",
      "verdict": "waiver",
      "rationale": "Modding surface; the sim has no mod loader and no equivalent extension point. See note N2."
    },
    {
      "what": "14. RunState.IterateHookListeners: every active player's deck cards, each immediately followed by its Enchantment -- deck cards are run listeners even while a combat is running (RunState.cs:548-562)",
      "verdict": "gap",
      "issue": "gap N5 (no run-level listener list): RunState.cs:545-596 makes every deck card and its enchantment a run listener at all times, in combat and out, and delegates to the combat list when there is a child combat. The sim has no run-level HookSystem: run-scoped hooks are duck-typed directly over run.relics (relics/base.py:205-235) and a deck card is never a listener. DORMANT, evidence executed: the same `dormancy_probes.py card-hooks` scan (203 card classes x 66 hook names) finds no sim card class implementing a run-scoped hook at all -- the six it implements are all combat hooks. Concrete trigger: porting any CardModel that overrides AfterRoomEntered/AfterRewardTaken/ShouldAddToDeck or another run-level hook."
    },
    {
      "what": "15. Only when childCombatState == null: each active player's non-melted relics, then Potions, then Modifiers/BadgeModels/MultiplayerScalingModel (RunState.cs:563-576)",
      "verdict": "faithful",
      "maps_to": "sts2_rl/relics/base.py:205-235 -- the sim's run-scoped hooks are duck-typed directly over run.relics in run.relics order, which is the same set and order this branch produces for a single player's Relics leg. The branch ALSO walks that player's Potions, and the sim declares run hooks only on relics/base.py, so the sets agree only if no potion is a run-level listener. Executed (`py audit/tools/dormancy_probes.py cs-potion-run-hooks`): of the 65 files under src/Core/Models/Potions, exactly ONE overrides any AbstractModel hook -- FairyInABottle.cs, with ShouldDie and AfterPreventingDeath. Both are dispatched off runState.IterateHookListeners(combatState) (Hook.cs:2229, 1044), i.e. they enter through the run iterator but delegate to the combat list whenever a combat is running; neither is one of the 44 hooks dispatched off runState.IterateHookListeners(null), which are the genuinely run-scoped ones. So no potion can be reached by this branch in a way the sim's relic-only walk misses. Modifiers/BadgeModels are note N4's waiver."
    },
    {
      "what": "16. Run-side yield uses the same lazy Contains filter (RunState.cs:577-583)",
      "verdict": "gap",
      "issue": "LEFT OPEN and RE-SCOPED 2026-08-01 (tier-2 round 13) under N5. This entry is about the RUN-side lazy Contains (RunState.cs:577-583), NOT the combat walk; round 13's R1 was combat-level only and N5 was explicitly out of scope. Moving it off the shared G7 paragraph, which is now wrong for the combat side, so closing the combat half does not read as closing the run half. Verified while checking: RunState.IterateHookListeners (RunState.cs:545-596) applies IsActiveForHooks at :550 (deck cards) and :567 (relics/potions) and the same lazy Contains at :577-583 -- the sim has no counterpart at run level.  The text it replaced read: gap G7 (no per-item liveness re-check, no IsActiveForHooks): C# yields `if (Contains(item))` LAZILY, per item (CombatState.cs:482-488), and Contains (549-599) drops any relic/potion/card/affliction/enchantment/orb whose HasBeenRemovedFromState is set or whose owner is not IsActiveForHooks (Player.cs:112,272,438,859-870); every sim dispatcher walks a `list(self._listeners)` snapshot with no re-check. DORMANT, evidence executed and REPRODUCIBLE FROM THE COMMITTED TREE: `py -m pytest test/ -q -p audit.tools.stale_listener_plugin`. The plugin (audit/tools/stale_listener_plugin.py) shadows every listener's bound hook method for the duration of one dispatch with a probe that re-checks `listener in hooks._listeners` AT THE MOMENT THE DISPATCHER CALLS IT -- exactly C#'s lazy Contains -- so a listener removed by an earlier listener in the same walk is caught. Over the whole suite (2476 passed / 30 xfailed) and 191,270 instrumented listener calls the only hit is on_enemy_side_end -> IntangiblePower x10 (Nemesis removing Intangible mid-dispatch) -- and C# makes that same call, because the PowerModel arm of Contains (CombatState.cs:599) checks only Owner.CombatState != null and never that the power is still on its owner. Concrete trigger: a listener that melts a relic, discards a potion or removes a card from combat while a later listener of the same category is still pending in that dispatch."
    },
    {
      "what": "17. ModHelper.IterateAllRunStateSubscribers (RunState.cs:584-587)",
      "verdict": "waiver",
      "rationale": "Modding surface; see note N2."
    },
    {
      "what": "18. If childCombatState != null the run walk appends the whole combat list, so an in-combat run-level dispatch order is deck cards -> mod subscribers -> combat listeners (RunState.cs:588-595)",
      "verdict": "gap",
      "issue": "gap N5 (no run-level listener list): RunState.cs:545-596 makes every deck card and its enchantment a run listener at all times, in combat and out, and delegates to the combat list when there is a child combat. The sim has no run-level HookSystem: run-scoped hooks are duck-typed directly over run.relics (relics/base.py:205-235) and a deck card is never a listener. DORMANT, evidence executed: the same `dormancy_probes.py card-hooks` scan (203 card classes x 66 hook names) finds no sim card class implementing a run-scoped hook at all -- the six it implements are all combat hooks. Concrete trigger: porting any CardModel that overrides AfterRoomEntered/AfterRewardTaken/ShouldAddToDeck or another run-level hook."
    },
    {
      "what": "19. Hook.IterateCombatHookListeners: `if (IsOverOrEnding && !IsStarting) yield break;` -- a dispatch that BEGINS after combat started ending reaches nobody (Hook.cs:55-58)",
      "verdict": "faithful",
      "rationale": "CLOSED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass -- the turn-start split, the combat-over hook gate, Glass Eye's reward set, the out-of-combat potion action and the choose-a-card selector arm. This is a CODE change, not a re-reading of the old code. `HookSystem._each` now implements the gate. A per-hook map `_COMBAT_GATED_HOOKS` (sts2_rl/hooks.py) names the 41 sim dispatchers whose C# counterpart is in the guarded bucket, each mapped to the C# dispatcher it IS, and `_each` returns immediately for one of those once `HookSystem.combat_is_over` -- `CombatState.phase == Phase.COMBAT_OVER`, which is set in `_end_combat`, i.e. the moment the ending begins, covering both IsEnding and IsOver. The run-side and bypass buckets are deliberately NOT gated, which is what keeps the kill/death/combat-end sequence working: the record's own executed witness, Daughter of the Wind paying out from on_card_played after a lethal Strike, is `Hook.AfterCardPlayed` -- one of the ten deliberate bypasses (Hook.cs:278) -- so that behaviour is CORRECT and is now pinned as such. Also fixed here: the inverse mismatch, `Hook.BeforeCardPlayed`, which IS guarded in C# and was ungated in the sim. EXECUTED: test/test_combat_over_hook_gate.py (9 tests).  The gap it replaced read: gap G8 (no IsOverOrEnding gate): Hook.IterateCombatHookListeners (Hook.cs:53-63) yields NOTHING to a dispatch that begins after combat started ending, and 73 of the 147 dispatchers go through it. The sim has no gate -- combat.py flips Phase.COMBAT_OVER only inside _end_combat and no dispatcher consults the phase. Executed: with the ported relic Daughter of the Wind (relics/daughter_of_the_wind.py:23-33) a lethal Strike still grants its 1 Block from on_card_played after _all_enemies_dead() is true. DORMANT because every effect currently reachable there is combat-scoped state the combat then discards. Concrete trigger: porting a listener on a guarded dispatcher that mutates run-level state (HP, gold, deck) from AfterCardPlayed/AfterCardDrawn/AfterCardExhausted/AfterShuffle/AfterEnergySpent; the conformance exporter is the near-term risk."
    },
    {
      "what": "20. The guard is evaluated ONCE at enumeration start, not per listener, so a dispatch that began while combat was live still runs every listener even if one of them ends combat; combat setup (IsStarting) is exempt (Hook.cs:31-51)",
      "verdict": "faithful",
      "rationale": "CLOSED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass -- the turn-start split, the combat-over hook gate, Glass Eye's reward set, the out-of-combat potion action and the choose-a-card selector arm. This is a CODE change, not a re-reading of the old code. The once-per-dispatch semantics come for free and are pinned. `Hook.IterateCombatHookListeners` is a C# iterator, so its guard runs when enumeration starts; `_each` is a Python generator, so the test at the top of it runs at the same point. A listener that ends the combat mid-dispatch therefore does not cut off the listeners after it, and the NEXT dispatch reaches nobody. IsStarting needs no counterpart: CombatState back-references itself into HookSystem early in __init__ and assigns `phase` further down, so setup dispatches find no phase at all and the gate is inert -- the exemption for free. EXECUTED: test/test_combat_over_hook_gate.py::test_the_guard_is_read_once_per_dispatch_not_once_per_listener.  The gap it replaced read: gap G8 (no IsOverOrEnding gate): Hook.IterateCombatHookListeners (Hook.cs:53-63) yields NOTHING to a dispatch that begins after combat started ending, and 73 of the 147 dispatchers go through it. The sim has no gate -- combat.py flips Phase.COMBAT_OVER only inside _end_combat and no dispatcher consults the phase. Executed: with the ported relic Daughter of the Wind (relics/daughter_of_the_wind.py:23-33) a lethal Strike still grants its 1 Block from on_card_played after _all_enemies_dead() is true. DORMANT because every effect currently reachable there is combat-scoped state the combat then discards. Concrete trigger: porting a listener on a guarded dispatcher that mutates run-level state (HP, gold, deck) from AfterCardPlayed/AfterCardDrawn/AfterCardExhausted/AfterShuffle/AfterEnergySpent; the conformance exporter is the near-term risk."
    },
    {
      "what": "21. Bucketing all 147 public dispatchers by how each one enumerates listeners (`py audit/tools/dormancy_probes.py hook-buckets`, brace-matched bodies): 73 go through the IterateCombatHookListeners guard, 63 call runState.IterateHookListeners directly (that iterator never consults the guard), 10 are combat-side dispatchers that bypass the guard deliberately by calling <x>.IterateHookListeners() themselves -- AfterBlockBroken (107), AfterCardPlayed (278), AfterCreatureAddedToCombat (362), AfterDamageGiven (389), AfterDiedToDoom (484), ModifyKeywordsInCombat (1595), ModifyUnblockedDamageTarget (2048), ShouldCreatureBeRemovedFromCombatAfterDeath (2214), ShouldStopCombatFromEnding (2442), ShouldPowerBeRemovedOnDeath (2495) -- and 1 enumerates nothing itself: ModifyDamage (1486), which delegates its whole walk to the private ModifyDamageInternal (2511, run-side). 73 + 63 + 10 + 1 = 147. Counting ModifyDamage's delegated walk as run-side instead gives the 73/64/10 the record first stated; both are stated so the arithmetic is checkable either way. FIX-PASS CORRECTION: the first pass's list of 10 was right but its 64 double-counted ModifyDamage.",
      "verdict": "faithful",
      "rationale": "CLOSED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass -- the turn-start split, the combat-over hook gate, Glass Eye's reward set, the out-of-combat potion action and the choose-a-card selector arm. This is a CODE change, not a re-reading of the old code. The census IS the fix. `_COMBAT_GATED_HOOKS` is the sim-name -> C#-name map for exactly the guarded bucket this step counted, re-derived with `py audit/tools/dormancy_probes.py hook-buckets`; the 63 run-side dispatchers, the 10 deliberate bypasses and ModifyDamage's delegated walk are all absent from it on purpose, each with the reason recorded next to the map. Sim hooks with no `public static` in Hook.cs at all (modify_damage_cap, modify_strength_given, on_attacked, on_card_retained, on_hp_changed, on_power_applied, on_creature_escaped, on_stunned, on_extra_turn) and the sim's per-enemy turn hooks (which have no C# counterpart to bucket until turn_structure's G5 is fixed) are ungated, the safe direction. Pinned by test_the_map_only_names_hooks_the_system_dispatches and test_the_map_is_the_size_the_census_says.  The gap it replaced read: gap G8 (no IsOverOrEnding gate): Hook.IterateCombatHookListeners (Hook.cs:53-63) yields NOTHING to a dispatch that begins after combat started ending, and 73 of the 147 dispatchers go through it. The sim has no gate -- combat.py flips Phase.COMBAT_OVER only inside _end_combat and no dispatcher consults the phase. Executed: with the ported relic Daughter of the Wind (relics/daughter_of_the_wind.py:23-33) a lethal Strike still grants its 1 Block from on_card_played after _all_enemies_dead() is true. DORMANT because every effect currently reachable there is combat-scoped state the combat then discards. Concrete trigger: porting a listener on a guarded dispatcher that mutates run-level state (HP, gold, deck) from AfterCardPlayed/AfterCardDrawn/AfterCardExhausted/AfterShuffle/AfterEnergySpent; the conformance exporter is the near-term risk."
    },
    {
      "what": "22. Base per-listener protocol: `await model.<Hook>(args); model.InvokeExecutionFinished();` (AbstractModel.cs:191-194)",
      "verdict": "waiver",
      "rationale": "InvokeExecutionFinished raises the ExecutionFinished event, which AbstractModel.cs:64-68 documents as 'a little unreliable, so you should only use it for UI things that can't be done any other way'. Pure presentation. See note N2."
    },
    {
      "what": "23. Choice-context variant: choiceContext.PushModel(model) ... PopModel(model) around the await (AfterCardDiscarded 186-195, AfterCardExhausted 237-246, AfterCardPlayed 278-294)",
      "verdict": "waiver",
      "rationale": "PlayerChoiceContext is the multiplayer choice-routing channel; the sim has one local player. See note N1."
    },
    {
      "what": "24. Hook-owned-context variant: read LocalContext.NetId, RETURN WITHOUT CALLING ANYONE if it is null, then build a HookPlayerChoiceContext per listener and await AssignTaskAndWaitForPauseOrCompletion (AfterDeath 452-461, AfterDiedToDoom 486-495, BeforeFlush 534-545, and the turn dispatchers)",
      "verdict": "waiver",
      "rationale": "Multiplayer net identity plus the async pause protocol; the sim's dispatch is synchronous and single-player. See note N1."
    },
    {
      "what": "25. Grouped-await variant: pause-aware dispatchers collect WaitForCompletion() into tasksToAwait and finish with await Task.WhenAll(tasksToAwait), so a pass boundary bounds the dispatcher's return, not the listeners' side effects (BeforeFlush 539-556, BeforeTurnEnd 1236-1263, AfterTurnEnd 1269-1292)",
      "verdict": "waiver",
      "rationale": "Async task plumbing with no synchronous analogue; see note N1."
    },
    {
      "what": "26. Every hook is a virtual no-op on AbstractModel (returning Task.CompletedTask or the unmodified value), so the dispatcher calls EVERY listener unconditionally and 'does not implement' equals 'implements as identity' (AbstractModel.cs:199-2440)",
      "verdict": "deliberate-divergence",
      "rationale": "The sim dispatches duck-typed -- `if hasattr(l, '<hook>')` (hooks.py:62 and the same line in all 66 dispatchers), stated as intentional in the module docstring ('Dispatch is duck-typed: a hook only calls listeners that define a matching method'). The observable is identical whenever the method names agree, because a C# no-op override and an absent Python method both contribute nothing; the only difference is a maintenance one (a misspelled hook name is a silent no-op in the sim and a compile error in C#), which is why test/test_hook_order.py's `trace` helper raises AttributeError on an unknown hook name."
    },
    {
      "what": "27. 24 of the 147 dispatchers run more than one COMPLETE listener pass -- 20 run two, and AfterPlayerTurnStart (882-908), AfterAutoPrePlayPhaseEntered (928-958), BeforeTurnEnd (1232-1265) and ModifyHpLost (1717-1769) run three or four",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: HookSystem._each runs the multi-pass shape. _PHASES = (_very_early, _early, '', _late) and for any hook some current listener phases, _each re-walks the whole ordered listener list once per suffix in that order (sts2_rl/hooks.py) -- the shape Hook.AfterPlayerTurnStart (Early/plain/Late, Hook.cs:882-908) and Hook.ModifyEnergyCostInCombat (plain/Late, Hook.cs:1574-1590) have. Hooks nobody phases stay a single walk, which is behaviourally identical to running empty passes. The recorded witness is wired: FreeAttackPower implements modify_card_energy_cost_late (sts2_rl/powers.py) against TangledPower's plain one, so the game's always-0 answer no longer depends on application order. WAS: hooks.py had one walk per hook and no phase concept at all."
    },
    {
      "what": "28. AbstractModel.cs declares 27 phase-suffixed hooks; nine belong to dispatchers this record owns -- AfterCardChangedPilesLate (359), AfterCardDrawnEarly (384), AfterCardPlayedLate (474), ModifyCardRewardCreationOptionsLate (1525), ModifyGeneratedMapLate (1627), TryModifyCardBeingAddedToDeckLate (1951), TryModifyCardRewardOptionsLate (1988), TryModifyEnergyCostInCombatLate (2016), TryModifyRewardsLate (2122)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: the phase suffixes are the sim's dispatch contract. A listener opts into a pass by defining <hook>_very_early / <hook>_early / <hook>_late alongside <hook>, and _phase_hooks() memoises which base names any registered listener phases (sts2_rl/hooks.py). Ports of the nine this record owns exist across the tree -- on_card_drawn_early (AfterCardDrawnEarly), modify_card_energy_cost_late (TryModifyEnergyCostInCombatLate), modify_card_reward_options_late (TryModifyCardRewardOptionsLate), modify_combat_rewards_late (TryModifyRewardsLate), modify_generated_map_late (ModifyGeneratedMapLate) -- so the declaration is expressible rather than merely dispatchable. WAS: no phase concept, so every phase-suffixed hook collapsed onto its plain sibling."
    },
    {
      "what": "29. The phases are load-bearing, not cosmetic: BufferPower.cs:17-19 states 'We use Late because other effects may reduce damage taken to 0 too, and it's more player-friendly for them to trigger first so that this power doesn't have to decrement'",
      "verdict": "faithful",
      "issue": "STALE, CLOSED as of today's tree (round 11, 2026-07-29). The exemplar this step named is fixed: `sts2_rl/powers.py:4568` BufferPower now implements `modify_hp_lost_late`, not the plain `modify_hp_lost` the 2026-07-27 narrowing found -- `grep -n \"def modify_hp_lost\" sts2_rl/powers.py` shows the 3 remaining PLAIN implementers (IntangiblePower:552, SlipperyPower:1818, HardenedShellPower:2318, all cap-style `min(amount, N)`, none reducing to exactly 0) and BufferPower is the sole `_late` one, so it now runs strictly after them in `_each`'s per-phase re-walk (guard G3's machinery, closed at step 27/28). The class docstring at powers.py:4555-4562 now cites BufferPower.cs's own 'we use Late...' comment as the reason for the port, i.e. the fix quotes the exact source this step's `what` quotes. EXECUTED: `py -m pytest test/test_powers.py -k buffer -q` -> `2 passed` -- `test_buffer_prevents_one_instance_of_hp_loss` (a bare hit reduced 15->0 by Buffer alone, stack spent) and `test_buffer_survives_a_fully_blocked_hit` (block absorbs the hit to 0 before modify_hp_lost runs at all, stack untouched) -- the second is this step's own 'a hit fully absorbed by block leaves the stack alone' case. No ported plain-pass listener currently zeroes an amount outright (they cap at >=1), so the load-bearing 1-vs-0 race the C# comment describes cannot be exercised beyond the block case today, but the ORDERING itself -- Buffer strictly after every plain-pass modify_hp_lost listener -- is now correct by construction, which is what this step actually verdicts (guard G3's phase machinery applied to this exemplar, not the not-yet-ported contention case). WAS (2026-07-27 narrowing, now false): 'the sim's BufferPower implements the PLAIN modify_hp_lost, not a _late variant... With Buffer on the plain pass it can reduce the amount to 0 ahead of another reducer, land in the modifiers list and spend a stack the game would not have spent.'"
    },
    {
      "what": "30. Each pass re-enumerates the listener list from scratch, so pass N+1 sees listeners added or removed during pass N (subject to steps 11-12 and 19)",
      "verdict": "faithful",
      "rationale": "FIXED 2026-07-28. `HookSystem._each` (sts2_rl/hooks.py) now calls `self._ordered()` inside the phase loop, once per pass, instead of binding one list before the loop -- so pass N+1 sees listeners added or removed during pass N, which is what C# gets for free by calling `IterateCombatHookListeners` separately per pass (Hook.cs:204 and :212 for AfterCardDrawn, :1580 and :1584 for ModifyEnergyCostInCombat). Within a single pass the iteration is still over a snapshot, which is also C#'s shape: `CombatState.IterateHookListeners` (CombatState.cs:410-493) builds a `new List<AbstractModel>` and then re-checks membership per item as it yields.  RECONCILED 2026-07-28 (Task 0, ledger-vs-code pass after the Tier 1 fix campaign): this entry was recorded `gap` against pre-campaign code and the divergence is closed today. The issue it replaced read: NARROWED 2026-07-27: the PASSES now exist (guard G3's machinery, sts2_rl/hooks.py _each). WHAT REMAINS is this step's own claim: the passes do NOT re-enumerate. _each binds `order = self._ordered()` ONCE and iterates that same cached list for every suffix, so a listener registered or unregistered during the VeryEarly pass is still absent from / present in the Late pass, where CombatState.IterateHookListeners is re-invoked per pass in C# and rebuilds the list from the creatures each time (CombatState.cs:410-493). Same family as guard G7 (the snapshot walk), one level up. ORIGINAL: there were no passes to re-enumerate."
    },
    {
      "what": "31. Additive (num += item.ModifyXAdditive(target, num, ...)) and multiplicative (num *= item.ModifyXMultiplicative(target, num, ...)) are SEQUENTIAL chains in which each listener receives the RUNNING value and the dispatcher folds the result in immediately, in decimal (Hook.cs:1320-1337 ModifyBlock, 2515-2538 ModifyDamageInternal)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27 for the MULTIPLICATIVE family, which was the divergent one. hooks.modify_damage_multiplicative and hooks.modify_block_multiplicative now fold each listener's factor into the running amount and return the new amount, matching ModifyDamageInternal (Hook.cs:2515-2538) and ModifyBlock (Hook.cs:1310-1340); the callers (DamageCmd.deal, BlockCmd.apply) apply the returned value directly instead of an aggregate. The recorded witness now agrees: Shrink x0.7 with Vulnerable x1.5 on 20 gives 21. The strict xfail that pinned this is gone from test/test_hook_order.py. The additive family still hands every listener the same pre-step base rather than the running total, which is exactly equal over integers (base + sum(a_i) == the sequential fold) and was settled on the merits by execution in the earlier fix pass. Same verdict now carried at the other two sites: damage_pipeline guard N3 and creature_card_cmds step 13. WAS: parallel sum/product applied once, giving 20 where the game gives 21."
    },
    {
      "what": "32. Chain family: x = item.ModifyX(..., x), a plain fold with no change-tracking. The 20 dispatchers this record owns with that shape, complete (census: `py audit/tools/dormancy_probes.py hook-buckets` for the 147, minus the 53 the other four seams claim by name, then classify each of the remaining 94 off its brace-matched body): ModifyAttackHitCount (1297), ModifyCardPlayCount (1372), ModifyCardRewardCreationOptions (1429), ModifyCardRewardUpgradeOdds (1473), ModifyEnergyGain (1606), ModifyGoldGained (1626), ModifyExtraRestSiteHealText (1646), ModifyGeneratedMap (1658), ModifyGeneratedMapLate (1671), ModifyMerchantCardPool (1794), ModifyMerchantCardRarity (1806), ModifyMerchantPrice (1818), ModifyNextEvent (1830), ModifyOddsIncreaseForUnrolledRoomType (1843), ModifyOrbPassiveTriggerCount (1855, chain + notification list, see step 38), ModifyOrbValue (1874), ModifyRestSiteHealAmount (1936), ModifySummonAmount (2032), ModifyUnknownMapPointRoomTypes (2061), ModifyXValue (2071). FIX-PASS CORRECTIONS: ModifyCardRewardUpgradeOdds and ModifyOrbPassiveTriggerCount were missing; ModifyStarCost (2015) was listed here and is a try-chain (step 34). Full family census of the 94 this record owns: 46 event, 20 chain, 9 try-chain, 14 predicate-AND, 3 predicate-OR-short-circuit, 2 predicate-OR-no-short-circuit -- so coverage is established BY FAMILY, and the event bucket is the one deliberately not enumerated by name (step 40).",
      "verdict": "faithful",
      "maps_to": "hooks.py's chain hooks are the same fold: modify_strength_given (165-168), modify_power_amount (180-183), modify_vulnerable_multiplier (191-194), modify_card_energy_cost (198-201), modify_max_energy (205-208), modify_energy_gain (212-215), modify_hand_draw (219-222), modify_card_play_count (231-234), modify_card_play_result_pile (243-246), modify_orb_value (250-253), modify_x_value (258-261) -- each rebinds the accumulator from the listener's return, so the running value reaches the next listener exactly as in C#."
    },
    {
      "what": "33. Cap family: track a running minimum and clamp as it falls (Hook.cs:2539-2555) -- a fourth aggregation shape the seed fact's three-family taxonomy does not name",
      "verdict": "faithful",
      "maps_to": "hooks.py:80-93 modify_damage_cap takes min over the non-None caps and returns it for the caller to apply (cmds.py:60-63), which is the same running-minimum-then-clamp as Hook.cs:2539-2555. The SITE (Hook.ModifyDamage) is damage_pipeline's and it verdicts the cap faithful too (its step 4), so this is the same verdict at both, per rule 3 -- see the doc's 'Cross-seam consequences' for the ownership policy: this record verdicts the aggregation-family SHAPE wherever it occurs, the owning seam verdicts the site's observable, and the two must agree."
    },
    {
      "what": "34. Try-chain family: the listener is asked `bool TryModifyX(...)` and the dispatcher either threads an out-value or records that the listener changed something. All 9 this record owns, complete (same census as step 32, classified off brace-matched bodies): value-threading -- ModifyEnergyCostInCombat (1574-1590, two passes, `item.TryModifyEnergyCostInCombat(card, modifiedCost, out modifiedCost)`) and ModifyStarCost (2015-2027, one pass, same shape); value-threading with a modifier list -- ModifyCardBeingAddedToDeck (1345-1367, two passes, `newCard != null` also required); collect-only (the listener mutates the collection it is handed and the dispatcher RETURNS the listeners that reported a change) -- ModifyCardRewardAlternatives (1413-1424), TryModifyCardRewardOptions (1445-1467, two passes, also returns a bool 'anyone changed it'), ModifyRestSiteOptions (1949-1960), ModifyRestSiteHealRewards (1965-1976), ModifyRewards (1981-2002); and fire-and-forget -- ModifyKeywordsInCombat (1595-1601), whose TryModifyKeywordsInCombat return value is DISCARDED, so it is a try-shaped event. The four collect-only ones plus ModifyCardBeingAddedToDeck are also step 38's notification lists. FIX-PASS CORRECTION: the first pass listed 5 and put ModifyStarCost under the chain family and ModifyRestSiteOptions / ModifyRestSiteHealRewards / ModifyKeywordsInCombat nowhere.",
      "verdict": "deliberate-divergence",
      "rationale": "The sim collapses `bool Try(out v)` into `v = f(v)` (hooks.py:196-201 and the other chain hooks), which produces the identical value because a C# listener that returns false leaves modifiedCost == originalCost. The one thing the boolean carries that the fold cannot is membership of the out-modifiers list, and that consequence is recorded separately at step 38 rather than duplicated here."
    },
    {
      "what": "35. Predicate family, AND with first-False short-circuit, several with an out preventer/modifier naming the vetoing listener. All 14 this record owns, complete (census: every `public static bool Should*` in Hook.cs classified off its brace-matched body): ShouldAddToDeck (2084), ShouldAfflict (2101), ShouldAllowAncient (2116), ShouldAllowHitting (2131), ShouldAllowMerchantCardRemoval (2146), ShouldAllowTargeting (2176), ShouldDisableRemainingRestSiteOptions (2254), ShouldDraw (2269), ShouldGenerateTreasure (2316), ShouldGainStars (2331), ShouldPlay (2361), ShouldProceedToNextMapPoint (2393), ShouldProcurePotion (2408), ShouldPowerBeRemovedOnDeath (2495). FIX-PASS CORRECTION: ShouldAllowSelectingMoreCardRewards (2161), ShouldPayExcessEnergyCostWithStars (2346) and ShouldRefillMerchantEntry (2423) were listed here and are wrong -- all three are `if (item.ShouldX(...)) return true;`, i.e. OR with first-True short-circuit, and have moved to step 36.",
      "verdict": "faithful",
      "maps_to": "hooks.py's predicates are the same AND-with-first-False short-circuit: should_die (588-594, with a preventer list out-param), should_remove_from_combat_after_death (607-611), should_clear_block (615-619), should_reset_energy (625-629), should_draw (638-642), should_flush_hand (648-652), should_allow_hitting (656-660), should_play_card (690-694), should_ethereal_trigger (698-702). The short-circuit makes the vetoer order-dependent, which is exactly why step 2's ordering is gap G2 (and turn_structure's G2). The missing preventer out-param matters at only one dispatcher this record owns, Hook.AfterPreventingDraw (1056-1067), whose sole implementer in the whole game is Fiddle.cs:39-43 and whose body is `Flash();` -- presentation only, so nothing observable is dropped."
    },
    {
      "what": "36. Predicate family, OR with first-True short-circuit -- 5 dispatchers, complete: ShouldAllowSelectingMoreCardRewards (2161-2171), ShouldPayExcessEnergyCostWithStars (2346-2356) and ShouldRefillMerchantEntry (2423-2433), which this record owns, plus ShouldStopCombatFromEnding (2442-2452) and ShouldTakeExtraTurn (2457-2467), which are turn_structure's",
      "verdict": "faithful",
      "maps_to": "The shape is faithful where the sim has it: hooks.py:662-671 should_take_extra_turn returns True on the first listener that does, matching Hook.cs:2459-2466 (its docstring says so), and it is exercised by the ported Pael's Eye (relics/paels_eye.py:27-34). The other four dispatchers have no sim dispatcher at all, and this entry makes NO faithfulness claim about them: ShouldStopCombatFromEnding's absence is turn_structure's gap G10 (its step 69) and the three run-side ones are unported run-level hooks, cross-referenced to gap N5 (no run-level HookSystem) rather than re-verdicted here. The shape is recorded here because the seed fact's 'predicate short-circuits on any False' does not describe it."
    },
    {
      "what": "37. Predicate family, OR with NO short-circuit -- `flag = flag || item.ShouldX(...)` calls every listener even after one returns true: ShouldForcePotionReward (2472-2480) and ShouldAllowFreeTravel (2485-2493). Complete: those are the only two in Hook.cs",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): (Task 11.) **THIS ENTRY'S PREMISE WAS FALSE; the sim was already faithful and NO code changed.** The entry claimed C#'s `flag = flag || item.ShouldX(...)` calls every listener without short-circuiting, and that the sim's short-circuiting `any(...)` therefore diverged. In fact C#'s `||` IS short-circuiting: the enclosing foreach keeps iterating (no break), but once `flag` is true the right operand -- the listener call -- is not evaluated, which is exactly what `any(...)` does. Verified three independent ways during review: (1) C# language semantics (ECMA-334 12.13); (2) a compiled and executed reproduction of the exact pattern, in which only the first listener was called; (3) same-file corroboration at Hook.cs:1451-1452, where the developers explicitly hoisted a listener call onto its own line (`bool flag2 = item.TryModifyCardRewardOptions(...); flag = flag || flag2;`) precisely to AVOID this skip -- proving the decompiled `||` here is genuine short-circuit IL, not a mis-rendered `|`. A first pass 'fixed' the sim to a full-visit loop and was REVERTED byte-identically once this was found; the correct behaviour is the original. The two hooks are the only members of this family (ShouldForcePotionReward, ShouldAllowFreeTravel; Hook.cs:2472-2493) and both ported implementers are side-effect free either way. Pins: test/test_tiny_dispatchers.py::TestPredicateShortCircuitsFaithfully asserts the second listener is NEVER called. Lesson for the queue: a record can be wrong about the C# itself, not merely stale.  The text it replaced read: The sim aggregates both with a short-circuiting `any(...)` generator (rewards.py:449: `any(relic.should_force_potion_reward(run, room_type) for relic in run.relics)`), so C# calls every listener where the sim stops at the first True and a later listener with a side effect would be skipped. DORMANT, evidence executed (`grep -rn 'ShouldForcePotionReward\\|ShouldAllowFreeTravel' --include=*.cs src/`): each hook has exactly ONE implementer in the decompiled game outside the run-modifier set -- src/Core/Models/Relics/WhiteBeastStatue.cs for ShouldForcePotionReward and src/Core/Models/Relics/WingedBoots.cs for ShouldAllowFreeTravel (src/Core/Models/Modifiers/Flight.cs is the second ShouldAllowFreeTravel implementer and run modifiers are note N4's waiver; the first pass mis-cited this file as src/Core/Modifiers/Flight.cs, a path that does not exist) -- so no second listener exists to be skipped, and both implementers' predicates are side-effect free. Concrete trigger: a second ported implementer of either hook, or one whose predicate has a side effect."
    },
    {
      "what": "38. Modifier-notification lists: chain/try-chain dispatchers with an `out modifiers` parameter (or, for two of them, an IEnumerable<AbstractModel> return) add a listener only when its call actually CHANGED the value. Complete list of the ones this record owns: ModifyEnergyGain (1610-1618) and ModifyGoldGained (1630-1638) compare with an INTEGER cast so a sub-1 decimal change does not count; ModifyCardPlayCount (1376-1384), ModifyCardPlayResultPileTypeAndPosition (1396-1405), ModifyCardBeingAddedToDeck (1350-1364) and ModifyOrbPassiveTriggerCount (1855-1869) track it exactly; TryModifyCardRewardOptions (1445-1467), ModifyRestSiteOptions (1949-1960), ModifyRestSiteHealRewards (1965-1976) and ModifyRewards (1981-2002) collect the listeners that returned true from a bool Try* and RETURN that list instead of using an out-param. The caller then fires the matching AfterModifying* event over exactly that subset. ModifyOrbPassiveTriggerCount, ModifyRestSiteOptions and ModifyRestSiteHealRewards were missing from the first pass's list.",
      "verdict": "gap",
      "issue": "hooks.py implements this mechanism for exactly one hook, modify_hp_lost (126-147, with after_modify_hp_lost at 149-154) -- damage_pipeline's site. The dispatchers this record owns have no modifiers list and no companion event: modify_energy_gain (210-215), modify_card_play_count (224-234), modify_card_play_result_pile (236-246), and the deck/reward ones have no sim counterpart at all. DORMANT for the card-play-count case by construction: the three ported AfterModifyingCardPlayCount listeners each consume their stack INSIDE the sim's modifier instead (powers.py:966-978 One-Two Punch, powers.py:3919-3928 Duplication, relics/throwing_axe.py:30-36 Throwing Axe, each with a source comment saying so), a deliberate compensation with the same observable. Concrete trigger: porting NoEnergyGainPower (the sole AfterModifyingEnergyGain implementer) or BowlerHat/Ectoplasm's AfterModifyingGoldGained legs, none of which is a sim listener on that hook today, and any future ported listener that must know it was outvoted."
    },
    {
      "what": "39. Dispatcher-level guards outside the loop: ModifyEnergyCostInCombat returns originalCost untouched when it is negative (Hook.cs:1576-1579), ShouldPowerBeRemovedOnDeath returns true when power.Owner.CombatState == null (2497-2500), and the LocalContext.NetId early return of step 24",
      "verdict": "faithful",
      "maps_to": "hooks.py:196-201 modify_card_energy_cost has no negative-cost arm and ends at max(0, cost), but its input cannot be negative and the C# guard's PURPOSE (X-cost cards, whose EnergyCost.Canonical is negative in the game) is served by a different representation entirely. Two independent reasons, both checked: (a) the sim does not encode X as a negative cost -- it carries a separate boolean `Card.energy_cost_x` (cards/base.py:87; Whirlwind is `energy_cost = 0, energy_cost_x = True`, cards/whirlwind.py:29,33, and powers.py:3868 comments the mapping 'EnergyCost.Canonical < 0') and EVERY consumer short-circuits on it BEFORE the chain is reached: combat.py:400-408 (play_card), combat.py:547 (auto_play_card), combat.py:697-701 (valid_actions), previews.py:179-181, full_env.py:354-355; (b) even for a non-X card the argument is always Card.energy_cost (cards/base.py:222-232), a property that already returns max(0, base + delta), and that property is the only producer of the argument at all seven call sites (combat.py:408, combat.py:701, env.py:88, env.py:94, env.py:140, full_env.py:355, previews.py:181). So the negative branch is unreachable in the sim by construction, not by luck. ShouldPowerBeRemovedOnDeath has no sim counterpart hook at all beyond should_remove_from_combat_after_death (a different predicate), and a sim Power always has an owner in a combat."
    },
    {
      "what": "40. Event dispatchers return no value and ignore listener output entirely: no cancellation, no ordering feedback (the ~60 `await model.X(); model.InvokeExecutionFinished();` bodies)",
      "verdict": "faithful",
      "maps_to": "hooks.py's event hooks are the same fire-and-forget walk (on_combat_start 267-269 through on_death 576-578); the sim likewise ignores return values."
    },
    {
      "what": "41. Sim registry: HookSystem.__init__ holds one flat self._listeners list plus a self.combat back-reference (hooks.py:37-41)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: the flat list is still the STORE, but it is no longer the dispatch ORDER. HookSystem gained _ordered(), which sorts the registration list into the game's derived per-creature walk -- allies then enemies, and per creature Powers -> Relics -> Potions -> Cards -- before every dispatch, with the result cached against (listener-list epoch, enemy order) (sts2_rl/hooks.py). That is the same sequence CombatState.IterateHookListeners produces at CombatState.cs:413-467. WAS: dispatch walked the raw registration list, whose category order was the mirror image of the game's."
    },
    {
      "what": "42. Sim registry: register() appends, unregister() is list.remove (identity, since no sim model defines __eq__) and raises ValueError when absent, which every caller wraps -- cmds.py:341-345, cmds.py:445-448, player.py:130-134, powers.py:89-92 (hooks.py:43-47)",
      "verdict": "faithful",
      "maps_to": "C# has no register/unregister at all -- membership is structural (a power is a listener because it is in creature.Powers). The sim's explicit registry is the mechanism that produces gap G2's ordering; the register/unregister PRIMITIVES themselves are consistent with the game's reference semantics: identity-based, idempotent-safe, and every removal site mirrors a C# removal site (PowerCmd.Remove / power expiry -> cmds.py:341 and powers.py:89, a potion leaving the belt -> player.py:130, a card removed from combat -> cmds.py:445)."
    },
    {
      "what": "43. Sim registration order in CombatState.__init__: history (combat.py:112) -> every card in player.all_cards with its enchantment right after it (124-133) -> relics via Relic.attach (158-159, relics/base.py:153-155) -> belt potions (164-166); powers join later as applied (cmds.py:326), mid-combat cards at cmds.py:460, mid-combat potions at player.py:120, monsters never",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: registration order no longer decides dispatch order. The registration sequence described here is unchanged, but _ordered() re-sorts it into the game's walk on every dispatch (sts2_rl/hooks.py), keyed on the owning creature and the listener's hook_category, so history stays ahead of the walk and powers precede relics precede potions precede cards per creature, as CombatState.cs:413-467 does. The 'monsters never' clause of this step is a separate finding and stays open as gap G5 (step 3): sts2_rl/monsters/base.py still never calls hooks.register, and two ported monsters (Queen, Slumbering Beetle) hand-roll a stand-in listener that declares CAT_POWER + 1 precisely to occupy the slot CombatState.cs:420 gives a MonsterModel. WAS: dispatch order WAS registration order."
    },
    {
      "what": "44. player.all_cards is hand + draw_pile + discard_pile + exhaust_pile (player.py:100-103), evaluated ONCE at combat start, so the card listener order is frozen for the combat",
      "verdict": "faithful",
      "issue": "Closed 2026-08-01 (tier-2 round 13): FIXED. The combat listener list is now DERIVED PER DISPATCH from live pile membership (CombatState.cs:449-467 walking AllPiles per PlayerCombatState.cs:70-80) instead of frozen at combat start, so a card that changes pile changes its dispatch position. PILE ORIENTATION IS INCLUDED: CardPile is top-first (CardPile.cs:160-167 Insert(0, ...); a draw takes Cards.FirstOrDefault(), CardPileCmd.cs:843) where the sim stores the draw pile's top at the END (player.py:581; CardPileCmd.add_to_draw converts a game index p to sim index count - p), so HookSystem._derive walks the DRAW PILE REVERSED. The pile's storage order is unchanged -- only the listener walk is flipped. Hand, discard and exhaust append at the end in both engines and need no flip. Measured cost of the reversal in isolation: nil (-0.4%). The inherited census figure of SIX hook-implementing card classes is wrong -- it is NINE (Bolas, Clash, Drum of Battle, Enthralled, Howl from Beyond, Normality, Regret, Stomp, Thrumming Hatchet; the old count missed HowlFromBeyondCard.after_auto_post_play_phase_entered among others). WHAT REASONING THIS REPLACES: that a frozen registration order was equivalent because no card hook could observe cross-card order -- the census behind that claim was itself undercounted.  The text it replaced read: gap G1 (card listener order is per-pile and re-derived in C#, frozen at combat start in the sim): CombatState.cs:449-467 walks AllPiles = Hand, Draw, Discard, Exhaust, Play (PlayerCombatState.cs:70-80) on EVERY dispatch, so a card that moves pile moves position in the listener list. combat.py:124 registers player.all_cards once, in the fixed order hand+draw+discard+exhaust (player.py:100-103), and never reorders. DORMANT, evidence executed (`py audit/tools/dormancy_probes.py card-hooks`: an MRO-aware scan of all 203 classes in _CARD_CLASSES against HookSystem's 66 hook names, ignoring anything defined on the Card base itself): card classes implement only six hooks, none able to observe cross-card order -- on_card_entered_combat (1: Stomp), on_card_exhausted (1: Drum of Battle), on_energy_spent (1: Stomp), on_player_turn_start (2: Bolas, Thrumming Hatchet, both self-only), on_player_turn_end (2: Howl from Beyond, Regret -- neither reads state the other writes), should_play_card (3: Clash, Enthralled, Normality -- an AND predicate whose sim dispatcher hooks.py:690-694 has no preventer out-param, so the vetoer's identity is unobservable). Concrete trigger: porting a card whose hook reads state another card's hook writes, or giving should_play_card a preventer out-param."
    },
    {
      "what": "45. Every one of the 66 sim dispatchers is `for l in list(self._listeners): if hasattr(l, '<hook>'): l.<hook>(...)` -- the list() copy makes the walk a snapshot, so a listener registered during a dispatch is not called and one unregistered during it still is",
      "verdict": "faithful",
      "issue": "Closed 2026-08-01 (tier-2 round 13): FIXED -- and the entry's premise was TRIPLY STALE by the time it was read. It said 'every one of the 66 sim dispatchers is `for l in list(self._listeners): if hasattr(...)`, the list() copy makes the walk a snapshot'. Wrong three separate ways: (a) there are 94 dispatchers, not 66; (b) the registration re-check had existed since 2026-07-28; (c) as of round 13 there is no _listeners snapshot in the dispatch path at all -- _each walks a list DERIVED from combat state and re-checks both registration and model state per item. WHAT REASONING THIS REPLACES: all three clauses of that premise.  The text it replaced read: gap G7 (no per-item liveness re-check, no IsActiveForHooks): C# yields `if (Contains(item))` LAZILY, per item (CombatState.cs:482-488), and Contains (549-599) drops any relic/potion/card/affliction/enchantment/orb whose HasBeenRemovedFromState is set or whose owner is not IsActiveForHooks (Player.cs:112,272,438,859-870); every sim dispatcher walks a `list(self._listeners)` snapshot with no re-check. DORMANT, evidence executed and REPRODUCIBLE FROM THE COMMITTED TREE: `py -m pytest test/ -q -p audit.tools.stale_listener_plugin`. The plugin (audit/tools/stale_listener_plugin.py) shadows every listener's bound hook method for the duration of one dispatch with a probe that re-checks `listener in hooks._listeners` AT THE MOMENT THE DISPATCHER CALLS IT -- exactly C#'s lazy Contains -- so a listener removed by an earlier listener in the same walk is caught. Over the whole suite (2476 passed / 30 xfailed) and 191,270 instrumented listener calls the only hit is on_enemy_side_end -> IntangiblePower x10 (Nemesis removing Intangible mid-dispatch) -- and C# makes that same call, because the PowerModel arm of Contains (CombatState.cs:599) checks only Owner.CombatState != null and never that the power is still on its owner. Concrete trigger: a listener that melts a relic, discards a potion or removes a card from combat while a later listener of the same category is still pending in that dispatch."
    },
    {
      "what": "46. The sim has no phase concept, no preventer for most predicates, no Contains re-check, no IsOverOrEnding gate, no per-listener choice context, and no run-level HookSystem (hooks.py in full)",
      "verdict": "gap",
      "live": false,
      "issue": "SETTLED 2026-07-29 (round 11) -- re-verified all six clauses against today's tree rather than trusting the 2026-07-27 narrowing, per instruction. Three are now fully closed, one is a genuine out-of-scope waiver, and two are real but narrower than stated: (1) phase concept -- CLOSED, unchanged: `_PHASES`/`_each` exist (hooks.py:36-38, 282-330). (2) preventer out-params -- CLOSED, unchanged: `should_die`/`should_clear_block` take `preventer` (hooks.py:1196, 1225). (3) IsOverOrEnding dispatch gate (guard G8) -- ALSO NOW CLOSED, correcting the narrowing's stale 'still pinned by two strict xfails': that fix landed 2026-07-28 (round 3, this record's own steps 19-21) via `_COMBAT_GATED_HOOKS` + `HookSystem.combat_is_over`, and EXECUTED today: `py -m pytest test/test_combat_over_hook_gate.py -q` -> `9 passed`; `grep -n xfail test/test_hook_order.py` finds zero `@pytest.mark.xfail` decorators (`py -m pytest test/test_hook_order.py -q` -> `53 passed`), confirming the 'two strict xfails' this clause named are gone. (4) Contains re-check (guard G7) -- NARROWED FURTHER, not closed: the REGISTRATION-liveness half is now real (hooks.py:308,319-320,326 -- `_each` skips any id not in `self._live`, closed by this record's own step 11 on 2026-07-28), so 'no per-item liveness re-check at all' is no longer true. What is still genuinely absent is C#'s OWNER-liveness leg of Contains -- `Owner.IsActiveForHooks`, which is `Creature.IsAlive` (Player.cs:112,272,438) -- for which grepping the whole sim (`grep -rin \"is_active_for_hooks\\|IsActiveForHooks\" sts2_rl/`) finds zero implementations, only one comment referencing the C# name. DORMANT, evidence executed: the only way this could diverge in a single-player sim is a player-owned listener that kills the OWNER synchronously inside a hook-dispatch pass that still has other player-owned listeners of the same category pending -- `grep -rn \"DamageCmd.deal(\" sts2_rl/relics/*.py sts2_rl/powers.py sts2_rl/potions.py` finds 30 call sites and none targets `self.owner`/the player as the damage target from inside a combat hook; the sim's two self-damaging relics, Fragrant Mushroom and Precarious Shears (`grep -rn \"lose_hp(self\" sts2_rl/relics/*.py`), both call `run.lose_hp` at RUN level (card-removal / gold events), outside any combat hook-dispatch pass, so there is no pass for a later same-category listener to be wrongly included in. (5) per-listener choice context -- unchanged, correctly a waiver under N1 (multiplayer choice-routing plumbing; no single-player content depends on it). (6) run-level HookSystem (guard N5) -- NARROWED FURTHER, not closed: a general run-level HookSystem still does not exist (`sts2_rl/hooks.py`'s `HookSystem` class is combat-only), and the OUT-OF-COMBAT dispatch for the ~40 of the 44 genuinely run-scoped hooks (step 15's count) is still duck-typed directly over `run.relics` only (relics/base.py:233-238 onward) with no card-listener path -- confirmed by re-reading `relics/base.py:201-238`, which shows the run-hook methods living only on `Relic`. But the record's own premise that 'a deck card is never a [run-level] listener' is now FALSE for a narrow slice: `RunState._map_listeners()` (run.py:1095-1097) is a dedicated deck-cards-plus-relics listener list used for exactly three run-scoped hook families -- `modify_generated_map`(`_late`), `modify_next_event`, `modify_unknown_map_point_room_types` (run.py:1129-1131, 1292-1293, 1347-1349) -- and `LanternKeyCard` now implements two of them (`cards/event_cards.py:400,410`, closed 2026-07-29 round 8, see event/war_historian_repy's DEFERRED-PORT leg-1 guard in that record). So N5 is a real, narrower gap: general run-level card-listening machinery is still absent for ~40 of 44 hooks, and a card CAN be a run listener today only for the 3-4 map-generation-family ones. DORMANT for the remaining slice, evidence executed: the same MRO-aware `card-hooks` census this record already cites (`py audit/tools/dormancy_probes.py card-hooks`) finds sim card classes implementing only combat hooks plus the two `LanternKeyCard` map-family overrides just named -- zero cards implement any of the OTHER ~40 run-scoped hook names, so there is no card today that the missing general machinery would misroute. Concrete trigger for (4): porting a player-owned relic/power/potion that damages its own owner from inside a combat hook. Concrete trigger for (6): porting any CardModel that overrides a run-scoped hook outside the map-generation family (e.g. AfterRoomEntered, AfterRewardTaken, ShouldAddToDeck). WAS (2026-07-27 narrowing, partly stale): 'WHAT REMAINS: no Contains re-check (guard G7), no IsOverOrEnding dispatch gate (guard G8, still pinned by two strict xfails), no per-listener choice context (waived under N1), and no run-level HookSystem (guard N5).'"
    }
  ],
  "guards": [
    {
      "what": "N1 -- multiplayer plumbing: PlayerChoiceContext push/pop (step 23), HookPlayerChoiceContext + AssignTaskAndWaitForPauseOrCompletion and the LocalContext.NetId early return (step 24), the grouped Task.WhenAll (step 25), and MultiplayerScalingModel (step 10). The OrbQueue listener block (step 8) used to be filed here and is NOT multiplayer -- it moved to note N7.",
      "verdict": "waiver",
      "rationale": "Genuinely out of scope: one local player, no async pausing, no net identity. Nothing a single-player run or replay can observe depends on any of it. The step-level waivers that cite N1 are steps 23, 24, 25 and the MultiplayerScalingModel clause of step 10."
    },
    {
      "what": "N2 -- presentation and modding: InvokeExecutionFinished / the ExecutionFinished event (step 22) and the ModHelper.IterateAll*Subscribers tails (steps 13, 17).",
      "verdict": "waiver",
      "rationale": "AbstractModel.cs:64-68 documents ExecutionFinished as 'a little unreliable, so you should only use it for UI things that can't be done any other way' -- presentation. ModHelper is the mod-loading extension point and the sim has no mod loader. Neither can change a single-player outcome."
    },
    {
      "what": "N3 -- CombatHistory is a sim-only listener, registered first (combat.py:111-112) so it observes every event before any relic or power. The game has no history listener: CombatManager.History is written by explicit calls (CardModel.cs:1930, 1956), not through the hook system.",
      "verdict": "deliberate-divergence",
      "rationale": "The observable is identical: CombatHistory only records, never mutates combat state, and registering it first is what makes the recording complete by the time a later listener reads it (relics/paels_eye.py:27-34 scans it; a grep for `combat.history` over sts2_rl finds 9 reader modules in all -- paels_eye, powers.py and seven card modules. FIX-PASS CORRECTION: the first pass named relics/whispering_earring.py as the second reader and it reads no history at all). Routing the recording through the listener list instead of through explicit call sites is a sim re-architecture with no behavioural consequence -- verified by reading every CombatHistory hook method, all of which only append entries."
    },
    {
      "what": "N4 -- Modifiers and BadgeModels as listeners (step 10; RunState.cs:573-574, CombatState.cs:470-477).",
      "verdict": "waiver",
      "rationale": "Modifiers are custom/daily-run mutators loaded from save.Modifiers (RunState.cs:296, 344) and a standard run has none. BadgeModels ARE cloned into every run (RunState.cs:332), but BadgeModel.cs:3-9 states they exist to 'keep track of stats for badges' and an executed grep over src/Core/Models/Badges/ finds no badge overriding any Should*/Modify*/TryModify* hook -- they subscribe to events for meta-progression accounting only. Out of scope for the same reason ascension values are, and unable to change an in-run outcome even if they were in scope."
    },
    {
      "what": "N5 -- the run/combat delegation split: in combat a run-level dispatch visits deck cards first and then the whole combat list (steps 14-18), and the sim has two disjoint dispatch systems instead (HookSystem inside a combat, duck-typing over run.relics outside one).",
      "verdict": "gap",
      "issue": "gap N5 (no run-level listener list): RunState.cs:545-596 makes every deck card and its enchantment a run listener at all times, in combat and out, and delegates to the combat list when there is a child combat. The sim has no run-level HookSystem: run-scoped hooks are duck-typed directly over run.relics (relics/base.py:205-235) and a deck card is never a listener. DORMANT, evidence executed: the same `dormancy_probes.py card-hooks` scan (203 card classes x 66 hook names) finds no sim card class implementing a run-scoped hook at all -- the six it implements are all combat hooks. Concrete trigger: porting any CardModel that overrides AfterRoomEntered/AfterRewardTaken/ShouldAddToDeck or another run-level hook."
    },
    {
      "what": "N6 -- AbstractModel.ShouldReceiveCombatHooks (AbstractModel.cs:53-62) is abstract and 19 model classes override it, but a whole-source grep for reads finds ZERO consumers; the real filtering is structural (which lists IterateHookListeners walks) plus Contains.",
      "verdict": "faithful",
      "rationale": "Documented-but-unread property. The sim has no counterpart and needs none: it performs the same filtering structurally, by choosing what to register. Recorded so a future reader does not mistake it for a missing gate. Executed: `grep -rn ShouldReceiveCombatHooks --include=*.cs .` over the whole decompiled tree returns only the abstract declaration and the 19 overrides, no reads."
    },
    {
      "what": "N7 -- character scope: the OrbQueue listener block (step 8; CombatState.cs:448) and the orb-shaped dispatchers this record owns that go with it (ModifyOrbValue 1874, ModifyOrbPassiveTriggerCount 1855, AfterOrbChanneled 844, AfterOrbEvoked 858, AfterModifyingOrbPassiveTriggerCount 784).",
      "verdict": "waiver",
      "rationale": "Orbs are the Defect's mechanic. The sim implements one character, the Ironclad, so there is no orb queue to place in the listener order and no orb listener whose position could be wrong. This is a CHARACTER-scope exclusion of the same kind as ascension values -- not multiplayer, which is why it no longer sits under N1. It becomes a real ordering question only if a second character is ported, at which point the slot is between PotionSlots and the cards (CombatState.cs:436-449) and gap G2 already covers getting it wrong."
    },
    {
      "what": "G4 -- the per-CardPlay bracket: CardModel.cs:1904-1965 loops `for (i = 0; i < playCount; i++)`, builds a FRESH CardPlay with PlayIndex = i each iteration (1919-1928) and fires Hook.BeforeCardPlayed (1929) AND Hook.AfterCardPlayed (1959) INSIDE the loop. Recorded as a guard in the fix pass: the doc and the pin table both carry G4, and the record carried no entry for it at all.",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: CombatState._resolve_card_play now fires before_card_played AND on_card_played INSIDE the `for play_index in range(play_count)` loop and stamps card.current_play_index each iteration (sts2_rl/combat.py), which is CardModel.cs:1904-1965's shape -- a fresh CardPlay with PlayIndex = i per iteration, Hook.BeforeCardPlayed at :1929 and Hook.AfterCardPlayed at :1959, the latter gated on the combat still being in progress. The recorded witness follows: a Throwing-Axe-doubled Strike advances Pen Nib's counter by 2. The enchantment's OnPlay is also a direct in-loop call after the card's own OnPlay and before Hook.AfterCardPlayed (CardModel.cs:1937-1945). The strict xfail that pinned this (test_before_card_played_fires_once_per_replay_iteration) is gone from test/test_hook_order.py. WAS: one bracket per logical play, wrapping the whole replay."
    },
    {
      "what": "G6 -- AfflictionModel is not a sim listener: CombatState.cs:458-461 adds cardModel.Affliction to the listener list immediately after its card (and its Enchantment after that) and AfflictionModel.cs:146 declares ShouldReceiveCombatHooks => true; combat.py:124-133 registers the card and its enchantment and never the affliction. Recorded as a guard in the fix pass: the doc carried G6, the record carried no entry.",
      "verdict": "faithful",
      "issue": "Closed 2026-08-01 (tier-2 round 13): FIXED (machinery); STILL DORMANT ON CONTENT. Affliction is a listener (hook_category = CAT_CARD, hook_is_card_rider, hook_contains = CombatState.cs:591) registered at all three acquisition sites (CardCmd.afflict, CardPileCmd._enter_combat for an already-afflicted clone, CombatState.__init__ for a deck card afflicted out of combat) and emitted immediately after its card and before its enchantment (CombatState.cs:456-465). CardCmd.clear_affliction now also nulls the affliction's `card` back-reference, mirroring AfflictionModel.ClearInternal (AfflictionModel.cs:249-254) -- without which HasCard, the first leg of the :591 arm, would have stayed true for a cleared affliction. DORMANCY RE-EXECUTED, not inherited: 0 of 7 sim afflictions and 1 of 10 C# afflictions (Hexed.cs) define a hook, and Hexed is unported. WHAT REASONING THIS REPLACES: 'the sim's frozen registration order (gap G1) cannot express the position' -- G1 is now derived, so the position is expressible and is in fact expressed.  The text it replaced read: gap G6 (missing listener category). DORMANT at both ends, evidence executed. Sim side (`py audit/tools/dormancy_probes.py affliction-hooks`): 0 of the 7 sim Affliction subclasses in sts2_rl/afflictions.py define any of HookSystem's 66 hook names, so there is nothing to register. Game side (`py audit/tools/dormancy_probes.py cs-affliction-hooks`): of the 10 files under src/Core/Models/Afflictions exactly ONE overrides an AbstractModel hook -- Hexed.cs, with AfterCardEnteredCombat -- and Hexed is unported (sts2_rl/afflictions.py:72-79 HexedAffliction is a data-only stub). Concrete trigger: porting Hexed's AfterCardEnteredCombat, or any future affliction with a hook override; at that point the affliction must register right after its own card, which the sim's frozen registration order (gap G1) cannot express either."
    },
    {
      "what": "G-R10 (found and FIXED 2026-08-01, round 13, R10): RunState.enter_point's TREASURE branch never dispatched Hook.ModifyRewards, where C#'s TreasureRoom.DoExtraRewardsIfNeeded (TreasureRoom.cs:75-97) does -- over an empty Room=Treasure RewardsSet (GenerateRewardsFor returns [] for TreasureRoom, RewardsSet.cs:206-246), called unconditionally even when Hook.ShouldGenerateTreasure suppressed the chest's own gold+relic grant. NOTE: RewardsCmd.OfferForRoomEnd is DEAD CODE (zero call sites tree-wide); the real path is DoExtraRewardsIfNeeded -> RewardsCmd.GenerateForRoomEnd (RewardsCmd.cs:55-60).",
      "verdict": "faithful",
      "issue": "DORMANT when found: all 8 ported modify_combat_rewards/_late implementers enumerated -- each is gated to Monster/Elite/Boss rooms or only touches pre-existing card_rewards groups (always empty here), so no ported relic could observe the missing dispatch. Fixed anyway (contained, same mechanism as R10's choke-point work): RunState.pending_treasure_extra_rewards + RunDriver._offer_treasure_extra_rewards; behavior-identity of the treasure grant proven by execution (identical gold/relic/shared-RNG state with the dispatch on vs off -- R10-review.md). Pinned: test/test_reward_dispatch_choke_point.py section D. Cross-ref: relic/wongos_mystery_ticket N7."
    },
    {
      "what": "F3-R13 (found 2026-08-01, round 13, R1): HookSystem.combat_is_over tests `phase == COMBAT_OVER` where C# gates on IsOverOrEnding",
      "verdict": "gap",
      "issue": "Hook.cs:53-63's IterateCombatHookListeners gates on `IsOverOrEnding && !IsStarting`; the sim's HookSystem.combat_is_over tests only `phase == Phase.COMBAT_OVER`, which mirrors `!IsInProgress`. This affects ALL 73 combat-gated dispatchers in the window between the killing blow and teardown. **The faithful predicate already exists** as CombatState.is_over_or_ending -- this is a one-site swap, not new machinery. CONFIRMED REACHABLE, not theoretical: R5's fix pass found DamageCmd's gate is the same `is_over` (phase) rather than `is_over_or_ending`, and on the pending-loss path ForgottenSoul dealt REAL DAMAGE (enemy 56->55) and burned a CombatTargets RNG draw where C# would have refused. **ADJACENT TO, NOT IDENTICAL WITH, creature_card_cmds/step19 + G4** (the heal guard): different file, different mechanism, and a DIFFERENT correct replacement -- that site needs bare `is_ending`, this one needs `is_over_or_ending`; swapping one in for the other OVER-GUARDS the heal. One fix cannot close both. A dedicated sweep is warranted (56 `is_over`/`COMBAT_OVER` reads); note a naive grep MISSES `getattr(combat, \"is_over\", False)`.",
      "live": true
    },
    {
      "what": "F-R13b (found 2026-08-01, round 13, R1; deferred deliberately): can_receive_powers and _combat_contains_creature read the eager removal PREDICTION",
      "verdict": "gap",
      "issue": "Round 13 gave Monster a `combat_removal_committed` EVENT flag set at C#'s actual CombatState.RemoveCreature statements (CreatureCmd.cs:529 under the :523/:527 gates, :601 via CreatureEscaped, MonsterModel.cs:450's deferred completion), because the sim's `is_removed_from_combat` is a PREDICTION that goes true at the HP write -- before ShouldDie (:505), ShouldCreatureBeRemovedFromCombatAfterDeath (:508) and AfterDeath (:519) have run. `can_receive_powers` and `_combat_contains_creature` STILL read the prediction and so diverge for the whole death sequence (demonstrated: all three read False mid-AfterDeath where C# would allow the power to land). **NOT blocked on footprint** -- both consumers live in cmds.py, which WAS in scope; the honest reason for deferring is live blast radius on PowerCmd.apply's gate. DORMANT TODAY, by enumeration: all four death-hook power-appliers target a live teammate, a fresh summon, or the player -- none a mid-death creature."
    },
    {
      "what": "F2-R13 (round 13, R1; reviewer-verified LIVE): run.py's _map_listeners returns [*relics, *deck] where RunState.cs:548-562 is DECK-FIRST",
      "verdict": "gap",
      "issue": "RunState.IterateHookListeners yields every deck card and its enchantment FIRST (RunState.cs:548-562), then relics and potions (:563-576). The sim's RunState._map_listeners (run.py:1113-1116) returns [*self.relics, *self.deck] -- the REVERSE. LIVE, not dormant, verified on BOTH sides by the review: map-hook implementers exist among CARDS (cards/event_cards.py, cards/spoils_map.py) AND among RELICS (relics/golden_compass.py), so they contend on the same hook today and the RELIC wins where C# gives the card precedence. Round 13 built the COMBAT-level derived walk but N5 (the run-level list) was explicitly out of scope. DO NOT fold this into N5's dormancy argument: N5 is about the ABSENCE of a run-level listener system; this is a concrete ordering divergence in the hand-rolled walk that exists.",
      "live": true
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-25"
}
