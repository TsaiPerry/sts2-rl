{
  "unit": "seam/power_cmd",
  "game_sources": [
    {
      "path": "src/Core/Commands/PowerCmd.cs",
      "sha256": "d344a2ecdd846e26a02fcd14d3ebf48f6c99056fa3b3e0ccbaa43fe9901dc993"
    },
    {
      "path": "src/Core/Hooks/Hook.cs",
      "sha256": "c75c8e9d5c5015b9e3b230af72e47c585e8d42bde412633e910cc68cc0be6588"
    },
    {
      "path": "src/Core/Models/PowerModel.cs",
      "sha256": "2cc44bddbdd6de6e1d3ded50110a0aace66bc37a942dbf1c5af2af08b9a80dbc"
    }
  ],
  "sim_sources": [
    {
      "path": "sts2_rl/cmds.py",
      "sha256": "59d6315eee45195c01a59d38ee15cdf3654970f5818bd970ab6101ced93dc18d"
    },
    {
      "path": "sts2_rl/hooks.py",
      "sha256": "3d39a103f9636007fe654337d08443cc48445a6b0a31c52c20f080487699bf0a"
    },
    {
      "path": "sts2_rl/powers.py",
      "sha256": "7b8fdd4f644f525d60c08fee405ddfe4dceaedfa05de64a80bb8e4412ea26ca1"
    }
  ],
  "steps": [
    {
      "what": "1. Apply<T>: guard CombatManager.Instance.IsEnding -> return null (PowerCmd.cs:69-72)",
      "verdict": "faithful",
      "issue": "CLOSED. `PowerCmd.apply` opens on `if is_ending(hooks): return` (sts2_rl/cmds.py), reproducing PowerCmd.cs:69-72. `IsEnding`, not `IsOverOrEnding`: CombatManager.cs:184-187 opens `if (!IsInProgress) return false`, which is what keeps the out-of-combat callers working. EXECUTED: test/test_combat_ending_command_guards.py::TestPowerCmd.  FIXED 2026-07-28 (round 4). The text it replaced read: PowerCmd.apply (cmds.py) has no combat-ending guard at all. See guard G6."
    },
    {
      "what": "2. Apply<T>: guard !target.CanReceivePowers -> return null (PowerCmd.cs:73-76; CanReceivePowers = CombatState!=null && Hook.ShouldAllowHitting, Creature.cs:308-322)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: PowerCmd.apply now opens with `if not hooks.should_allow_hitting(target): return` (sts2_rl/cmds.py), which is PowerCmd.cs:73-76's guard -- CanReceivePowers is Hook.ShouldAllowHitting (Creature.cs:308-322), the same predicate the damage pipeline already used. A debuff no longer lands where damage would not: Vulnerable does not stick to a reviving Test Subject and the AoE power potions skip an Eye With Teeth mid-Illusion revival, matching C#'s HittableEnemies. NOTE: the mid-pipeline re-check at PowerCmd.cs:133 (step 16) and the IsEnding half (steps 1, 6, 24) are still absent -- guard G6."
    },
    {
      "what": "3. FindExistingInstanceForStacking dispatches on power.InstanceType: Instanced->always null, InstancedPerApplier->match by Applier, None->match by power id only (PowerCmd.cs:77-78,165-174; PowerModel.cs:144)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-30 (tier-2 campaign): PowerCmd.apply's stacking branch (sts2_rl/cmds.py) now mirrors FindExistingInstanceForStacking's three-way dispatch (PowerCmd.cs:165-174; PowerInstanceType, PowerModel.cs:144): PowerInstanceType.NONE (the sim's unchanged default) still finds the existing instance by id; INSTANCED forces \"not found\", so a second application starts a genuinely new, independently hook-registered instance instead of merging into the first's on_stack; INSTANCED_PER_APPLIER forces \"not found\" only when the found instance's applier differs from this application's applier (same applier still stacks). `target.powers` is still a single dict slot per id -- this is a dispatch fix, not a container rewrite -- so an INSTANCED/INSTANCED_PER_APPLIER \"not found\" overwrites that slot; the instance it replaces is not touched or unregistered and keeps ticking toward its own expiry, because the hook system dispatches from its own listener list (`hooks.register`/`_listeners`, sts2_rl/hooks.py), not from `target.powers` -- only a direct `target.powers` lookup now sees just the newest instance. `Power._expire` (sts2_rl/powers.py) was made identity-safe (`if owner.powers.get(id) is self: del ...`) so an orphaned instance's own expiry cannot delete a different, still-live instance's dict entry. `Power.instance_type` (sts2_rl/powers.py, default `PowerInstanceType.NONE`) mirrors `PowerModel.InstanceType` and is set to INSTANCED on AutomationPower, HeistPower, PanachePower, RollingBoulderPower, SandpitPower, ThieveryPower, ToricToughnessPower, WitheringPresencePower, and to INSTANCED_PER_APPLIER on StranglePower -- 9 of the 11 ported declarers (see their own entries). TheBombPower and SwipePower are DELIBERATELY NOT migrated (stay at the PowerInstanceType.NONE default): both already reproduce the observable behaviour that matters through their own pre-existing hand-rolled workarounds (TheBombPower's `bombs` list; SwipePower's `stolen_cards` list, both sts2_rl/powers.py), and routing either through the generic dispatch would silence its `on_stack` override and regress it -- confirmed for SwipePower specifically against `RunState.finish_combat`'s escaped-hopper deck-removal reconciliation walk (sts2_rl/run.py:1517-1520), which depends on every steal landing in ONE instance's `stolen_cards`. TDD: test/test_powers.py::TestPowerInstanceType (12 tests) exercises the three dispatch branches directly at the PowerCmd.apply layer plus each fixed unit's own behaviour; full suite 3359 passed / 2 failed (pre-existing floor_49 fixture gap, untouched) / 6 xfailed -- zero new failures. This closes the DISPATCH mechanism itself. It does NOT close `power/the_bomb/InstanceType` or `power/swipe/InstanceType`, which stay open with this same reasoning recorded on them directly.  The text it replaced read: LIVE 2026-07-29 (round 11) -- same mechanism as guard G5, re-verdicted there from \"no currently-demonstrated collision\" to LIVE with an executed trigger. This step names the C# dispatch site (PowerCmd.cs:77-78 calling FindExistingInstanceForStacking at :165-174); G5 carries the full divergence/observable/trigger/fix/radius, including the executed probe (`.superpowers/sdd/unlabelled/probes/seam-power-cmd-g5-automation2.py`) showing two AutomationPower applications 6 card-draws apart merge into one Python instance and fire a single GainEnergy(2) at draw #10 instead of C#'s two independent GainEnergy(1) events at draws #10 and #16. See guard G5 for the full record; do not re-derive a separate verdict for this step."
    },
    {
      "what": "4. Branch: no existing instance -> new-power Apply(...) pipeline (steps 6-23); existing instance -> ModifyAmount(...) pipeline (steps 24-37), nulling the result if it returns exactly 0 (PowerCmd.cs:79-87)",
      "verdict": "gap",
      "live": false,
      "issue": "CITATION CORRECTED 2026-07-31 (tier-2 campaign, Task 18), verdict UNCHANGED: this entry's text cites `step6` as 'settled this round, DORMANT'. step6 is now CLOSED (faithful) -- PowerCmd.apply gained the raw amount == 0 bail. The structural point this entry makes is otherwise unaffected: Task 18's phase separation happened WITHIN the one collapsed sim call site and did not un-collapse C#'s two independently-coded pipelines.  The text it replaced read: DORMANT 2026-07-29 (round 11), executed re-check of every guard pair this structural note points at. The sim's single code path (`sts2_rl/cmds.py PowerCmd.apply`, current lines 508-605) really does collapse C#'s two independently-coded pipelines (Apply: PowerCmd.cs:101-159; ModifyAmount: :215-271), and that collapse is a real architectural divergence from PowerCmd.cs:79-87's branch. But every guard-level difference the note names is ALREADY individually accounted for elsewhere in this same record, and none of them currently produces a broken interaction beyond what its own entry already tracks: steps 6 vs 25 (amount==0 / CanReceivePowers) -- the amount==0 half is this record's own `step6` (settled this round, DORMANT, see below); the CanReceivePowers half is guard N4 (already `faithful`, on the grounds that C#'s ModifyAmount itself never checks it either, an asymmetry already present in the C# source, not a sim-vs-C# divergence). Steps 15/23 vs 33 (BeforeApplied/AfterApplied absent from ModifyAmount) -- step 15 is already `deliberate-divergence`, verified by execution in guard N2 (TemporaryStrengthPower/TemporaryDexterityPower's applier-omission achieves the same 'double once' outcome as C#'s explicit double-dip guard). Steps 18 vs 29 (History bookkeeping unconditional-vs-gated) are both `waiver`ed as telemetry. Re-read PowerCmd.cs:101-159 and :215-271 side by side to confirm the three hook calls (BeforePowerAmountChanged, ModifyPowerAmountGiven, ModifyPowerAmountReceived) really do run in the same order in both C# pipelines, which is what lets the sim's one collapsed call (cmds.py:553) stand in for both -- confirmed, no reordering. No currently-reachable content exercises a guard difference this record does not already track under its own name. If a FUTURE addition needs one of Apply's guards to differ from ModifyAmount's in a way not already covered (e.g. a new listener sensitive to the CanReceivePowers-in-ModifyAmount asymmetry N4 already flags), this structural note is where that would surface."
    },
    {
      "what": "5. Apply<T> returns `power as T` (may be a fully-populated-but-unregistered PowerModel when Artifact zeroed the amount, per step 17) (PowerCmd.cs:88)",
      "verdict": "waiver",
      "rationale": "The sim's PowerCmd.apply returns None (no return value at all) and no sim caller inspects a return value from PowerCmd.apply, so this C#-side return-contract subtlety (a non-null-but-unregistered power object) has no sim-side analogue to diverge from."
    },
    {
      "what": "6. Apply(power,...): guard IsEnding || amount==0m || !target.CanReceivePowers -> return (PowerCmd.cs:103-106)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): PowerCmd.apply now bails on a raw amount == 0 before any hook runs (PowerCmd.cs:103-106), gated on `existing is None` so it does not wrongly refuse a legitimate zero-offset re-stack -- C#'s own ModifyAmount does not refuse one either.  The text it replaced read: NARROWED FURTHER 2026-07-29 (round 11), re-verified against today's `sts2_rl/cmds.py`. The prior text's claim that the `IsEnding` disjunct has no sim counterpart is now STALE: `PowerCmd.apply` opens with a single `if is_ending(hooks): return` (cmds.py, current line 529) that covers Apply<T>'s IsEnding (step 1, already `faithful`/CLOSED), Apply's IsEnding (this step's own :103 half) and ModifyAmount's IsEnding (step 24, already `faithful`/CLOSED) in one shared guard, since the sim has one code path for all three C# sites -- consistent with how steps 1 and 24 were already closed. WHAT ACTUALLY REMAINS is only the `amount == 0m` disjunct. RE-EXECUTED (`.superpowers/sdd/unlabelled/probes/seam-power-cmd-basic.py`): `PowerCmd.apply(hooks, player, StrengthPower, 0)` on a fresh combat still creates and registers a `Strength(0)` instance (`player.powers == ['strength']`, confirmed live in `hooks._listeners`), where C# returns at PowerCmd.cs:103 before ever constructing the power. DORMANT because no reachable trigger was found: searched every `PowerCmd.apply(...)` call site in `sts2_rl/` whose amount argument is a runtime-computed value rather than a card-magnitude literal (`SuckPower.after_attack`, powers.py:1962-1965; `SurprisePower.on_death`'s HeistPower hand-off, powers.py:2045-2046; the cards-played-this-turn Strength/Dexterity pair, powers.py:2474-2478) -- every one of them is explicitly guarded with `if <value> > 0` / `if <value> == 0: return` BEFORE the `PowerCmd.apply` call, so none of today's ported content reaches this code path with `amount == 0`. This is not an exhaustive scan of all ~150 call sites (the rest pass hardcoded nonzero literals by inspection, e.g. `1`, `self._power_amount` fixed at card-init time), but every dynamically-computed one found is already defensively zero-guarded at the call site, consistently across five different files/authors -- evidence of a real pattern, not a coincidence in one port. Concrete trigger that would make this live: a ported card/power that applies a power with a runtime-computed amount and OMITS the zero-guard the existing ports all use."
    },
    {
      "what": "7. Apply(power,...): guard combatState==null -> return (PowerCmd.cs:107-111)",
      "verdict": "waiver",
      "rationale": "The sim's CombatState/HookSystem construction guarantees a non-null combat context for any creature that can be passed to PowerCmd.apply; there is no code path constructing a Creature outside an active CombatState. Structurally unreachable in the sim's architecture."
    },
    {
      "what": "8. Apply(power,...): re-check FindExistingInstanceForStacking; if found, delegate to ModifyAmount and return (defensive re-check for direct callers of this overload) (PowerCmd.cs:112-117)",
      "verdict": "waiver",
      "rationale": "This guards against C#'s two public entry points (Apply<T> and the non-generic Apply(power,...)) being called independently by different game code; the sim exposes only one PowerCmd.apply entry point (keyed by power_cls, not a pre-constructed PowerModel instance), so the redundant re-check has no separate call site to protect against."
    },
    {
      "what": "9. power.AssertMutable(); power.Applier = applier (PowerCmd.cs:118-119)",
      "verdict": "faithful",
      "maps_to": "The sim's power_cls(owner=target, amount=amount, hooks=hooks, applier=applier) constructor sets self.applier = applier at construction (powers.py Power.__init__). Same effect (Applier recorded before any hook fires that might read it)."
    },
    {
      "what": "10. Event: Hook.BeforePowerAmountChanged(combatState, power, amount, target, applier, cardSource) fires with the RAW amount; UnsettlingLamp latches TriggeringCard here (PowerCmd.cs:120; Hook.cs:1006-1016; UnsettlingLamp.cs:71-104)",
      "verdict": "gap",
      "live": false,
      "issue": "DORMANT 2026-07-29 (round 11). The absence of a standalone BeforePowerAmountChanged-equivalent event is itself `deliberate-divergence` (already so-labelled in this entry's own text -- UnsettlingLamp's sim latching, `sts2_rl/relics/unsettling_lamp.py:36-43`, is folded into the same `modify_power_amount` call and keys off a card bracket instead of a `cardSource` param, verified still true against today's file). The one real remaining `gap` this step points at is the sign-awareness of the LATCH condition, which belongs to mechanism `power_cmd/G2` (out of this batch's scope; `power_cmd/G2` is the queue's own blocker entry, settled by a different pass, not this record's `steps` array). Re-checked G2's dormancy premise directly since this step depends on it: `grep -rli \"malaise\\|resonance\" sts2_rl/cards/` returns nothing -- Malaise and Resonance (the only two C# cards that apply a negative-amount Buff-typed power to an enemy, the shape that would expose the sign-awareness gap) are still unported, confirming G2's dormancy holds as of today's code, not merely as of its last-recorded date. No new trigger found."
    },
    {
      "what": "11. modifiedAmount = amount (PowerCmd.cs:121)",
      "verdict": "faithful",
      "maps_to": "cmds.py:297 `amount = hooks.modify_power_amount(power_cls, target, amount, applier)` starts from the same raw amount."
    },
    {
      "what": "12. Guard applier!=null && combatState.ContainsCreature(applier): modifiedAmount = Hook.ModifyPowerAmountGiven(...) additive-sum-then-multiplicative-product chain, tracks givenModifiers; UnsettlingLamp doubles here (PowerCmd.cs:122-126; Hook.cs:1884-1912; UnsettlingLamp.cs:106-129)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): C#'s three ordered phases are now three separate dispatches. The single flat registration-order chain is gone: modify_power_amount_given_additive / _given_multiplicative (the exact additive-sum-THEN-multiplicative-product shape of Hook.cs:1888-1912, which is not commutative with a naive fold) run under a real `applier is not None and combat_contains_creature(applier)` gate (PowerCmd.cs:122-123), then modify_power_amount_received runs unconditionally (Hook.cs:1917-1930 has no applier gate). ArtifactPower and RuinedHelmet are now REAL LISTENERS on the received chain instead of a hard-coded block outside the hook loop. CENSUS CORRECTION: this entry's dormancy reason was stale -- it argued Lamp and RuinedHelmet are disjoint because Lamp gates on the STATIC power_type, which Task 17 replaced with sign-aware type_for_amount. The conclusion held for a deeper reason the record never stated: Lamp is a GIVEN-side override (UnsettlingLamp.cs:106-129) and RuinedHelmet/Artifact are RECEIVED-side (RuinedHelmet.cs:32-53, ArtifactPower.cs:17-36) -- they were never on the same C# hook at all, and the sim's collapsed chain was the ONLY thing that ever put them in a registration-order race. That collapse was the bug this guard named, and it is now structurally impossible. Pinned in test/test_power_modifier_phases.py (26 tests).  The text it replaced read: DORMANT 2026-07-29 (round 11), mechanism `power_cmd/G3`. `cmds.py`'s single `hooks.modify_power_amount(power_cls, target, amount, applier)` call (current line 553) still runs unconditionally regardless of whether `applier` is `None` or a combat-absent creature -- no `applier != null && combatState.ContainsCreature(applier)` gate exists, confirmed against today's code. But the ONLY given-side general listener, `UnsettlingLamp.modify_power_amount` (`relics/unsettling_lamp.py:45-54`), already self-gates to `applier is self.player` (line 48) before doing anything -- and the player is always a valid, in-combat `ContainsCreature`-true creature whenever it can be an `applier` at all, so C#'s missing gate has no behaviour to differ on for this listener: Lamp's own narrower condition already subsumes it. No other `modify_power_amount` listener exists (re-confirmed via the same 2-listener census as guard G3, settled this round). UnsettlingLamp's own doubling is present and, per prior verification (guard N2, still valid against today's cmds.py ordering -- the fix landing `modify_power_amount` before the Artifact early-return), correctly ordered. Concrete trigger that would make this live: a future given-side listener whose gate is WEAKER than `applier is self.player` -- e.g. one that fires for any applier, including a dead/removed one still nominally on the `Enemies` list, where C#'s `ContainsCreature` would refuse it and the sim would not."
    },
    {
      "what": "13. modifiedAmount = Hook.ModifyPowerAmountReceived(...) unconditional predicate chain (TryModifyPowerAmountReceived) evaluated against the already-doubled amount; ArtifactPower and RuinedHelmet intercept here (PowerCmd.cs:127; Hook.cs:1914-1931; ArtifactPower.cs:17-36; RuinedHelmet.cs:32-53)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): (Task 17.) STALE duplicate text. This step's prose still described the pre-fix static Artifact typing, but the mechanism's own guard entry (power_cmd guard G1) has read `faithful` / 'FIXED 2026-07-28' since that round. Re-verified today against the C#: `Power.type_for_amount` (powers.py) matches PowerModel.cs:460-471's GetTypeForAmount (the AllowNegative-implies-Counter simplification is real; spot-checked StrengthPower.cs:11-13 and ArtifactPower.cs:13), and PowerCmd.apply's Artifact branch consults it, matching ArtifactPower.cs:24's `GetTypeForAmount(amount) != PowerType.Debuff`. The adjacent skip-next-tick site deliberately keeps the STATIC `power_type` check, correctly mirroring PowerCmd.cs:144's static `.Type` test rather than GetTypeForAmount. The named pin (test_hook_order.py::TestPowerCmdOrder::test_artifact_blocks_negative_signed_debuff) carries no xfail and passes. NO code change was needed or made.  The text it replaced read: The sim's Artifact interception (cmds.py:299-306) is a hard-coded special case entirely OUTSIDE the hook-listener system, keyed on the static power_cls.power_type == PowerType.DEBUFF rather than C#'s sign-aware GetTypeForAmount(amount) check that ArtifactPower.TryModifyPowerAmountReceived performs. This is the seam's central finding -- see guard G1 (dormant; needs Malaise/Resonance ported) and G3 (architecture)."
    },
    {
      "what": "14. Guard multiplayer scaling: combatState.Players.Count>1 && target is primary/secondary enemy && power.ShouldScaleInMultiplayer -> power.GetScaledAmountForMultiplayer(...) (PowerCmd.cs:128-131)",
      "verdict": "waiver",
      "rationale": "Multiplayer-only mechanic; the sim is single-player Ironclad-only, out of scope per the audit prompt's multiplayer waiver."
    },
    {
      "what": "15. Event: await power.BeforeApplied(target, modifiedAmount, applier, cardSource), unconditional even at modifiedAmount==0; TemporaryStrengthPower/TemporaryDexterityPower recursively apply their internal power here, carrying the same applier/cardSource (PowerCmd.cs:132; TemporaryStrengthPower.cs:146-156)",
      "verdict": "deliberate-divergence",
      "rationale": "The sim has no BeforeApplied-equivalent hook stage; TemporaryStrengthPower/TemporaryDexterityPower's sim counterparts (powers.py:840-870,920-940) instead apply their internal StrengthPower/DexterityPower directly from __init__/on_stack, and OMIT the applier parameter entirely on that internal call (rather than replicating C#'s explicit HasDoubledTemporaryPowerSource double-dip guard). Verified to produce the same net outcome for the currently-ported Mangle-family content -- see guard N2."
    },
    {
      "what": "16. Guard target.CanReceivePowers (re-checked) (PowerCmd.cs:133)",
      "verdict": "faithful",
      "issue": "CLOSED. The mid-pipeline re-check exists: `PowerCmd.apply` re-tests `hooks.should_allow_hitting(target)` after `modify_power_amount` and the Artifact branch, immediately before the power is constructed (PowerCmd.cs:133). It guards the NEW-power path alone, as C# does -- Apply<T> routes an existing instance to ModifyAmount, which never consults CanReceivePowers. EXECUTED: test_combat_ending_command_guards.py::test_a_mid_pipeline_hittability_change_refuses_the_power.  FIXED 2026-07-28 (round 4). The text it replaced read: NARROWED 2026-07-27: the parenthetical claim is no longer true -- the sim DOES have a CanReceivePowers concept in the power pipeline now, at the ENTRY (PowerCmd.apply's opening `if not hooks.should_allow_hitting(target): return`, sts2_rl/cmds.py; see step 2). WHAT REMAINS is this step's own subject: there is no MID-PIPELINE re-check. C# re-tests target.CanReceivePowers at PowerCmd.cs:133, after the given/received modifier chains have run and after any listener they invoked could have changed the target's hittability; the sim tests it once, before hooks.modify_power_amount. Guard G6."
    },
    {
      "what": "17. power.ApplyInternal(target, modifiedAmount, silent): complete no-op if modifiedAmount==0m (power never registered, Owner never set); otherwise sets Owner, SetAmount, registers via Owner.ApplyPowerInternal (PowerCmd.cs:135; PowerModel.cs:564-573)",
      "verdict": "faithful",
      "maps_to": "Faithful GIVEN A NONZERO AMOUNT: cmds.py's branch at line 308 (`if power_cls.id in target.powers: ... else: power = power_cls(...); target.powers[power_cls.id]=power; hooks.register(power)`) sets the owner and registers the power on the target exactly as ApplyInternal does. The sim does NOT reproduce ApplyInternal's 0-amount no-op -- a 0-amount application registers a live Power(0) (verified by execution; see step 6) -- but that divergence is OWNED BY STEP 6 (C# returns at PowerCmd.cs:103 before ever reaching ApplyInternal), not re-flagged here. Correcting an earlier claim: the Artifact block at cmds.py:299-306 does NOT make the zero case unreachable by construction -- it never fires for buffs at all, and for debuffs only when the target already holds Artifact."
    },
    {
      "what": "18. Guard modifiedAmount!=0m: History.PowerReceived(...) (PowerCmd.cs:136-139)",
      "verdict": "waiver",
      "rationale": "Run/combat telemetry and history bookkeeping, not gameplay logic; out of scope per the audit prompt's presentation/telemetry waiver (mirrors damage_pipeline's step 17.3 waiver for the analogous DamageDealt stat bump)."
    },
    {
      "what": "19. Guard power.IsVisible && CombatManager.Instance.IsInProgress: wait 0.1-0.25s (PowerCmd.cs:140-142)",
      "verdict": "waiver",
      "rationale": "Presentation-only VFX pacing delay; out of scope."
    },
    {
      "what": "20. Guard target.Side==Player && power.Type==Debuff: power.SkipNextDurationTick=true, unconditional on modifiedAmount (PowerCmd.cs:144-147)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: the skip_next_tick assignment moved INSIDE the new-power branch of PowerCmd.apply's if/else (sts2_rl/cmds.py), so it can no longer be re-armed on the `existing` instance when a debuff re-stacks. That matches C#, which sets SkipNextDurationTick only in Apply (PowerCmd.cs:144-147) and never in ModifyAmount (PowerCmd.cs:215-271). A second Vulnerable or Weak stack applied in the same turn no longer skips a tick it should take, so the debuff expires on time. Pinned by test_restacking_a_player_debuff_does_not_rearm_skip_next_tick, which now passes. WAS: the assignment sat at function scope after the branch, on the shared `power` variable."
    },
    {
      "what": "21. Guard givenModifiers!=null: await Hook.AfterModifyingPowerAmountGiven(combatState, givenModifiers, power) (PowerCmd.cs:148-150; Hook.cs:796-809)",
      "verdict": "gap",
      "live": false,
      "issue": "Re-scoped 2026-07-31 (tier-2 campaign, Task 18), verdict UNCHANGED (still a dormant gap). The dormancy reason has changed in kind: this was 'no sim counterpart at all', and the counterpart now EXISTS and is correctly wired -- hooks.after_modify_power_amount_given is dispatched at C#'s position (PowerCmd.cs:148-151, :238-241) with the real modifiers out-list and the real power. What keeps it dormant is only that its sole C# implementer, SneckoSkull, is Flash-only and unported. A future given-side listener now has something to hook into.  The text it replaced read: DORMANT 2026-07-29 (round 11); mechanism tagged `damage_pipeline/G2` by the queue, settled here on this step's own executed merits (any implied change to `damage_pipeline`'s own record is in HANDOFF, not written here). No sim counterpart to `Hook.AfterModifyingPowerAmountGiven` exists, confirmed. EXECUTED census of the C# side: `grep -rn \"override.*AfterModifyingPowerAmountGiven\" src/` returns exactly ONE implementer in the entire decompiled game -- `SneckoSkull.cs:32-36` -- and its body is `Flash(); return Task.CompletedTask;`, i.e. presentation-only VFX with zero gameplay effect (compare the wait-only steps 19/36 in this same record, both `waiver`ed). `SneckoSkull` is unported (`find sts2_rl/relics -iname \"*snecko*\"` returns only `snecko_eye.py`/`fake_snecko_eye.py`, not `snecko_skull`). Since the hook's sole C# implementer can never produce an observable gameplay difference even if ported as-is, this cannot become live from any content the C# source currently supports -- only a hypothetical NEW C# listener with a real effect would change that. See HANDOFF re: `damage_pipeline/G2`."
    },
    {
      "what": "22. await Hook.AfterModifyingPowerAmountReceived(combatState, receivedModifiers, power); ArtifactPower.AfterModifyingPowerAmountReceived calls PowerCmd.Decrement(this) here -- the actual Artifact-stack-consumption mechanism; RuinedHelmet sets UsedThisCombat=true here (PowerCmd.cs:152; Hook.cs:811-824; ArtifactPower.cs:38-41; RuinedHelmet.cs:55-60)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): Closed alongside G4, which names this exact mechanism: AfterModifyingPowerAmountReceived is dispatched at C#'s position with the real modifiers list and the real power, and its two real listeners (Artifact's Decrement, RuinedHelmet's mark-used) now ride it.  The text it replaced read: DORMANT 2026-07-29 (round 11); mechanism tagged `damage_pipeline/G2` by the queue, settled on this step's own executed merits (any implied change to `damage_pipeline`'s own record is in HANDOFF). RE-EXECUTED census: `grep -rn \"override.*AfterModifyingPowerAmountReceived\" src/` returns exactly TWO implementers game-wide -- `ArtifactPower.cs:38` and `RuinedHelmet.cs:55` -- confirming no drift. Re-read both sim inlines against today's code: `cmds.py`'s Artifact block (current lines 558-565: sign-aware `type_for_amount` check, then `artifact.amount -= 1`, `hooks.on_power_amount_changed(...)`, expire-if-<=0, early return) reproduces `ArtifactPower.TryModifyPowerAmountReceived` + `AfterModifyingPowerAmountReceived`'s combined effect; `RuinedHelmet.modify_power_amount` (`relics/ruined_helmet.py:27-43`) sets `self._used = True` inline at the point of doubling, reproducing `RuinedHelmet.AfterModifyingPowerAmountReceived`'s `UsedThisCombat = true`. Both inline reimplementations are individually faithful for CURRENT content (no third listener exists to be silently dropped by the missing generic dispatch), so no live wrong behaviour is observable today. The gap is architectural only -- no `modifiers`-out-list, no generic dispatch a future listener needing the 'only react on the hit(s) I actually changed' pattern could hook into. Concrete trigger: a THIRD C# listener of `AfterModifyingPowerAmountReceived` gets ported, or ArtifactPower/RuinedHelmet's own inline logic drifts out of sync with a future C# change to either method (nothing currently enforces that the two inlines track the hook body)."
    },
    {
      "what": "23. Guard modifiedAmount!=0m: await power.AfterApplied(applier, cardSource); await Hook.AfterPowerAmountChanged(combatState, choiceContext, power, modifiedAmount, applier, cardSource) (PowerCmd.cs:153-157; Hook.cs:1018-1026)",
      "verdict": "faithful",
      "maps_to": "Faithful GIVEN A NONZERO AMOUNT: the sim's on_power_applied hook call (cmds.py:327, in the new-power else-branch) is the AfterApplied/AfterPowerAmountChanged analogue for brand-new powers. C# fires TWO events here (power.AfterApplied then Hook.AfterPowerAmountChanged) where the sim fires only on_power_applied and reserves on_power_amount_changed for the stacking branch (cmds.py:312-314) -- verified harmless for currently-ported content because every sim listener that cares registers BOTH callbacks with a shared handler (ViciousPower, powers.py:1172-1188; the Strength/Dexterity-steal base, powers.py:3101-3109), so no listener can miss the new-power case. Correcting an earlier claim: this step is NOT reached 'only with a nonzero amount by construction' -- the sim happily runs the whole new-power branch at amount 0 (verified by execution), where C# returns at PowerCmd.cs:103. That zero-amount divergence is owned by step 6, not re-flagged here."
    },
    {
      "what": "24. ModifyAmount: guard CombatManager.Instance.IsEnding -> return 0 (PowerCmd.cs:217-220)",
      "verdict": "faithful",
      "issue": "CLOSED by the same entry guard as step 1: the sim has ONE code path through `PowerCmd.apply` and both C# guards (Apply<T> at :69-72 and ModifyAmount at :217-220) sit on the same call, so the stacking branch is refused too. EXECUTED: test_combat_ending_command_guards.py::test_stacking_onto_an_existing_power_is_refused, which stacks onto an EXISTING power and so exercises the ModifyAmount branch specifically.  FIXED 2026-07-28 (round 4). The text it replaced read: Folded into guard G6 -- the sim's single code path has no IsEnding guard anywhere, including the stacking case."
    },
    {
      "what": "25. ModifyAmount: guard combatState==null -> return 0; NO CanReceivePowers check in this path (PowerCmd.cs:221-226)",
      "verdict": "faithful",
      "maps_to": "The sim's stacking branch (cmds.py:308-321) likewise has no CanReceivePowers-equivalent check -- this is a faithful absence, since C# itself omits the check in ModifyAmount too (see guard N4)."
    },
    {
      "what": "26. ModifyAmount: Event Hook.BeforePowerAmountChanged(combatState, power, offset, owner, applier, cardSource) (PowerCmd.cs:227)",
      "verdict": "gap",
      "live": false,
      "issue": "DORMANT 2026-07-29 (round 11), same conclusion as `step10` and the same underlying code -- the sim's single `modify_power_amount` call (`cmds.py`, current line 553) serves as the only analogue to both C# sites (:120 for Apply, this step's :227 for ModifyAmount) since the sim has one collapsed code path (`power_cmd/step4`). The only live-eligible remainder is the sign-awareness of UnsettlingLamp's latch, which is guard `power_cmd/G2` (not owned by this batch) and re-confirmed still dormant today: `grep -rli \"malaise\\|resonance\" sts2_rl/cards/` returns nothing, so the only two C# cards that would exercise it remain unported."
    },
    {
      "what": "27. ModifyAmount: guard applier!=null && ContainsCreature(applier): modifiedOffset = Hook.ModifyPowerAmountGiven(...) (PowerCmd.cs:228-233)",
      "verdict": "gap",
      "live": false,
      "issue": "DORMANT 2026-07-29 (round 11) -- same conclusion as `step12` and the same code (the sim's one collapsed call, `power_cmd/step4`), mechanism `power_cmd/G3`. Re-confirmed: the only given-side listener (`UnsettlingLamp`) self-gates to `applier is self.player`, which already subsumes the missing `applier != null && ContainsCreature(applier)` guard for all current content; no second given-side listener exists to expose an ordering or reachability gap. See `step12` for the full re-executed evidence (listener census, Lamp's own gate)."
    },
    {
      "what": "28. ModifyAmount: modifiedOffset = Hook.ModifyPowerAmountReceived(...), unconditional, same mechanism as step 13 -- Artifact/RuinedHelmet also intercept re-stacking (PowerCmd.cs:234)",
      "verdict": "faithful",
      "maps_to": "STALE 2026-07-29 (round 11) -- this step's mechanism is `power_cmd/G1`, which was independently FIXED and reconciled to `faithful` on 2026-07-28 (see this record's own `guards[G1]` entry: `Power.type_for_amount` now ports `GetTypeForAmount` and `PowerCmd.apply` consults it). RE-EXECUTED against today's code: `cmds.py`'s Artifact check (current line 558) is `if power_cls.type_for_amount(amount) == PowerType.DEBUFF:` -- the sign-aware check, NOT the static `power_cls.power_type` this step's `issue` text (written before the fix landed) still described. Since the sim's Artifact block runs BEFORE the new-vs-stacking branch (`cmds.py:558` precedes the `if power_cls.id in target.powers` split at :567), it uniformly covers this ModifyAmount-path step too -- the fix that closed G1 for `step13`'s new-application case closes it here as well, being the same code. The uniformity-of-interception half of the old text (Artifact intercepting both Apply and ModifyAmount alike) remains true and is folded into this `faithful` maps_to. NOTE: the SEPARATE, still-open concern this step's old text also pointed at -- guard G3, Artifact being a hard-coded block outside the generic hook-listener system rather than a real `modify_power_amount` listener -- is a DIFFERENT mechanism from this step's own `power_cmd/G1` tag and is unaffected by this flip; G3 was independently settled `dormant` this round (see this record's `guards[G3]`). `power_cmd/step13` (not in this batch, same file) carries the identical stale text and was NOT re-verdicted here -- see HANDOFF."
    },
    {
      "what": "29. ModifyAmount: History.PowerReceived(...), UNCONDITIONAL here (unlike step 18's if-modifiedAmount!=0 gate) (PowerCmd.cs:235)",
      "verdict": "waiver",
      "rationale": "Telemetry/history bookkeeping; out of scope, same rationale as step 18."
    },
    {
      "what": "30. ModifyAmount: newAmount = power.Amount + (int)modifiedOffset; power.SetAmount(newAmount, silent) (PowerCmd.cs:236-237)",
      "verdict": "faithful",
      "maps_to": "cmds.py:311 `existing.on_stack(amount)` -- default Power.on_stack (powers.py:64-66) is `self.amount += amount`, i.e. the same additive combine using the post-modifier amount."
    },
    {
      "what": "31. ModifyAmount: guard modifiers!=null: AfterModifyingPowerAmountGiven(...) (PowerCmd.cs:238-241)",
      "verdict": "gap",
      "live": false,
      "issue": "Re-scoped 2026-07-31 (tier-2 campaign, Task 18), verdict UNCHANGED (still a dormant gap). The dormancy reason has changed in kind: this was 'no sim counterpart at all', and the counterpart now EXISTS and is correctly wired -- hooks.after_modify_power_amount_given is dispatched at C#'s position (PowerCmd.cs:148-151, :238-241) with the real modifiers out-list and the real power. What keeps it dormant is only that its sole C# implementer, SneckoSkull, is Flash-only and unported. A future given-side listener now has something to hook into.  The text it replaced read: DORMANT 2026-07-29 (round 11), same conclusion and same underlying absence as `step21` (the sim has no `AfterModifyingPowerAmountGiven` counterpart on either the new-power or stacking path, being one collapsed code path). Re-confirmed: `SneckoSkull.cs:32-36` is still the sole C# implementer of this hook in the whole game, still `Flash()`-only presentation, still unported. See `step21` for the full census."
    },
    {
      "what": "32. ModifyAmount: AfterModifyingPowerAmountReceived(...), same Artifact-Decrement/RuinedHelmet mechanism as step 22 (PowerCmd.cs:242)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): Closed alongside G4, which names this exact mechanism: AfterModifyingPowerAmountReceived is dispatched at C#'s position with the real modifiers list and the real power, and its two real listeners (Artifact's Decrement, RuinedHelmet's mark-used) now ride it.  The text it replaced read: DORMANT 2026-07-29 (round 11), same conclusion as `step22` and the same code -- the inline Artifact-decrement block (`cmds.py:558-565`) and `RuinedHelmet`'s inline `self._used = True` both run before the branch that distinguishes new-application from stacking (`power_cmd/step4`'s collapse), so they cover this ModifyAmount-path step too. Re-confirmed the 2-listener census (`ArtifactPower.cs:38`, `RuinedHelmet.cs:55`) is unchanged; no third listener exists. See `step22` for the full re-executed evidence."
    },
    {
      "what": "33. ModifyAmount: guard (int)modifiedOffset!=0: AfterPowerAmountChanged(...); NO BeforeApplied/AfterApplied in this path (PowerCmd.cs:243-246)",
      "verdict": "faithful",
      "maps_to": "cmds.py:312-314 fires hooks.on_power_amount_changed(power_cls.id, target, existing.amount-old_amount, applier) in the stacking (existing) branch -- distinct from on_power_applied (step 23's analogue), matching C#'s distinction between AfterPowerAmountChanged (stacking) and AfterApplied (new-power-only)."
    },
    {
      "what": "34. ModifyAmount: guard power.ShouldRemoveDueToAmount(): await Remove(power). AllowNegative powers removed only at exactly Amount==0; non-AllowNegative removed at Amount<=0 (PowerCmd.cs:247-250; PowerModel.cs:478-489)",
      "verdict": "faithful",
      "maps_to": "cmds.py:317-320 `if existing.amount == 0 or (existing.amount < 0 and not existing.allow_negative): existing._expire()` -- verified case-by-case equivalent to ShouldRemoveDueToAmount's branches (amount==0 always removes either way; negative amount only removes when allow_negative is False; negative amount with allow_negative True never auto-removes, matching AllowNegative's `Amount==0`-only check)."
    },
    {
      "what": "35. ModifyAmount: monster-intent UI update for monster owners (PowerCmd.cs:251-265)",
      "verdict": "waiver",
      "rationale": "Presentation/UI bookkeeping (intent icon refresh); out of scope."
    },
    {
      "what": "36. ModifyAmount: guard power.IsVisible && IsInProgress: wait (PowerCmd.cs:266-269)",
      "verdict": "waiver",
      "rationale": "Presentation-only pacing delay; out of scope, same rationale as step 19."
    },
    {
      "what": "37. ModifyAmount: return newAmount (PowerCmd.cs:270)",
      "verdict": "faithful",
      "maps_to": "cmds.py's stacking branch does not return a value the way C# does, but no sim caller of PowerCmd.apply inspects a return value (mirrors step 5's waiver rationale)."
    },
    {
      "what": "38. Remove(power): guard power!=null (PowerCmd.cs:289)",
      "verdict": "faithful",
      "maps_to": "PowerCmd.remove (cmds.py:334-346) and the internal _expire() helper (powers.py:86-92) both guard on the power actually being present before acting."
    },
    {
      "what": "39. Remove: power.RemoveInternal() fires the Removed event, Owner.RemovePowerInternal(this) (PowerCmd.cs:291; PowerModel.cs:575-580)",
      "verdict": "faithful",
      "maps_to": "_expire() (powers.py:86-92) pops the power from owner.powers and unregisters it from the hook system -- same net effect (removal + unhooking), no separate 'Removed' event needed since nothing in the sim listens for it."
    },
    {
      "what": "40. Remove: wait 0.2-0.4s (PowerCmd.cs:292)",
      "verdict": "waiver",
      "rationale": "Presentation-only pacing delay; out of scope."
    },
    {
      "what": "41. Remove: power.AfterRemoved(power.Owner) (PowerCmd.cs:293)",
      "verdict": "faithful",
      "maps_to": "No sim power currently overrides an AfterRemoved-equivalent hook in a way distinguishable from _expire's own unregister step; recorded as faithful by absence of any ported content needing the distinction."
    }
  ],
  "guards": [
    {
      "what": "G1: Sign-aware power typing missing from Artifact interception (PowerCmd.apply's Artifact check uses the static power_cls.power_type class attribute instead of C#'s GetTypeForAmount(amount))",
      "verdict": "faithful",
      "rationale": "FIXED 2026-07-28. `Power.type_for_amount(amount)` (sts2_rl/powers.py) ports `PowerModel.GetTypeForAmount` (PowerModel.cs:460-471) and `PowerCmd.apply` (sts2_rl/cmds.py) now consults it instead of the static `power_cls.power_type`, so a negative-amount application of a Buff-typed `allow_negative` power reaches the Artifact branch and is blocked, exactly as ArtifactPower.cs:24 tests `canonicalPower.GetTypeForAmount(amount) != PowerType.Debuff`. C#'s first clause is `StackType == Counter && AllowNegative`; the sim has no `PowerStackType` (that is power_cmd guard G5, still open), but every AllowNegative power in the game is Counter-stacking -- StrengthPower.cs:14, DexterityPower.cs:14, FocusPower.cs and ShriekPower.cs declare it outright and ShrinkPower's is Counter unless IsInfinite -- so `allow_negative` alone carries the clause for all ported content, and the second clause (`!AllowNegative && Type == Debuff && amount < 0 -> Buff`) is ported literally. The skip-next-tick site is deliberately NOT changed: PowerCmd.cs:144 tests the static `power.Type`, not GetTypeForAmount. STILL DORMANT on ported content -- the enemy-side direction's only C# sources are Malaise.cs:39 and Resonance.cs:33, neither ported, and the player-side direction is unreachable in principle. Pinned by test_artifact_blocks_negative_signed_debuff, whose strict xfail marker is deleted and which now passes.  RECONCILED 2026-07-28 (Task 0, ledger-vs-code pass after the Tier 1 fix campaign): this entry was recorded `gap` against pre-campaign code and the divergence is closed today. The issue it replaced read: MECHANISM (confirmed): cmds.py:299 checks `power_cls.power_type == PowerType.DEBUFF` (a fixed class attribute) instead of C#'s sign-aware `canonicalPower.GetTypeForAmount(amount) != PowerType.Debuff` (ArtifactPower.cs:24; definition PowerModel.cs:460-471: `if (StackType==Counter && AllowNegative && amount<0) return Debuff;`). Strength/Dexterity are Counter + AllowNegative + Type=>Buff on both sides, so a negative-amount application is a Debuff by C#'s rule but bypasses the sim's Artifact branch entirely. REACHABILITY (dormant, corrected in fix pass 1): this gap is dormant in BOTH directions. (a) Player-side -- a player-held Artifact meeting an incoming negative-amount Strength/Dexterity steal -- is unreachable because NO player-side ArtifactPower source exists anywhere in the game. Exhaustive check: `grep -rl ArtifactPower --include=*.cs` over the whole decompiled game returns exactly 12 files -- PowerModel/AbstractModelSubtypes plumbing, ArtifactPower.cs itself, Mocks/MockArtifactMonster.cs, ONE card (Expose.cs:40-43, which only REMOVES Artifact from an enemy, never grants it), and 8 monsters (Aeonglass.cs:84, Chomper.cs:52, CubexConstruct.cs:91, MechaKnight.cs:76, PunchConstruct.cs:74, TheAdversaryMkOne.cs:32, TheAdversaryMkTwo.cs:36, TheAdversaryMkThree.cs:34) every one of which SELF-applies (`PowerCmd.Apply<ArtifactPower>(..., base.Creature, N, base.Creature, null)`). No relic, potion, event, or card grants ArtifactPower to a player, so the player-targeted steals at sts2_rl/monsters/glory/the_lost_and_forgotten.py:54,99 and sts2_rl/monsters/underdocks/lagavulin_matriarch.py:106-107 can never meet a player-held Artifact. (b) Enemy-side -- the player applying a negative-amount Buff to an Artifact-holding enemy -- needs a card that does so, and the only two in C# are Malaise.cs:39 and Resonance.cs:33, NEITHER of which is ported (grep confirms no sts2_rl/cards/*.py match). CONCRETE TRIGGER that would make this live: porting Malaise or Resonance (each applies negative StrengthPower to an enemy, and enemies genuinely do hold Artifact -- Mecha Knight, Cubex Construct, the Adversary line). G1 and G2 are therefore two faces of ONE dormant gap with the same named trigger. Pinned with an xfail exercising the enemy-side (Malaise/Resonance) direction: test/test_hook_order.py::TestPowerCmdOrder::test_artifact_blocks_negative_signed_debuff."
    },
    {
      "what": "G2: Same sign-aware gap in UnsettlingLamp's own doubling condition, plus an amount<=0 early bail; currently dormant",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): (Task 17.) Unsettling Lamp is now sign-aware, and **the sim's own docstring had the C# BACKWARDS**: it claimed the sim was 'missing' an `amount <= 0` early bail that C# has. UnsettlingLamp.cs read in full (168 lines) has NO such guard anywhere -- both BeforePowerAmountChanged (:71-104, the latch) and ModifyPowerAmountGivenMultiplicative (:106-129, the doubling) gate purely on `power.GetTypeForAmount(amount) != PowerType.Debuff` (:97, :124). The sim's bail WAS the divergence: it rejected exactly the negative-Strength applications C# doubles (the Malaise.cs:40 / Resonance.cs:33 / SharedFate.cs:39 shape, all unported). The bail is deleted and the condition now mirrors C#'s sign-aware test. Duration ticks are structurally safe, more strongly than the record's argument assumed: every sim tick routes through `PowerCmd.modify_amount`, which adds to `power.amount` directly and never calls `hooks.modify_power_amount` -- the only entry point into the Lamp's listener -- so a tick cannot reach the check at all today, independent of sign (C#'s own protections, Decrement passing applier=null and GetTypeForAmount's sign-flip on non-AllowNegative debuffs, remain valid for a future wired-in state). The 933T Mecha Knight conformance behaviour is unchanged (test_glory.py::TestMechaKnight green). NOT closed by this: power_cmd/G4 and the ten AfterModifying* variants under damage_pipeline/G2 remain blocked on DISPATCH ARCHITECTURE, not on sign-awareness -- `hooks.modify_power_amount` still returns a bare int with no modifiers out-list, and Artifact is still hard-coded outside the generic listener system (power_cmd/G3). Pins: test/test_power_type_for_amount.py (14 tests).  The text it replaced read: sts2_rl/relics/unsettling_lamp.py:44-53's modify_power_amount bails on `amount <= 0` and then checks the static `power_cls.power_type != PowerType.DEBUFF`, instead of C#'s `power.GetTypeForAmount(amount) != PowerType.Debuff` (UnsettlingLamp.cs:124), which classifies a NEGATIVE amount on an AllowNegative buff (e.g. Strength/Dexterity) as Debuff and would double it. C#'s Malaise.cs:40 and Resonance.cs:33 both apply negative StrengthPower to an enemy with applier=player, cardSource=this -- exactly the shape Lamp is meant to double. Neither card is ported in the sim yet (grep confirms no sts2_rl/cards/*.py match for either name), so this is dormant, but it would misfire (fail to ever double) the instant either card is ported, since the sim's amount<=0 guard rejects ANY negative-amount application before the sign-aware check would even matter."
    },
    {
      "what": "G3: C#'s three ordered phases (BeforePowerAmountChanged event / ModifyPowerAmountGiven chain / ModifyPowerAmountReceived predicate chain) collapsed into one registration-order-dependent chain; Artifact bypasses the hook-listener system entirely",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): C#'s three ordered phases are now three separate dispatches. The single flat registration-order chain is gone: modify_power_amount_given_additive / _given_multiplicative (the exact additive-sum-THEN-multiplicative-product shape of Hook.cs:1888-1912, which is not commutative with a naive fold) run under a real `applier is not None and combat_contains_creature(applier)` gate (PowerCmd.cs:122-123), then modify_power_amount_received runs unconditionally (Hook.cs:1917-1930 has no applier gate). ArtifactPower and RuinedHelmet are now REAL LISTENERS on the received chain instead of a hard-coded block outside the hook loop. CENSUS CORRECTION: this entry's dormancy reason was stale -- it argued Lamp and RuinedHelmet are disjoint because Lamp gates on the STATIC power_type, which Task 17 replaced with sign-aware type_for_amount. The conclusion held for a deeper reason the record never stated: Lamp is a GIVEN-side override (UnsettlingLamp.cs:106-129) and RuinedHelmet/Artifact are RECEIVED-side (RuinedHelmet.cs:32-53, ArtifactPower.cs:17-36) -- they were never on the same C# hook at all, and the sim's collapsed chain was the ONLY thing that ever put them in a registration-order race. That collapse was the bug this guard named, and it is now structurally impossible. Pinned in test/test_power_modifier_phases.py (26 tests).  The text it replaced read: DORMANT 2026-07-29 (round 11), executed re-check. `hooks.py`'s `modify_power_amount` (current lines 545-557, drifted from the citation's :170-183 -- see SURPRISES) is still a single flat chain over hook-registration order with no phase separation, unlike C#'s three separately-sequenced calls (PowerCmd.cs:120,125,127) that guarantee given-before-received regardless of listener registration order; `ArtifactPower` is still hard-coded as a direct block in `cmds.py` (current lines 555-565), outside the hook loop entirely. RE-EXECUTED the listener census: `grep -n \"def modify_power_amount\" sts2_rl/` returns exactly THREE definitions -- the dispatcher itself (`hooks.py:545`) and its only two listeners, `UnsettlingLamp` (`relics/unsettling_lamp.py:45`) and `RuinedHelmet` (`relics/ruined_helmet.py:27`) -- confirming the count is still exactly 2, no drift. Re-verified domain-disjointness by reading both bodies against today's code: `UnsettlingLamp.modify_power_amount` gates on the STATIC `power_cls.power_type != PowerType.DEBUFF` (line 51) -- since `StrengthPower.power_type` is the static class attribute `BUFF` (not sign-aware; this is guard G2's own separately-tracked finding), Lamp can NEVER trigger on `StrengthPower` regardless of sign, and `RuinedHelmet.modify_power_amount` only triggers `power_cls is StrengthPower` (line 37) -- so the two listeners are domain-disjoint BY CONSTRUCTION, not by coincidence of current content, and no ordering question between them can ever arise while both bodies are unchanged. No third listener exists to introduce an overlap. Concrete trigger that would make this live: a THIRD `modify_power_amount` listener whose domain overlaps either existing one (e.g. a relic reacting to Strength gains the way RuinedHelmet does), at which point registration order (not C#'s fixed given-before-received sequence) would decide behaviour."
    },
    {
      "what": "G4: No AfterModifyingPowerAmountGiven/AfterModifyingPowerAmountReceived companion-event machinery",
      "verdict": "faithful",
      "issue": "Closed 2026-07-31 (tier-2 campaign): The AfterModifyingPowerAmountGiven/Received companion machinery now exists and is dispatched at C#'s positions, with a modifiers out-list carrying exactly the listeners that actually changed the value (Hook.cs:1888-1930's `if (num2 != 0m)` / `if (num3 != 1m)` / Try-returns-true semantics). ArtifactPower's charge spend (ArtifactPower.cs:38-41) and RuinedHelmet's mark-used (RuinedHelmet.cs:55-60) now ride the companion event instead of being hand-inlined. THIS TOOK TWO PASSES: the first fired both companions BEFORE the power was registered or stacked, where C# runs ApplyInternal (PowerCmd.cs:135) and SetAmount (:237) FIRST and only then dispatches (:148-152, :238-242) -- caught by the task reviewer. A third facet the controller found on top: C#'s Apply wraps its ENTIRE tail, ApplyInternal through both companions, inside `if (target.CanReceivePowers)` (:133-158), so a target failing that re-test must get NEITHER companion. Both are fixed, and the dispatch now carries the REAL power instance rather than None. The load-bearing subtlety, pinned explicitly: ApplyInternal's `if (!(amount == 0m))` is INTERNAL to it, so a debuff fully zeroed by Artifact attaches nothing but STILL fires both companions -- which is how Artifact spends its charge on a debuff it blocks. The empty-list skip is equivalent to C#'s unconditional call, verified: both companion hooks iterate listeners and filter on modifiers.Contains(modifier) (Hook.cs:796-824), so an empty list is a genuine no-op.  The text it replaced read: Hook.cs declares AfterModifyingPowerAmountGiven (Hook.cs:796-809) and AfterModifyingPowerAmountReceived (Hook.cs:811-824), both of which the brief specifically flagged (2 of the 13 AfterModifying* variants Task 5's damage_pipeline audit found the sim implements only 1 of). The sim's hooks.py:170-183 modify_power_amount returns a bare aggregated int with no out-param modifiers list at all -- unlike its sibling modify_hp_lost/after_modify_hp_lost pair (hooks.py:126-154), which Task 5's re-audit found DOES carry this machinery (built for BufferPower). This is not merely a bookkeeping gap: ArtifactPower.AfterModifyingPowerAmountReceived (ArtifactPower.cs:38-41) is the actual method that calls PowerCmd.Decrement(this) in C# -- the sim reimplements that specific effect inline in cmds.py:301-305, and RuinedHelmet.AfterModifyingPowerAmountReceived (RuinedHelmet.cs:55-60), a SECOND real C# listener, likewise has its 'mark used' side effect folded directly into modify_power_amount in the sim (relics/ruined_helmet.py:37) instead of a separate after-hook. Framing corrected in fix pass 1: this is NOT dormant machinery with a single listener -- C# has two live listeners on AfterModifyingPowerAmountReceived today, and the sim hand-inlines BOTH. Each inline reimplementation is individually faithful in effect (verdict on steps 22/32 is about the missing dispatch, not about wrong Artifact/RuinedHelmet behavior), and no THIRD listener exists to be silently dropped -- but the correctness of both existing listeners currently rests on hand-inlining rather than on the mechanism, and the sim's modify_power_amount (hooks.py:170-183) returns a bare aggregated int with no out-param modifiers list at all, so a future listener needing the 'only react on the hit(s) where I actually changed something' pattern has nothing to hook into. AfterModifyingPowerAmountGiven (Hook.cs:796-809) is separately absent with no sim analogue at all."
    },
    {
      "what": "G5: No PowerInstanceType (Instanced / InstancedPerApplier) distinction in FindExistingInstanceForStacking",
      "verdict": "faithful",
      "issue": "RESIDUE CLOSED 2026-08-02 (phase 1 of prompts/entity-obs-schema.md), verdict UNCHANGED (still faithful) -- the one piece of residue the entry below explicitly declined to claim closed is now closed, re-audited directly rather than rehashed on trust. `full_env.py`'s `_power_rows` (sts2_rl/full_env.py) no longer writes one `(presence, fine, coarse)` triple per power id; it writes one padded ROW per power INSTANCE, walking `creature.powers.values()` -- the same ordered `creatures.PowerList` phase 0 introduced, oldest-first, C#'s own application order (`Creature.Powers`, Creature.cs:326) -- so two instances of one power id are two separate rows, not one collapsed slot. Each row carries its own `amount` (two float encodings, fine/coarse) and its own `_power_aux`: a per-instance numeric field the `(id, amount)` pair alone can't express (e.g. `the_bomb`'s own blast `damage`, `toric_toughness`'s own `block`, `automation`/`panache`'s own `cards_left`), looked up explicitly on `pw.id` -- never duck-typed off an attribute name, so an unrelated power sharing an attribute name can't be misread. VERIFIED BY EXECUTION, not read alone: `PowerCmd.apply(hooks, player, TheBombPower, 5)` then `TheBombPower, 2)` with `set_damage(40)`/`set_damage(70)` on the two returned instances produces two rows in `build_combat_obs`'s `player.powers` block, oldest first, each with its own `amount` and its own `aux` -- `test/test_power_instances.py::TestObservationResidue::test_two_instances_are_two_distinct_observation_rows_with_their_own_aux` (the same tripwire this residue was pinned by, inverted into a closure test rather than deleted -- the record that the gap ever existed outlives the fix) and the independent witness `test/test_combat_obs_v4.py::test_the_bomb_two_instances_are_two_rows_with_distinct_aux` both pass; both re-run directly for this re-audit (64 passed). This was never a `PowerCmd` divergence to begin with -- a fixed-width RL observation vector has no C# counterpart at all -- so its closure does not change this guard's own `faithful` verdict; it only retires the residue note. `sts2_rl/full_env.py` (`_power_rows`/`_power_aux`) and `sts2_rl/creatures.py` (`PowerList.values`, already cited by this record's `extra_sources` from phase 0) are what this closure rests on; `full_env.py` is added to `extra_sources` accordingly. CORRECTION to the superseded phase-0 text below, found on re-audit: it cites the `test_obs_byte_identity` golden (its filename de-pathed here on purpose -- see below) as its evidence that the observation stream was byte-identical to the pre-change tree. That claim was true when written -- phase 0 changed only the power CONTAINER (`Creature.powers`), not the observation encoding, so byte-identity was the right bar and the golden proved it. The citation is dead now, and it is dead because of THIS project's own work, not bit-rot: phase 1 deliberately retired that golden test module (confirmed: no file of that name exists anywhere under `test/` today), because it existed to pin exactly the `obs_segments`/`power_triples` encoding phase 1 replaced with the row-per-instance schema this closure describes -- once that encoding was gone, the golden had nothing left to be a regression detector for, and the observation changing was phase 1's entire point, not a regression to catch. Recorded honestly rather than partially: the golden's stored digests covered the WHOLE observation dict, including the reward/terminated/truncated stream alongside the power/relic/card blocks, so retiring it also retired byte-identity coverage for that other half, and nothing in phase 1 supplies a replacement for it -- a real, if narrow, hole this closure does not claim to fill. The old text's own citation to that now-nonexistent file is de-pathed just below, in place, so `citation_check.py` stops reporting a dead pointer to a file this project deliberately deleted -- the claim and its date are untouched, only the file-path token is. The text it replaced read: Container rewrite landed 2026-08-01 (entity-obs-schema phase 0), verdict UNCHANGED (still faithful) -- but three claims the text below makes are now FALSE and are corrected here rather than left to mislead the next reader. (1) \"`target.powers` is still a single dict slot per id -- this is a dispatch fix, not a container rewrite\": it IS a container now. `Creature.powers` is `creatures.PowerList`, C#'s ordered `List<PowerModel>` (Creature.cs:34) with C#'s own accessors -- `get`/`[]` = GetPower, a FirstOrDefault over the list (:571); `instances(id)` = GetPowerInstances (:581); `values()` = Powers, the whole list in application order (:326); `add`/`discard` = ApplyPowerInternal/RemovePowerInternal (:600-612, :641-650), the latter by IDENTITY. So an INSTANCED \"not found\" APPENDS instead of overwriting, and no instance is orphaned from its owner any more: the hook walk (CombatState.cs:416 AddRange(creature.Powers)), the turn-start snapshot (:673-679) and the death strip (:667-671) each see every instance, as C# does. This also silently REVERSES a direction the text below states: `target.powers[id]` reports the OLDEST instance now, not the newest, because C#'s GetPower is a FirstOrDefault -- which is why every sim caller that configured \"the instance I just applied\" by re-fetching by id was moved onto `PowerCmd.apply`'s RETURN VALUE (the sim's `Apply<T>`, PowerCmd.cs:66-87): cards/colorless_skills.py (The Bomb SetDamage), cards/event_cards.py (Toric Toughness SetBlock), cards/crimson_mantle.py, cards/inferno.py, monsters/glory/knights.py (Dampen add_caster), monsters/hive/thieving_hopper.py (Swipe StolenCard). (2) \"TheBombPower and SwipePower are DELIBERATELY NOT migrated\": both ARE migrated. Their hand-rolled substitutes (`bombs` fuse list, `stolen_cards` bucket) existed only because the dict could not hold two instances, and both are retired -- see `power/the_bomb/InstanceType` and `power/swipe/InstanceType`, closed with this change. All 11 ported declarers now carry the real dispatch. (3) INSTANCED_PER_APPLIER was narrower than C#: the old code tested the applier of the ONE dict-visible instance, where PowerCmd.cs:168 is `GetPowerInstances(id).FirstOrDefault(p => p.Applier == applier)` -- a search over ALL instances. With two appliers on one target, a third application by the FIRST applier used to start a third instance instead of stacking onto its own; it now stacks. Pinned by test/test_power_instances.py::TestFindExistingInstanceForStacking. The orphaned-instance surfaces the text below leaves for future work are closed by construction: `Power._expire` and `PowerList.discard` remove by identity, `_strip_powers_after_death` walks `values()` (every instance), and `PowerCmd.remove(target, id)` removes ONE instance, the first -- matching `PowerCmd.Remove<T>(creature)` (:278-281), which resolves through `GetPower<T>()`. RESIDUE, explicitly not claimed closed: `full_env.py` still encodes one triple per power ID, so the OBSERVATION collapses instances (phase 1 of prompts/entity-obs-schema.md; pinned by test/test_power_instances.py::TestObservationResidue). That is an RL-schema limit with no C# counterpart, not a PowerCmd divergence. Full suite green, and the observation stream over fixed seeded episodes is byte-identical to the pre-change tree (golden: `test_obs_byte_identity`, retired by phase 1 -- see the correction above). The NOTE below about `powers.py`'s docstring mis-citing this guard for the absent PowerStackType still stands and is still not fixed here.  The text it replaced read: Closed 2026-07-30 (tier-2 campaign): PowerCmd.apply's stacking branch (sts2_rl/cmds.py) now mirrors FindExistingInstanceForStacking's three-way dispatch (PowerCmd.cs:165-174; PowerInstanceType, PowerModel.cs:144): PowerInstanceType.NONE (the sim's unchanged default) still finds the existing instance by id; INSTANCED forces \"not found\", so a second application starts a genuinely new, independently hook-registered instance instead of merging into the first's on_stack; INSTANCED_PER_APPLIER forces \"not found\" only when the found instance's applier differs from this application's applier (same applier still stacks). `target.powers` is still a single dict slot per id -- this is a dispatch fix, not a container rewrite -- so an INSTANCED/INSTANCED_PER_APPLIER \"not found\" overwrites that slot; the instance it replaces is not touched or unregistered and keeps ticking toward its own expiry, because the hook system dispatches from its own listener list (`hooks.register`/`_listeners`, sts2_rl/hooks.py), not from `target.powers` -- only a direct `target.powers` lookup now sees just the newest instance -- for automation/rolling_boulder/toric_toughness specifically, `target.powers[id].amount` now reports that newest instance's OWN amount rather than the old (incorrect) merged sum, so anything reading it directly (e.g. full_env.py's observation encoding) sees changed values after this fix, not just changed timing. `Power._expire` (sts2_rl/powers.py) was made identity-safe (`if owner.powers.get(id) is self: del ...`) so an orphaned instance's own expiry cannot delete a different, still-live instance's dict entry. `Power.instance_type` (sts2_rl/powers.py, default `PowerInstanceType.NONE`) mirrors `PowerModel.InstanceType` and is set to INSTANCED on AutomationPower, HeistPower, PanachePower, RollingBoulderPower, SandpitPower, ThieveryPower, ToricToughnessPower, WitheringPresencePower, and to INSTANCED_PER_APPLIER on StranglePower -- 9 of the 11 ported declarers (see their own entries). TheBombPower and SwipePower are DELIBERATELY NOT migrated (stay at the PowerInstanceType.NONE default): both already reproduce the observable behaviour that matters through their own pre-existing hand-rolled workarounds (TheBombPower's `bombs` list; SwipePower's `stolen_cards` list, both sts2_rl/powers.py), and routing either through the generic dispatch would silence its `on_stack` override and regress it -- confirmed for SwipePower specifically against `RunState.finish_combat`'s escaped-hopper deck-removal reconciliation walk (sts2_rl/run.py:1517-1520), which depends on every steal landing in ONE instance's `stolen_cards`. TDD: test/test_powers.py::TestPowerInstanceType (12 tests) exercises the three dispatch branches directly at the PowerCmd.apply layer plus each fixed unit's own behaviour; full suite 3359 passed / 2 failed (pre-existing floor_49 fixture gap, untouched) / 6 xfailed -- zero new failures. This closes the DISPATCH mechanism itself. It does NOT close `power/the_bomb/InstanceType` or `power/swipe/InstanceType`, which stay open with this same reasoning recorded on them directly. It also does NOT address the orphaned-instance surfaces `PowerCmd.remove` and `_strip_powers_after_death` (cmds.py:139-155) leave for future work: both still only ever touch the dict-visible instance, so an orphaned INSTANCED instance from a currently-unreachable two-simultaneous-instances case would keep its hook registration live past either one -- unreachable today by the same reachability argument as the DORMANT per-unit entries, so not fixed here.  The text it replaced read: LIVE 2026-07-29 (round 11), re-verdicted from the prior round's \"no currently-demonstrated collision\" close. RE-CONFIRMED count (`grep \"InstanceType\" src/Core/Models/Powers/*.cs`): 21 hits, 19 `Instanced` + 2 `InstancedPerApplier` (`OblivionPower.cs:27`, `StranglePower.cs:29`); PowerCmd.cs:165-174's FindExistingInstanceForStacking dispatches on this, `Instanced` always returning null (never merges -- every application is an independent PowerModel with its own internal counter state) where the sim's stacking check (cmds.py:567, `if power_cls.id in target.powers`) always merges via the target power's `on_stack` (PowerInstanceType.None behaviour), unconditionally, for every one of the 11 ported Instanced/InstancedPerApplier powers. DIVERGENCE: `AutomationPower` (powers.py:3985-4009, `InstanceType.Instanced` at AutomationPower.cs:27) has no `on_stack` override, so the sim's default `Power.on_stack` (`self.amount += amount`) merges a second application into the first instance's single `cards_left` counter instead of giving it an independent one, as C#'s `AfterCardDrawn` (`GetInternalData<Data>().cardsLeft`, AutomationPower.cs:36-51) does per-instance. `PanachePower` (powers.py:4166-4212) has the identical shape (own `cards_left` counter, no `on_stack` override). OBSERVABLE, executed (`.superpowers/sdd/unlabelled/probes/seam-power-cmd-g5-automation2.py`): apply AutomationPower(1) to the player, fire 6 `on_card_drawn` events, apply AutomationPower(1) again (on_stack merges to `amount=2`, ONE instance, `cards_left` unaffected at 4) -- then 4 more draws (10 total since the 1st apply) fire a SINGLE `GainEnergy(2)` event; the next 6 draws (16 total, the point the SECOND instance's own fresh 10-counter would fire in C#) fire NOTHING. C# would instead fire `GainEnergy(1)` at draw #10 (instance A) and a SEPARATE `GainEnergy(1)` at draw #16 (instance B, whose own `cardsLeft` starts at 10 from ITS OWN creation at draw #6). Same total (2 energy) but wrong timing -- the sim pays out the second copy's energy up to 6 draws early and pays nothing at the point the real second instance fires, so a combat ending between draw #10 and #16 nets the sim's player energy the real game would still be withholding. TRIGGER: reachable via ordinary duplicate colorless-card acquisition -- `AutomationCard`/`PanacheCard` (`sts2_rl/cards/colorless_powers.py:17-41,207-228`) are un-gated Uncommon Power-card combat rewards with no uniqueness check anywhere in `sts2_rl/rewards.py` (`grep -i \"unique\\|dedup\" sts2_rl/rewards.py` returns nothing), so a run can pick the same colorless card from two different reward screens and play both copies in one combat. FIX: give `Power.on_stack` real multi-instance semantics for the Instanced/InstancedPerApplier subset (a per-application sub-state list, the way `TheBombPower.on_stack` already does at powers.py:4325-4327 for its `bombs` list) rather than the silent default merge; `ToricToughnessPower`'s own docstring (powers.py:1467-1471) already flags the same unaddressed risk for itself. RADIUS: `PowerCmd.apply`'s single stacking branch (cmds.py:567-580) and every one of the 11 ported Instanced/InstancedPerApplier powers' `on_stack` (default in `powers.py Power.on_stack`): AutomationPower, HeistPower, PanachePower, RollingBoulderPower, SandpitPower, StranglePower (InstancedPerApplier, powers.py:3289-3327 -- dormant per its own single-applier-only content, see this record's prior analysis), SwipePower (checked separately: ThievingHopper's state machine visits its THIEVERY move exactly once per combat, so no live collision there), TheBombPower (has a correct workaround, not actually broken), ThieveryPower, ToricToughnessPower (self-documented approximation, not yet demonstrated live), WitheringPresencePower. The TEN unported ones are CoveredPower, FlankingPower, GuardedPower, KnockdownPower, MagicBombPower, MonologuePower, NightmarePower, OblivionPower, OrbitPower, TagTeamPower (each verified absent from sts2_rl/ by class-name grep) -- they will need the distinction considered when ported. NOTE (scope correction): `sts2_rl/powers.py:64-65`'s docstring cites this same guard id (`power_cmd/G5`) for the sim's separate absence of `PowerStackType` (`Counter`/`Single`/`None`, `PowerModel.cs:236`) -- a DIFFERENT C# enum from `PowerInstanceType` (`PowerModel.cs:144`), which this guard is actually about. That citation looks like a mis-attribution, not a restatement; flagged for the controller rather than corrected here since PowerStackType's absence is outside this guard's own scope."
    },
    {
      "what": "G6: No CombatManager.IsEnding / CanReceivePowers guard backstop anywhere in PowerCmd.apply",
      "verdict": "faithful",
      "issue": "CLOSED, including the observation round 4 carried forward. Duration ticks route through the command now: `Power._tick` is PowerCmd.Decrement (PowerCmd.cs:179-182) calling the new `PowerCmd.modify_amount`, which opens on ModifyAmount's `IsEnding` guard (:217-220) and closes on its ShouldRemoveDueToAmount tail (:247-250); `_tick_duration` is TickDownDuration (:190-200), consuming SkipNextDurationTick before the guard is ever reached. NOT included, and deliberately: the ModifyPowerAmountGiven / ModifyPowerAmountReceived chains ModifyAmount runs at :229-233. Wiring them into the decrement path is blocked on THIS record's own guard G2 -- the sim's Unsettling Lamp is missing C#'s `amount <= 0` early bail, so it would double a -1 duration tick into -2. FIXED 2026-07-29 (round 5). The text it replaced read: CLOSED. Both halves: (a) the IsEnding guard is at the top of `PowerCmd.apply`, covering Apply<T> (:69-72), Apply (:103) and ModifyAmount (:217-220), which are one path in the sim; (b) the mid-pipeline CanReceivePowers re-check (:133) is in place on the new-power branch. `CombatState.is_ending` is the predicate (CombatManager.cs:180-202): not `phase == COMBAT_OVER`, but a live recomputation of `a loss is pending OR no primary enemy is alive with nothing vetoing the end`, which already carries Hook.ShouldStopCombatFromEnding. NOTE, carried forward as a separate observation and not claimed closed: the sim's duration ticks go through `Power._tick`, which mutates `amount` directly rather than routing through PowerCmd, so ModifyAmount's guard does not reach them.  FIXED 2026-07-28 (round 4). The text it replaced read: NARROWED 2026-07-28 (round 3), same observation as creature_card_cmds' G14: `HookSystem._each` now stops a guarded-bucket dispatch once the combat is over, so a PowerCmd.apply running in the ending window no longer reaches listeners on those hooks. WHAT REMAINS is unchanged: (a) PowerCmd.apply has no IsEnding guard anywhere in its single code path -- neither Apply<T>'s (PowerCmd.cs:69-72) nor ModifyAmount's (:217-220) -- so the power is still applied; and (b) there is no MID-PIPELINE CanReceivePowers re-check (PowerCmd.cs:133), only the entry one.  NARROWED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass; what follows is what is still open. The issue it replaced read: NARROWED 2026-07-27: the CanReceivePowers ENTRY guard now exists. PowerCmd.apply opens with `if not hooks.should_allow_hitting(target): return` (sts2_rl/cmds.py), reproducing PowerCmd.cs:73-76 (spec step 2) with the same predicate damage_pipeline's note N1 covers for the damage path. WHAT REMAINS: (a) no IsEnding guard anywhere in the sim's single code path -- neither Apply<T>'s (PowerCmd.cs:69-72, step 1), nor Apply's (PowerCmd.cs:103, step 6), nor ModifyAmount's separate re-check (PowerCmd.cs:217-220, step 24); (b) no mid-pipeline CanReceivePowers re-check (PowerCmd.cs:133, step 16). The IsEnding half belongs to the same family as hook_dispatch's guard G8, which is still pinned by two strict xfails. ORIGINAL: none of these guards existed at any point in PowerCmd.apply."
    },
    {
      "what": "N1: IsVisible is entirely absent from the sim's Power model; C#'s power.IsVisible gate (UnsettlingLamp.cs:93, ArtifactPower.cs:29-33) provably never varies",
      "verdict": "waiver",
      "rationale": "A full grep of src/Core/Models/Powers/*.cs for `IsVisibleInternal` (the only way to override the default-true PowerModel.IsVisible, PowerModel.cs:150-167) returns zero matches anywhere in the entire C# codebase -- not just Ironclad-scope. IsVisible is therefore provably always true for every power in the current game, in both the Lamp's own latch gate and Artifact's own intercept gate. The sim's total omission of the concept produces no currently observable divergence, and cannot until new C# content introduces an invisible power (which would itself require a source-code change upstream, not just an unported-content gap) -- this is dead code in the source itself, not merely unported."
    },
    {
      "what": "N2: TemporaryStrengthPower/TemporaryDexterityPower's internal wrapped-power application omits applier in the sim instead of C#'s explicit HasDoubledTemporaryPowerSource double-dip guard",
      "verdict": "deliberate-divergence",
      "rationale": "C#'s TemporaryStrengthPower.BeforeApplied (TemporaryStrengthPower.cs:146-156) re-applies its InternallyAppliedPower (StrengthPower) via a full separate PowerCmd.Apply<T> call carrying the SAME applier/cardSource as the wrapper's own application, which would let UnsettlingLamp double both the wrapper (e.g. ManglePower, a Debuff) AND the internal StrengthPower independently (quadrupling) without UnsettlingLamp.HasDoubledTemporaryPowerSource (UnsettlingLamp.cs:164-167) explicitly detecting and skipping the second doubling. The sim's TemporaryStrengthPower/TemporaryDexterityPower counterparts (powers.py:840-870, 920-940) instead OMIT the applier parameter entirely on the internal StrengthPower/DexterityPower application (`PowerCmd.apply(hooks, owner, StrengthPower, self._sign * amount)`, no applier= kwarg) -- since UnsettlingLamp's own modify_power_amount requires `applier is self.player` to trigger at all, the applier-less internal call is naturally skipped, achieving the same 'double once, not twice' outcome by a different mechanism. VERIFIED BY EXECUTION in fix pass 1 (the earlier record rested on a hand-trace a reviewer could not re-derive). Sim run, throwaway script against the current worktree: with UnsettlingLamp active and the card bracket open, `PowerCmd.apply(hooks, enemy, ManglePower, 3, applier=player)` produces `{'strength': StrengthPower(-6), 'mangle': ManglePower(6)}` -- net -6 Strength, doubled exactly once. Control with no Lamp: `{'strength': StrengthPower(-3), 'mangle': ManglePower(3)}`. Real card play of the ported Mangle (StrengthLoss 10) with Lamp: `{'strength': StrengthPower(-20), 'mangle': ManglePower(20)}`; without Lamp: `-10`/`10`. Playing Mangle then Bash leaves Bash's Vulnerable at 2 (undoubled), so the Lamp's once-per-combat activation is spent exactly once. C# side, read line-by-line and matching: Hook.BeforePowerAmountChanged latches TriggeringCard=Mangle and adds ManglePower to DoubledPowers (UnsettlingLamp.cs:71-104; ManglePower.GetTypeForAmount(+3)=Debuff); ModifyPowerAmountGivenMultiplicative returns 2m for the wrapper because HasDoubledTemporaryPowerSource(ManglePower) is false (its InternallyAppliedPower is StrengthPower, not ManglePower), giving ManglePower(6); TemporaryStrengthPower.BeforeApplied (TemporaryStrengthPower.cs:145-156) then re-applies StrengthPower with Sign*6 = -6 carrying the same applier/cardSource, and on THAT call HasDoubledTemporaryPowerSource(StrengthPower) is true (UnsettlingLamp.cs:164-167), so the multiplicative returns 1m -- StrengthPower(-6), not -12. Both codebases therefore net -6 Strength (and -20 for the real 10-point Mangle). EQUIVALENCE HOLDS. Recorded as deliberate-divergence rather than faithful because the mechanisms genuinely differ (C# detects the double-dip explicitly; the sim avoids it by omitting `applier` so Lamp's `applier is self.player` gate fails) and a future power with a DIFFERENT double-dip shape -- e.g. one whose internal application legitimately needs an applier, or a Lamp-like relic that does not gate on applier -- could expose the difference."
    },
    {
      "what": "N3: Artifact's owner-scoping (target != base.Owner in C#, a hook-iteration self-filter) vs the sim's direct target.powers.get(\"artifact\") lookup",
      "verdict": "faithful",
      "rationale": "C#'s ArtifactPower.TryModifyPowerAmountReceived is discovered via IterateCombatHookListeners (iterating every hook-listening model in the whole combat) and self-filters with `if (target != base.Owner) return false` (ArtifactPower.cs:19-23) to only intercept when the power is being applied to Artifact's own owner. The sim's cmds.py:300 `target.powers.get(\"artifact\")` directly looks up whether the TARGET (not some other creature) holds Artifact -- architecturally a special case instead of a hook-iteration self-filter, but semantically identical: both only ever have the target's own Artifact stack intercept a debuff aimed at that same target."
    },
    {
      "what": "N4: ModifyAmount's missing CanReceivePowers check (spec step 25) is a faithful absence, not a divergence",
      "verdict": "faithful",
      "rationale": "C#'s ModifyAmount (PowerCmd.cs:215-271) itself only checks combatState==null, never CanReceivePowers, unlike Apply<T>/Apply(power,...) which check it twice (steps 2, 16). The sim's stacking branch (cmds.py:308-321) likewise has no CanReceivePowers-equivalent check -- both codebases omit it identically in the re-stacking path, so this is not a sim-vs-C# divergence at all, merely an asymmetry already present within the C# source between its own two power-application pipelines.",
      "issue": "CITATION CORRECTED 2026-07-31 (tier-2 campaign, Task 18), verdict UNCHANGED: this entry's text cites `step6` as 'settled this round, DORMANT'. step6 is now CLOSED (faithful) -- PowerCmd.apply gained the raw amount == 0 bail. The structural point this entry makes is otherwise unaffected: Task 18's phase separation happened WITHIN the one collapsed sim call site and did not un-collapse C#'s two independently-coded pipelines.  The text it replaced read: "
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-25",
  "extra_sources": [
    {
      "path": "src/Core/Combat/CombatState.cs",
      "sha256": "b3150c69fae0c33b48b7957c3a67bc4e5867d1641e05062cd3f5edd0a9d9f2e2",
      "side": "game"
    },
    {
      "path": "src/Core/Entities/Creatures/Creature.cs",
      "sha256": "f2483a7c55b0b0a248552a9ff458237d3682eb9f1b893866dcb32a0e07ffc8f1",
      "side": "game"
    },
    {
      "path": "sts2_rl/creatures.py",
      "sha256": "9152cb64c277b8c7e5162bcae7d59af41f160a1a347ac586423c41af5be9c530",
      "side": "sim"
    },
    {
      "path": "sts2_rl/full_env.py",
      "sha256": "4ba9a145147eb29e6f42a10acb70986873bf1bb12d47a12ef764a423572f312f",
      "side": "sim"
    }
  ]
}
