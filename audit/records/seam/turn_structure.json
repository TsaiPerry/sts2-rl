{
  "unit": "seam/turn_structure",
  "game_sources": [
    {
      "path": "src/Core/Combat/CombatManager.cs",
      "sha256": "38d7193b2ec448cec170d7c96645059b2f7ef46ae2f12a6a3863299b30125840"
    },
    {
      "path": "src/Core/Combat/PlayerTurnPhase.cs",
      "sha256": "33cda99dd8304176d76e668ebbab28741030c00dcfa5c1f34e177b39a75b8009"
    },
    {
      "path": "src/Core/Combat/CombatState.cs",
      "sha256": "b3150c69fae0c33b48b7957c3a67bc4e5867d1641e05062cd3f5edd0a9d9f2e2"
    },
    {
      "path": "src/Core/Entities/Creatures/Creature.cs",
      "sha256": "f2483a7c55b0b0a248552a9ff458237d3682eb9f1b893866dcb32a0e07ffc8f1"
    },
    {
      "path": "src/Core/Entities/Players/PlayerCombatState.cs",
      "sha256": "c908ea3144a466dc0d979ec44ade207dd692d6b23b9512afbcccae3468fea123"
    },
    {
      "path": "src/Core/Models/CardModel.cs",
      "sha256": "bb3ea9a22a833e6505bbf70dd9d3ec209f00003f2d68f5933a9e074a0d22fc21"
    },
    {
      "path": "src/Core/Models/MonsterModel.cs",
      "sha256": "9d1f6a08d79769d8a293d0ace1c96a789f6818d33b2cb7d4200e615c1c6cdbea"
    },
    {
      "path": "src/Core/Hooks/Hook.cs",
      "sha256": "c75c8e9d5c5015b9e3b230af72e47c585e8d42bde412633e910cc68cc0be6588"
    },
    {
      "path": "src/Core/Commands/PowerCmd.cs",
      "sha256": "d344a2ecdd846e26a02fcd14d3ebf48f6c99056fa3b3e0ccbaa43fe9901dc993"
    },
    {
      "path": "src/Core/Models/Powers/WeakPower.cs",
      "sha256": "631e963cbc39801fd98236e18e2fc645a12b3c118e82b4133cdb93afc9bfaaca"
    },
    {
      "path": "src/Core/Models/Powers/VulnerablePower.cs",
      "sha256": "d4cc015ea3ab96f8245de67d7a5a64799d5ba09f8d77ccaf7a9708065be9d204"
    },
    {
      "path": "src/Core/Models/Powers/FrailPower.cs",
      "sha256": "0c1090da7732348da9a8c5ab595b0ed11e30079298c94c51983f5660c65cc4dd"
    },
    {
      "path": "src/Core/Models/Relics/SturdyClamp.cs",
      "sha256": "5b9ad3cff05c51540b6559891a2e1cb87022834d9fc68f9f6be336f6f52834df"
    },
    {
      "path": "src/Core/Models/Relics/HornCleat.cs",
      "sha256": "c410e13417a8b4ccb1acde332b8c4197901c746a76ba49aee8f387dff21eeb2f"
    },
    {
      "path": "src/Core/Models/Relics/CaptainsWheel.cs",
      "sha256": "1ed14a0fedcf0e97e38765d61c6a5d16bfb829609ad6ff8612aff9f9b73e603d"
    },
    {
      "path": "src/Core/Models/Relics/Anchor.cs",
      "sha256": "8ec8f84f20049c2b3613be8e6705a5a669ec6b2338984091d2774b4b39f5b647"
    },
    {
      "path": "src/Core/Models/Relics/PaelsEye.cs",
      "sha256": "0ff4149872257b8969c808d66f1e293c0aa33980f41be77618f2802d59b02d3e"
    },
    {
      "path": "src/Core/Models/Relics/RunicPyramid.cs",
      "sha256": "872632e4196b24167fe7a681827829d05486b29b4483467aa0c33f383ef5a3e3"
    },
    {
      "path": "src/Core/Models/Powers/BarricadePower.cs",
      "sha256": "a5ecba6bc6007bbe200879d545ec975002bd6230145248e3cf9732b73cd5eb04"
    },
    {
      "path": "src/Core/Models/Relics/CloakClasp.cs",
      "sha256": "1b8e9638fab92d0ea0322f48e9c4e48fe10ae7f2fb6696b21932dc1d73995f0d"
    },
    {
      "path": "src/Core/Models/Relics/Orichalcum.cs",
      "sha256": "26ea7db10392ce478bef226d3b112f7b17575177d3d839ce176c135f031c06f8"
    },
    {
      "path": "src/Core/Models/Relics/RoyalPoison.cs",
      "sha256": "ddbb27b4723f3646672d78d24807eab8da79bfa7b3ce9210bcb3a282f3ca2161"
    },
    {
      "path": "src/Core/Models/Relics/JossPaper.cs",
      "sha256": "97237a51eda6199f48455edf7e83c061a73b97980279ee4a6cfdd149561b6bf2"
    },
    {
      "path": "src/Core/Models/Relics/UnceasingTop.cs",
      "sha256": "a00ca92b089176e93f9377585333323848fc7f7b50781397621a55ec54e3d323"
    },
    {
      "path": "src/Core/Models/Enchantments/Imbued.cs",
      "sha256": "46444d4f929ca036a9aec3ce332e04cf53c04671be3362de4939043669d7cb71"
    }
  ],
  "sim_sources": [
    {
      "path": "sts2_rl/combat.py",
      "sha256": "fc5d23e271b3b6c5618d6d81d545b406bd5eb3c9954b7dca28552a162fd5ed9f"
    },
    {
      "path": "sts2_rl/player.py",
      "sha256": "df4ede6550fabed2fc9f20b6935521523b5c3ed599303f9c9f0620a2f041eec0"
    },
    {
      "path": "sts2_rl/hooks.py",
      "sha256": "05530b1a10b7059a3447e7b6541d23b8bec6ff84f69660b49cf219f8383fc6ef"
    },
    {
      "path": "sts2_rl/creatures.py",
      "sha256": "34e44031a337c75e1c2685e0521a075d95fa7a1478ca8594e6a138651c361078"
    },
    {
      "path": "sts2_rl/monsters/base.py",
      "sha256": "b52d8d15ba11856c157ae544d7837db5c35df881260a0065cf1d2b97917bd392"
    },
    {
      "path": "sts2_rl/powers.py",
      "sha256": "c639bbbe8867fd8b295f85422a4eb0315764af32ae785c4ce010a0d4a29e6cf2"
    },
    {
      "path": "sts2_rl/cards/base.py",
      "sha256": "41353618867e55be53cb277579e9964c09fd60239f7f7a7ad0ee1a54c43da6da"
    },
    {
      "path": "sts2_rl/relics/sturdy_clamp.py",
      "sha256": "8c88a47ae07a148e2171b6df592cf9860638f4db01c5c2b35cd40d68d0c1dea1"
    },
    {
      "path": "sts2_rl/relics/horn_cleat.py",
      "sha256": "5142219d3758ef8eee61f9c513a5211915edb09122190d174e3e5f77bb68e5cb"
    },
    {
      "path": "sts2_rl/relics/captains_wheel.py",
      "sha256": "68b0cc3f2130b718c54c557bb202b81767500a9d5b20ffa0d09131bb032ca85e"
    },
    {
      "path": "sts2_rl/relics/anchor.py",
      "sha256": "e77e6679508b418d9702662595518c274bd3b40f18a2f5094e33c20a907c312c"
    },
    {
      "path": "sts2_rl/relics/paels_eye.py",
      "sha256": "404484e5433027a2742642c9bae296d7ac6533fe9567f2e38bc571ac9db8956b"
    },
    {
      "path": "sts2_rl/relics/runic_pyramid.py",
      "sha256": "313c805b499bd5a3c6340b8d34b9d9b01052908585d1392e060ac75b04424399"
    },
    {
      "path": "sts2_rl/cards/barricade_card.py",
      "sha256": "8957c6e32b0a2e03774b66619b13e1c031febbb579fedb446145dda4510e1bf9"
    },
    {
      "path": "sts2_rl/relics/cloak_clasp.py",
      "sha256": "0be75771fbd507ec9984fabce3b9d1f0041a8a768bee9f09a9779a30c33aae67"
    },
    {
      "path": "sts2_rl/relics/orichalcum.py",
      "sha256": "e58715b053b65a089aff4f658fa16eb2c450175ab920b2fa0aa5226b8752f32b"
    },
    {
      "path": "sts2_rl/relics/royal_poison.py",
      "sha256": "8a9397eef70c2263a3edc53db632154737df2ef517c8118118b5420af27b7860"
    },
    {
      "path": "sts2_rl/relics/joss_paper.py",
      "sha256": "8094d054cfbfa051cc19b6bd040394778d9969f0d5a5e57286ace2a570702822"
    },
    {
      "path": "sts2_rl/relics/electric_shrymp.py",
      "sha256": "418018af76beb2284a8e2aa300d263a5878caea3eb43f19d2ed032c982f2f23b"
    },
    {
      "path": "sts2_rl/relics/unceasing_top.py",
      "sha256": "3e06b1987a63140b0bdf50ac523ed5247533aee8bb70315da5518129916505fe"
    },
    {
      "path": "sts2_rl/relics/whispering_earring.py",
      "sha256": "d3ebaaf14a91c1a44a9c2867ec33df98425c5b111bed2f6ec1305d98292dcd07"
    },
    {
      "path": "sts2_rl/cards/apparition.py",
      "sha256": "2e3f76a090559cfc244803561d216616a6128a29cd3b3d91ef54a6fea223fcee"
    },
    {
      "path": "sts2_rl/enchantments.py",
      "sha256": "044485116b3b5da76128dff3179154c664c899a8e4e49a0c5b62e208fd0c4369"
    }
  ],
  "steps": [
    {
      "what": "1. SetUpCombat: throw if already set up; IsStarting=true; MultiplayerScalingModel.OnCombatEntered; clear _playersTakingExtraTurn; per player ResetCombatState() then PopulateCombatState(RunState.Rng.Shuffle, state); NetCombatCardDb.StartCombat; per creature AddCreature; CombatSetUp event (CombatManager.cs:350-378)",
      "verdict": "faithful",
      "rationale": "CombatState.__init__ (combat.py:72-209) builds the five piles from the run deck and shuffles the draw pile on the run's own stream, then adds the encounter's creatures. The multiplayer-scaling, NetCombatCardDb and CombatSetUp-event legs are N2 (single-player/presentation). The already-set-up throw is structurally impossible: the sim constructs a fresh CombatState per combat (see step 74)."
    },
    {
      "what": "2. StartCombatInternal: per creature AfterCreatureAdded -> AfterAddedToRoom(), and `if (IsEnemy && CurrentSide == Player) Monster.RollMove(players)` -- every enemy's FIRST intent is rolled here, while IsInProgress is still false (CombatManager.cs:394-398, 860-867)",
      "verdict": "faithful",
      "issue": "CLOSED 2026-07-29 (round 10, the Tier 1 residue). The opening RollMove now lives where CombatManager.AfterCreatureAdded puts it and carries the gate this step is about. `CombatState.after_creature_added` (sts2_rl/combat.py) is the port of CombatManager.cs:858-867: it rolls only when `_state.CurrentSide == CombatSide.Player`, and it is called from combat setup's per-creature loop (CombatManager.cs:394-398, before Hook.BeforeCombatStart at :404) and from CreatureCmd.add (CreatureCmd.cs:70, after the slot re-sort and before Hook.AfterCreatureAddedToCombat). `MachineMonster.__init__` no longer rolls; it starts on UNSET_MOVE, the sim's port of `MonsterModel.NextMove`'s initial `new MoveState()` (MonsterModel.cs:239 -> MoveState.cs:42-45, `this(\"UNSET_MOVE\", UnsetMove)` with no intents), so an un-rolled creature holds a real intent-less move rather than null in both engines. Both roll sites share `CombatState._prepare_for_next_turn`, which applies FindNextMoveState's `!_performedFirstMove && IsMove -> return` stickiness (MonsterMoveStateMachine.cs:60-63) -- needed because the hand-rolled monsters have no machine to apply it for them, and needed in the other direction too: the player-turn-start pass must NOT skip a creature that has never been rolled, or an enemy-side spawn is stranded on UNSET_MOVE for the whole combat. LIVE, and it was live at all 10 sim CreatureCmd.add call sites: `py audit/tools/state_machine_probes.py spawn-roll` reports that every one fires from a monster move, a monster death or a power, i.e. during the ENEMY side -- the arm C# does not roll. Executed witness: Two-Tailed Rat, one of only 2 of 83 ported MachineMonsters whose opening roll costs a draw (the other 81 start on a move, so FindNextMoveState returns before GetNextState) AND the one that summons its own kind. Three rats plus a CALL_FOR_BACKUP spawn: the sim took 1 MonsterAi draw at summon time and then skipped the spawn in the pass (3 draws); it now takes 0 at summon and 4 in the pass, in enemy-list order -- same total, and the draws the spawn and its already-acting siblings receive are no longer swapped for the rest of the combat. Pinned by test/test_tier1_residue.py (5 tests, both arms of the gate) and by the corrected test_hook_order.py::test_a_mid_combat_spawn_rolls_its_move_exactly_once, which asserted the OLD split (ctor rolls, add does not) and whose exactly-once property is unchanged. WAS: the FIRST intent was rolled inside MachineMonster.__init__, so it was not gated on CurrentSide the way CombatManager.cs:860-867 gates it."
    },
    {
      "what": "3. IsInProgress = true; IsStarting = false; then Hook.BeforeCombatStart(runState, state) -- combat is already in progress when it fires, and it fires BEFORE StartTurn (CombatManager.cs:399-403; Hook.cs:311-324)",
      "verdict": "faithful",
      "rationale": "combat.py:208-209 is exactly this pair: `self.hooks.on_combat_start()` then `self.player.start_turn()`. Verified by execution: a listener that sets block from on_combat_start runs before any turn-start hook (that the block is then wrongly cleared is step 12's finding, guard G6, not this step's ordering)."
    },
    {
      "what": "4. Banner / FTUE / scaled wait, then `await StartTurn()` (CombatManager.cs:406-419)",
      "verdict": "waiver",
      "rationale": "Banner, FTUE gating and CustomScaledWait are presentation (guard N2). The `await StartTurn()` this wraps is step 5's and is mirrored at combat.py:209."
    },
    {
      "what": "5. StartTurn guard `!IsInProgress` -> return (CombatManager.cs:424-427)",
      "verdict": "faithful",
      "rationale": "PlayerCombatState.start_turn (player.py:151) has no guard of its own, but every sim call site is guarded: combat.py:641-642 returns from end_turn unless phase == PLAYER_TURN, combat.py:673 gates the post-enemy-side start_turn on `phase != COMBAT_OVER`, and combat.py:209 runs it during construction when the combat is by definition in progress. The extra-turn call at 651 sits behind the same 641 guard."
    },
    {
      "what": "6. SetPhaseForAllPlayers(PlayerTurnPhase.None) -- every player's phase is None for the whole enemy turn and the first part of their own (CombatManager.cs:429; PlayerTurnPhase.cs:11)",
      "verdict": "faithful",
      "issue": "CLOSED. The phase model exists and None is entered at CombatManager.cs:429 on both sides. See guard G8.  FIXED 2026-07-29 (round 5). The text it replaced read: The sim has two phases (Phase.PLAYER_TURN / Phase.COMBAT_OVER, combat.py:35-37) against the game's six, and never leaves PLAYER_TURN while the combat is live -- `_execute_enemy_turn` (combat.py:279-284) tracks the side in a separate `current_side` string and leaves `phase` alone. There is no window in which the sim's phase says 'None'. See guard G8."
    },
    {
      "what": "7. Participants: isExtraPlayerTurn = _playersTakingExtraTurn.Count > 0; on a player-side extra turn only those players participate, otherwise CreaturesOnCurrentSide (playersStartingTurn empty on the enemy side) (CombatManager.cs:430-448; CombatState.cs:133)",
      "verdict": "faithful",
      "rationale": "Single-player: the player-side participant set is always [the player] and the enemy-side set is always self.enemies, which _run_enemy_turns (combat.py:287) iterates. The extra-turn subsetting is multiplayer bookkeeping (guard N2) and collapses to the same one-element list."
    },
    {
      "what": "8. Per participant creature.BeforeTurnStart(side) -> for every power, AmountOnTurnStart = Amount -- a snapshot taken before anything else in the turn (CombatManager.cs:449-455; Creature.cs:673-679)",
      "verdict": "gap",
      "issue": "The sim has no per-power turn-start amount snapshot at all: `grep -rn amount_on_turn_start sts2_rl/` returns 0 hits. C# has three readers, two of which are ported -- DrawCardsNextTurnPower (`AmountOnTurnStart == 0` suppresses both the extra draw and the removal, DrawCardsNextTurnPower.cs:28,37) and HelloWorldPower; SummonNextTurnPower is unported. The sim's DrawCardsNextTurnPower (powers.py:2737-2754) adds `self.amount` from modify_hand_draw and expires from on_player_turn_started with no such guard, so a stack applied DURING the turn-start window would draw and expire in the same turn where C# defers both to the next one. DORMANT: the sim's only auto-play slot inside turn start is on_player_turn_started (player.py:186), which runs AFTER _draw, so no ported path applies either power inside the window today. Goes live with any turn-start effect that applies a next-turn power."
    },
    {
      "what": "9. Hook.BeforeSideTurnStart(state, side, participants) -- fires for BOTH sides, before any block is cleared (CombatManager.cs:456-460; Hook.cs:1144-1159)",
      "verdict": "faithful",
      "issue": "CLOSED on both sides. The enemy leg now exists: `CombatState._run_enemy_turns` opens by capturing the side's participants (`_side_participants`, CombatManager.cs:444) and firing `HookSystem.before_enemy_side_start` ONCE (:458) before any block is cleared and long before any enemy moves. HardenedShellPower -- whose C# override has no participants filter at all (HardenedShellPower.cs:71) -- is on it unfiltered, and PlatingPower's round-1 arm stays player-side because PlatingPower.cs:43 tests `side != CombatSide.Player`. The sim expresses C#'s one `(state, side, participants)` hook as a PAIR of dispatchers, one per side, which is the idiom it already used for Hook.AfterTurnEnd (`after_player_turn_end` / `on_enemy_side_end`); it is behaviour-preserving because every C# implementer opens by testing `side` or `participants.Contains(Owner)` and the two sides never start a turn at the same moment. EXECUTED: test/test_enemy_side_per_side.py::TestSidePassStructure, TestSideStartListeners::test_hardened_shell_resets_on_both_sides_unfiltered.  FIXED 2026-07-28 (round 4). The text it replaced read: STILL OPEN on the ENEMY SIDE only. The player side now has the hook: `HookSystem.before_side_turn_start` is Hook.BeforeSideTurnStart (CombatManager.cs:458), fired at the top of PlayerCombatState.start_turn -- after the per-turn card-cost reset, BEFORE the block clear, the energy reset and the enemies' next-move roll -- and all 18 ported implementers this step lists (Pocketwatch, Orichalcum, Kunai, Shuriken, Red Mask, Velvet Choker, Bag of Marbles, Music Box, Ornamental Fan, Ripple Basin, Brilliant Scarf, Beating Remnant, Demon Tongue, Fake Orichalcum, Pollinous Core, Rainbow Ring, and the Plating/HardenedShell/Aggression/Sloth powers) were moved onto it, each read off its own C# `public override`. EXECUTED: test/test_turn_start_split.py. WHAT REMAINS is the enemy-side half, which is guard G5's: the sim's _run_enemy_turns brackets each enemy individually and has no side-level point to hang the hook on.  NARROWED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass; what follows is what is still open. The issue it replaced read: The sim has no pre-block-clear turn-start hook and no enemy-side turn-start hook: player.py:151-159 goes straight from reset_turn_cost_modifiers to should_clear_block, and the enemy side has only the per-enemy on_enemy_turn_start (combat.py:301). 26 C# classes override BeforeSideTurnStart, many ported (Pocketwatch, Orichalcum, Kunai, Shuriken, Red Mask, Velvet Choker, Bag of Marbles, Music Box, Ornamental Fan, Ripple Basin, Plating/HardenedShell/Aggression/Sloth powers). DORMANT for the player side: every ported one is a per-turn counter reset or latch that does not read block or energy, so moving it to on_player_turn_start (player.py:169, after the clear and the energy reset) is unobservable -- Pocketwatch's `_cardsPlayedLastTurn` snapshot (Pocketwatch.cs:73-81 vs relics/pocketwatch.py:31-34) still lands before modify_hand_draw. The enemy-side half is guard G5's."
    },
    {
      "what": "10. Player side only: SetPhaseForAllPlayers(Start); PlayerActionsDisabled = false; clear _playersReadyToEndTurn and _playersReadyToBeginEnemyTurn; _inPlayerTurnSetup = true; _deferredEndTurnTransition = null; turn banner (CombatManager.cs:461-477)",
      "verdict": "faithful",
      "issue": "CLOSED. Start is entered at CombatManager.cs:464 and the setup window is opened with it. See guards G8 and N1; the ready-flag and banner legs remain N2.  FIXED 2026-07-29 (round 5). The text it replaced read: No phase model (guard G8) and no setup-window flag (guard N1) in the sim. The ready-flag and banner legs are N2."
    },
    {
      "what": "11. Player side and !isExtraPlayerTurn only: `foreach (enemy in _state.Enemies) enemy.PrepareForNextTurn(_state.PlayerCreatures)` -> Monster.RollMove(targets). EVERY enemy's next move is rolled at the start of the PLAYER's turn, unconditionally -- including one that was stunned and skipped its move, and one that has just spawned. An extra player turn skips the roll entirely (CombatManager.cs:478-484; Creature.cs:546-554; MonsterModel.cs:415-418)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: CombatState._roll_enemy_intents (sts2_rl/combat.py) is one pass over self.enemies in list order, calling telegraph_next_move per enemy, and it is invoked from exactly two places -- combat setup and the top of each new player turn in end_turn -- never from the extra-turn branch, matching CombatManager.cs:478-484's `if (!isExtraPlayerTurn)` gate. telegraph_next_move now has ONE engine call site (verified by grep), so no monster advances its machine as part of performing a move. A stunned enemy performs its synthetic STUNNED move and is therefore rolled by the next pass like any other, which is what removes the one-draw MonsterAi desync. WAS: each monster advanced its own machine at the end of its move, and a stunned enemy took no draw where the game takes one."
    },
    {
      "what": "12. Per participant `await creature.AfterTurnStart(side)` -> ClearBlock(), EXCEPT a player whose PlayerCombatState.TurnNumber == 1, which returns before clearing anything (CombatManager.cs:492-499; Creature.cs:681-692)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: PlayerCombatState.start_turn wraps the whole clear in `if not self._first_turn:` (sts2_rl/player.py), so a player on turn 1 returns before ClearBlock exactly as Creature.cs:681-692 does. Block granted by a combat-start effect now survives into the first enemy turn."
    },
    {
      "what": "13. ClearBlock: `if (Hook.ShouldClearBlock(CombatState, this, out preventer)) Block = 0; else await Hook.AfterPreventingBlockClear(CombatState, preventer, this)` (Creature.cs:718-728; Hook.cs:2193-2204, 1032-1038)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: hooks.should_clear_block takes an out-param `preventer` list and the sim gained hooks.after_preventing_block_clear, dispatched only to the vetoing listener (sts2_rl/hooks.py); both the player arm (sts2_rl/player.py start_turn) and the enemy arm (sts2_rl/combat.py _run_enemy_turns) call the predicate with a preventer and fire the else-arm event, matching Creature.cs:718-728. Sturdy Clamp's port now caps from after_preventing_block_clear instead of hand-rolling it onto turn start."
    },
    {
      "what": "14. A SECOND, SEPARATE loop over the same participants: `await Hook.AfterBlockCleared(state, creature)` -- UNCONDITIONAL. It fires for a creature that had no block, for a creature whose clear was PREVENTED, and for a player on turn 1 whose AfterTurnStart returned early (CombatManager.cs:500-507; Hook.cs:119-125)",
      "verdict": "gap",
      "issue": "NARROWED 2026-07-27, RE-OPENED 2026-07-28 -- the CONDITION was fixed, the LOOP was not. CLOSED: on_block_cleared now fires OUTSIDE the `if should_clear_block(...)` arm on both sides -- PlayerCombatState.start_turn (sts2_rl/player.py) and CombatState._run_enemy_turns (sts2_rl/combat.py:358-363) -- and the enemy arm's extra `enemy.block > 0` gate is gone, so the event reaches a participant with no block, one whose clear a listener prevented, and a turn-1 player whose AfterTurnStart returned early. Anchor / Fake Anchor / Horn Cleat / Captain's Wheel therefore re-arm behind Barricade, Sturdy Clamp and Burrowed. STILL OPEN: there is no SECOND, SEPARATE loop, which is what this entry is about. CombatManager.StartTurn runs `foreach (Creature item3 in creaturesStartingTurn) await item3.AfterTurnStart(...)` (CombatManager.cs:492-497) and THEN a second `foreach (Creature item4 in creaturesStartingTurn) await Hook.AfterBlockCleared(_state, item4)` (CombatManager.cs:500-507), both before ExecuteEnemyTurn -- so on the enemy side EVERY enemy clears its block and gets the event before ANY enemy moves. The sim does the clear (combat.py:359-362) and the event (combat.py:363) inside the SAME per-enemy loop that then performs that enemy's move (combat.py:391/:393), so enemy N's clear happens only after enemies 1..N-1 have already acted. Observable whenever an enemy's move (or an on_block_cleared listener) grants block to a creature LATER in `combat.enemies`: the sim wipes it at that creature's just-in-time clear and the game does not. DORMANT today by accident, not by design -- the only ported cross-enemy block grant is Guardbot's GUARD -> Fabricator (sts2_rl/monsters/glory/fabricator.py:41-49) and the sim's spawn order happens to put the Fabricator BEFORE its Guardbot; closing audit/records/monster/fabricator.json's GetNextSlot guard (FabricatorNormal.Slots seats the bots at indexes 0-1 and the Fabricator at 2) makes this LIVE. The player side is single-participant, so only the enemy side carries it. Rule 7: sts2_rl/monsters/glory/fabricator.py and the fabricator record are not hashed here."
    },
    {
      "what": "15. Per player participant: SetupPlayerTurn(player, choiceContext) (steps 16-22), each awaited to its pause-or-completion point (CombatManager.cs:508-519)",
      "verdict": "faithful",
      "rationale": "Single-player: one call, and the sim inlines its body into player.py:161-186. The HookPlayerChoiceContext pause plumbing is N2."
    },
    {
      "what": "16. SetupPlayerTurn guards: player.Creature.IsDead -> return; null combat state -> warn and return (CombatManager.cs:629-639)",
      "verdict": "faithful",
      "issue": "CLOSED. SetupPlayerTurn's IsDead guard is ported as `PlayerCombatState._setup_player_turn`. See guard G13.  FIXED 2026-07-29 (round 5). The text it replaced read: player.py:151 has no is_dead guard. DORMANT for the ordinary turn-boundary path -- combat.py:673 will not call start_turn once _end_combat has flipped the phase -- but it is exactly the guard that would have contained guard G13's live case (Royal Poison killing the player during turn-1 setup leaves the sim in Phase.PLAYER_TURN with a dead player). Same family, recorded here as the missing guard and in G13 as the missing win check."
    },
    {
      "what": "17. `if (Hook.ShouldPlayerResetEnergy(state, player)) ResetEnergy()` (Energy = MaxEnergy) `else AddMaxEnergyToCurrent()` (Energy += MaxEnergy). MaxEnergy is itself Hook.ModifyMaxEnergy(state, player, player.MaxEnergy) (CombatManager.cs:641-649; PlayerCombatState.cs:101, 162-170; Hook.cs:2378-2388, 1770-1782)",
      "verdict": "gap",
      "issue": "The arithmetic matches exactly (player.py:163-167), but the two hooks fire in the OPPOSITE order: the sim calls modify_max_energy first (163) and should_reset_energy second (164), where C# evaluates ShouldPlayerResetEnergy first and only then reads MaxEnergy inside the chosen branch. DORMANT: both sim dispatchers are pure aggregations over listeners and no ported should_reset_energy or modify_max_energy implementation mutates state, so the swap is unobservable today; it goes live with the first side-effecting implementation of either, or with any modify_max_energy listener that reads a value the reset branch changes."
    },
    {
      "what": "18. `await Hook.AfterEnergyReset(state, player)` (CombatManager.cs:650; Hook.cs:503-509)",
      "verdict": "faithful",
      "rationale": "player.py:168 `self._hooks.on_energy_reset(self)`, immediately after the energy assignment and before the next hook. Verified by execution in the end_turn sequence pin."
    },
    {
      "what": "19. `await Hook.BeforeHandDraw(state, player, choiceContext)` (CombatManager.cs:652; Hook.cs:588-601)",
      "verdict": "faithful",
      "rationale": "Maps to on_player_turn_start (player.py:169 / hooks.py:279-283) -- the sim's pre-draw turn-start slot, fired after on_energy_reset and before modify_hand_draw. Verified by execution: the sequence pin records on_energy_reset -> on_player_turn_start -> modify_hand_draw."
    },
    {
      "what": "20. `handDraw = Hook.ModifyHandDraw(state, player, 5m, out modifiers)` then `await Hook.AfterModifyingHandDraw(state, modifiers)`; base is the const baseHandDrawCount = 5 (CombatManager.cs:40, 654-655; Hook.cs:1684-1700, 739-745)",
      "verdict": "gap",
      "issue": "modify_hand_draw is ported with the same base of 5 (player.py:171, PlayerCombatState.DRAW_PER_TURN), but there is no after_modifying_hand_draw hook in sts2_rl/hooks.py. DORMANT: C# has four implementers and the two ported ones are cosmetic -- Pocketwatch.cs:67-71 is a bare `Flash()` and PollinousCore's is the same shape; MindRotPower is ported but its override is also presentation-only, and PaleBlueDotPower is unported. Goes live with the first implementation that has a gameplay effect."
    },
    {
      "what": "21. Turn 1 only: first move every card whose enchantment sets ShouldStartAtBottomOfDrawPile to the BOTTOM (MoveToBottomInternal), then move every Innate card NOT already moved to the TOP (MoveToTopInternal); handDraw = min(max(handDraw, innateCount), CardPile.MaxCardsInHand) (CombatManager.cs:657-672)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: PlayerCombatState.start_turn's first-turn branch now runs BOTH pile moves in CombatManager.cs:657-672's order -- every draw-pile card whose enchantment sets should_start_at_bottom_of_draw_pile is moved to the BOTTOM first, then the Innate cards not already moved (`c not in sinking`, the `.Except(list)` clause) go to the TOP and raise the turn-1 draw count (sts2_rl/player.py). An Imbued card no longer occupies an opening-hand slot."
    },
    {
      "what": "22. `await CardPileCmd.Draw(choiceContext, handDraw, player, fromHandDraw: true)` then `await Hook.AfterPlayerTurnStart(state, choiceContext, player)` (CombatManager.cs:673-675; Hook.cs:882-895)",
      "verdict": "faithful",
      "rationale": "player.py:183-186: `self._draw(draw_count, from_hand_draw=True)` then `on_player_turn_started`. The fromHandDraw flag is ported through to should_draw/on_card_drawn. Verified by execution in the sequence pin (modify_hand_draw -> 5x [should_draw, on_card_drawn] -> on_player_turn_started)."
    },
    {
      "what": "23. `await Hook.AfterSideTurnStart(state, side, participants)` -- every listener's AfterSideTurnStart, then a SECOND full pass of every listener's AfterSideTurnStartLate. Fires for BOTH sides, and on the player side only after every player's SetupPlayerTurn has finished (CombatManager.cs:520-524; Hook.cs:1163-1175)",
      "verdict": "faithful",
      "issue": "CLOSED on both sides. `after_enemy_side_start` fires ONCE (:522) after the block-clear and AfterBlockCleared passes have completed for every enemy and before ExecuteEnemyTurn walks them for their moves, and `HookSystem._each`'s suffix walk gives it the second AfterSideTurnStartLate pass for free. PoisonPower, DemonFormPower, SlowPower and PlatingPower's decay moved onto it; SandpitPower onto `after_enemy_side_start_late` (SandpitPower.cs:70). EXECUTED: test/test_enemy_side_per_side.py::TestSideStartListeners -- with two enemies BOTH take their Poison tick before the first one attacks, and a plain `after_enemy_side_start` listener still sees Sandpit's pre-decrement amount.  FIXED 2026-07-28 (round 4). The text it replaced read: STILL OPEN on the ENEMY SIDE only. The player side is now two ordered dispatchers, as C# has it: `on_player_turn_started` is Hook.AfterPlayerTurnStart (CombatManager.cs:673-675, SetupPlayerTurn's last line) and the NEW `after_side_turn_start` is Hook.AfterSideTurnStart (:520-524), fired afterwards from PlayerCombatState.start_turn; the second AfterSideTurnStartLate pass is HookSystem._each's `_late` suffix walk. 50 listeners were re-slotted onto whichever of the two they actually override. WHAT REMAINS: the ENEMY side still has no side-scoped turn-start event at all (guard G5), which is why PoisonPower -- now correctly on `after_side_turn_start` for the player leg -- still uses the per-enemy on_enemy_turn_start for the enemy leg.  NARROWED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass; what follows is what is still open. The issue it replaced read: The player side maps to on_player_turn_started (hooks.py:285-295), which the sim fires in a single pass -- SandpitPower is a ported AfterSideTurnStartLate implementation with no separate slot (guard G12). The ENEMY side has no counterpart at all: the sim never fires a side-scoped enemy turn-start event (guard G5), which is why the ported PoisonPower (powers.py:484-490), whose C# home is exactly this hook (PoisonPower.cs:55-73), sits on the per-enemy on_enemy_turn_start instead."
    },
    {
      "what": "24. Player side: per player, PlayerCombatState.OrbQueue.AfterTurnStart(ctx) (CombatManager.cs:526-537)",
      "verdict": "waiver",
      "rationale": "Orbs are the Defect's; the sim is Ironclad-only (guard N3, and the shared scope rule for non-Ironclad characters)."
    },
    {
      "what": "25. Player side: every dead player and every player NOT in playersStartingTurn is auto-SetReadyToEndTurn(canBackOut: false); if that makes everyone ready, release the deferred transition and return (CombatManager.cs:543-555)",
      "verdict": "waiver",
      "rationale": "Pure multiplayer readiness bookkeeping: with one player the set is either empty or the whole game, and the deferred-transition release is guard N1's. Multiplayer-only plumbing is out of scope by the shared rules."
    },
    {
      "what": "26. Player side: per player, RunAutoPrePlayPhase(ctx, setupTask, player) -- await the setup task, Phase = AutoPrePlay, Hook.AfterAutoPrePlayPhaseEntered, Phase = Play. Strictly AFTER steps 23 and 24, so start-of-turn auto-plays run after every turn-start relic and power (CombatManager.cs:556-572, 613-619; Hook.cs:928-938)",
      "verdict": "faithful",
      "rationale": "CLOSED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass -- the turn-start split, the combat-over hook gate, Glass Eye's reward set, the out-of-combat potion action and the choose-a-card selector arm. This is a CODE change, not a re-reading of the old code. VERIFIED 2026-07-28 (round 3). Both named users are in fact already on the phase hook -- Whispering Earring implements `after_auto_pre_play_phase_entered_late` and the Imbued enchantment `after_auto_pre_play_phase_entered` -- so the 'hand-rolled onto the neighbouring slot' residue was stale text. This round closes the ordering witness that survived it: Crossbow is genuinely Hook.AfterSideTurnStart (Crossbow.cs:17) and moved onto the new `after_side_turn_start` dispatcher, fired from PlayerCombatState.start_turn strictly before `after_auto_pre_play_phase_entered`. So C#'s 'Crossbow runs first' is structural now, not registration luck.  The gap it replaced read: NARROWED 2026-07-27: the PHASE HOOK now exists. hooks.after_auto_pre_play_phase_entered is dispatched from PlayerCombatState.start_turn strictly after the post-draw turn-start slot (sts2_rl/player.py), which is CombatManager.cs:556-572's position, and MayhemPower (AfterAutoPrePlayPhaseEntered in C#) is wired to it. WHAT REMAINS: the two ported users this step named are still hand-rolled onto the neighbouring slot -- Whispering Earring (WhisperingEarring.cs:41 AfterAutoPrePlayPhaseEnteredLate) and the Imbued enchantment (Imbued.cs:20 AfterAutoPrePlayPhaseEntered) both fire from on_player_turn_started, the sim's AfterSideTurnStart slot. Crossbow is genuinely AfterSideTurnStart (Crossbow.cs:17), so with all three on one hook their relative order is registration luck where C# runs Crossbow strictly first. ORIGINAL: neither the phase nor the hook existed."
    },
    {
      "what": "27. `await CheckWinCondition()` -- AFTER the player's turn setup and the auto-pre-play phase, before the play phase opens (CombatManager.cs:573)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: CombatState.__init__ now calls self._check_win_condition() immediately after the combat-start player.start_turn(), and end_turn re-checks both conditions after each subsequent start_turn (sts2_rl/combat.py) -- CombatManager.cs:573's slot. _check_win_condition RECOMPUTES (tests player.is_dead and _all_enemies_dead()) rather than reading the cached phase, so a turn-start listener that lands a killing blow or kills the player ends the fight there. Royal Poison no longer leaves a dead player in Phase.PLAYER_TURN with a legal action set. NOTE: the four mid-pipeline sites remain cached-flag tests -- see guard G13, still a gap."
    },
    {
      "what": "28. If still IsInProgress: unpause the action executor, set the queue to PlayPhase, IsEnemyTurnStarted = false, _inPlayerTurnSetup = false, fire TurnStarted; then ReleaseDeferredEndTurnTransitionIfNeeded() (CombatManager.cs:575-586)",
      "verdict": "faithful",
      "issue": "CLOSED. `_start_player_turn`'s `finally` is ReleaseDeferredEndTurnTransitionIfNeeded. See guard N1; the action-executor and queue legs remain N2.  FIXED 2026-07-29 (round 5). The text it replaced read: The action-executor/queue/event legs are N2, but the _inPlayerTurnSetup teardown and the deferred-transition release have no sim analogue -- see guard N1."
    },
    {
      "what": "29. Enemy-side branch: IsEnemyTurnStarted = true, TurnStarted, checksum, WaitForUnpause, CheckWinCondition, then `if (IsInProgress) await ExecuteEnemyTurn(actionDuringEnemyTurn)` (CombatManager.cs:588-604)",
      "verdict": "faithful",
      "rationale": "STALE: the code this text describes (`combat.py:666-671`, a cached-flag/`_all_enemies_dead()`-only check right before the enemy side) no longer decides reachability by itself, and even on its own terms it is not the last word. `_end_turn_internal` today (sts2_rl/combat.py) does read `phase == COMBAT_OVER` then `_all_enemies_dead()` right after `after_player_turn_end` -- neither is a recompute of a PLAYER death -- but `_execute_enemy_turn()` immediately calls `_run_enemy_turns()`, whose FIRST substantive step after the side-start hooks is `self._check_win_condition()` (a real recompute: `_has_pending_loss` tests `player.is_dead` OR an explicit flag) with `if self.phase == Phase.COMBAT_OVER: return` directly after it, BEFORE the per-enemy move loop -- the sim's own comment cites this as CombatManager.cs:598's exact position ('CheckWinCondition guards the whole of ExecuteEnemyTurn, so a side-start hook that killed the last enemy (or the player) ends the combat before any move'). EXECUTED: `.superpowers/sdd/unlabelled/probes/seam-damage-turn-step29.py` -- gave the player DisintegrationPower (a real ported Knowledge-Demon curse; 'Knowledge Demon's Curse of Knowledge', sts2_rl/cards/knowledge_curses.py) with enough amount to kill at 5 HP, which fires from `after_player_turn_end_late` -- exactly the `after_player_turn_end` call this step is about. Traced with instrumentation: `_execute_enemy_turn` IS entered with a stale `PLAYER_TURN` phase (confirming the described gap's PREMISE at that one check), but the enemy inside `_run_enemy_turns` never gets to act -- `_check_win_condition()` catches the pending loss and returns first. Output: `enemy acted after player death: False`. So the OBSERVABLE the brief worried about (the enemy side running with a dead player) does not happen; the described local check is imprecise but the pipeline as a whole is faithful to C#'s per-step CheckWinCondition placement. Flipped to faithful; `issue` removed. Same mechanism as steps 27/41/49/56, all closed under guard G13 (also faithful)."
    },
    {
      "what": "30. ExecuteEnemyTurn guard `!IsInProgress` -> return; optional test hook actionDuringEnemyTurn() (CombatManager.cs:1063-1071)",
      "verdict": "faithful",
      "rationale": "_execute_enemy_turn (combat.py:279-284) is reached only from end_turn's guarded tail (combat.py:666-671). The actionDuringEnemyTurn parameter is a C# test seam with no gameplay meaning."
    },
    {
      "what": "31. Iterate a SNAPSHOT of _state.Enemies.ToList(), skipping any creature the state no longer contains -- a monster spawned or removed during the enemy side does not change this turn's participant list (CombatManager.cs:1072-1074)",
      "verdict": "faithful",
      "rationale": "combat.py:287 iterates `list(self.enemies)` -- the same snapshot -- and the only mid-combat mutation of that list is the append at cmds.py:263, so a monster spawned during the enemy side is correctly excluded. Removal is modelled by is_gone rather than by deleting the entry (verified: no `enemies.remove` anywhere in sts2_rl/), and combat.py:292 skips gone creatures unless retained_after_death, which is the ContainsCreature test."
    },
    {
      "what": "32. Per enemy: intent animation, then `await enemy.TakeTurn()` -- throws unless the creature is a monster on the enemy side, and runs Monster.PerformMove() ONLY if !Monster.SpawnedThisTurn. There is NO IsDead guard: a corpse the combat retained still takes its turn (CombatManager.cs:1076-1081; Creature.cs:706-716)",
      "verdict": "gap",
      "issue": "The no-IsDead-guard half is faithfully ported (combat.py:288-292 keeps a retained_after_death corpse in the loop -- that is how a withered Decimillipede segment reaches REATTACH). The SpawnedThisTurn half has no sim counterpart: `grep -rn spawned_this_turn sts2_rl/` returns 0 hits, and there is no OnSideSwitch to clear it either (step 67). DORMANT: I could not construct a reachable C# path where the flag survives to TakeTurn -- SetUpForCombat sets it for every monster (MonsterModel.cs:409-413), SwitchSides clears it for every creature at every side switch including extra turns (CombatManager.cs:1420-1423; MonsterModel.cs:479-483), and a monster spawned during the enemy side is already outside step 31's snapshot. Recorded as a gap rather than faithful because the sim reproduces the observable by accident (the snapshot) rather than by modelling the flag."
    },
    {
      "what": "33. PerformMove: scaled wait -> IsPerformingMove = true -> move = NextMove -> move.PerformMove(combatState.PlayerCreatures) -> MoveStateMachine.OnMovePerformed(move) (Task 10) -> history -> IsPerformingMove = false -> if the monster is now dead and Hook.ShouldCreatureBeRemovedFromCombatAfterDeath -> combatState.RemoveCreature (MonsterModel.cs:434-453)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: MachineMonster.take_turn now performs the move and calls machine.on_move_performed and nothing else -- the machine advance moved out to telegraph_next_move, whose only engine caller is CombatState._roll_enemy_intents (sts2_rl/monsters/state_machine.py, sts2_rl/combat.py). That puts the roll at CombatManager.cs:478-484 as MonsterModel.PerformMove leaves it. IsPerformingMove still has no counterpart and no ported content reads it."
    },
    {
      "what": "34. Per enemy, after its move: WaitForUnpause, `await CheckWinCondition()`, `if (!IsInProgress) return` (CombatManager.cs:1082-1088)",
      "verdict": "faithful",
      "rationale": "STALE. Both halves of the original claim no longer hold. (1) The per-enemy 'on_enemy_turn_start' hook the brief's witness depends on ('a listener killing the player from on_enemy_turn_start') does not exist any more -- `grep -n 'def on_enemy_turn' sts2_rl/hooks.py` returns nothing; guard G5 (closed 2026-07-28, round 4) replaced the per-creature enemy-side hooks with side-scoped ones (`before_enemy_side_start`/`after_enemy_side_start`/`before_enemy_side_end`/`on_enemy_side_end`). (2) The 'sibling branch... calls _end_combat[with the hook firing]' no longer exists either: today's `_run_enemy_turns` (sts2_rl/combat.py) has, after each enemy's move, `if self.player.is_dead: self.phase = Phase.COMBAT_OVER; self.result = ...; return` -- inlined rather than routed through `_check_win_condition`/`_process_pending_loss` -- but per guard G10 (closed), a C# LOSS fires NO hook at all (`ProcessPendingLoss` sets the phase and fires nothing), so the inlined branch and the shared helper now produce IDENTICAL observables (no hook) rather than disagreeing ones. EXECUTED: `.superpowers/sdd/unlabelled/probes/seam-damage-turn-step34.py` -- a 2-enemy combat where the first enemy's move (monkeypatched) kills the player; result: `phase: Phase.COMBAT_OVER`, `result: CombatResult(player_won=False, ...)`, `hooks fired: []`, `second enemy acted: False` -- matching C#'s per-enemy CheckWinCondition-then-return exactly (no hook, no further enemy). Flipped to faithful; `issue` removed. Guard G10 already closed; this step just hadn't been re-verdicted with it."
    },
    {
      "what": "35. After the loop: checksum, then `await EndEnemyTurn()` (CombatManager.cs:1091-1092)",
      "verdict": "faithful",
      "rationale": "combat.py:344-345 falls through from the per-enemy loop into the side-end handling under a `phase != COMBAT_OVER` guard. The checksum is N2."
    },
    {
      "what": "36. EndEnemyTurn guards: IsInProgress; THROW if CurrentSide != Enemy; WaitForUnpause (CombatManager.cs:819-827)",
      "verdict": "faithful",
      "rationale": "combat.py:344 gates the side-end on `phase != Phase.COMBAT_OVER`. The side assertion is an invariant check; _execute_enemy_turn sets and restores current_side in a try/finally (combat.py:280-284) so it cannot be violated."
    },
    {
      "what": "37. EndEnemyTurnInternal: `await Hook.BeforeTurnEnd(state, Enemy, CreaturesOnCurrentSide)` -- three complete listener passes in order: BeforeSideTurnEndVeryEarly, then BeforeSideTurnEndEarly, then BeforeSideTurnEnd (CombatManager.cs:1250-1251; Hook.cs:1238-1261)",
      "verdict": "faithful",
      "rationale": "STALE. Guard G11 (this exact slot) closed 2026-07-28 (round 4) and guard G12 (the sub-phase flattening) closed 2026-07-28 as well, but this step entry was never re-verdicted alongside them. EXECUTED: read sts2_rl/hooks.py and sts2_rl/combat.py today -- `before_enemy_side_end` exists (hooks.py, dispatched through `_each`, which walks the `_very_early`/`_early`/plain/`_late` suffixes exactly like every other HookSystem dispatcher, matching Hook.cs:1238-1261's three-pass shape) and `_run_enemy_turns` calls it (`self.hooks.before_enemy_side_end()`) immediately before `self.player.end_of_turn_cleanup()` and `self.hooks.on_enemy_side_end()` -- CombatManager.cs:1250-1256's order. The brief's premise ('There is no enemy-side BeforeTurnEnd slot in the sim at all') is false today. Flipped to faithful; `issue` removed."
    },
    {
      "what": "38. `foreach (player in _state.Players) PlayerCombatState.EndOfTurnCleanup()` -- per card in EVERY pile: ExhaustOnNextPlay = false, HasSingleTurnRetain = false, HasSingleTurnSly = false, drop every local energy-cost modifier flagged EndOfTurn and every temporary star cost flagged ClearsWhenTurnEnds (CombatManager.cs:1252-1255; PlayerCombatState.cs:268-274; CardModel.cs:1610-1623)",
      "verdict": "faithful",
      "issue": "CLOSED. Ported, between the two enemy-side hooks. See guard G7.  FIXED 2026-07-29 (round 5). The text it replaced read: First of the two per-round C# sites; the sim has no counterpart at either. See guard G7."
    },
    {
      "what": "39. `await Hook.AfterTurnEnd(state, Enemy, enemies)` -- every listener's AfterSideTurnEnd (awaited as a group), THEN every listener's AfterSideTurnEndLate. This is where Vulnerable, Weak and Frail tick down, each overriding AfterSideTurnEnd with a `side == CombatSide.Enemy` guard (CombatManager.cs:1256; Hook.cs:1265-1291; WeakPower.cs:48-53, VulnerablePower.cs:59-64, FrailPower.cs:35-40)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: hooks.on_enemy_side_end is dispatched through HookSystem._each like every other hook, so an `on_enemy_side_end_late` listener would get its own complete pass after the plain one (sts2_rl/hooks.py) -- Hook.cs:1265-1291's AfterSideTurnEnd -> AfterSideTurnEndLate shape. The step's named implementation moved to its real side and phase: DisintegrationPower now implements after_player_turn_end_late (sts2_rl/powers.py). The tick placement was already faithful. WAS: a single flattened listener walk with no Late pass."
    },
    {
      "what": "40. PowerCmd.TickDownDuration(power): `if (power.SkipNextDurationTick) { SkipNextDurationTick = false; }` else `await Decrement(power)` = ModifyAmount(..., -1m, ...) (PowerCmd.cs:190-200)",
      "verdict": "faithful",
      "rationale": "Power._tick_duration (powers.py:77-84) is a line-for-line port of the consume half: it clears skip_next_tick and returns, otherwise decrements by one through the power-amount pipeline. The SET site (PowerCmd.cs:146) is Task 6's and is already a pinned gap there (the sim re-arms on re-stacking); this record owns the consume site only, and it is faithful."
    },
    {
      "what": "41. Back in EndEnemyTurn: `await CheckWinCondition()`; then `if (!IsEnding) { SwitchSides(); WaitForUnpause(); await StartTurn(); }` (CombatManager.cs:830-837)",
      "verdict": "faithful",
      "issue": "CLOSED. `_check_win_condition()` then the `not is_ending` gate. See guard G13.  FIXED 2026-07-29 (round 5). The text it replaced read: combat.py:673-675 tests only `phase != Phase.COMBAT_OVER`, which is a cached flag set by a previous _end_combat, not a recomputation. A death caused by an on_enemy_side_end listener (19 ported implementations) leaves phase untouched and the sim starts a fresh player turn. Same mechanism as steps 27/29/49/56 -- guard G13."
    },
    {
      "what": "42. SetReadyToEndTurn(player, canBackOut, actionDuringEnemyTurn): idempotent per player; fires PlayerEndedTurn; returns immediately unless AllPlayersReadyToEndTurn() (CombatManager.cs:684-698)",
      "verdict": "waiver",
      "rationale": "Multiplayer readiness handshake; with one player 'all players ready' is true on the first call, which is exactly `CombatState.end_turn` being invoked. The PlayerEndedTurn event is presentation (N2)."
    },
    {
      "what": "43. Choose the transition: if a player-driven action is currently running, wait for it first (WaitForActionThenEndTurn), else run AfterAllPlayersReadyToEndTurn directly. If _inPlayerTurnSetup is true the whole transition is DEFERRED into _deferredEndTurnTransition and released at every exit of StartTurn (CombatManager.cs:699-714, 722-735, 1095-1099)",
      "verdict": "faithful",
      "issue": "CLOSED. The deferral is ported (`_deferred_end_turn_transition`); WaitForActionThenEndTurn is N2. See guard N1.  FIXED 2026-07-29 (round 5). The text it replaced read: No deferral machinery in the sim. The ported Whispering Earring auto-plays real cards during what C# calls the setup window and hand-rolls a recursion guard instead (`if combat.is_over or self.turn != start_turn: break`, relics/whispering_earring.py:27-43). See guard N1."
    },
    {
      "what": "44. AfterAllPlayersReadyToEndTurn: guard IsInProgress; EndingPlayerTurnPhaseOne = true; drain the queue (WaitUntilQueueIsEmptyOrWaitingOnNonPlayerDrivenAction); run EndPlayerTurnPhaseOneInternal(); enqueue ReadyToBeginEnemyTurnAction; EndingPlayerTurnPhaseOne = false (CombatManager.cs:1101-1116)",
      "verdict": "waiver",
      "rationale": "The ActionExecutor / ActionQueueSynchronizer layer and its two EndingPlayerTurnPhase flags are the async-scheduling machinery the sim replaces with a straight-line synchronous end_turn (guard N2). The substantive body is step 45 onward."
    },
    {
      "what": "45. EndPlayerTurnPhaseOneInternal guards: null state -> return; THROW if CurrentSide != Player; WaitForUnpause (CombatManager.cs:1145-1154)",
      "verdict": "faithful",
      "rationale": "combat.py:641-642 returns from end_turn unless phase == PLAYER_TURN, which is the same precondition; the side assertion cannot be violated (see step 36)."
    },
    {
      "what": "46. Participants: the extra-turn players if any, else every player (CombatManager.cs:1155-1159)",
      "verdict": "faithful",
      "rationale": "CORRECTED IN THE FIX PASS (was 'waiver', which violated the one-mechanism-one-verdict rule -- steps 7, 15, 50, 58 and 59 are the SAME participant-set collapse and were already 'faithful', and step 58 literally reads 'participants as in step 46'). Read on the merits, 'faithful' is right and the waiver was wrong: this is not multiplayer-only machinery with no single-player counterpart (that would be step 25's readiness handshake, or step 42's) -- it is a participant selection that HAS a single-player counterpart and produces the identical observable. Verified against the source: playersEndingTurn is `_playersTakingExtraTurn.Count > 0 ? _playersTakingExtraTurn.ToList() : _state.Players.ToList()` (CombatManager.cs:1155-1159); the sim has exactly one player, so on a normal round the list is [the player] and on an extra-turn round _playersTakingExtraTurn is also [the player]. Both arms are the same one-element list, and the sim's end_turn (combat.py:639-685) runs the whole phase-one pipeline for that one player unconditionally. Same observable outcome, so 'faithful', with the multiplayer subsetting itself cited to N2."
    },
    {
      "what": "47. Per player: Phase = AutoPostPlay, then Hook.AfterAutoPostPlayPhaseEntered(ctx, state, player); in a SECOND pass, await each context and set Phase = End (CombatManager.cs:1160-1176; Hook.cs:910-920)",
      "verdict": "faithful",
      "issue": "CLOSED. Both legs. The phase transitions bracket the hook exactly as CombatManager.cs:1165/:1175 do, and the content residue was already stale -- Howl From Beyond has been on `after_auto_post_play_phase_entered` since round 3. See guard G8.  FIXED 2026-07-29 (round 5). The text it replaced read: NARROWED 2026-07-27: the PHASE HOOK now exists and is correctly placed. hooks.after_auto_post_play_phase_entered is fired at the very top of CombatState.end_turn, strictly BEFORE hooks.on_player_turn_end (sts2_rl/combat.py) -- CombatManager.cs:1160-1176's position relative to Hook.BeforeTurnEnd -- and StampedePower is wired to it (sts2_rl/powers.py), so Stampede's auto-plays now always precede Cloak Clasp's hand count. WHAT REMAINS: the other ported implementer is still on the neighbouring slot -- the Howl From Beyond card (HowlFromBeyond.cs:32 is AfterAutoPostPlayPhaseEntered) auto-plays itself from on_player_turn_end (sts2_rl/cards/howl_from_beyond.py), i.e. the sim's BeforeTurnEnd slot. ORIGINAL: neither the phase nor the hook existed and BOTH implementers were re-wired."
    },
    {
      "what": "48. `await Hook.BeforeTurnEnd(state, Player, participants)` -- the same three-pass dispatcher as step 37 (CombatManager.cs:1177-1180)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: hooks.on_player_turn_end is dispatched through HookSystem._each, which runs a complete pass per phase suffix in the order _very_early, _early, plain, _late (sts2_rl/hooks.py) -- Hook.cs:1238-1261's BeforeSideTurnEndVeryEarly -> Early -> plain shape. All three sub-phases have ported content in their real slots: Orichalcum's Block-snapshot is on_player_turn_end_very_early (sts2_rl/relics/orichalcum.py), Pael's Eye's hand exhaust is on_player_turn_end_early (sts2_rl/relics/paels_eye.py) and Cloak Clasp stays plain, so [cloak_clasp, orichalcum] and [orichalcum, cloak_clasp] now both give the game's answer. NOTE: Fake Orichalcum is still single-phase -- see guard G12, still a gap."
    },
    {
      "what": "49. `if (await CheckWinCondition()) return` -- a turn-end effect that ends the fight aborts the rest of the pipeline (CombatManager.cs:1181-1184)",
      "verdict": "faithful",
      "issue": "CLOSED. A real recomputation that returns. See guard G13.  FIXED 2026-07-29 (round 5). The text it replaced read: combat.py:655-657 tests `phase == Phase.COMBAT_OVER`, a cached flag, rather than recomputing the condition; an on_player_turn_end listener that kills the last enemy without routing through a path that calls _end_combat leaves the pipeline running. Guard G13."
    },
    {
      "what": "50. Per player: DoTurnEnd(player, ctx) (steps 51-54), each on its own choice context; then await all of them (CombatManager.cs:1185-1199)",
      "verdict": "faithful",
      "rationale": "Single-player: one call, inlined as combat.py:658 `self._process_turn_end_cards()`. Per-player choice contexts are N2."
    },
    {
      "what": "51. DoTurnEnd: `await OrbQueue.BeforeTurnEnd(ctx)`; `if (IsOverOrEnding) return` (CombatManager.cs:1216-1222)",
      "verdict": "faithful",
      "rationale": "STALE, by the entry's own logic. This entry already says it 'carries G13's verdict alongside steps 27/29/41/49/56' because its substantive leg is the SAME sim check as those steps (the `_check_win_condition()` call between `on_player_turn_end` and `_process_turn_end_cards`). All of 27/41/49/56 are now `faithful` (guard G13 closed 2026-07-29, round 5) and step29 is flipped `faithful` in this pass. EXECUTED: read `_end_turn_internal` (sts2_rl/combat.py) at the site this step maps to -- `self._check_win_condition()` immediately after `self.hooks.on_player_turn_end(self.player)` and before `self._process_turn_end_cards()` -- which is a real recompute (`_has_pending_loss` / `_all_enemies_dead()`), not the cached `phase == Phase.COMBAT_OVER` read the brief's text still describes. The brief's own citation (`combat.py:655-657`) is stale line numbers from before the round-5 rewrite. Flipped to faithful, matching G13 and steps 27/41/49/56. The orb leg (OrbQueue.BeforeTurnEnd) stays waived under N3 (Defect-only, out of scope), unchanged."
    },
    {
      "what": "52. Partition the hand IN PILE ORDER into two lists: a card with HasTurnEndInHandEffect -> turnEndCards; ELSE IF Keywords.Contains(Ethereal) && Hook.ShouldEtherealTrigger(state, card) -> the ethereal list. A card with BOTH goes into turnEndCards only (CombatManager.cs:1223-1237; CardModel.cs:1043; Hook.cs:2286-2296)",
      "verdict": "faithful",
      "rationale": "combat.py:357 builds the ethereal list as `c.is_ethereal and not c.has_turn_end_in_hand_effect` gated on should_ethereal_trigger, and combat.py:364 takes every has_turn_end_in_hand_effect card -- the same else-if partition in the same hand order."
    },
    {
      "what": "53. Exhaust EVERY ethereal card first, in order, via CardCmd.Exhaust(ctx, card, causedByEthereal: true) -- the whole ethereal pass completes before any turn-end effect runs (CombatManager.cs:1238-1241)",
      "verdict": "faithful",
      "rationale": "combat.py:357-361 is a complete loop over the ethereal list before the turn-end-effect loop at 364. Verified by execution: with a hand of [Dazed, Burn, Strike] the sequence pin records should_ethereal_trigger -> on_card_exhausted -> (Burn's) on_card_discarded -> should_flush_hand."
    },
    {
      "what": "54. Then, per turn-end card, OnTurnEndInHandWrapper(ctx): CardPileCmd.Add(this, PileType.Play) (the card sits in the Play pile for the whole effect) -> wait -> OnTurnEndInHand(ctx) -> if Ethereal CardCmd.Exhaust(ctx, this, causedByEthereal: true) ELSE CardPileCmd.Add(this, Discard). The wrapper does NOT re-consult ShouldEtherealTrigger (CombatManager.cs:1242-1245; CardModel.cs:1682-1698)",
      "verdict": "faithful",
      "issue": "CLOSED. The wrapper decides on the raw keyword. See guard G15.  FIXED 2026-07-29 (round 5). The text it replaced read: combat.py:370 DOES re-consult it: `if card.is_ethereal and self.hooks.should_ethereal_trigger(card)`. DORMANT on both sides -- `grep -rl 'override.*ShouldEtherealTrigger'` over the whole decompiled game returns ZERO files and `grep -rn 'def should_ethereal_trigger' sts2_rl/` returns only the hooks.py dispatcher, so the predicate is always true and the two branches coincide. Goes live with the first implementation on either side. The Play-pile limbo half is faithful in effect: combat.py:365 removes the card from the hand before running the effect and only files it afterwards, so a reshuffle the effect triggers cannot pick it up."
    },
    {
      "what": "55. Per player: `await Hook.BeforeFlush(state, player)` (CombatManager.cs:1200-1206; Hook.cs:532-538)",
      "verdict": "gap",
      "issue": "No before_flush hook in sts2_rl/hooks.py; there is no slot between _process_turn_end_cards (combat.py:658) and the flush (661-662). DORMANT: C#'s three implementers are SlumberingEssence.cs, WellLaidPlansPower.cs and a mock, and none is ported (verified: no sts2_rl file for either). Goes live with the first port."
    },
    {
      "what": "56. Checksum, then `await CheckWinCondition()` (CombatManager.cs:1207-1208)",
      "verdict": "faithful",
      "issue": "CLOSED. A real recomputation. See guard G13.  FIXED 2026-07-29 (round 5). The text it replaced read: combat.py:659-660 tests the cached phase again rather than recomputing. Guard G13; the checksum is N2."
    },
    {
      "what": "57. AfterAllPlayersReadyToBeginEnemyTurn: guard IsInProgress; EndingPlayerTurnPhaseTwo = true; queue -> NotPlayPhase; AboutToSwitchToEnemyTurn event; `await Task.Yield()`; EndPlayerTurnPhaseTwoInternal(); SwitchFromPlayerToEnemySide() (CombatManager.cs:1259-1272)",
      "verdict": "waiver",
      "rationale": "Action-queue mode switching, a C# event and a scheduler yield -- guard N2. The two substantive calls are steps 58-64 and step 65."
    },
    {
      "what": "58. EndPlayerTurnPhaseTwoInternal: THROW if CurrentSide != Player; participants as in step 46 (CombatManager.cs:1279-1289)",
      "verdict": "faithful",
      "rationale": "Single-player participant list; the side assertion cannot be violated (step 36)."
    },
    {
      "what": "59. Per player: FlushPlayerHand(player, ctx) (steps 60-63); then await all (CombatManager.cs:1290-1304)",
      "verdict": "faithful",
      "rationale": "Single-player: one call, combat.py:661-662."
    },
    {
      "what": "60. FlushPlayerHand guards: player.Creature.IsDead -> return; null state -> warn and return (CombatManager.cs:1313-1323)",
      "verdict": "faithful",
      "issue": "CLOSED. Covered by `discard_hand`'s IsOverOrEnding guard in a one-player sim, and pinned by test. See guard G13.  FIXED 2026-07-29 (round 5). The text it replaced read: player.discard_hand (player.py:188-197) has no is_dead guard. Same mechanism and same verdict as step 16: dormant on the ordinary path (combat.py:655-660 returns first if a turn-end effect killed the player) but part of the same family as guard G13's live case."
    },
    {
      "what": "61. `flag = Hook.ShouldFlush(state, player)`; per card IN HAND ORDER: `!flag || card.ShouldRetainThisTurn` -> cardsToRetain, else cardsToFlush. A false ShouldFlush retains the WHOLE hand (CombatManager.cs:1327-1338; Hook.cs:2301-2311)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: CombatState.end_turn now calls `self.player.discard_hand(flush=self.hooks.should_flush_hand())` instead of gating the call itself, and PlayerCombatState.discard_hand treats flush=False as 'every card is retained' -- cardsToFlush is empty, the batched move is skipped, and the TAIL still runs (sts2_rl/combat.py, sts2_rl/player.py) -- which is CombatManager.cs:1327-1346's shape. The sim's on_hand_emptied therefore fires either way, so Joss Paper's deferred Ethereal-exhaust credit is no longer stranded by Runic Pyramid or Ringing Triangle. The card partition on card.retain was already faithful. NOTE: AfterFlush and EndOfTurnCleanup still have no sim counterpart at all -- steps 63 and 38, guard G7."
    },
    {
      "what": "62. `if (cardsToFlush.Count > 0) await CardPileCmd.Add(cardsToFlush, PileType.Discard)` -- one batched Add for the whole flush (CombatManager.cs:1339-1343)",
      "verdict": "faithful",
      "rationale": "player.py:193-196 moves all flushed cards in one operation. The per-card hook order inside the move (on_card_discarded firing before the cards actually change piles) is creature_card_cmds' G11 and is not re-verdicted here."
    },
    {
      "what": "63. `await Hook.AfterFlush(state, player, ctx, cardsToFlush, cardsToRetain)` -- UNCONDITIONAL, fired even when nothing was flushed -- then PlayerCombatState.EndOfTurnCleanup(), also unconditional and the SECOND of its two per-round sites (CombatManager.cs:1344-1346; Hook.cs:560-570)",
      "verdict": "gap",
      "live": false,
      "issue": "NARROWED 2026-07-29: the EndOfTurnCleanup half is CLOSED (guard G7, closed round 5) -- `PlayerCombatState.discard_hand` (sts2_rl/player.py) calls `self.end_of_turn_cleanup()` unconditionally as its last line, the second of C#'s two per-round sites, verified by reading the source today. What remains is AfterFlush alone: no `after_flush` hook exists in sts2_rl/hooks.py (`grep -n after_flush` finds only comments citing it as still missing). DORMANT, enumerated by execution: `grep -rl 'override.*Task AfterFlush' src/` over the decompiled game returns exactly ONE file, `Bookmark.cs` (a Rare relic that shaves 1 energy cost off a random retained card with nonzero cost) -- and `grep -rn bookmark sts2_rl/` (case-insensitive) returns zero hits: Bookmark is not ported. So AfterFlush's sole C# implementer has no sim counterpart to observe the missing hook. The sim's `on_hand_emptied` is confirmed NOT AfterFlush's counterpart -- player.py's own comment says C# excludes the flush from `CheckForEmptyHand` (CombatManager.cs:880-883) and the sim now matches (guard G16, closed). Guard G7 is closed; this entry now only carries the AfterFlush half."
    },
    {
      "what": "64. `await Hook.AfterTurnEnd(state, Player, participants)` -- the player-side AfterSideTurnEnd/AfterSideTurnEndLate pair, AFTER the hand flush (the Parrying Shield slot) (CombatManager.cs:1305-1310)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27 on both of its legs. (1) The AfterSideTurnEndLate pass exists: hooks.after_player_turn_end goes through HookSystem._each, and DisintegrationPower is wired to after_player_turn_end_late (sts2_rl/hooks.py, sts2_rl/powers.py) -- Hook.cs:1265-1291's two-pass shape. (2) The hook is no longer downstream of a skippable flush: end_turn always calls discard_hand (with flush as an argument) before firing after_player_turn_end (sts2_rl/combat.py), so guard G4's skip cannot swallow it. The placement after the flush was already faithful and Parrying Shield still depends on it."
    },
    {
      "what": "65. SwitchFromPlayerToEnemySide: null state -> return; clear _playersTakingExtraTurn, then per player Hook.ShouldTakeExtraTurn(state, player) -> repopulate it. Evaluated at the very END of the turn-end pipeline, after steps 42-64 have all run (CombatManager.cs:1356-1373; Hook.cs:2457-2467)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: CombatState.end_turn now evaluates hooks.should_take_extra_turn at the BOTTOM of the turn-end pipeline (sts2_rl/combat.py) -- after the auto-post-play phase, on_player_turn_end, the turn-end card effects, the flush and after_player_turn_end -- and an extra turn skips only the enemy side, which is CombatManager.cs:1360-1373's position inside SwitchFromPlayerToEnemySide. With Pael's Eye held, the full turn-end trace now runs. WAS: the predicate was tested at the TOP of end_turn and short-circuited the entire pipeline."
    },
    {
      "what": "66. SwitchSides(): on the player side with no extra turn -> CurrentSide = Enemy. Otherwise -> CurrentSide = Player, and IncrementTurnNumber() for the extra-turn players; on a NORMAL round it is every player AND RoundNumber++. An extra turn therefore advances TurnNumber but NOT RoundNumber (CombatManager.cs:1387-1419; PlayerCombatState.cs:157-160)",
      "verdict": "faithful",
      "issue": "CLOSED. `turn` and `round_number` are two counters and SwitchSides' two arms advance them differently. See guard N4.  FIXED 2026-07-29 (round 5). The text it replaced read: CombatState.turn (combat.py:108) is one counter incremented on every player turn including extra turns (combat.py:650, 674). See guard N4."
    },
    {
      "what": "67. `foreach (creature in _state.Creatures) creature.OnSideSwitch()` -- a monster clears SpawnedThisTurn, a player does nothing; then the TurnEnded event (CombatManager.cs:1420-1424; Creature.cs:694-704; MonsterModel.cs:479-483)",
      "verdict": "gap",
      "live": false,
      "issue": "RE-VERIFIED 2026-07-29: still true. `grep -rn spawned_this_turn sts2_rl/` returns 0 hits, and `grep -rn 'side_switch\\|OnSideSwitch' sts2_rl/*.py` finds only comments citing the C# source (SwitchSides), never a verb -- no `on_side_switch` dispatcher exists in sts2_rl/hooks.py. Same mechanism as step 32 (turn_structure/step32, which owns the reachability analysis: this entry carries its verdict, per the brief's own mechanism assignment and binding rule 3). Step 32's own DORMANT enumeration (unchanged, re-read this pass, still current): SetUpForCombat sets SpawnedThisTurn for every monster, SwitchSides clears it for every creature at every side switch including extra turns, and a monster spawned during the enemy side already falls outside the per-turn snapshot (step 31) -- so no reachable C# path was found where the flag survives to a `TakeTurn` that reads it. `live: false` added to match step 32's dormancy finding, since step 32 itself did not carry an explicit `live` key despite its prose already being DORMANT (a formatting gap in step 32's own entry, out of this batch's scope to fix -- see SURPRISES)."
    },
    {
      "what": "68. Per extra-turn player: `await Hook.AfterTakingExtraTurn(state, player)`; then WaitForUnpause() and `await StartTurn(actionDuringEnemyTurn)` (CombatManager.cs:1375-1384; Hook.cs:1220-1226)",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: the fold is undone. Pael's Eye's hand exhaust moved to its real slot, on_player_turn_end_early (sts2_rl/relics/paels_eye.py, mirroring PaelsEye.cs:118-128's BeforeSideTurnEndEarly), so hooks.on_extra_turn now carries only the AfterTakingExtraTurn bookkeeping and is fired after the whole turn-end pipeline and before the fresh start_turn (sts2_rl/combat.py) -- the order CombatManager gives Hook.AfterTakingExtraTurn then StartTurn."
    },
    {
      "what": "69. IsEnding: !IsInProgress -> false; _pendingLoss != null -> true; any (e.IsAlive && e.IsPrimaryEnemy) -> false; Hook.ShouldStopCombatFromEnding(state) -> false; else true. IsOverOrEnding is IsEnding || !IsInProgress (CombatManager.cs:180-220; Creature.cs:252-277; Hook.cs:2442-2452)",
      "verdict": "faithful",
      "issue": "CLOSED. The pending-loss two-step exists (`lose_combat` / `_process_pending_loss`), so `is_ending` reads `_has_pending_loss or _all_enemies_dead()`. See guard G10.  FIXED 2026-07-29 (round 5). The text it replaced read: NARROWED 2026-07-27: Hook.ShouldStopCombatFromEnding now HAS a sim dispatcher. hooks.should_stop_combat_from_ending returns True on the first listener that does (sts2_rl/hooks.py) and CombatState._all_enemies_dead consults it as the last gate after the primary-enemy test (sts2_rl/combat.py), which is CombatManager.cs:196's position; AdaptablePower and InfestedPower are its ported implementers. The primary-enemy predicate itself was already faithful (guard N5). WHAT REMAINS: the pending-loss two-step -- C# has `_pendingLoss != null -> true` in IsEnding and processes the loss on the next CheckWinCondition, where the sim ends the combat synchronously inside _end_combat. Guard G10."
    },
    {
      "what": "70. CheckWinCondition: a pending loss -> ProcessPendingLoss() (null the pending loss, IsInProgress = false, fire the CombatEnded C# EVENT only -- no hook whatsoever) and return true; else IsEnding -> EndCombatInternal() and return true; else false (CombatManager.cs:1046-1059, 945-965)",
      "verdict": "faithful",
      "issue": "CLOSED. Two asymmetric exits, and the losing one fires no hook. See guard G10.  FIXED 2026-07-29 (round 5). The text it replaced read: The sim's _end_combat (combat.py:347-350) fires on_combat_end(player_won) for BOTH outcomes, where C# fires no hook at all on a loss. Guard G10."
    },
    {
      "what": "71. EndCombatInternal (the VICTORY path): record TurnNumber; IsInProgress = false; phase None; PlayerActionsDisabled = false; clear extra turns; per player `await ReviveBeforeCombatEnd()`; Hook.AfterCombatEnd(runState, state, room); History.Clear(); room.OnCombatEnded(); write the replay; per player AfterCombatEnd(); Hook.AfterCombatVictory(runState, state, room); hover-tips, turn accounting, saves, achievements, CombatWon and CombatEnded events (CombatManager.cs:970-1033; Hook.cs:328-336, 340-348)",
      "verdict": "faithful",
      "issue": "CLOSED. Revive, then Hook.AfterCombatEnd, then Hook.AfterCombatVictory. See guard G10; the saves/achievements/event legs remain N2.  FIXED 2026-07-29 (round 5). The text it replaced read: _end_combat collapses AfterCombatEnd and AfterCombatVictory into one on_combat_end(player_won) and has no ReviveBeforeCombatEnd step. Six C# relics implement AfterCombatVictory and four are ported (Black Blood, Burning Blood, Meat on the Bone, plus Belt Buckle / Sword of Stone / War Hammer). Guard G10."
    },
    {
      "what": "72. LoseCombat() only RECORDS _pendingLoss = new PendingLossState(state, room) (idempotent); the loss is processed at the next CheckWinCondition. HandlePlayerDeath(player) separately removes every card in all five piles from combat and zeroes energy and stars (CombatManager.cs:923-951)",
      "verdict": "faithful",
      "issue": "CLOSED. `lose_combat()` is idempotent and only marks. HandlePlayerDeath's card/energy/star teardown stays unobservable (step 74). See guard G10.  FIXED 2026-07-29 (round 5). The text it replaced read: The sim has neither the deferral nor HandlePlayerDeath's card/energy/star teardown; a lost combat just flips phase and result. Guard G10; the teardown half is unobservable because the sim discards the whole CombatState afterwards (step 74)."
    },
    {
      "what": "73. CheckForEmptyHand(ctx, player): `IsInProgress && !IsExecutingCardOrPotionEffect(player) && hand is empty` -> Hook.AfterHandEmptied. Called ONLY after a card play and after a potion use -- explicitly NOT from ending the turn (CombatManager.cs:887-893; Hook.cs:611-621)",
      "verdict": "faithful",
      "issue": "CLOSED. Both call sites, and the full `!IsExecutingCardOrPotionEffect` gate. See guard G16.  FIXED 2026-07-29 (round 5). The text it replaced read: NARROWED 2026-07-27: one of C#'s two real call sites now exists. CombatState gained _check_for_empty_hand(), which skips when the combat is over and otherwise fires on_hand_emptied on an empty hand, and use_potion calls it after the potion resolves (sts2_rl/combat.py) -- PotionModel.cs:340's site. WHAT REMAINS: (a) the CARD-PLAY site (CardModel.cs:1992) has no sim caller -- _check_for_empty_hand has exactly one call site (verified by grep) -- so the hook is still never fired after a card play; (b) PlayerCombatState.discard_hand still fires on_hand_emptied from the end-of-turn flush, the one site CombatManager.cs:880-883 deliberately excludes and UnceasingTop.cs:25-35 explains why; (c) there is no IsExecutingCardOrPotionEffect / PlayerTurnPhase gate. Unceasing Top's port compensates by living on on_card_played. Guard G16."
    },
    {
      "what": "74. Reset(graceful): cancel the CT; per creature Reset() + RemoveCreature + state.RemoveCreature; _state = null; _pendingLoss = null; DebugForcedTopCardOnNextShuffle = null; IsInProgress/IsStarting/IsEnemyTurnStarted = false; History.Clear(); clear _cardOrPotionEffectDepth (CombatManager.cs:899-921)",
      "verdict": "deliberate-divergence",
      "rationale": "C# reuses one CombatManager singleton across a run and must tear its state back down; the sim constructs a fresh CombatState per combat (RunState.create_combat) and lets the old one be collected, so every field this method clears is already gone. Same observable outcome by construction, deliberately different architecture."
    }
  ],
  "guards": [
    {
      "what": "G1: Hook.AfterBlockCleared is a separate UNCONDITIONAL loop; the sim fires on_block_cleared only when the block was actually cleared",
      "verdict": "faithful",
      "issue": "CLOSED. The remaining half -- the two complete passes -- is now what the sim does: `_run_enemy_turns` clears block for every captured participant (CombatManager.cs:492-499), THEN fires `on_block_cleared` for every one of them (:500-507), THEN dispatches the side-start hook, and only then walks the enemies for their moves. Enemy N is unblocked before enemy 1 acts. EXECUTED: test_enemy_side_per_side.py::test_three_complete_passes_then_one_side_start and ::test_every_enemy_is_unblocked_before_any_of_them_moves.  FIXED 2026-07-28 (round 4). The text it replaced read: G1 has two halves and only one closed. See steps[13] for the full derivation. CLOSED (the condition): both sides now fire on_block_cleared unconditionally, outside the should_clear_block arm -- PlayerCombatState.start_turn (sts2_rl/player.py) and CombatState._run_enemy_turns (sts2_rl/combat.py:358-363), the latter with its extra `enemy.block > 0` gate removed. STILL OPEN (the two loops): CombatManager.cs:492-497 and :500-507 are two complete passes over `creaturesStartingTurn` that both finish before ExecuteEnemyTurn, whereas the sim clears and fires inside the per-enemy turn loop, so enemy N's block clear lands after enemies 1..N-1 have moved. Dormant today only because the sole ported cross-enemy block grant (Guardbot -> Fabricator) spawns after its target in the sim's enemy list. WAS: the clear and the event were fused into one arm, which is the half that is fixed."
    },
    {
      "what": "G2: there is no after_preventing_block_clear hook and no `preventer` identity; Sturdy Clamp hand-rolls its cap onto on_player_turn_start and applies it unconditionally",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: hooks.should_clear_block takes an out-param `preventer` list and the sim gained hooks.after_preventing_block_clear, which dispatches ONLY to the listener that vetoed (sts2_rl/hooks.py) -- Creature.cs:718-728's else-arm. Sturdy Clamp's port now caps the retained block from after_preventing_block_clear (sts2_rl/relics/sturdy_clamp.py), so reaching the body already means it was the preventer, which is what SturdyClamp.cs:31-46's `if (this != preventer ...) return` encodes. With Barricade also held, Barricade is the first vetoer and Sturdy Clamp's cap does not run. Pinned by test_sturdy_clamp_does_not_cap_when_it_is_not_the_preventer, which now passes. WAS: no hook, no preventer identity, and an unconditional cap hand-rolled onto turn start."
    },
    {
      "what": "G3: the extra-turn check short-circuits the ENTIRE turn-end pipeline",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: CombatState.end_turn runs the complete turn-end pipeline -- auto-post-play phase, on_player_turn_end, turn-end card effects, discard_hand, after_player_turn_end -- and only THEN evaluates hooks.should_take_extra_turn, which skips the enemy side alone (sts2_rl/combat.py). That is CombatManager.cs:1360-1373's position, after both EndPlayerTurnPhaseOne and PhaseTwo. Pael's Eye's own hand exhaust moved to on_player_turn_end_early rather than riding on_extra_turn. Pinned by test_extra_turn_still_runs_the_turn_end_pipeline, which now passes. WAS: the predicate was tested at the top of end_turn and everything after it was skipped."
    },
    {
      "what": "G4: a false ShouldFlush skips the whole flush tail (AfterFlush, EndOfTurnCleanup and the sim's on_hand_emptied), where C# runs it unconditionally",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: the predicate became an ARGUMENT rather than a gate. end_turn calls `self.player.discard_hand(flush=self.hooks.should_flush_hand())` and discard_hand treats flush=False as C# does -- cardsToFlush empty, batched move skipped, TAIL still run (sts2_rl/combat.py, sts2_rl/player.py) -- matching CombatManager.cs:1327-1346. The live path this guard identified is closed: on_hand_emptied now fires whether or not the flush happened, so Joss Paper's deferred Ethereal credit is delivered behind Runic Pyramid and Ringing Triangle. AfterFlush and EndOfTurnCleanup are still absent, but they were never this guard's live leg -- they remain step 63 and guard G7."
    },
    {
      "what": "G5: the enemy side is per-enemy in the sim and per-side in the game",
      "verdict": "faithful",
      "issue": "CLOSED. `_run_enemy_turns` is one SIDE turn, not N creature turns: capture the participants (CombatManager.cs:444), Hook.BeforeSideTurnStart once (:458), a complete block-clear pass (:492-499), a complete AfterBlockCleared pass (:500-507), Hook.AfterSideTurnStart once (:522), CheckWinCondition (:598), the moves (:1072-1090), then Hook.BeforeTurnEnd (:1251) and Hook.AfterTurnEnd (:1256) once each. The sim's two per-creature slots are GONE, which is the right outcome rather than a re-slotting: C# has no per-creature turn-start or turn-end hook at all, so every listener that sat on them was implementing a side hook. Fifteen powers were moved, each read off its own `public override`: Regen/Ritual/Hatch/Slumber/EscapeArtist/Asleep/BattlewornDummyTimeLimit to `on_enemy_side_end` (AfterSideTurnEnd), Poison/DemonForm/Slow/Plating-decay to `after_enemy_side_start`, Sandpit to `after_enemy_side_start_late`, HardenedShell to `before_enemy_side_start`, Asleep's Plating-drop to `before_enemy_side_end_very_early` and Plating's block grant to `before_enemy_side_end_early`. TWO REAL BEHAVIOUR CHANGES fell out and are confirmed against source: (a) a Tough Egg laid during the enemy side now takes that same side's Hatch decrement, which is exactly why ToughEgg.cs:133 applies 2 on the enemy side and 1 elsewhere; and (b) RegenPower's `!Owner.IsDead` (RegenPower.cs:22) had to be ported, because the per-enemy loop used to have returned before a dead owner could heal. The order trace that pinned the divergence (test_enemy_side_is_interleaved_per_enemy) was replaced by test/test_enemy_side_per_side.py.  FIXED 2026-07-28 (round 4). The text it replaced read: NARROWED 2026-07-28 (round 3): the PLAYER side's two side-scoped hooks now exist. `before_side_turn_start` (CombatManager.cs:458) and `after_side_turn_start` (:522) are real sim dispatchers fired from PlayerCombatState.start_turn, so the player leg of every BeforeSideTurnStart/AfterSideTurnStart listener is no longer mapped onto a neighbouring slot. WHAT REMAINS is exactly this guard's own subject and is untouched: _run_enemy_turns still does [clear block -> on_enemy_turn_start -> move -> on_enemy_turn_end] per enemy where C# runs three complete passes over every participant, then ONE Hook.AfterSideTurnStart, then the moves. The two new dispatchers are deliberately player-side only for that reason -- there is no side-level point on the enemy side to hang them on until this guard is fixed, and hooks.py says so at the map. The dormancy argument and the passing order trace (test_enemy_side_is_interleaved_per_enemy) are unchanged.  NARROWED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass; what follows is what is still open. The issue it replaced read: MECHANISM: C# has no per-creature turn-start or turn-end hook at all -- BeforeTurnStart (449-455), AfterTurnStart/ClearBlock (492-499) and AfterBlockCleared (500-507) each run as a complete pass over every participant, then Hook.AfterSideTurnStart fires ONCE (522), then the moves (1072-1090), then one Hook.BeforeTurnEnd (1251) and one Hook.AfterTurnEnd (1256). The sim's _run_enemy_turns (combat.py:286-345) does [clear block -> on_enemy_turn_start -> move -> on_enemy_turn_end] per enemy and only on_enemy_side_end once. DORMANT, reachability checked rather than assumed: every ported listener on these hooks self-filters to its own owner -- all 7 on_enemy_turn_start and all 8 on_enemy_turn_end implementations in sts2_rl/powers.py open with `if self.owner is enemy` / `if enemy is self.owner`, and ZERO relic, card, enchantment or potion implements any of the three (verified by grep: the only definitions outside hooks.py are in powers.py). So the N-fold dispatch is neutralised. It goes live the moment (a) any cross-enemy effect touches a LATER-indexed enemy's block or turn-start power, or (b) PoisonPower becomes reachable -- it is ported (powers.py:484-490) and C# fires it from AfterSideTurnStart (PoisonPower.cs:55-73), i.e. for ALL enemies before ANY move, but a grep of sts2_rl for PoisonPower outside powers.py finds only the __init__.py re-export, so nothing applies it today. CORRECTION to the inherited doc, which said 'nine on_enemy_turn_end implementations': there are eight. Pinned as a PASSING order trace so that a future fix has to change it deliberately: test_enemy_side_is_interleaved_per_enemy."
    },
    {
      "what": "G6: the sim clears the player's block on turn 1; the game does not",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: PlayerCombatState.start_turn guards the whole clear with `if not self._first_turn:` (sts2_rl/player.py), reproducing Creature.cs:681-692's early return for a player whose TurnNumber == 1. Block granted before the first turn now survives into the first enemy turn. Pinned by test_player_block_is_not_cleared_on_turn_one, which now passes."
    },
    {
      "what": "G7: PlayerCombatState.EndOfTurnCleanup has no sim counterpart at either of its two sites",
      "verdict": "faithful",
      "issue": "CLOSED. PlayerCombatState.EndOfTurnCleanup is ported at BOTH C# sites and the three single-turn card states it exists to clear now exist. `Card.end_of_turn_cleanup` (sts2_rl/cards/base.py) clears `exhaust_on_next_play`, `_has_single_turn_retain`, `_has_single_turn_sly` and then the turn-scoped cost modifiers (CardModel.cs:1610-1623, CardEnergyCost.cs:331-335); `PlayerCombatState.end_of_turn_cleanup` walks `all_cards`, i.e. every pile (PlayerCombatState.cs:268-274). It is called from `discard_hand`'s tail (FlushPlayerHand, CombatManager.cs:1346) and from `_run_enemy_turns` BETWEEN the two enemy-side hooks (EndEnemyTurnInternal, :1252-1255). The start-of-turn `reset_turn_cost_modifiers` loop is GONE: the sim's reset window was a full enemy turn wider than the game's. `should_retain_this_turn` (CardModel.cs:590-600) is what the flush partitions on now, and `exhaust_on_next_play` is consumed where GetResultPileTypeForCardPlay consumes it (:2076-2078), before OnPlay rather than after. `is_sly_this_turn` (:619-629) exists with no consumer, because no sim content grants or reads Sly -- the STATE is here because EndOfTurnCleanup's job is to clear it.  FIXED 2026-07-29 (round 5). The text it replaced read: MECHANISM: C# runs it twice per round -- at the end of the enemy turn for every player (CombatManager.cs:1252-1255) and inside each player's FlushPlayerHand (1346). Per card in EVERY pile it clears ExhaustOnNextPlay, HasSingleTurnRetain, HasSingleTurnSly and the turn-scoped cost modifiers (PlayerCombatState.cs:268-274; CardModel.cs:1610-1623). The sim's only per-turn card reset is Card.reset_turn_cost_modifiers (cards/base.py:265-269), which clears _cost_delta_this_turn / _free_this_turn / _cost_this_turn and nothing else, and it runs at the START of the next player turn (player.py:153-155) rather than at either end-of-turn site. Two consequences: the reset window is a full enemy turn wider than the game's, and there is no single-turn Retain / single-turn Sly / ExhaustOnNextPlay state at all. DORMANT, and the dormancy evidence was RE-EXECUTED in the fix pass rather than inherited: `grep -rn --include=*.py <field> sts2_rl/` gives `has_single_turn_retain 0 hits`, `single_turn_sly 0 hits`, `exhaust_on_next_play 0 hits` -- so no ported effect can grant any of the three states EndOfTurnCleanup exists to clear. The counterpart grep for the one reset the sim does have, `grep -rn --include=*.py reset_turn_cost_modifiers sts2_rl/`, returns exactly four lines: cards/base.py:118 (a comment), cards/base.py:245 (the constructor), cards/base.py:265 (the definition) and player.py:155 (its ONE call site, inside start_turn) -- confirming there is no end-of-turn site on either of C#'s two. ExhaustOnNextPlay's only C# setter is CardPileCmd.AutoPlayFromDrawPile(forceExhaust), unported. Goes live with the first single-turn Retain/Sly grant, or with any effect that reads a cost modifier during the enemy turn."
    },
    {
      "what": "G8: the AutoPrePlay and AutoPostPlay phases do not exist; their two hooks are hand-rolled onto neighbouring slots",
      "verdict": "faithful",
      "issue": "CLOSED. The six-value PlayerTurnPhase model exists: sts2_rl/turn_phase.py is a direct port of PlayerTurnPhase.cs, an IntEnum in the game's declaration order because UnceasingTop.IsValidPhase (:29-36) compiles to `(uint)(phase - 2) <= 2u` and the ORDINALS are load-bearing. `PlayerCombatState.turn_phase` is driven at every C# transition: None at StartTurn's head on both sides (CombatManager.cs:429), Start after Hook.BeforeSideTurnStart (:464), AutoPrePlay/Play bracketing the auto-pre-play hook (:616/:618), AutoPostPlay/End bracketing the auto-post-play hook (:1165/:1175), and None again at EndCombatInternal (:978). Unceasing Top reads it. The content residue this guard listed was ALREADY STALE when round 4 wrote it: Howl From Beyond, Whispering Earring and the Imbued enchantment were all on their real phase hooks -- verified again by reading the three files.  FIXED 2026-07-29 (round 5). The text it replaced read: NARROWED 2026-07-28 (round 3), and the content residue this guard listed was already stale -- verified by reading the three files. Howl From Beyond implements `after_auto_post_play_phase_entered`, Whispering Earring `after_auto_pre_play_phase_entered_late` and the Imbued enchantment `after_auto_pre_play_phase_entered`: all three are on their real phase hooks, not on neighbouring slots. This round closed the last leg of the AutoPrePlay witness: Crossbow really is Hook.AfterSideTurnStart (Crossbow.cs:17) and moved onto the new `after_side_turn_start` dispatcher, which PlayerCombatState.start_turn fires strictly before the AutoPrePlay phase -- so 'C# runs Crossbow first' is structural now rather than registration luck. WHAT REMAINS is the guard's other half only: the six-value PlayerTurnPhase model itself is still absent (steps 6, 10, 28, 43).  NARROWED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass; what follows is what is still open. The issue it replaced read: NARROWED 2026-07-27: BOTH PHASES NOW EXIST as real engine steps. hooks.after_auto_pre_play_phase_entered is fired from PlayerCombatState.start_turn strictly after the post-draw turn-start slot (CombatManager.cs:556-572), and hooks.after_auto_post_play_phase_entered is fired at the top of CombatState.end_turn strictly before hooks.on_player_turn_end (CombatManager.cs:1160-1176) -- sts2_rl/player.py and sts2_rl/combat.py. Two implementers are wired to their real phases: StampedePower (after_auto_post_play_phase_entered) and MayhemPower (after_auto_pre_play_phase_entered), which closes this guard's AutoPostPlay witness -- Stampede's auto-plays now always precede Cloak Clasp's hand count. WHAT REMAINS is content still hand-rolled onto neighbouring slots: Howl From Beyond (HowlFromBeyond.cs:32, AfterAutoPostPlayPhaseEntered) auto-plays from on_player_turn_end; Whispering Earring (WhisperingEarring.cs:41, AfterAutoPrePlayPhaseEnteredLate) and the Imbued enchantment (Imbued.cs:20) both auto-play from on_player_turn_started. Crossbow really is AfterSideTurnStart (Crossbow.cs:17), so the AutoPrePlay witness is intact: all three sit on one hook and their order is registration luck where C# runs Crossbow strictly first. The six-value PlayerTurnPhase model itself is still absent (steps 6, 10, 28, 43). ORIGINAL: neither phase nor either hook existed."
    },
    {
      "what": "G9: enemy intents are rolled at the end of each monster's own move, not in one pass at the start of the player's turn",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: CombatState._roll_enemy_intents (sts2_rl/combat.py) is one pass over the enemy list, in list order, run from combat setup and from the top of each fresh player turn in end_turn -- and NOT from the extra-turn branch, matching CombatManager.cs:478-484's `if (!isExtraPlayerTurn)`. telegraph_next_move now has exactly one engine call site (verified by grep) and MachineMonster.take_turn no longer advances the machine, so the roll is no longer tacked onto a monster's own move. A stunned enemy performs its synthetic STUNNED move and is rolled by the next pass like any other, which removes the one-draw MonsterAi desync this guard measured. `performed_first_move` still stands in for FindNextMoveState's `!_performedFirstMove && IsMove -> return` guard (MonsterMoveStateMachine.cs:60-63). The remaining spawn-roll placement (a fresh monster rolls in its constructor rather than from a side-gated AfterCreatureAdded) is monster_state_machine's guard G9, not this one."
    },
    {
      "what": "G10: the combat-end path collapses five C# distinctions into one hook, and the sim's two player-death exits disagree with each other",
      "verdict": "faithful",
      "issue": "CLOSED. The combat-end path keeps C#'s distinctions. `_check_win_condition` is CheckWinCondition (CombatManager.cs:1046-1059): the pending loss is tested FIRST, so it wins a simultaneous death. `_process_pending_loss` (ProcessPendingLoss, :956-965) sets the phase and result and fires NO HOOK AT ALL. `_end_combat_internal` (EndCombatInternal, :970-1030) revives a dead player to 1 HP (ReviveBeforeCombatEnd, Player.cs:821-827) BEFORE any hook, then fires `on_combat_end` (Hook.AfterCombatEnd, :988) and then `on_combat_victory` (Hook.AfterCombatVictory, :999, two passes via the `_early` suffix walk). `lose_combat()` is the deferral: LoseCombat (:945-951) only MARKS, because :941-943 says the processing must wait for a safe point. Four relics moved onto their real hooks: Chosen Cheese is AfterCombatEnd (ChosenCheese.cs:16), Burning Blood and Black Blood are AfterCombatVictory, Meat on the Bone is AfterCombatVictoryEarly -- and none of them tests `player_won` any more, because the losing path has no dispatch to test. Part (d) was stale: round 4's per-side rewrite of `_run_enemy_turns` had already removed the two disagreeing player-death exits.  FIXED 2026-07-29 (round 5). The text it replaced read: NARROWED 2026-07-27: one of the five distinctions is now modelled. Hook.ShouldStopCombatFromEnding has a sim dispatcher, hooks.should_stop_combat_from_ending, consulted by CombatState._all_enemies_dead as the last gate after the primary-enemy test (sts2_rl/hooks.py, sts2_rl/combat.py) -- CombatManager.cs:196's position -- with AdaptablePower and InfestedPower as ported implementers. WHAT REMAINS, all re-verified against today's code: (a) _end_combat still fires on_combat_end(player_won) on BOTH outcomes where a C# LOSS fires no hook at all (CombatManager.cs:945-965); (b) there is no pending-loss deferral and no ReviveBeforeCombatEnd; (c) AfterCombatEnd and AfterCombatVictory are still one hook; (d) _run_enemy_turns STILL has two disagreeing player-death exits -- the on_enemy_turn_start arm calls _end_combat(player_won=False) while the post-move arm sets phase and result by hand and returns without firing on_combat_end (sts2_rl/combat.py). ORIGINAL: all five distinctions were collapsed and there was no should_stop_combat_from_ending hook at all."
    },
    {
      "what": "G11: Hook.BeforeTurnEnd(Enemy) has no sim counterpart",
      "verdict": "faithful",
      "issue": "CLOSED. `before_enemy_side_end` is Hook.BeforeTurnEnd for the enemy side (CombatManager.cs:1251), fired once from `_run_enemy_turns` immediately before `on_enemy_side_end` (AfterTurnEnd, :1256), and it gets the _very_early/_early/plain/_late suffix walk from `HookSystem._each` like every other dispatcher. Both ported enemy-side listeners this guard enumerated are on it at their real phases: AsleepPower's Plating-drop at `before_enemy_side_end_very_early` (AsleepPower.cs:38) and PlatingPower's block grant at `before_enemy_side_end_early` (PlatingPower.cs:61). EXECUTED: test_enemy_side_per_side.py::test_asleep_drops_plating_before_plating_grants_it.  FIXED 2026-07-28 (round 4). The text it replaced read: MECHANISM: C# fires the same three-pass BeforeTurnEnd dispatcher for the ENEMY side at CombatManager.cs:1251, immediately before EndOfTurnCleanup and AfterTurnEnd. The sim's _run_enemy_turns has only the per-enemy on_enemy_turn_end (combat.py:341) and the side-scoped on_enemy_side_end (345), which maps to AfterTurnEnd; there is no slot between them. DORMANT, and the inherited content list was WRONG -- re-enumerated by execution in the fix pass. `grep -rln 'BeforeSideTurnEndVeryEarly|BeforeSideTurnEndEarly|override.*Task BeforeSideTurnEnd' src/Core/Models/Powers/` over the decompiled game returns eight files: AsleepPower (VeryEarly, 38), PlatingPower (Early, 61), ChainsOfBindingPower (80), DoomPower (68), HailstormPower (30), SandpitPower (139), TheBombPower (38) and a mock. Of those, DoomPower and HailstormPower -- both named in the inherited rationale as ported -- are NOT ported (`grep -n '^class DoomPower|^class HailstormPower' sts2_rl/powers.py` returns nothing); ChainsOfBindingPower (powers.py:3292) and TheBombPower (powers.py:3748) ARE ported but their BeforeSideTurnEnd is the PLAYER side, and both sit on on_player_turn_end (powers.py:3331, 3779), so neither touches this enemy-side slot. The genuinely ported ENEMY-side content is two powers, each owner-filtered: PlatingPower's BeforeSideTurnEndEarly block grant went onto on_enemy_turn_end (powers.py:1076-1078, `if enemy is self.owner`), and AsleepPower's BeforeSideTurnEndVeryEarly Plating-drop went onto on_enemy_turn_START (powers.py:1853-1857, `if enemy is self.owner and self.amount <= 1`, comment 'Mirrors BeforeSideTurnEndVeryEarly') -- one slot further from the C# position than the inherited text said. With one enemy the per-enemy slot and the side slot coincide, so nothing is observable today. It goes live in any multi-enemy fight where one of those powers must act after the LAST enemy's move rather than after its own."
    },
    {
      "what": "G12: the sub-phase ordering inside BeforeTurnEnd / AfterTurnEnd / AfterSideTurnStart is flattened into single listener walks",
      "verdict": "faithful",
      "issue": "CLOSED, and one third of it was STALE rather than open. (a) The AfterSideTurnStart leg closed with step 23. (b) SandpitPower is on `after_enemy_side_start_late` now that the enemy side has side-scoped slots (guard G5). (c) FAKE ORICHALCUM WAS ALREADY CORRECT: relics/fake_orichalcum.py has carried `on_player_turn_end_very_early` (the `Block > 0` snapshot, FakeOrichalcum.cs:46) and plain `on_player_turn_end` (the grant, :60) since the phase machinery landed; this entry had not been re-read since. What WAS still flat, and is the genuinely new finding, is the OTHER half of that interaction: PlatingPower's grant sat on the plain pass on both sides where PlatingPower.cs:61 is BeforeSideTurnEndEARLY and says why in as many words -- so the ordering FakeOrichalcum.cs:40-45 spells out (snapshot before Plating, Plating before end-of-turn damage) was luck. Both legs moved: `on_player_turn_end_early` and `before_enemy_side_end_early`. EXECUTED: test_fake_merchant.py::test_fake_orichalcum_is_suppressed_by_platings_earlier_grant.  FIXED 2026-07-28 (round 4). The text it replaced read: NARROWED 2026-07-28 (round 3): the AfterSideTurnStart leg is no longer flattened. `on_player_turn_started` (Hook.AfterPlayerTurnStart) and the new `after_side_turn_start` (Hook.AfterSideTurnStart) are two ordered dispatchers, and each still gets the _very_early/_early/plain/_late suffix walk, so a ported AfterSideTurnStartLate implementation now has a real slot (`after_side_turn_start_late`). WHAT REMAINS is unchanged and is content, not machinery: FakeOrichalcum.cs has BOTH BeforeSideTurnEndVeryEarly (the `Block > 0` snapshot, :46) and BeforeSideTurnEnd (the grant, :60) where the sim reads and grants inside ONE plain on_player_turn_end; and SandpitPower.cs:70 is AfterSideTurnStartLate where the sim's SandpitPower sits on the per-creature on_enemy_turn_start -- an ENEMY-side unit, so it waits on guard G5.  NARROWED 2026-07-28 (round 3) by the gap-queue continuation's post-section-A pass; what follows is what is still open. The issue it replaced read: NARROWED 2026-07-27: the MACHINERY is in. Every HookSystem dispatcher goes through _each, which runs a complete listener pass per suffix in the order _very_early, _early, plain, _late (sts2_rl/hooks.py) -- the shape Hook.cs:1238-1261, 1265-1291 and 1163-1175 have. The guard's own witness is closed: Orichalcum's Block snapshot is on_player_turn_end_very_early and its grant is plain on_player_turn_end (sts2_rl/relics/orichalcum.py), Pael's Eye's hand exhaust is on_player_turn_end_early, DisintegrationPower is after_player_turn_end_late, so [cloak_clasp, orichalcum] and [orichalcum, cloak_clasp] both give the game's 11 block. WHAT REMAINS is content not yet moved onto its phase: FakeOrichalcum.cs has BOTH BeforeSideTurnEndVeryEarly (the `Block > 0` snapshot, :46) and BeforeSideTurnEnd (the grant, :60), where sts2_rl/relics/fake_orichalcum.py reads `player.block == 0` and grants inside ONE plain on_player_turn_end -- reintroducing exactly the ordering luck Orichalcum was fixed for; and SandpitPower.cs:70 is AfterSideTurnStartLate where the sim's SandpitPower sits on the per-creature on_enemy_turn_start. ORIGINAL: no phase passes existed at all."
    },
    {
      "what": "G13: CheckWinCondition runs at three of the game's six sites, and none of the three recomputes the condition",
      "verdict": "faithful",
      "issue": "CLOSED. All six C# sites are real recomputations now. Added this round: CombatManager.cs:1181 (after Hook.BeforeTurnEnd, so DoTurnEnd does not run), :1207-1208 (closing EndPlayerTurnPhaseOneInternal, so the flush does not run) and :830 (EndEnemyTurn, whose `if (!IsEnding)` gate is what stops a fresh player turn being handed out). The four inline `_all_enemies_dead()/is_dead` pairs in play_card, auto_play, use_potion and the turn tail were CheckWinCondition with the tie-break the wrong way round; they call `_check_win_condition()` now, so a simultaneous death resolves as a LOSS as CombatManager.cs:1048 does. Steps 16 and 60: SetupPlayerTurn's IsDead guard is ported (`PlayerCombatState._setup_player_turn` is only entered for a live player, so a dead one gets no energy, no draw, no Hook.AfterPlayerTurnStart and no AutoPrePlay phase, while the SIDE hooks around it still run). FlushPlayerHand's needs no separate guard in a one-player sim: a dead player makes IsEnding true through the pending-loss arm, so `discard_hand`'s existing IsOverOrEnding guard already refuses -- pinned by test rather than by a redundant line.  FIXED 2026-07-29 (round 5). The text it replaced read: NARROWED 2026-07-28 (round 4): the enemy-side site is now a real recomputation of BOTH conditions. `_run_enemy_turns` calls `_check_win_condition()` after the side-start hooks and before ExecuteEnemyTurn, which is CombatManager.cs:598's site; the per-enemy `_all_enemies_dead()` reads it used to make were inside the loop and only covered the win side.  NARROWED 2026-07-27: the LIVE case is closed and a real recompute helper now exists. CombatState._check_win_condition tests player.is_dead and _all_enemies_dead() rather than reading the cached phase, and it is called immediately after the combat-start player.start_turn() (sts2_rl/combat.py) -- CombatManager.cs:573's site, which this guard identified as the missing one -- and again after each subsequent start_turn. Royal Poison, Festive Popper, Mercury Hourglass and Mr Struggles can no longer leave a dead player in Phase.PLAYER_TURN. WHAT REMAINS: the mid-pipeline sites are still cached-flag reads, not recomputations -- after the auto-post-play phase, after on_player_turn_end (CombatManager.cs:1181), after the turn-end card effects (CombatManager.cs:1207-1208) and before the fresh player turn (CombatManager.cs:830) all test `self.phase == Phase.COMBAT_OVER`, so a listener that kills the last enemy or the player without routing through _end_combat is not detected; only the win side is recomputed before the enemy side runs. SetupPlayerTurn's and FlushPlayerHand's IsDead guards (steps 16 and 60) are also still absent. ORIGINAL: three of six sites, none of them a recomputation, and nothing at all after the turn-1 setup."
    },
    {
      "what": "G14: the turn-1 ShouldStartAtBottomOfDrawPile pass is missing, so an Imbued card can be drawn into the opening hand",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: PlayerCombatState.start_turn's first-turn branch now runs the bottom-of-draw-pile move BEFORE the Innate move and excludes the moved cards from the Innate pass (`c not in sinking`), which is CombatManager.cs:657-672's order and its `.Except(list)` clause (sts2_rl/player.py). An Imbued card is sunk to the bottom of the draw pile and no longer occupies an opening-hand slot, so the turn-1 hand is 5 cards on every seed. Pinned by test_imbued_card_starts_at_the_bottom_of_the_draw_pile, which now passes."
    },
    {
      "what": "G15: the turn-end-in-hand wrapper re-consults should_ethereal_trigger in the sim; C# does not",
      "verdict": "faithful",
      "issue": "CLOSED. `_process_turn_end_cards` no longer re-consults `should_ethereal_trigger` for the cards that HAVE a turn-end effect. Hook.ShouldEtherealTrigger is consulted exactly once per turn end, in DoTurnEnd's partition (CombatManager.cs:1233) and only for the cards with no turn-end effect; CardModel.OnTurnEndInHandWrapper (:1690) decides on the raw keyword. Still dormant in shipped content on both sides -- no card is both Ethereal and HasTurnEndInHandEffect, and no C# model overrides the hook -- so the witness is a synthetic card in test/test_turn_structure_gaps.py::TestG15EtherealWrapperDoesNotReconsult.  FIXED 2026-07-29 (round 5). The text it replaced read: MECHANISM: CardModel.OnTurnEndInHandWrapper (CardModel.cs:1682-1698) decides the card's destination on the raw keyword -- `if (Keywords.Contains(Ethereal)) Exhaust else Add(Discard)` -- and never re-consults Hook.ShouldEtherealTrigger, which was already consulted in step 52's partition (and only for the cards that had NO turn-end effect). combat.py:370 re-consults it: `if card.is_ethereal and self.hooks.should_ethereal_trigger(card)`. So a false predicate sends an Ethereal turn-end card to the discard pile in the sim and to the exhaust pile in the game. DORMANT on both sides, verified: `grep -rl 'override.*ShouldEtherealTrigger'` over the whole decompiled game returns ZERO files and `grep -rn 'def should_ethereal_trigger' sts2_rl/` returns only the hooks.py dispatcher, so the predicate is constant-true and the branches coincide. Goes live with the first implementation on either side."
    },
    {
      "what": "G16: on_hand_emptied is fired from the end-of-turn flush -- the one site C# deliberately excludes -- and from nowhere else",
      "verdict": "faithful",
      "issue": "CLOSED. `on_hand_emptied` fires from C#'s two call sites and from neither of the sim's old ones. `_check_for_empty_hand` is called after every card play (CardModel.cs:1992, the site the sim never had) and after every potion use (PotionModel.cs:340), and `discard_hand` no longer fires it at all -- CombatManager.cs:880-883 excludes the flush in as many words. The gate is complete: `IsInProgress && !IsExecutingCardOrPotionEffect(player) && hand empty`, with the effect depth a real nesting counter (`CombatState._card_or_potion_effect`, CombatManager.cs:310-338) released in a `finally` around the play-count loop and the potion body. Both compensating ports are gone: Unceasing Top listens on `on_hand_emptied` with its real phase gate (UnceasingTop.cs:29-36 = {AutoPrePlay, Play, AutoPostPlay}) instead of hand-rolling the condition on `on_card_played`, and Joss Paper's deferred Ethereal credit is on `after_player_turn_end` -- its real hook is AfterSideTurnEnd (JossPaper.cs:116-124), dispatched from CombatManager.cs:1307 AFTER FlushPlayerHand, which is exactly the 'after the flush occurs' its own comment (:71-75) asks for.  FIXED 2026-07-29 (round 5). The text it replaced read: NARROWED 2026-07-27: one of C#'s two call sites exists. CombatState gained _check_for_empty_hand(), which returns early when the combat is over and otherwise fires on_hand_emptied on an empty hand, and CombatState.use_potion calls it after the potion resolves (sts2_rl/combat.py) -- PotionModel.cs:340's site, which is what makes the draw reachable from all 51 potions. WHAT REMAINS: (a) the card-play site (CardModel.cs:1992) has no caller -- _check_for_empty_hand has exactly one call site in the whole tree; (b) PlayerCombatState.discard_hand still fires on_hand_emptied from the flush, the one site CombatManager.cs:880-883 excludes; (c) there is no IsExecutingCardOrPotionEffect or PlayerTurnPhase gate. Unceasing Top's port still compensates by living on on_card_played and Joss Paper still depends on the flush-time firing. ORIGINAL: exactly one call site, and it was the excluded one."
    },
    {
      "what": "G17: Joss Paper's port uses `card.is_ethereal` as its causedByEthereal proxy, so ANY mid-turn exhaust of an Ethereal card is deferred to the flush; C# defers only the turn-end ethereal path",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: the CAUSE is now a parameter. hooks.on_card_exhausted takes `caused_by_ethereal` (sts2_rl/hooks.py), CombatState._process_turn_end_cards passes True at both of the game's two `causedByEthereal: true` sites -- the turn-end ethereal exhaust pass (CombatManager.cs:1240) and the turn-end-in-hand wrapper (CardModel.cs:1692) -- and every other exhaust path, including ExhaustCmd.exhaust, leaves it False (sts2_rl/combat.py, sts2_rl/cmds.py). Joss Paper's port branches on the parameter rather than on card.is_ethereal (sts2_rl/relics/joss_paper.py), so a mid-play-phase exhaust of Apparition is credited at once. WAS: `card.is_ethereal` deferred every Ethereal exhaust to the flush."
    },
    {
      "what": "G18: Pael's Eye's AnyCardsPlayedThisTurn predicate is missing BOTH of its C# clauses -- the turn-1 Whispering Earring short-circuit and the auto-play exclusion",
      "verdict": "faithful",
      "issue": "Closed 2026-07-27: relics/paels_eye.py's _any_cards_played_this_turn now carries BOTH clauses -- on turn 1 it returns True when a Whispering Earring is among the combat's relics (PaelsEye.cs:152), and the history scan filters `if not e.is_auto_play` (PaelsEye.cs:155). The auto-play flag it reads is real: hooks.on_card_played carries `is_auto_play` and CombatHistory records it on the CardPlayedEntry. The predicate is shared by should_take_extra_turn and the on_player_turn_end_early hand exhaust, as it is in C#."
    },
    {
      "what": "N1: the _inPlayerTurnSetup / _deferredEndTurnTransition race guard has no sim analogue",
      "verdict": "faithful",
      "issue": "CLOSED. The race guard is ported. `CombatState._start_player_turn` opens the window (`_inPlayerTurnSetup = true`, CombatManager.cs:470) around `player.start_turn()` and closes it in a `finally` -- ReleaseDeferredEndTurnTransitionIfNeeded (:722-735), which :720 says \"must be called on every exit path ... so a deferred transition is never dropped\". `end_turn()` inside the window STORES the transition (:702-714) instead of running it, so an end-turn from a card auto-played during AutoPrePlay can no longer recurse into the tail of StartTurn. `is_player_ready_to_end_turn` is the readiness predicate; with one player `AllPlayersReadyToEndTurn()` (:695) is satisfied by that player alone, so readiness and the deferral are the same instant. Whispering Earring's hand-rolled guard turned out to be FAITHFUL rather than a workaround -- WhisperingEarring.cs:58-69 breaks on three conditions of its own -- but it was missing two of them; it now breaks on IsOverOrEnding, IsPlayerReadyToEndTurn and the turn change, in the source's order, and the middle one only became reachable with this machinery.  FIXED 2026-07-29 (round 5). The text it replaced read: CombatManager.cs:56-65, 700-714, 722-735 exists exactly so that a card auto-played during the AutoPrePlay phase can end the turn without the transition racing the tail of StartTurn. The sim's start_turn is synchronous and an end_turn re-entered from inside it would recurse. Recorded as a gap and NOT a waiver: the ported Whispering Earring auto-plays real cards during what C# calls the setup window (relics/whispering_earring.py:27-43) and guards against the recursion by hand (`if combat.is_over or self.turn != start_turn: break`), which is a workaround for exactly this missing machinery. Dormant only because no ported turn-1 auto-playable card ends the turn. Carries G8's precedence (same missing phase model)."
    },
    {
      "what": "N2: presentation, animation, SFX, banners, checksums, replay writing, saves, achievements, the ActionExecutor / ActionQueueSynchronizer layer, and every HookPlayerChoiceContext pause/resume path",
      "verdict": "waiver",
      "rationale": "Out of scope by the shared audit rules (presentation and multiplayer plumbing), and the sim is single-player and synchronous. Steps 4, 10, 25, 28, 35, 42, 44, 56, 57 and 71 each cite it for their non-substantive legs; in every one of those steps the substantive leg is verdicted separately."
    },
    {
      "what": "N3: orbs (OrbQueue.AfterTurnStart at CombatManager.cs:533, OrbQueue.BeforeTurnEnd at 1218)",
      "verdict": "waiver",
      "rationale": "Orbs are the Defect's and the sim is Ironclad-only; non-Ironclad character content is an explicit waiver under the shared scope rules. Steps 24 and 51."
    },
    {
      "what": "N4: RoundNumber and TurnNumber are one counter in the sim",
      "verdict": "faithful",
      "issue": "CLOSED. `CombatState.round_number` is a second counter. `self.turn` stays PlayerCombatState.TurnNumber -- bumped on every player turn including extras (CombatManager.cs:1417), which is what Horn Cleat, Captain's Wheel, Sparkling Rouge, Royal Poison and Anchor read. `round_number` is CombatState.RoundNumber -- initialised to 1 (CombatState.cs:156) and bumped only on SwitchSides' normal-round arm (:1413). CombatHistory stamps its entries with `round_number` now, matching CombatHistory.cs:40-120, so an extra player turn is the same round and \"did X happen this turn\" survives it.  FIXED 2026-07-29 (round 5). The text it replaced read: CombatState.turn (combat.py:108) is incremented once per player turn INCLUDING extra turns (combat.py:650, 674), whereas SwitchSides increments PlayerCombatState.TurnNumber for extra-turn players but CombatState.RoundNumber ONLY on a normal round (CombatManager.cs:1405-1418). AbstractModel.cs:1125 explicitly directs content to use AfterSideTurnStart 'with a RoundNumber check', so the two are meant to be distinguishable. Recorded as a gap carrying G3's precedence (it is only reachable on the extra-turn path, which G3 already makes divergent); dormant on its own because no ported content reads a round counter that an extra turn would skew -- the ported turn-number readers (Horn Cleat turn 2, Captain's Wheel turn 3, Sparkling Rouge turn 3, Royal Poison turn 1, Anchor turn 1) all read TurnNumber, which is what self.turn models."
    },
    {
      "what": "N5: the win-condition PREDICATE itself is faithful",
      "verdict": "faithful",
      "issue": "CLOSED. The predicate was always faithful; this entry carried G10's verdict so the rollup stayed actionable. G10 is closed, so it carries a faithful one.  FIXED 2026-07-29 (round 5). The text it replaced read: _all_enemies_dead (combat.py:272-277) reproduces IsPrimaryEnemy exactly -- a full-source grep confirms MinionPower is the only power that sets OwnerIsSecondaryEnemy, so `'minion' not in e.powers` is the right test and IllusionPower is NOT a secondary-enemy marker despite reviving its owner -- and its `primaries or self.enemies` fallback matches C#'s behaviour when every enemy is a minion. The two things missing from the SURROUNDING machinery, Hook.ShouldStopCombatFromEnding and the pending-loss two-step, are G10's; this guard carries G10's verdict so the rollup stays actionable rather than recording a faithful predicate in isolation."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-25"
}