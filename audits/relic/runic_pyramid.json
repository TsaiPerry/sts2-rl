{
  "unit": "relic/runic_pyramid",
  "game_source": {
    "path": "src\\Core\\Models\\Relics\\RunicPyramid.cs",
    "sha256": "872632e4196b24167fe7a681827829d05486b29b4483467aa0c33f383ef5a3e3"
  },
  "sim_source": {
    "path": "sts2_rl\\relics\\runic_pyramid.py",
    "sha256": "313c805b499bd5a3c6340b8d34b9d9b01052908585d1392e060ac75b04424399"
  },
  "hooks": {
    "Rarity": {
      "maps_to": "rarity = RelicRarity.ANCIENT (runic_pyramid.py:14)",
      "verdict": "faithful",
      "rationale": "Matches RunicPyramid.cs:8. Obtainable: the ported Darv shrine offers it (events/darv.py:33); Ancient relics are correctly absent from the transcribed grab bag."
    },
    "ShouldFlush": {
      "maps_to": "should_flush_hand (runic_pyramid.py:16-17), dispatched by HookSystem.should_flush_hand (hooks.py:644-651) from CombatState.end_turn (combat.py:661)",
      "verdict": "gap",
      "issue": "Rollup of guards G1 and N1 per binding rule 4. The predicate itself is faithful -- the sim returns False unconditionally, matching RunicPyramid.cs:10-17 for the owner -- but the sim's CONSUMER of the predicate diverges: audits/seam/turn_structure.json step 61 and guard G4 record that C#'s FlushPlayerHand treats ShouldFlush == false as 'retain every card' and still runs Hook.AfterFlush and PlayerCombatState.EndOfTurnCleanup (CombatManager.cs:1327-1346), while combat.py:661-662 gates the whole discard_hand call on the predicate and so skips the tail as well. That record labels the mechanism a LIVE gap and names relics/runic_pyramid.py:16-17 as one of its two ported witnesses; binding rule 3 -- one verdict per mechanism -- so this entry cites and matches it rather than re-deriving it. Rule 7: turn_structure.json, CombatManager.cs, combat.py, hooks.py and relics/joss_paper.py are NOT hashed by this record; the seam record hashes the machinery."
    }
  },
  "guards": [
    {
      "what": "G1 (LIVE, cited not re-derived): a false should_flush_hand skips the flush TAIL, so Joss Paper's deferred Ethereal credit is stranded",
      "verdict": "gap",
      "issue": "MECHANISM and OBSERVABLE are audits/seam/turn_structure.json guard G4's, which reads: 'a turn that exhausts 5 Ethereal cards leaves the next hand at 6 cards with [joss_paper] and at 5 with [joss_paper, runic_pyramid] -- the deferred credit is stranded (_ethereal_pending stays at 5) and the Joss Paper draw never happens, where the real game draws it either way.' Both sides ported: Runic Pyramid comes from the Darv shrine (events/darv.py:33), Joss Paper is an Uncommon grab-bag relic, and relics/joss_paper.py:42 is the sim's only on_hand_emptied listener. Recorded here because the seam owns the machinery and the content record owns the relic's observable -- the same ownership split relic/unsettling_lamp G1 uses for hook_dispatch G4. EXECUTED for this record, confirming the port's own half (py tools/audit/relic_probes_b14.py pyramid): with no relics the hand goes 5 -> 5 across a turn and should_flush is True; with [runic_pyramid] it goes 5 -> 10 and should_flush is False. Rule 7: turn_structure.json, relics/joss_paper.py and combat.py are not hashed here."
    },
    {
      "what": "N1: C#'s `if (player != base.Owner) return true;` (RunicPyramid.cs:12-15) -- the sim's hook takes no player argument at all",
      "verdict": "faithful",
      "rationale": "The clause exists so one player's Runic Pyramid does not retain a DIFFERENT player's hand; HookSystem.should_flush_hand (hooks.py:644-651) is zero-argument and cannot express it. Structurally satisfied rather than dropped: the sim is single-player by design (CombatState holds exactly one PlayerCombatState), so `player != Owner` is unsatisfiable. A multiplayer-only parameter, noted in the mapping per the shared contract's scope section rather than filed as a divergence. Rule 7: hooks.py and combat.py are not hashed here."
    },
    {
      "what": "N2: the relic has no state, no combat-boundary reset and no other override -- checked against PROMPT.md bug classes 10 and 13",
      "verdict": "faithful",
      "rationale": "RunicPyramid.cs is 18 lines with exactly two members (Rarity, ShouldFlush) and no fields; runic_pyramid.py mirrors that. Read both files rather than trusting the bucket, with py tools/audit/relic_probes.py sweep-reset's 200-relic 'hold no state at all' bucket as corroboration only. Nothing to reset, so classes 10 and 13 cannot fire."
    },
    {
      "what": "N3: no Flash(), no base.Status, no CanonicalVars -- the port drops no presentation member either",
      "verdict": "faithful",
      "rationale": "Unusual for a relic and worth recording: RunicPyramid.cs declares no display members at all, so unlike most units in this batch there is nothing to waive under the presentation exclusion."
    }
  ],
  "verdict": "gap",
  "audited": "2026-07-26"
}
