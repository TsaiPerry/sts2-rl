# Prompt: SP3 — converge new Ironclad seed 933T39V18D (2nd full-run conformance seed)

Paste into a fresh session. Work in `c:\Users\Perry\Desktop\sts2-rl`. This
continues SP3 Task-6/Task-9 convergence (`[[sp3-task9-convergence]]`,
`[[sp3-89u-act1-reconcile-and-relics]]`) with a **brand-new convergeable seed**
captured on 2026-07-23.

---

## Why this seed matters

Until now **89U21BV1TZ was the only Ironclad conformance seed** — the other four
(`DJDCSAQZNR` Regent / `L081UMJX4M` Silent / `QRWCVDPZN5` Necrobinder /
`TZEKRYTSNT` Defect) are un-ported characters that can never converge
(`[[sp3-seeds-are-5-characters]]`). So the sim's fidelity has effectively been
checked against a **single** run.

**933T39V18D is a second, independent, complete Ironclad victory run** (Neow →
The Architect kill, 49 floors, 633 commands, ascension 0, acts
`[overgrowth, hive, glory]`). Different map, encounters, cards, relics, and
bosses than 89U (e.g. act-1 boss `CEREMONIAL_BEAST_BOSS`, an act-3
`Mecha Knight` fight) — so it will exercise content 89U never touched and is the
strongest new fidelity check available. **Goal of this session: make the sim
completely correct for this seed** (zero player-state divergence, no premature
stops, unregressed combat-stream counters), capturing each fidelity gap it finds.

## Provenance / integrity caveat (read before trusting the fixture)

The full-run log for this seed is a **stitch of two recording sessions** (the
original play session corrupted its floor_1/2/46 logs when a later in-game
"Load Save" re-record misfired; details in the capture session). It was
reconstructed and **validated by replaying it in-game to victory**:

- Floors 1–46 = original session (cumulative log, 553 cmds, ends `MoveToMapCoord 0`).
- Floors 46→victory = a Load-Save continuation from the exact floor-46 save
  (80 cmds, begins `PlayCard 3 # FASTEN … Mecha Knight 297/300`).
- Seam is contiguous (no gap/overlap); total 633 cmds; ends
  `THE_ARCHITECT … ProceedToNextAct`.

The **per-act truncation saves floor_18 and floor_34 are pristine originals**
(same session as floors 1–46), so their `run.save` HP checkpoints are clean, and
their cumulative action logs are exact **prefixes** of the stitched floor_49 log
(verify: floor_18 = first 226 cmds, floor_34 = first 431 cmds of floor_49).

Source-of-truth files on disk:
- Stitched complete run + victory save:
  `C:\Users\Perry\Desktop\sts2-run-backups\20260723-125401\933T39V18D-COMPLETE\{actions.sts2replay, run.save}`
- Full 49-floor backup (every floor has its own `run.save`):
  `C:\Users\Perry\Desktop\sts2-run-backups\20260723-125401\933T39V18D-recording\`
- Live game folder (floor_49 = the stitched log; **floors 1/2/46 are zeroed —
  do not use them**):
  `%APPDATA%\SlayTheSpire2\RunReplays\logs\933T39V18D\`

---

## Step 1 — install the conformance fixture

Mirror the 89U layout exactly: `Resources/<seed>/{floor_18,floor_34,floor_49}/`,
each with `actions.sts2replay` + `run.save`. Fixture root is
`~/Desktop/RunReplays/RunReplays/Resources` (`REC` in the tests).

```
BK="C:/Users/Perry/Desktop/sts2-run-backups/20260723-125401/933T39V18D-recording"
CMP="C:/Users/Perry/Desktop/sts2-run-backups/20260723-125401/933T39V18D-COMPLETE"
DST="C:/Users/Perry/Desktop/RunReplays/RunReplays/Resources/933T39V18D"
mkdir -p "$DST/floor_18" "$DST/floor_34" "$DST/floor_49"
cp "$BK/floor_18/actions.sts2replay" "$BK/floor_18/run.save" "$DST/floor_18/"
cp "$BK/floor_34/actions.sts2replay" "$BK/floor_34/run.save" "$DST/floor_34/"
cp "$CMP/actions.sts2replay" "$CMP/run.save"                 "$DST/floor_49/"
```

Verify before proceeding:
- `floor_49/actions.sts2replay` has **633** command lines, starts at
  `ChooseEventOption 0 # NEOW…`, ends `ProceedToNextAct`.
- floor_18 log = exact prefix (226 cmds) and floor_34 log = exact prefix
  (431 cmds) of floor_49's first 553 commands.
- `parse_save` on each `run.save` returns sane `current_act_index`
  (0→ after act1 done at floor_18, etc.) and non-zero `player_current_hp` /
  `player_max_hp`. Record the three `(hp, max_hp)` checkpoints — these are the
  gate's ground truth.

This adds files to the **RunReplays git repo** (fixtures). Stage them; do not
commit (Perry commits — but note RunReplays is a *different* repo than sts2-rl,
confirm its own commit convention with Perry).

## Step 2 — register the seed in the gate

In `test/test_conformance_player_state.py`:
- Add `"933T39V18D"` to `SEEDS`.
- Add a `_XFAIL_CONVERGENCE["933T39V18D"]` entry with an honest initial reason
  (fill in after the first triage, e.g. "2nd Ironclad run; converging — <first
  divergence>"). It is Ironclad, so unlike the other 4 it is **expected to
  converge**; the xfail is temporary and drops when green.
- `player_checkpoints()` and `_ACT_FLOORS = {0:18, 1:34, 2:49}` already work for
  this seed (same act-boundary floors as 89U — confirmed: floor_18 log has 1
  `ProceedToNextAct`, floor_34 has 2). If a boundary floor turns out different,
  adjust per-seed rather than hard-coding globally.

## Step 3 — converge (same loop as the 89U grind)

Follow `[[docs/superpowers/prompts/2026-07-22-sp3-seed-convergence-grind.md]]`
verbatim for method — it is the canonical procedure. In short:

- Triage: `py tools/converge_triage.py 933T39V18D floor_49 2`
  (`[DETECTOR 1]` wrong-stream draws, `[DETECTOR 2/2b]` stream-counter & per-cmd
  Hand/Enemies diffs — **fix earliest room first**, `[DETECTOR 3]` HP deltas).
- Priority order: **map/progression stops → max-HP deltas → current-HP deltas**
  (current-HP drift is usually downstream of a forced combat; converge the
  earliest diverging combat).
- **Ground truth = the decompiled game** at
  `c:\Users\Perry\Desktop\Slay the Spire 2\src` (`[[original-means-game-source]]`).
  When a fix changes sim behavior to match the game, update legacy tests to the
  game-correct value — never weaken a real regression guard.
- Re-use the established combat-parity mechanics from `[[sp3-task9-convergence]]`
  (net_id targeting, PileType.Play reshuffle limbo, transform-append,
  reward pool = full `GetUnlockedCards`, `SelectHandCards`, killing-blow skips
  `AfterDamageReceived` — see `[[monster-move-weight-vs-cooldown-bug]]`).

Expect this seed to surface **new** gaps 89U never hit (its distinct events,
relics, potions, enemies, and the `CEREMONIAL_BEAST_BOSS` / act-3 fights). Each
genuinely un-ported piece of content is a fidelity debt: port it if tractable,
else `xfail(reason=...)` the specific seed with an accurate reason and note it.

## Gates & rules

- Seed gate: `py -m pytest test/test_conformance_player_state.py -k full_run_player_state_parity -q`
- Full suite (~3.5 min): `py -m pytest test/ -q` (must stay green + unregressed).
- **Never `git commit` / `git push`** in sts2-rl (`[[sts2-no-auto-commit]]`,
  CLAUDE.md rule 4). Stage with `git add <paths>` and stop; Perry reviews/commits
  in batches. Use `from __future__ import annotations` + lazy in-method imports.

## Definition of done

`933T39V18D`'s `test_full_run_player_state_parity` case goes XPASS→PASS (mark and
`_XFAIL_CONVERGENCE` entry removed), with **zero** `player_hp` / `player_max_hp`
divergence at every act boundary, no premature map stop, and unregressed
combat-stream counters — replaying the real recorded full run end-to-end. Full
suite green. Stage everything (sts2-rl + the RunReplays fixture) and report the
staged diffs to Perry, plus a short list of any new fidelity gaps found (worth a
memory each).
