# Tier 2 round 13 — continue the dormant-gap campaign

You are the **controller** of a subagent-driven fidelity campaign on `sts2-rl`,
a Slay the Spire 2 combat simulator. Round 12 finished 2026-07-31 and merged to
`main` at `dc26373e`. This prompt continues that work.

Read first, in this order:
1. `docs/superpowers/plans/2026-07-31-tier2-round12-retro.md` — the lessons.
   **They are the most important input to this round.**
2. `audit/GAP-QUEUE.md` — the round-12 banner at the top, then the Status table.
3. `.superpowers/sdd/progress.md` — the per-task ledger, and its
   "Minor findings carried to final review" list, which is a backlog.

## Ground truth

- The decompiled C# at `c:\Users\Perry\Desktop\Slay the Spire 2` **is the
  specification.** Non-ascension values. Cite `file:line` for every claim.
- Never trust a count stated in prose, including in this file. Re-run
  `py audit/tools/gap_queue.py counts`.
- Use the `py` launcher; there is no `python` on PATH.
- **Never `git commit` or `git push` unless the user explicitly asks.** Stage
  only. (Round 12 committed *only* because the user asked for a merge.)

## Where round 12 left it

`counts` at merge: **372 gap entries / 349 mechanisms / 7 live**, and
**0 live entries in any of the six seam records** — the engine tier is clean.
Suite: 2 failed / 3766 passed / 6 xfailed. **The 2 failures are
`test_conformance_floor_state.py` missing the
`RunReplays/.../933T39V18D/floor_49` fixture. They are an environment gap.
NEVER "fix" them, and never count them as regressions.**

## The five lessons, as operating rules

These cost real rework in round 12. Treat them as binding.

1. **A green suite is not evidence of fidelity.** Two round-12 fixes
   *introduced* divergences that every test passed over, because a dormant
   mechanism has no listener that would notice. **Every reviewer must re-read
   the C# control flow around the change**, not just run tests.
2. **The brief can be the bug.** A round-12 brief instructed a wrong behavior
   and the implementer complied and defended it. **Every review dispatch must
   say: "do not defer to the brief; the C# decides"**, and name the specific
   claim to re-derive.
3. **"Dormant" is not safer than "live."** A dormancy verdict was overturned
   when a third consumer nobody had enumerated turned out to be reachable.
   Ask *"what else reads this?"*, not *"does the recorded consumer still hold?"*
4. **Records are wrong about their reasoning more often than their verdicts.**
   When closing an entry, state which **reasoning** you replaced.
5. **Close conservatively.** Narrow instead of closing when any site remains.
   A round whose count only ever falls is not measuring honestly — round 12's
   queue went UP by one when a new live gap was found.

## Parallel execution (this is what made round 12 work)

Waves of 2-4 concurrent Sonnet implementers, each with a reviewer afterwards.

- **Disjoint FILE footprints, not just disjoint mechanisms.** `sts2_rl/cmds.py`
  and `sts2_rl/hooks.py` are the contended files — only one lane owns each.
  Pair one engine lane with content lanes (`cards/`, `relics/`, `potions/`,
  `events/`, `monsters/`).
- **Implementers never touch `audit/**`.** They propose closes and queue
  annotations in a report; the controller applies them. This is what makes
  concurrency safe.
- **Forbid in every dispatch:** `git commit`, `push`, `add`, `stash`,
  `checkout`, `reset`, `restore`, and the "temporarily revert the fix to see
  RED, then restore" technique (one agent did that while another was live in
  the same worktree). Get RED by writing the test **before** the fix.
- **Scope every reviewer's diff to the reviewed task's declared paths.** A bare
  `git diff` shows every lane at once.
- Recreate `closer.py` (round 12's helper) from the retro's tooling notes: it
  must preserve each record's `ensure_ascii` style **and** trailing-newline
  presence (11 records lack one), or a one-line re-verdict rewrites the file.
  Prove it round-trips all 848 records before using it.
- `GAP-QUEUE.md` is **CRLF**; normalize after any programmatic edit.
- `gap_queue.py`'s `local_id` for a hooks entry is the **first token** of the
  key (`OnUse`), not the whole key. Match by prefix.

## The work, in recommended order

### 1. The two families round 12 never started

- **Play-pile family** — `creature_card_cmds/N9`, `/step99`, `/step51`,
  `/step56`. The sim has no Play pile; `combat.py`'s `_resolve_card_play`
  collapses the leg. **This also unblocks the last unwired
  `AfterCardChangedPiles` site** (`creature_card_cmds/G8`, narrowed in round 12
  to exactly one remaining site: the manual play at `CardPileCmd.cs:683`).
- **`hook_dispatch` registry family** — `G1` + `G7` + `G5` + `G6`, then `N5`.
  The queue's own radius note says this family "lands together or not at all":
  `G7`'s per-item liveness re-check needs `G1`'s derived listener list, and
  `G6`'s ordering needs `G1` too. **Beware performance** — this is the hot
  dispatch path. Round 12 measured `end_turn` before/after and required the
  `HookSystem._has_listener_for` presence cache; do the same.

### 2. Live gaps found in round 12 and left open

- **`run.reward_offer_selector` is never wired by `driver.py`** (set only in
  test files, confirmed by repo-wide grep), so take-or-skip reward screens
  **auto-accept in real play**. This is probably the largest known live
  divergence in the sim right now. Verify, scope, fix.
- **`event/the_future_of_potions/g15`** — `events/base.py::Event.offer_card_reward`
  has no reroll surface at all, so reward modifiers cannot reach it. Needs new
  capability, not a missing call.
- **Consolidate the reward-modifier dispatch.** C# has exactly one choke point
  (`RewardsSet.GenerateWithoutOffering`, `RewardsSet.cs:136`); the sim
  dispatches at several construction sites. Round 12 fixed two sites per-site
  because consolidating was out of footprint — **the next event ported this way
  reintroduces the same bug.** Note the trap: `driver.py`'s `_offer_rewards` has
  callers that already dispatch, so a naive central call double-fires and
  duplicates gold/relics.
- **`power/skittish/AfterAttack`** — needs an AttackCommand-level hook (fires
  once per attack after all hits) that the sim has no analogue for. Architecture.
- **`relic/_auto_keep` / `relic/kifuda`** — `CardSelectorPrefs(.., 0, 3)` with
  `Cancelable = false`: the player may confirm having enchanted fewer cards than
  are eligible; the sim's driver always fills the count.

### 3. The 56 unlabelled entries

Neither live nor shown dormant. Settle each by **execution**: fix it, or replace
the silence with a dormancy *enumeration* naming every consumer you checked.
Round 12's overturned verdict came from an incomplete enumeration — be explicit
about what you looked at.

### 4. Backlog in the ledger

`.superpowers/sdd/progress.md`'s "Minor findings" list carries deferred items,
including: `card/breakthrough` is an uncounted 6th `card/_is_dead_early_return`
site; `monster/vantom`'s `StatusIntent(3)` is an uncounted 4th
`_intent_count_lost` site; `selectors.py`'s `to_draw_top` cost ranking is
unclamped; `state_machine_probes.py`'s zero-weight probe greps stale message
text; `creature_card_cmds/step19`'s closure wrongly equates the sim's `is_over`
with C#'s `IsEnding`.

## Watch item

`PowerCmd.apply` got **28-42% slower** in round 12 (three dispatches where there
was one — the architecturally correct split). `end_turn` is a net 14.8% *faster*
because a live `import` statement in `combat_is_over` was cached. 147 files call
`apply`; if training throughput regresses, start there.

## Deliverables

Per task: a brief, an implementer report, a reviewer verdict, applied record
closes + queue annotations, gates green. Per round: a banner at the top of
`audit/GAP-QUEUE.md` in the established shape — a before/after table, what
shipped, **an explicit "what this round did NOT do"**, findings that outrank the
fixes, and anything left open and unowned. Update `.superpowers/sdd/progress.md`
as you go; it survives context compaction and is authoritative.
